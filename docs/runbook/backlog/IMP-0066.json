{
  "acceptance": "uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog_migration.py -k doc_anchor",
  "category": "tool",
  "date": "2026-08-05",
  "detail": "**`verified_against` 可達性的判準與守衛都不對,IMP-0038 只涵蓋其中一半。**(刻意不改 IMP-0038 本體——那是它的欄位,且 `site/text-first-compact` 正在重寫該列;此處記錄供其 owner 吸收。) 三件事:① **判準錯**:`ops/docs_lint.sh:418-421` 驗的是 **HEAD 可達**,而 CI 解析的是 **origin/main 可達**。本 repo 本地 main 超前 origin(實測 `origin/main...main` = `0 9`),兩者不等價 → 分支本地必綠、CI 全紅,**實測規模:50 份 doc 帶 sha 錨點,其中 21 份對 `origin/main` 不可達**(reviewer 獨立盤點得同一數字);IMP-0038 建檔時記「約 10 份」是低估一倍以上——正確數字由 `site/text-first-compact` 的 `a45e26e6e` 寫回該條本體,此處不複述。這正是 IMP-0038 至今 open 且 `docs-lint` 被排除於 linux CI 的機制原因。把判準改成 `origin/main` 可達**就是 IMP-0038 的出場條件**。② **另一種假綠,與 IMP-0038 不同型**:3 份 doc 寫的是**字面** `verified_against: HEAD`(`docs/plans/2026-05-23-notebook-editorial-stack.md`、`docs/specs/2026-05-23-notebook-editorial-cover-design.md`、`docs/specs/2026-05-23-notebook-editorial-stack-design.md`)——自我滿足的錨點,**任何可達性檢查都永遠判不了它**。③ **正控斷言在缺少驗證對象時靜默 skip,而那正是它該響的環境**(由 `feat/backlog-store` 回報,已獨立複驗):`ops/tests/test_backlog_migration.py:721-726` 的 origin-可達正控被 `pytest.skip(\"no origin/main\")` 守著——**沒有 `origin/main` 的環境(未 fetch 的 clone、淺 clone 的 CI)正是 `_doc_anchor()` 會降級成 local main 的環境**,於是斷言在最需要它的地方消失、套件仍全綠。**這是「檢查存在但在該響的時候不可能響」的第四個變體**(前三個:IMP-0044 gate 只能紅不能綠、IMP-0059 一條寫著卻永不會響的檢查、IMP-0056 測試替身比真實依賴仁慈——三次是隔數週各自被發現的)。同類還有本條 ② 的字面 `verified_against: HEAD`:不是值錯,是**那個位置的檢查在結構上不可能紅**。通則已寫進 `docs/sop/doc_sync.md` §步驟 4。下一步:該 skip 改成紅並明示 `git fetch origin`,或至少計數並在總結印出。④ **規則的 owner 文檔自己違反規則**:`docs/sop/doc_sync.md` 的 `verified_against: f0d37ca4` 對 main 與 origin/main **皆不可達**。已於本 commit bump 到 `0c9e3b7c` 並把判準寫進該檔。",
  "fix_site": "ops/tests/test_backlog_migration.py:721",
  "groomed_at": "",
  "groomed_by": "",
  "id": "IMP-0066",
  "plan": "[草稿遭退回 2026-08-05，退回理由：① 步驟 1 指定的 pin 機制自我否定：要求「讀本檔自身原始碼 `Path(__file__).read_text()`，斷言字面 `pytest.skip` 加上 `no origin/main` 已不存在」——寫下這條斷言的那一行本身就含有該字面值，於是 needle 永遠命中，測試在步驟 2 做完後仍然紅，永遠綠不了。要繞開得自己想出拆字串或改用 `inspect.getsource(另一個函式)`，那是 plan 沒給、小模型得自行發明的判斷。② acceptance 不會變色：`-k doc_anchor` 今天前景實跑是「1 passed, 47 deselected」，唯一被選中的就是 `test_doc_anchor_is_reachable_from_origin_main`；本 repo 有 origin/main，那條 skip 分支根本走不到，所以步驟 2 的 skip→fail 對它是 no-op（紅綠不變）。整條 acceptance 能否偵測到修好，完全取決於新測試的名字剛好含 `doc_anchor` 才會被 `-k` 選中——plan 從頭到尾沒說要這樣命名，只在旁白裡假設「這條新測試會是唯一的紅」。命名照一般直覺（例如 test_origin_main_probe_does_not_skip）就會被 deselect，acceptance 修前修後都綠。]\n\n以下為被退回的原草稿，重做時請先讀上面的退回理由：\n本條可執行的殘留只有 ③。①② 是 IMP-0038 的欄位（entry 的 resolution 自己寫「待 IMP-0038 的 owner 吸收」），④ 已於 f918d6ecc 修畢——不要在本次一併動它們。\n1. 先寫 regression pin 看紅：在 ops/tests/test_backlog_migration.py 的 test_doc_anchor_is_reachable_from_origin_main（:708-726）之後新增一條測試，讀本檔自身原始碼（Path(__file__).read_text()）並斷言字面 pytest.skip 加上 no origin/main 這個 reason 已不存在。理由：這是「檢查存在但在該響的環境靜默跳過」的第四個變體（前三個 IMP-0044 / IMP-0059 / IMP-0056），pin 讓下一個人不會順手改回 skip。現況實測：uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog_migration.py -k doc_anchor 是 1 passed，所以這條新測試會是唯一的紅。\n2. 把 :721-723 的 pytest.skip(\"no origin/main\") 換成 pytest.fail(...)，訊息要明講兩件事：沒有 origin/main 的環境（未 fetch 的 clone、淺 clone 的 CI）正是 BACKLOG._doc_anchor() 會降級成 local main 的環境，所以這裡不能沉默；修法是先跑 git fetch origin main 再重跑。\n3. 保留 :719-720 的 anchor == \"unknown\" skip 不動——那是真的沒有 git，與本條的失效模式無關，混在一起會讓紅的意義變模糊。\n4. 不需要新增 fixture repo：本測試跑在 BACKLOG.ROOT（ops/tests/test_backlog_migration.py:28 的 ROOT，即 KG repo 本身），它一定有 origin。\n5. 落地後把本條的 resolution 更新成「③ 已修（skip 改 fail + 原始碼 pin），①② 仍屬 IMP-0038，④ 已於 f918d6ecc 修畢」，指令：./ops/backlog.py update IMP-0066 --resolution \"...\" --commit。狀態不要標 fixed，因為 ①② 還在。",
  "resolution": "④ 已修(`f918d6ecc`)。③ 的通則已寫進 `docs/sop/doc_sync.md`,實例待 `feat/backlog-store` 處理。①② 待 IMP-0038 的 owner 吸收。",
  "schema": "kg.backlog.entry.v1",
  "severity": "med",
  "source": "IMP-0063 的錨點改寫 review",
  "status": "open",
  "stream": "IMP"
}
