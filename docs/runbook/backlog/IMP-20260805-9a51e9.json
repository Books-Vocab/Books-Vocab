{
  "category": "tool",
  "cost": "M",
  "date": "2026-08-05",
  "detail": "schema 稱 `resolution` 為「可回溯的關鍵欄」並明令不得以分支名代替 hash，但 `validate_entry`（:190-215）只驗必填欄存在與 stream/category/severity/status 詞彙——`status: fixed` 配空的、純散文的、或分支名的 resolution 一律 0 problems。同族第三個洞：`verdict` 是宣告過的閉集（`VERDICTS`），但只在 extractor 裡被守；`update <id> --verdict TOTALLY-BOGUS --commit` 實測 rc=0 落地，validate 仍報 0 problems。**受害者四筆不是兩筆**（判準取 `docs_lint.sh` 的兩段式：`rev-parse --verify <sha>^{commit}` 再 `merge-base --is-ancestor <sha> HEAD`，看 resolution 位置 0 的 sha；34 筆 fixed 中 30 清 4 紅）：IMP-0005（`813356b1`，物件根本不在 odb）、IMP-0063（位置 0 是分支名 `feat/freeze-ui-world`，分支已刪）、IMP-0001（`7c95a02`）與 IMP-0002（`3671b89`）——**物件存在但不是 HEAD 或 origin/main 的祖先**，PR #968 被 rebase-merge 遺下的 dangling commit。後兩筆是主線用 `git cat-file -e` 掃描漏掉的，因為該呼叫對 dangling object 回 0；那是同一輪裡我第二次被自己寫的偵測器騙（第一次是 incidental hash 滿足 any 斷言）。**四筆全部可救、無永久損失**：IMP-0001 到 `98aa04fe6`、IMP-0002 到 `fc2c7d454`（`git patch-id --stable` 位元組相同，確定）；IMP-0005 到 `52a89295a`（寫入該 hash 的 commit `a52519c4c` 的直接父，其 docs_lint 改動與 detail 逐字相符，極高信心；literal 字串本身確定不可復原）；IMP-0063 從來沒有 squash，工作以五個普通 commit 落地（`e1164bc3f` `59376883e` `e2e343a14` `8e5e79a9e` `f918d6ecc`）。**另一條 prose-only 不變式也已破**：platform-steward 的「open 無 next action」在 IMP-0064、IMP-0065、IMP-20260805-afc14b 三筆為空，而其餘 27 筆未結案有 26 筆是破折號開頭的 next action，慣例明確且可機器化。（反之 wont-fix 須附理由 3/3 成立、未破，原始框架的推測被證偽。）**而且 `validate` 根本沒有自動呼叫者**：`.github/workflows/` 與 `ops/*.sh` 全無，只有 `ops/tests/test_backlog.py` import 該模組——所以不是「機器面只到詞彙為止」，是機器面根本沒被排程。散文欄位的可檢查性上限就是位置啟發式：位置 0 規則實測 33/34 成立，但它表達不了 IMP-0063 的五個 sha，遇到 resolution 以重新取證戳記開頭就崩，且 IMP-0028 那筆裸的未加反引號 sha 會逼 regex 放寬到重新引入歧義。",
  "fix_site": "ops/backlog.py（:105 之後新增 TRACE_FIELDS、:190-205 之後新增 check_traceability/_git_reachable/parse_shas、:190-205 _check_vocabulary 加 verdict 檢查、:386-391 MUTABLE_FIELDS、:1054 --resolution 預設、:1097 附近 update parser 加 --fixed-by、:1191 _cmd_show、:1197-1210 _cmd_validate、:1330-1357 _cmd_render）＋ ops/tests/test_backlog.py（新增 15 條測試）＋ ops/test_ops.sh:24-63,:92-97（新 backlog group）＋ ops/tests/test_ops_ci_coverage.sh:39-66（LINUX_GROUPS）＋ ops/worktree_orchestrate.py:373-376,:459-463,:534（ledger 桶 + backlog-validate gate + covered tuple）＋ ops/tests/test_gate_can_fail.sh:69-84,:143-152（PROOFS + probes + 紅綠 body）＋ ops/tests/test_worktree_orchestrate.py（新增 4 條測試）＋ docs/reference/tech_index.md:131,:144 ＋ .claude/skills/kg-receipt/SKILL.md:21,:54 ＋ docs/runbook/backlog/*.json（回填 38 筆 fixed_by ＋ 22 筆 next action ＋ 1 筆越界 verdict）＋ docs/runbook/improvement_backlog.md（由 ops/backlog.py render --commit 再生成，不手改）",
  "fixed_by": [
    "bb65a92cb",
    "34f293097"
  ],
  "id": "IMP-20260805-9a51e9",
  "plan": "[2026-08-05 第五版，未通過對抗驗證但只差四處具名修正。驗證者逐段實測 24 段中 23 段今天真紅、紅因全部與本條相關、行號抽查 30+ 個全對。退回根據四點（R1/R2 為硬缺陷，R3/R4 較輕）：\n草稿的證據面比前一版硬得多——24 段我逐段單獨實測，23 段今天真紅、紅因全部與本條相關（見 segments_checked），行號抽查 30+ 個全對（含第 4 版自行更正的 :1054 / :373-376 / :180-188 三處），U1 的 IN-SYNC 前提我親自驗到 rc=0 byte-identical，C2 的 group 內容今天實跑綠。但有三處必須修才能落地，前兩處是硬缺陷：\n\n【R1 — 錯行，而且錯在「寫入之前 vs 之後」這個語意分水嶺（必修）】\nE2 與母條 fix_site 都把 `add` 守衛的落點寫成 `ops/backlog.py:1155 附近`。實測：`_cmd_add` 從 **:1136** 開始，它的**第一個語句**就是 `add_entry(...)`（:1137-1151），而 `add_entry` 在 :376 `_write_atomic(path, _dumps(payload))` 就把檔案落地了（tech_index:131 亦逐字寫 `add`(**立即落地**)）。:1155 是 `print(f\"{entry['id']} …\")` 那一行——守衛放在那裡，raise 發生在檔案已經寫進 store 之後，直接和 E2 自己那條測試的 `assert list(store.glob(\"*.json\")) == []`（plan 原文「並斷言 store 內沒有多出檔案（沒有半落地）」）矛盾。小模型照 `:1155 附近` 插碼 → 測試紅 → 最可能的反應是去弱化那條 `glob == []` 斷言（正是本條想封的形狀）。\n修法：把 E2 的落點改寫成「`_cmd_add` 起於 :1136；守衛必須插在 :1137 的 `add_entry(` 呼叫**之前**」，並加一句「`add_entry` 於 :376 即寫檔，守衛在其後等於半落地」。\n\n【R2 — 一個完全無人看守的步驟，且 step_guards 對它的宣稱是假的（必修）】\nA2 要求新增 `parse_shas(value)`。但：(a) A4b 的 `check_traceability` 規格是「`fixed_by` 非空且**每顆 sha** `probe or _git_reachable` 回 \"\"」，沒說要經過 `parse_shas`；(b) A5 的 `--fixed-by` 用 `type=lambda s: s.split()`，也不經過它。於是 `parse_shas` 是規格化的死碼：24 段鏈沒有一段碰到它，A1 的 7 條測試沒有一條碰到它。\n而 step_guards 逐字寫「A2（TRACE_FIELDS/_SHA_RE/parse_shas）→ 缺任一則 A3 的 `+ TRACE_FIELDS` 直接 NameError」——**對 parse_shas 是假的**：省略它不會在任何地方 NameError（`_SHA_RE` 另有 A1 第七條的 wont-fix 裸 sha 行為守著，只有 parse_shas 裸奔）。\n這不只是死碼，是可觸發的錯誤：`parse_shas` 存在的理由是 plan 自己寫的「同時吃 list 與空白分隔字串，讓手改過的檔也讀得回來」。既然 check_traceability 被規格成直接迭代 `fixed_by`，一個手改成 `\"a1b2c3d e4f5a6b\"`（字串）的檔會被逐字元迭代 → 每個字元各跑一次兩段式 git → 一堆假 problem；而 `if not entry.get(\"fixed_by\")` 對非空字串為真，所以連「沒填」都偵測不到。整條路徑無實作規格、無測試。\n修法：① A4b 明寫 `for sha in parse_shas(payload.get(\"fixed_by\")):`；② A1 第一條測試加一個 fixture，`fixed_by` 直接手寫成空白分隔**字串**（例如用 `json.dump` 寫檔或 `update_entry(store, id, fixed_by=\"98aa04fe6 fc2c7d454\")`），並讓它落在 problem 精確等式的 clean 側——這樣「沒接 parse_shas」立刻紅。\n\n【R3 — D7 的可執行紅/綠 proof body 不在 24 段鏈的射程內（該補一段）】\n段19/20 只是讀 `ops/tests/test_gate_can_fail.sh` 的**文字**（probes 區塊含 `docs/runbook/backlog/`、PROOFS 含 `backlog-validate`）。鏈裡沒有任何一段**執行**那支腳本。所以 D5+D6 做了、D7 的 body 寫成空 no-op，24 段照樣全綠。草稿誠實標了這點並指名 cutover 的 `ops-shell:test_gate_can_fail.sh` block gate 當權威守衛——那確實是機器守衛，但我另外查到草稿沒說的一件事，讓這個缺口比它自述的更值得補：**`gate-can-fail` 不在 CI**（`ops/tests/test_ops_ci_coverage.sh:98-116` 的 EXCLUDED_GROUPS 第 3 筆逐字 `\"gate-can-fail|data-ui-world|its ui-quality-fast proof needs the same UI World assets as ui-quality-gate\"`；我實跑 `--print-linux-groups | grep -c gate-can-fail` = 0）。也就是 D 新造的那道 gate，它的「能紅」證明**只在做 cutover 那台機器上跑過那一次，之後 CI 永遠不會再跑**。諷刺的是 C2 花力氣把 93 筆測試接進 CI，卻讓自己造的 gate 的 can-fail proof 留在 CI 之外。\n修法：鏈尾加一段 `./ops/test_ops.sh gate-can-fail`，並比照段24 誠實標注「今天就綠，判別力只在 D7 的 body」；或在 C3 順手評估把 `gate-can-fail` 的排除理由重驗（那是另一條 IMP-0054 形狀的工作，可不在本條）。\n\n【R4 — 段23 的 fail-open 方向（一行可補）】\n`test \"$(grep 'backlog.py add' SKILL.md | grep -c -- '--resolution')\" -eq \"$(grep -c 'backlog.py add' SKILL.md)\"`：兩邊同為 0 時 rc=0。今天 total=2 withflag=0 所以紅（我實測），但這段無法區分「旗標寫進文件了」與「那兩處提及被刪掉了」。加 `&& test \"$(grep -c 'backlog.py add' .claude/skills/kg-receipt/SKILL.md)\" -ge 2` 即封。\n\n【不構成 REVOKE 但下一版該順手改的小事】\n- 段24 今天 rc=0（`0 problems`）。草稿已具名披露且鏈在段1 就斷，我不因此扣分；但它與段9 對「回填缺漏」判別力重疊，可考慮直接寫成「段24 = 回填 regression，與段9 冗餘，保留是為了讓 CLI 入口本身也被跑過」。\n- 計時宣稱偏高：草稿說新 group 約 16s（migration 50 passed / 7.62s）。我實跑 `test_backlog.py + test_backlog_migration.py` = **93 passed in 1.20s, rc=0**。筆數（43+50=93）對，秒數是冷 uv 啟動的數字，別當 CI 成本依據。\n- A 的 plan 引 `:285` 當「另一筆 status=fixed 空 resolution 的 fixture」作為「規則不可進 validate_store」的理由：:282-289 是 `test_list_filters_by_status`，它根本不呼叫 `validate_store`，對該結論零約束力。真正咬住的只有 `:233-241` 的 `test_validate_is_clean_on_a_healthy_store`（我讀過，逐字 `assert BACKLOG.validate_store(store) == []`，3 筆 open + resolution=\"\"）。刪掉 :285 那個引用，別讓下一位以為有兩道約束。\n- E6 的 render 決定性理由引 `list_entries` 為 `:457-499`，notes 同一件事寫 `:469-499`。實測 `def list_entries` 在 **:469**。統一成 :469。\n- `check_traceability` 的 problem dict 用 `\"id\"` 當鍵（A1 測試 `p[\"id\"]`），而既有 `validate_store` 的 problem 一律帶 `\"path\"`（:282 `{**problem, \"path\": str(path)}`）。兩種形狀並存不算錯（測試會擋住走錯的實作），但 A4b 該明寫「problem 必帶 `id` 與 `kind`」，免得實作者照既有慣例寫 `path` 然後去改測試。\n\n[無人看守的步驟] 共盤 5 個子項、24 個會改動 repo 的步驟。**有守且我驗過斷言碰得到的 21 個**：B1→段5/6；B2→段5（三路：檔案路徑精確等式、CLI rc=64、合法值迴圈擋「一律拒絕」）；B3→段6（`validate_store(DEFAULT_STORE)==[]`，IMP-20260805-0fc07a 的 `FIXED-ROOT-CAUSE` 不在 VERDICTS :116-122 ∪ `DUPLICATE-OF-` :123）；A1→段7-13；A2 之 TRACE_FIELDS→A3 的 `+ TRACE_FIELDS` 真 NameError（exec_module :44 在 collection 期就爆）；A2 之 `_SHA_RE`→段13 行為守；A3→段10 + 既有 :564 雙向等式（我讀過，會紅）；A4a→段8；A4b→段7；A5→段10；A6→段11；A7→段12（且我確認該 fixture 在 `validate_store` 下零 problem，rc 0→2 的唯一來源就是這條接線）；A8→段9+段24；A9→段21；C1→段2；C2 存在→段1，C2 內容選擇→段4 的精確集合等式（讀 `ops/test_ops.sh` 真實文字 vs 檔案系統 glob）；C3→段3（外加 :152 的 unclassified）；C4→段22；C5→段4；D1→段17（比對 cmd 不只比 name，擋空殼）；D2→段18；D3/D4→段17/18；D5→段19；D6→段20；E1→段14；E2→段15；E3→段24+段9；E4→段23；E5→段16；E6→段16。\n\n**無人看守 / 守衛宣稱為假的步驟 —— 2 個：**\n\n【U-1（硬）：A2 的 `parse_shas()`】\n24 段沒有一段碰到它，A1 的 7 條測試沒有一條碰到它，A4b 的規格不經過它（逐字「`fixed_by` 非空且每顆 sha `probe or _git_reachable` 回 \"\"」），A5 的 `--fixed-by` 也不經過它（`type=lambda s: s.split()`）。step_guards 對它的宣稱「缺任一則 A3 的 `+ TRACE_FIELDS` 直接 NameError」**經實測為假**——`TRACE_FIELDS` 與 `parse_shas` 是兩個獨立定義，省略後者不會在任何地方 NameError。而它存在的理由（plan 逐字「同時吃 list 與空白分隔字串，讓手改過的檔也讀得回來」）是一條真實路徑：`fixed_by` 若被手改成 `\"a1b2c3d e4f5a6b\"`，被直接迭代就變逐字元 probe（每個字元各跑一次兩段式 git → 一堆假 problem），而 `if not entry.get(\"fixed_by\")` 對非空字串為真、連「沒填」都測不出來。\n**該補的斷言**：① A4b 明文改成 `for sha in parse_shas(payload.get(\"fixed_by\")):`；② A1 第一條測試新增一個 fixture，其 `fixed_by` 是**空白分隔字串**（`BACKLOG.update_entry(store, hand[\"id\"], fixed_by=\"98aa04fe6 fc2c7d454\")`，兩顆都放進 `_probe` 的可達集合），並讓它落在 problem 精確等式的 **clean 側**——這樣「沒接 parse_shas 而直接迭代字串」會產出一堆 `fixed-commit-unreachable`，等式立刻紅；同時「完全沒寫 parse_shas」會 NameError。這一條同時把 U-1 從「死碼」變成「有語意的守衛」。\n\n【U-2（次要，草稿已誠實標注，但比它自述的更空）：D7 的紅/綠 proof body】\n24 段裡**沒有任何一段執行** `ops/tests/test_gate_can_fail.sh`；段19/20 只是 pytest 讀該檔的**文字**（probes 區塊含 `docs/runbook/backlog/`、PROOFS 區塊含 `backlog-validate` 且 2/3 欄是 executed kind）。所以 D5+D6 做了、D7 的 body 寫成空 no-op 或漏寫，24 段全綠。草稿指名 cutover 的 `ops-shell:test_gate_can_fail.sh` block gate 當權威守衛，那是真的機器守衛（該腳本 :173-174 登記 declared_cheap、:417/:427 對帳、`assert_red`/`assert_green` 強制 `grep -qF` marker，空 no-op 過不了）；但草稿沒查到、我查到的是：**`gate-can-fail` 在 `ops/tests/test_ops_ci_coverage.sh:98-116` 的 EXCLUDED_GROUPS 內**（逐字 `\"gate-can-fail|data-ui-world|its ui-quality-fast proof needs the same UI World assets as ui-quality-gate\"`；`--print-linux-groups | grep -c gate-can-fail` = 0）。⇒ D 新造的那道 gate，它的「能紅」證明只在做 cutover 的那台機器上跑過一次，之後 CI 永不再跑。\n**該補的斷言**：鏈尾加 `./ops/test_ops.sh gate-can-fail`，並比照段24 誠實標「今天就綠，判別力只在 D7 的 body 與 D5/D6 的一致性」。（把 `gate-can-fail` 的 CI 排除理由重驗是另一條 IMP-0054 形狀的工作，不必塞進本條。）\n\n【另外一個「守衛存在但方向不完整」的點（不算 unguarded，列出供補）】\n段23 的 `test A -eq B` 在兩邊同為 0 時 rc=0。今天 total=2/withflag=0 所以紅，但它無法區分「E4 做了」與「那兩處 `backlog.py add` 提及被刪了」。補 `&& test \"$(grep -c 'backlog.py add' .claude/skills/kg-receipt/SKILL.md)\" -ge 2` 即封閉。\n\n[逐段實測摘要] HEAD=caca86e6c，cwd=/Users/chenliangyu/project/kg，/bin/zsh，前景阻塞，rc 直取（不經管線）。24 段全部我今天親自跑，零沿用。\n\n段1 `./ops/test_ops.sh backlog` → **rc=1**。輸出逐字 `════════ backlog ════════` / `✗ unknown test group: backlog` / `passed groups: 0` / `failed groups: 1` / `elapsed: 0s` / `failed: backlog`。紅因：`ops/test_ops.sh` 的 `run_one` case 標籤無 `backlog)`，落到 `*) return 64`（:172，工作目錄版）。\n段2 `awk '/^DEFAULT_TESTS=\\(/…' ops/test_ops.sh | grep -qx backlog` → **rc=1**。同一 awk 解出 **38** 行；**反向控制** `grep -qx release` → **rc=0**。紅因：DEFAULT_TESTS（:24-63）無 backlog，非解析器故障。\n段3 `./ops/tests/test_ops_ci_coverage.sh --print-linux-groups | grep -qx backlog` → **rc=1**。腳本自身輸出 **24** 行。紅因：LINUX_GROUPS（:39-66）無 backlog。\n段4 `pytest -q ops/tests/test_backlog.py -k test_the_backlog_group_runs_every_backlog_test_file` → **rc=5**，`43 deselected in 0.02s`。\n段5 `-k test_validate_rejects_an_out_of_vocabulary_verdict` → **rc=5**，`43 deselected in 0.01s`。（最接近的既有 id 是 `test_validate_rejects_out_of_vocabulary_values`，`an_out_of` vs `out_of`、`verdict` vs `values`，非子字串。）\n段6 `-k test_the_real_ledger_has_no_out_of_vocabulary_verdict` → **rc=5**，`43 deselected in 0.02s`。\n段7 `-k test_a_fixed_entry_must_name_a_reachable_commit` → **rc=5**，`43 deselected in 0.02s`。\n段8 `-k test_a_commit_that_is_not_an_ancestor_of_head_is_not_evidence` → **rc=5**，`43 deselected in 0.02s`。\n段9 `-k test_the_real_ledger_is_fully_traceable` → **rc=5**，`43 deselected in 0.02s`。\n段10 `-k test_update_can_set_fixed_by_through_the_cli` → **rc=5**，`43 deselected in 0.03s`。\n段11 `-k test_show_prints_the_commits_that_fixed_it` → **rc=5**，`43 deselected in 0.02s`。\n段12 `-k test_validate_reports_a_fixed_entry_with_no_commit` → **rc=5**，`43 deselected in 0.02s`。\n段13 `-k test_a_wont_fix_entry_must_state_a_reason_that_is_not_a_bare_sha` → **rc=5**，`43 deselected in 0.03s`。\n段14 `-k test_an_unfinished_entry_must_state_a_next_action` → **rc=5**，`43 deselected in 0.03s`。\n段15 `-k test_add_refuses_an_open_entry_without_a_next_action` → **rc=5**，`43 deselected in 0.03s`。\n段16 `-k test_the_generated_view_matches_the_store` → **rc=5**，`43 deselected in 0.03s`。\n（段4-16 的共同紅因由 `--collect-only` 證實：該檔今天 **43 tests collected, rc=0**，38 個 `def test_` + 7 個參數化展開；13 個新選擇器無一是任何既有 id 的子字串 ⇒ 紅是名字不存在，不是選中別的測試、不是收集失敗。）\n段17 `pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_plan_backlog_ledger_change_selects_backlog_validate` → **rc=5**，`151 deselected in 0.11s`。實質紅因（靜態核對）：`ops/worktree_orchestrate.py` 桶只有 :373 ios / :374 ds / :375 docs(.md) / :376 backend(.py) / :477 ops_py / :506 ops_sh，全檔無 `docs/runbook/backlog` 字串。\n段18 `-k test_gate_plan_backlog_ledger_change_is_covered` → **rc=5**，`151 deselected in 0.05s`。實質紅因：:534 covered tuple 逐字 `(ios, ds, docs, backend, ops_py, ops_sh)`；:537 綠 note、:539 產生 `no gate and no neutral rule covers: …`；NEUTRAL_RULES :279 無 ledger 規則。\n段19 `-k test_gate_can_fail_probes_a_ledger_change` → **rc=5**，`151 deselected in 0.04s`。實質紅因：`ops/tests/test_gate_can_fail.sh:143-152` 的 probes 今天 10 筆（ios×3、tech_index.md、backend×2、ops/worktree_orchestrate.py、ops/docs_lint.sh、design-system/tokens.json、README.md），**無 `docs/runbook/backlog/`**。\n段20 `-k test_gate_can_fail_declares_the_ledger_gate_in_both_directions` → **rc=5**，`151 deselected in 0.06s`。實質紅因：:69-84 的 PROOFS 今天 14 筆，逐一讀過，**無 `backlog-validate`**。\n段21 `grep '^| \\`backlog.py\\` |' docs/reference/tech_index.md | grep -q 'fixed_by'` → **rc=1**。producer 命中 1 行（我另量 `grep -c` = 1），consumer 未命中 ⇒ fail-closed 方向，非空管線。該列今天通篇無 `fixed_by`（末段逐字止於「測試 `ops/tests/test_backlog.py` + `ops/tests/test_backlog_migration.py`(含對真實 ledger 的全欄位 round-trip)」）。\n段22 `grep '^| \\`test_ops.sh\\` |' docs/reference/tech_index.md | grep -q '\\`backlog\\`'` → **rc=1**。producer 命中 1 行；該列逐條列 group 名稱（…/`docs-lint` / `ios-ops` / …），無 `` `backlog` ``。另查無假命中風險：`` `ops/tests/test_backlog.py` `` 不含「反引號+backlog+反引號」形狀。\n段23 `test \"$(grep 'backlog.py add' SKILL.md | grep -c -- '--resolution')\" -eq \"$(grep -c 'backlog.py add' SKILL.md)\"` → **rc=1**，我另印出 `withflag=0 total=2`。紅因：`grep -n 'backlog.py add' .claude/ski]\n\n以下為第五版草稿（disposition=split，5 個子項 A 可回溯性 / B verdict 詞彙 / C 測試 group / D cutover 路由 / E 未結案 next action），修掉 R1-R4 即可蓋章：\n本條拆為 5 筆子項執行（A 可回溯性、B verdict 詞彙、C 測試 group、D cutover 路由、E 未結案 next action），每筆自成可執行 entry，各自的 plan / acceptance / step_guards 見子項。母條本身不改任何檔（純拆分）。\n\n【落地順序】B → A → C → D → E。理由與依賴：\n- B 最小且與其他四筆零耦合（只碰 `ops/backlog.py:190-205` 的 `_check_vocabulary`），先做可先拿一次紅轉綠。\n- A 建立 `fixed_by` 欄位、`check_traceability()` 與 CLI 接線，是 D、E 的前置：D 的 gate 命令是 `ops/backlog.py validate`（沒有 A 的接線它是空 gate），E 的「未結案要有 next action」規則寫在 A 建的 `check_traceability` 的 else 分支裡。\n- C 把 backlog 測試檔接進 `./ops/test_ops.sh` 與 CI 分類面。**C 與 D 之間沒有依賴**：D 的四條新測試寫在 `ops/tests/test_worktree_orchestrate.py`，而該檔已由既有 group `worktree-orchestrator`（`ops/test_ops.sh:92-97`）跑、且該 group 已在 `ops/tests/test_ops_ci_coverage.sh:39-66` 的 LINUX_GROUPS（:48）內、被 `.github/workflows/ops-suite.yml:49` 動態展開，所以 D 的測試天生就在 CI 上，不靠 C。\n- E 必須整筆（規則 + 回填 + `add` 入口守衛 + 再生成投影）同一個 commit 落地：規則先落地而回填未做，會讓 A 的 `test_the_real_ledger_is_fully_traceable` 與 `./ops/backlog.py validate` 立刻轉紅。\n\n【第 4 版相對第 3 版的兩處實質修正：兩個「必要卻無人看守的 repo 改動」】\n\n**修正 1（U1）：`docs/runbook/improvement_backlog.md` 的再生成，第 3 版完全沒提，現在是 E 的具名步驟 E5，並由新測試 E6 永久釘住。**\n事實鏈（皆已核對）：該檔是 `ops/backlog.py render` 的產出（`docs/registry.yml:575-583`：`kind: generated` / `generator: ops/backlog.py` / `triggers: improvement_filed, improvement_resolved`），檔頭 `docs/runbook/improvement_backlog.md:11-13` 逐字寫「⚠️ **GENERATED — 不要手改這個檔。**」。`render_view()`（`ops/backlog.py:993-1017`）的 IMP 表走 `_render_table(imp, LEGACY_COLUMNS)`（:1008），該表**鎖在 legacy 8 欄**（理由見 :972-976 的註解：留住 importer 的 round-trip 可逆性），`resolution` 是第 8 欄（同一組 8 欄在 `ops/tests/test_backlog_migration.py:38-41` 的 HEADER 逐字可見）。抽樣核對：`docs/runbook/improvement_backlog.md:123`（IMP-20260805-24146e，status `open`）該列末尾逐字是 `| — |`。所以 **E3 回填 22 筆空 resolution ⇒ 該檔必然有 22 列改變**；A8 的 `fixed_by` 與 B3 的 `verdict` 都不是表格欄位，不造成投影漂移——精確地說，漂移由 E3 造成。\n而今天沒有任何機器看得到這件事：`ops/docs_lint.sh:180-188` 對 `kind: generated` 只驗「generator 檔存在」，沒有 drift 檢查；cutover 的 docs 桶是 `p.startswith(\"docs/\") and p.endswith(\".md\")`（`ops/worktree_orchestrate.py:375`），只有在該 .md **被改動時**才生 gate，忘了改就零 gate；`ops/tests/test_backlog_migration.py:497-532` 那幾條打真 ledger 的斷言只驗它 parse 得動、round-trip 乾淨，對「內容過期」永遠綠。後果直擊 E 的目的：JSON 裡 22 筆有了 next action，而 platform-steward 與所有 agent 實際讀的那份表仍是 22 個 `—`。\n修法：E5 在 E1+E2+E3 的**同一個 commit** 跑 `./ops/backlog.py render --commit`（**不要**給 `--verified-against`：預設走 `_doc_anchor()`（`ops/backlog.py:1256-1301`）＝與 origin/main 的 merge-base，給 HEAD 會在 rebase 後變成孤立錨點，那正是 IMP-0038 的形狀）。E6 新增測試 `test_the_generated_view_matches_the_store`：從 `docs/runbook/improvement_backlog.md` 的 doc-meta 取出 `verified_against` 的值（今天是第 7 行 `verified_against: 0c9e3b7c8`），把它餵進 `BACKLOG.render_view(BACKLOG.DEFAULT_STORE, verified_against=<該值>)`，斷言結果與檔案內容**逐位元組相等**。\n兩個刻意的選擇，接手者不要改掉：\n(a) **anchor 從檔案裡取，不呼叫 `_doc_anchor()`**。呼叫它的話，origin/main 一往前 merge-base 就變，這條測試會在沒人動 ledger 的日子無故轉紅——那種測試會被停用，等於沒有。從檔案取之後，這條測試只斷言「投影內容與 store 一致」這一件事。\n(b) **用測試而不是用 `diff <(./ops/backlog.py render) docs/runbook/improvement_backlog.md` 當驗收段**：diff 形式今天就綠（該檔今天與 render 輸出 byte-identical），只能當一次性回歸哨兵；測試形式今天紅（名字不存在 ⇒ pytest 選不到 ⇒ rc=5），而且落地後被 C2 的 CI group 與 cutover 的 ops-pytest gate 永久跑，是機器守衛不是一次性驗收。\n(c) **不要把「投影是否過期」塞進 `validate_store()`**：那個函式被幾十個 `tmp_path` fixture 呼叫（例如 `ops/tests/test_backlog.py:233-241`），讓它去比對真實 repo 的檔會讓所有 fixture 荒謬地失敗；塞進 `_cmd_validate` 又只在 `store == DEFAULT_STORE` 時成立，是一個條件式特例。投影一致性的自然檢查點就是測試（它本來就只對真實 repo 斷言）。\n(d) **不要讓 `add` 自動 render**。那會讓每次 filing 都改同一個 161KB 檔，正是一筆一檔要消滅的併發衝突形狀（成因寫在 `ops/backlog.py:6-21`）。\n殘留缺口（誠實記錄，不假裝關閉）：一個**只**新增 ledger JSON、完全不碰 `ops/**` 的 commit，cutover 只會排 D 的 `backlog-validate` gate（它不驗投影），CI 也不會被觸發（`.github/workflows/ops-suite.yml:15-21` 的 paths 只有 `ops/**` / `devops.sh` / `start.sh` / 該 workflow 自己），所以那種 commit 造成的投影過期會延遲到下一次任何 `ops/**` 改動的 CI 才被抓到。這是刻意接受的代價：把「一個沒人看守的必要步驟」換成「一個延遲但必然出聲的守衛」。\n\n**修正 2（U2）：C2 的 group 內容改為只跑兩個 backlog 測試檔，並新增測試釘死這個選擇。**\n第 3 版讓新 group 跑 `ops/tests/test_backlog.py` + `ops/tests/test_worktree_orchestrate.py`。後者**已經**由 `ops/test_ops.sh:92-97` 的 `worktree-orchestrator` group 在跑，而該 group 在 LINUX_GROUPS（`ops/tests/test_ops_ci_coverage.sh:48`）內、被 workflow :49 動態展開，所以它天天在 CI 跑（實測：該檔單跑 151 passed / 69.91s；與 test_backlog.py 合跑 194 passed / 77.32s ⇒ 新 group 的 80 秒裡有約 70 秒是既有 group 的重工，每次 CI 都付）。同時真正沒人跑的是 `ops/tests/test_backlog_migration.py`（實測 50 passed / 7.62s，含 :497-532 那幾條打真 ledger 的 fidelity 斷言）：`ops/test_ops.sh`、`ops/tests/test_ops_ci_coverage.sh`、三個 workflow 全 repo grep 零命中，第 3 版落地後它仍然沒人跑。本條 detail 自己寫的病是「backlog store 的測試不在 CI 觸發面上」，而第 3 版對其中 50 筆一字未治。\n修法：C2 的 group body 逐字為\n```\n    backlog)\n      \"$UV_BIN\" run --no-project --python 3.13 --with pytest pytest -q \\\n        ops/tests/test_backlog.py \\\n        ops/tests/test_backlog_migration.py\n      ;;\n```\n（成本約 16s，淨新增 93 筆 CI 覆蓋。）C5 新增測試 `test_the_backlog_group_runs_every_backlog_test_file`，讀 `ops/test_ops.sh` 的文字，切出 `backlog)` 到下一個 `;;` 之間的區塊，把區塊裡出現的 `ops/tests/test_backlog*.py` 路徑集合與 `(ROOT/\"ops\"/\"tests\").glob(\"test_backlog*.py\")` 的實際檔名集合做**精確相等**斷言。沒有它，下一個人新增 `ops/tests/test_backlog_foo.py` 又會漏，而 acceptance 的段2（名字在 DEFAULT_TESTS）與段3（名字在 LINUX_GROUPS）都偵測不到。\n\n【兩個設計決策（裁決指定，接手者照做不要自行改道）】\n- `fixed_by` 在 JSON 檔內存成**字串陣列**（裁決原文 `fixed_by: [sha,…]`）。CLI 旗標收單一字串、由 argparse 的 `type=` 就地 `.split()` 成 list，所以 `--fixed-by \"a b c\"` 落地成 `[\"a\",\"b\",\"c\"]`。`parse_shas()` 同時吃 list 與空白分隔字串，讓手改過的檔也讀得回來。\n- 可達性的參考系是 **HEAD** 不是 origin/main（裁決逐字指定，且刻意偏離 `docs/sop/doc_sync.md:25-27`）。判準是兩段式：先 `git rev-parse --verify --quiet <sha>^{commit}`，再 `git merge-base --is-ancestor <sha> HEAD`。單段不夠——`rev-parse --verify` 對 rebase 遺下的 dangling commit 回 0，正是本條 detail 裡四個受害者被漏掉的原因（實測五顆 dangling：`7c95a02` / `3671b89` / `398046fa2` / `826f34b01` / `9826b4a06` 皆 stage1 通過、stage2 失敗；`813356b1` 連 stage1 都不過；正控 `ce5296e2c` 兩段皆通）。\n\n【三個實作陷阱（踩下去會被迫弱化規則，先寫在這裡）】\n1. **新規則一律不得放進 `validate_entry()`（`ops/backlog.py:234-261`）或 `validate_store()`（:264-284）**。`ops/tests/test_backlog.py:233-241` 的 `test_validate_is_clean_on_a_healthy_store` 用三筆 `status=open` / `resolution=\"\"` 的 fixture 斷言 `validate_store(store) == []`；:282-289 另有一筆 `status=\"fixed\"` 且 resolution 空的 fixture。規則放進去就會讓一票既有測試變紅，然後實作者會去改規則而不是改碼。可回溯性與 next-action 規則一律住在新函式 `check_traceability(store, *, probe=None)`，由 `_cmd_validate`（:1197-1210）呼叫。\n2. **E2 的 `add` 守衛只能放在 CLI 層 `_cmd_add`（:1155 附近），不可放進 `add_entry()`（:291-377）**。`ops/tests/test_backlog.py:54-74` 的 `_entry_kwargs` / `_add` 預設就是 `status=\"open\"` + `resolution=\"\"`，放進 library 會讓將近四十條既有測試同時爆。守衛的測試因此必須走真入口 `BACKLOG.main([\"add\", …])` 並斷言 rc=64（`main` 的 `except (BacklogError, ValueError)` 在 :1371-1375 把它轉成 64），同時斷言 store 內沒有多出檔案。\n3. **`check_traceability` 必須讀結構化的 `fixed_by`，不得回退去掃 `resolution` 散文找 sha**。這是裁決的核心（「resolution 原文仍權威——incidental hash 永遠進不了 fixed_by」），而且它可以被繞過而所有其他斷言照樣綠：實測一個「fixed_by 為空就掃 resolution 的 `[0-9a-f]{7,40}`」的實作，在**零回填**下真實 store 只剩 4 筆紅（IMP-0001 / IMP-0002 / IMP-0005 / IMP-20260805-afc14b），也就是實作者做 4 列而不是 38 列就能全綠。封死它的東西是 A1 第一條測試裡的 `incidental` fixture（`status=\"fixed\"`、`fixed_by` 空、resolution 散文含一顆 probe 會判為可達的 hash）加上 problem 集合的**精確等式**：掃散文的實作會判它 clean，等式立刻紅。\n\n【成本】回填後 `check_traceability(DEFAULT_STORE)` 要跑約 68 顆 sha × 2 次 git shell-out。實測 120 次同型 git 呼叫 6.2s（含 uv 啟動），所以 `./ops/backlog.py validate` 會從約 0.1s 變成約 7s。可接受（它是 cutover 的 block gate 與人工指令，不在熱路徑），本輪不做批次化——批次化會脫離裁決指定的兩段式機制。\n\n【行號更正（第 3 版寫錯的，這版是今天實際 Read 過的行）】\n- `--resolution` 的 `default=\"\"` 在 `ops/backlog.py:1054`（逐字 `p_add.add_argument(\"--resolution\", default=\"\")`），不是 :1055。\n- cutover 的桶定義在 `ops/worktree_orchestrate.py:373-376`（:373 `ios = …`、:374 `ds = …`、:375 `docs = …`、:376 `backend = …`），不是 :374-377。\n- `ops/docs_lint.sh` 的 generated 檢查是 :180-188（只驗 generator 存在），不是 :180-185。\n- E4 的理由改為「文件化的 `add` 契約必須含強制旗標」，不是「照舊範例抄的 agent 會拿到 rc=64」：`.claude/skills/kg-receipt/SKILL.md:21` 與 :54 的 `ops/backlog.py add --stream IMP ...` 都是散文裡帶省略號的提及，不是可直接複製的完整命令。\n\n[該版 acceptance]\ncd /Users/chenliangyu/project/kg && ./ops/test_ops.sh backlog && awk '/^DEFAULT_TESTS=\\(/{f=1;next} /^\\)/{f=0} f{gsub(/[[:space:]]/,\"\");if($0!=\"\")print}' ops/test_ops.sh | grep -qx backlog && ./ops/tests/test_ops_ci_coverage.sh --print-linux-groups | grep -qx backlog && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_the_backlog_group_runs_every_backlog_test_file && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_validate_rejects_an_out_of_vocabulary_verdict && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_the_real_ledger_has_no_out_of_vocabulary_verdict && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_a_fixed_entry_must_name_a_reachable_commit && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_a_commit_that_is_not_an_ancestor_of_head_is_not_evidence && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_the_real_ledger_is_fully_traceable && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_update_can_set_fixed_by_through_the_cli && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_show_prints_the_commits_that_fixed_it && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_validate_reports_a_fixed_entry_with_no_commit && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_a_wont_fix_entry_must_state_a_reason_that_is_not_a_bare_sha && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_an_unfinished_entry_must_state_a_next_action && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_add_refuses_an_open_entry_without_a_next_action && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_the_generated_view_matches_the_store && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_plan_backlog_ledger_change_selects_backlog_validate && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_plan_backlog_ledger_change_is_covered && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_can_fail_probes_a_ledger_change && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_can_fail_declares_the_ledger_gate_in_both_directions && grep '^| `backlog.py` |' docs/reference/tech_index.md | grep -q 'fixed_by' && grep '^| `test_ops.sh` |' docs/reference/tech_index.md | grep -q '`backlog`' && test \"$(grep 'backlog.py add' .claude/skills/kg-receipt/SKILL.md | grep -c -- '--resolution')\" -eq \"$(grep -c 'backlog.py add' .claude/skills/kg-receipt/SKILL.md)\" && ./ops/backlog.py validate\n\n[該版 step_guards]\n母條層級：每個子項「只做其他四筆、獨獨漏掉它（或漏掉它裡面的某一步）」時，24 段鏈哪一段轉紅。母條自身不改任何檔（純拆分），故無其他步驟需要守衛。\n\n- **漏掉 A**（fixed_by 欄位 + check_traceability + CLI 接線 + wont-fix 規則 + 回填 38 筆）→ 段7 `-k test_a_fixed_entry_must_name_a_reachable_commit` rc=5。測試存在而只漏回填 → 段9 `test_the_real_ledger_is_fully_traceable`（斷言 `check_traceability(BACKLOG.DEFAULT_STORE) == []`）與段24 `./ops/backlog.py validate` rc=2。實作改走「掃 resolution 散文」的捷徑 → 段7 的 `incidental` fixture + problem 集合精確等式咬住（注入的 probe 對 `abc1234` 回 \"\"，掃散文的實作會判它 clean，等式立刻不成立）。漏 `--fixed-by` 旗標 → 段10（`main([\"update\", …, \"--fixed-by\", …, \"--commit\"])` 會走到 argparse 的 SystemExit 2）＋既有 `ops/tests/test_backlog.py:558-564` 的 `mutable - dests == _KNOWN_FLAGLESS_FIELDS`（:564）在 `fixed_by` 進了 MUTABLE_FIELDS 卻沒有 flag 時失敗。漏 `_cmd_show` 接線 → 段11。漏 `_cmd_validate` 接線 → 段12（斷言 `main([\"validate\",\"--store\",<單筆 fixed 無 fixed_by 的 store>]) == 2`；反向已查核：該筆在 `validate_store` 下零 problem，所以 rc 由 0 變 2 的唯一來源就是這個接線）。漏 tech_index 同步 → 段21 rc=1。\n- **漏掉 B**（verdict 閉集）→ 段5 rc=5。測試在而未修真實 ledger → 段6 `test_the_real_ledger_has_no_out_of_vocabulary_verdict`（斷言 `validate_store(BACKLOG.DEFAULT_STORE) == []`）紅，因為 `IMP-20260805-0fc07a` 的 verdict 今天是 `FIXED-ROOT-CAUSE`，而 `VERDICTS`（`ops/backlog.py:116-122`）＋ `_DUPLICATE_VERDICT_PREFIX`（:123）不含它。\n- **漏掉 C**（測試 group）→ 段1 rc=1（`unknown test group: backlog`）。只做 run_one case 而漏 DEFAULT_TESTS → 段2 rc=1。漏 LINUX_GROUPS → 段3 rc=1（另有 `ops/tests/test_ops_ci_coverage.sh:143-157` 的「每個 declared group 必須恰好分類一次」在 CI 與 `test_ops.sh ops-ci-coverage` 兩處會報 `backlog is unclassified`）。**group 內容選錯**（把已在 CI 的 `test_worktree_orchestrate.py` 塞進來重跑、或漏掉 `test_backlog_migration.py`）→ 段4 `test_the_backlog_group_runs_every_backlog_test_file` 的精確集合等式咬住（它讀 `ops/test_ops.sh` 的真實文字，不是讀計劃）。漏 tech_index 同步 → 段22 rc=1。\n- **漏掉 D**（ledger gate）→ 段17 rc=5。只做 gate 而漏 :534 的 covered tuple → 段18 紅（coverage note 會逐字回 `no gate and no neutral rule covers: docs/runbook/backlog/…json`）。漏 `ops/tests/test_gate_can_fail.sh:143-152` 的探針 → 段19 紅。漏 :69-84 的 PROOFS 宣告 → 段20 紅；反向（宣告了卻沒探針）由該腳本 :183-187 的 stale-entry 檢查咬住，未宣告則由 :166 咬住。漏寫紅/綠 proof body → `ops/tests/test_gate_can_fail.sh` 自己在 :173-174 登記 declared_cheap、:416-431 對帳，且 `assert_red`（:110-121）/`assert_green`（:123-135）都強制 `grep -qF` marker，空 no-op 過不了；而改動該腳本必然在 cutover 觸發 `ops-shell:test_gate_can_fail.sh` block gate（已用 `plan_gates` 探針實測確認：cmd 就是該腳本本身）。\n- **漏掉 E**（未結案 next action）→ 段14 rc=5。規則在而漏回填 → 段24 `./ops/backlog.py validate` rc=2 且段9 一併紅。漏 `add` 入口守衛 → 段15 rc=5。**漏掉 E5 的 `./ops/backlog.py render --commit`**（本版新增的守衛，第 3 版此處完全無人看守）→ 段16 `test_the_generated_view_matches_the_store` 紅：它從 `docs/runbook/improvement_backlog.md` 的 doc-meta 取 `verified_against`，餵進 `BACKLOG.render_view(BACKLOG.DEFAULT_STORE, verified_against=…)`，與檔案內容做逐位元組相等；E3 改了 22 筆 resolution 而沒 render，第 8 欄那 22 列必然不同（今天該檔與 render 輸出 byte-identical，`docs/runbook/improvement_backlog.md:123` 那列末尾逐字 `| — |`，所以差異必然出現）。漏 kg-receipt 同步 → 段23 rc=1。\n- **兩段今天綠與否的誠實標註**：段24 今天就綠（rc=0 / `0 problems`），它只能抓回填缺漏，不能抓規則未落地；其餘 23 段今天全紅。段16 今天紅（名字不存在，rc=5），落地後它的判別力來自真實檔案比對，不來自「將要新增的死碼」。\n- **已知殘留缺口（不假裝關閉）**：一個只新增 ledger JSON、不碰 `ops/**` 的 commit，cutover 只排 D 的 `backlog-validate`（不驗投影），CI 也不會被觸發（`.github/workflows/ops-suite.yml:15-21` 的 paths 只有 `ops/**` / `devops.sh` / `start.sh` / 該 workflow 自己），所以那類 commit 造成的投影過期會延遲到下一次任何 `ops/**` 改動的 CI 才出聲。\n\n[該版 children JSON]\n[{\"detail\": \"`ops/backlog.py` 的 `validate` 對「resolution 是不是可回溯的」零檢查：`validate_entry`（:234-261）只驗必填欄存在與 stream/category/severity/status 詞彙，所以 `status: fixed` 配空的、純散文的、或分支名的 resolution 一律 0 problems。實測受害者四筆（判準取 `ops/docs_lint.sh:403-426` 的兩段式：`git rev-parse --verify --quiet <sha>^{commit}` 再 `git merge-base --is-ancestor <sha> HEAD`）：IMP-0005（`813356b1` 物件不在 odb）、IMP-0063（位置 0 是已刪分支名 `feat/freeze-ui-world`）、IMP-0001（`7c95a02`）與 IMP-0002（`3671b89`）——後兩者物件存在但不是 HEAD 的祖先，是 PR #968 被 rebase-merge 遺下的 dangling commit，用單段 `git cat-file -e` / `rev-parse --verify` 掃會判成合法證據。同族：`wont-fix` 須附理由這條也只是散文慣例（今天 4 筆理由長度 13/151/433/171，全部合規，落地成本近乎零）。\", \"category\": \"cli\", \"severity\": \"high\", \"plan\": \"在 `ops/backlog.py` 加專用結構化欄位 `fixed_by` 與獨立檢查函式 `check_traceability()`，resolution 原文仍權威——incidental hash 永遠進不了 `fixed_by`。九個步驟：\\n\\n**A1** 在 `ops/tests/test_backlog.py` 檔尾（現有 586 行之後）新增 7 條測試，名字逐字如下，acceptance 的 `-k` 與之逐字一致：`test_a_fixed_entry_must_name_a_reachable_commit`、`test_a_commit_that_is_not_an_ancestor_of_head_is_not_evidence`、`test_the_real_ledger_is_fully_traceable`、`test_update_can_set_fixed_by_through_the_cli`、`test_show_prints_the_commits_that_fixed_it`、`test_validate_reports_a_fixed_entry_with_no_commit`、`test_a_wont_fix_entry_must_state_a_reason_that_is_not_a_bare_sha`。第一條的 fixture 與斷言形狀是本子項的核心，照抄：\\n```python\\ndef _probe(reachable):\\n    return lambda sha: \\\"\\\" if sha in reachable else \\\"unknown-object\\\"\\n\\ndef test_a_fixed_entry_must_name_a_reachable_commit(tmp_path):\\n    store = tmp_path / \\\"backlog\\\"\\n    fine = _add(store, detail=\\\"fixed and traceable\\\", status=\\\"fixed\\\")\\n    BACKLOG.update_entry(store, fine[\\\"id\\\"], fixed_by=[\\\"98aa04fe6\\\"])\\n    naked = _add(store, detail=\\\"fixed but names nothing\\\", status=\\\"fixed\\\")\\n    gone = _add(store, detail=\\\"fixed, names an object nobody has\\\", status=\\\"fixed\\\")\\n    BACKLOG.update_entry(store, gone[\\\"id\\\"], fixed_by=[\\\"deadbeef\\\"])\\n    incidental = _add(store, detail=\\\"fixed, with a reachable hash that lives only in prose\\\",\\n                      status=\\\"fixed\\\", resolution=\\\"landed in abc1234, see the PR\\\")\\n    unfinished = _add(store, detail=\\\"still open\\\", status=\\\"open\\\",\\n                      resolution=\\\"— next: 先讀 ops/backlog.py:190-205\\\")\\n    kinds = {p[\\\"id\\\"]: p[\\\"kind\\\"] for p in BACKLOG.check_traceability(\\n        store, probe=_probe({\\\"98aa04fe6\\\", \\\"abc1234\\\"}))}\\n    assert kinds == {naked[\\\"id\\\"]: \\\"fixed-without-commit\\\",\\n                     gone[\\\"id\\\"]: \\\"fixed-commit-unreachable\\\",\\n                     incidental[\\\"id\\\"]: \\\"fixed-without-commit\\\"}\\n```\\n`incidental` 與**精確等式**（`==` 不是 `in`）不可省：沒有它，一個「`fixed_by` 為空就回退掃 resolution 的 `[0-9a-f]{7,40}`」的實作會通過其他所有斷言，而回填只需做 4 列（實測：散文回退 + 零回填 ⇒ 38 筆裡 34 筆通過），整個結構化欄位的設計會被靜默丟棄而每條驗收照樣綠。`abc1234` 在 probe 裡被宣告為可達，所以掃散文的實作會判 `incidental` clean，等式立刻紅。\\n第二條測試直接驗 `_git_reachable` 的三種回傳：一顆真實可達的 commit 回 `\\\"\\\"`、一顆 dangling commit（今天 `7c95a02` / `3671b89` / `398046fa2` / `826f34b01` / `9826b4a06` 皆是：stage1 通過、stage2 失敗）回 `not-ancestor-of-head`、一顆不存在的物件（`813356b1`）回 `unknown-object`。這是本子項唯一直接測純函式的地方，其餘全部走 CLI/真 store。\\n第七條測試（wont-fix）：fixture 一筆 `status=\\\"wont-fix\\\"` + 空 resolution → `wont-fix-without-reason`；一筆 resolution 是裸 sha（例如 `\\\"1d05ed89\\\"`）→ `wont-fix-reason-is-a-bare-sha`；一筆正常散文理由 → 無 problem。\\n\\n**A2** 在 `ops/backlog.py:105`（GROOM_FIELDS）之後新增 `TRACE_FIELDS = (\\\"fixed_by\\\",)`、`_SHA_RE = re.compile(r\\\"^[0-9a-f]{7,40}$\\\")` 與 `parse_shas(value) -> list[str]`（吃 list 也吃空白分隔字串，讓手改過的檔讀得回來；空/None 回 `[]`）。\\n**A3** `MUTABLE_FIELDS`（:386-391）尾端 `+ TRACE_FIELDS`。\\n**A4** 新增 `_git_reachable(sha) -> str`（兩段式，`cwd=ROOT`，可達回 `\\\"\\\"`，否則回具名 reason）與 `check_traceability(store, *, probe=None)`（每筆依 status 分派：`fixed` 要 `fixed_by` 非空且每顆 sha `probe or _git_reachable` 回 `\\\"\\\"`；`wont-fix` 要 resolution 非空且不是裸 sha；未結案的規則由子項 E 補在同一個 else 分支）。放在 `validate_entry` 之後、`validate_store` 之前。**絕對不要**把這些規則放進 `validate_entry`/`validate_store`：`ops/tests/test_backlog.py:233-241` 用三筆 open/空 resolution 的 fixture 斷言 `validate_store(store) == []`，:285 另有一筆 `status=\\\"fixed\\\"` 空 resolution 的 fixture，放進去會讓一票既有測試變紅。\\n**A5** `p_update` 加 `--fixed-by`（`ops/backlog.py:1097` 附近，`dest=\\\"fixed_by\\\"`, `type=lambda s: s.split()`）。\\n**A6** `_cmd_show`（:1191）的欄位迴圈加 `+ TRACE_FIELDS`。\\n**A7** `_cmd_validate`（:1197-1210）把 `check_traceability(args.store)` 的結果併進 problems（rc 仍由 :1210 的 `2 if problems else 0` 決定）。\\n**A8** 回填 38 筆 `status: fixed` 的 `fixed_by`（`docs/runbook/backlog/*.json`），一律用 `./ops/backlog.py update <id> --fixed-by \\\"<sha> [<sha>…]\\\" --commit`，不要手改檔。取值規則：34 筆取 resolution **位置 0** 的 sha（該規則實測 33/34 成立；唯一例外 IMP-0028 那筆是沒加反引號的裸 sha，人工確認後再填）。四筆不可用位置 0，答案已定：IMP-0001→`98aa04fe6`、IMP-0002→`fc2c7d454`（此二者與原 dangling commit 的 `git patch-id --stable` 位元組相同，確定）、IMP-0005→`52a89295a`（寫入該 hash 的 commit `a52519c4c` 的直接父，其 docs_lint 改動與該筆 detail 逐字相符；原 literal `813356b1` 確定不可復原）、IMP-0063→五顆 `e1164bc3f` `59376883e` `e2e343a14` `8e5e79a9e` `f918d6ecc`（該工作從來沒有 squash）。回填錯不可能靜默通過：落地後 `./ops/backlog.py validate` 會逐顆跑兩段式。\\n**A9** `docs/reference/tech_index.md:131` 的 `| \\\\`backlog.py\\\\` |` 那列補上 `fixed_by` / `--fixed-by` / validate 的新語意（該列今天逐條列出子指令與欄位家族，缺這一筆下個 agent 就不知道有這個欄位）。\", \"acceptance\": \"cd /Users/chenliangyu/project/kg && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_a_fixed_entry_must_name_a_reachable_commit && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_a_commit_that_is_not_an_ancestor_of_head_is_not_evidence && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_the_real_ledger_is_fully_traceable && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_update_can_set_fixed_by_through_the_cli && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_show_prints_the_commits_that_fixed_it && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_validate_reports_a_fixed_entry_with_no_commit && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_a_wont_fix_entry_must_state_a_reason_that_is_not_a_bare_sha && grep '^| `backlog.py` |' docs/reference/tech_index.md | grep -q 'fixed_by' && ./ops/backlog.py validate\", \"acceptance_red_evidence\": \"全部在 cwd=/Users/chenliangyu/project/kg、HEAD=caca86e6c、/bin/zsh、前景阻塞、單條 timeout 180 下逐段單獨量測（★=上一輪對抗驗證者在同一顆 HEAD 的實測值；本輪我因 Bash 工具中斷無法重跑，但紅因由我親自用 Read 核對）。★`-k test_a_fixed_entry_must_name_a_reachable_commit` → rc=5，`43 deselected in 0.04s`。★`-k test_a_commit_that_is_not_an_ancestor_of_head_is_not_evidence` → rc=5，`43 deselected in 0.04s`。★`-k test_the_real_ledger_is_fully_traceable` → rc=5，`43 deselected in 0.04s`。★`-k test_update_can_set_fixed_by_through_the_cli` → rc=5，`43 deselected in 0.05s`。★`-k test_show_prints_the_commits_that_fixed_it` → rc=5，`43 deselected in 0.03s`。★`-k test_validate_reports_a_fixed_entry_with_no_commit` → rc=5，`43 deselected in 0.05s`。★`-k test_a_wont_fix_entry_must_state_a_reason_that_is_not_a_bare_sha` → rc=5，`43 deselected in 0.05s`。★`grep '^| \\\\`backlog.py\\\\` |' docs/reference/tech_index.md | grep -q 'fixed_by'` → rc=1。★`./ops/backlog.py validate` → rc=0（`0 problems`）——**今天就綠**，具名披露：它是 post-backfill regression guard，只抓「規則落地了但 38 筆沒回填」，對「規則有沒有落地」零判別力，所以它不是本子項的主要紅色證據，前八段才是。紅因確認：pytest 的七個選擇器今天一個都選不到東西——我 Read 完 ops/tests/test_backlog.py 全 586 行，列出今天全部 38 個 `def test_`（38 函式 + 7 個參數化展開 = 43 collected，與 `43 deselected` 吻合），七個新名字沒有一個是任何既有 test id 的子字串。tech_index 那段的紅因我逐字核對：:131 該列存在且通篇無 `fixed_by`。負控前提也已核對：IMP-0063 的 resolution 含 `0c9e3b7c`，該 sha 兩段式皆通（實測 `_git_reachable(\\\"0c9e3b7c\\\")` 回 `''`），所以 A1 的 `incidental` fixture 確實能殺死散文回退實作。\", \"step_guards\": \"A1（7 條測試）→ acceptance 前七段 rc=5，測試不存在。A2（TRACE_FIELDS/_SHA_RE/parse_shas）→ 缺任一則 A3 的 `+ TRACE_FIELDS` 直接 NameError，`ops/tests/test_backlog.py:44` 的 `SPEC.loader.exec_module(BACKLOG)` 在 collection 期就爆，七段全紅。A3（MUTABLE_FIELDS += TRACE_FIELDS）→ (a) A1 第一條測試裡的 `BACKLOG.update_entry(store, fine[\\\"id\\\"], fixed_by=[…])` 撞 `ops/backlog.py:401-403` 的 unknown-field raise；(b) 既有 `ops/tests/test_backlog.py:558-564` 的 `assert mutable - dests == _KNOWN_FLAGLESS_FIELDS`（:564）在 `fixed_by` 進了 MUTABLE 卻沒有 flag 時失敗——這條擋「半落地」。A4a（`_git_reachable`）→ 第二條測試三個回傳值逐一斷言；若整個函式缺席，第一條測試在 `probe or _git_reachable` 處 NameError，雙重守。A4b（`check_traceability`）→ 第一條測試首行 AttributeError。A5（`--fixed-by`）→ 第四條測試走真入口 `BACKLOG.main([\\\"update\\\", <id>, \\\"--store\\\", str(store), \\\"--fixed-by\\\", \\\"98aa04fe6 fc2c7d454\\\", \\\"--commit\\\"]) == 0` 並斷言檔內落地成 `[\\\"98aa04fe6\\\",\\\"fc2c7d454\\\"]`（list 不是 str）；旗標不存在時 argparse 直接 SystemExit 2；另外 A8 的 38 次回填命令本身就要用這個旗標，自封。A6（`_cmd_show`）→ 第五條測試用 capsys 斷言 `main([\\\"show\\\", <id>, \\\"--store\\\", str(store)])` 的輸出含該 sha；已查核 fixture 其他欄位不含該字串，斷言不會被別的來源滿足。A7（`_cmd_validate` 接線）→ 第六條測試斷言 `main([\\\"validate\\\",\\\"--store\\\",str(solo)]) == 2`，其中 solo 是單筆 `status=fixed` 且無 `fixed_by` 的 store；反向已查核：該筆在 `validate_store` 下零 problem（REQUIRED_FIELDS 齊、詞彙合法、groom 欄空使 `_check_groom` 於 :216-217 早退），所以 rc 由 0 變 2 的唯一來源就是這個接線。A8（回填 38 筆）→ 第三條測試 `check_traceability(BACKLOG.DEFAULT_STORE) == []` 與 acceptance 末段 `./ops/backlog.py validate` rc=2；想用散文捷徑省掉 34 筆的話，第一條測試的 `incidental` 精確等式先擋住（散文回退實作在真實 store 上只剩 4 筆紅，正是要封的洞）。A9（tech_index）→ acceptance 第八段 rc=1（今天實測紅）。\", \"fix_site\": \"ops/backlog.py:105 之後（TRACE_FIELDS/_SHA_RE/parse_shas）、:234-261 之後（_git_reachable/check_traceability）、:386-391（MUTABLE_FIELDS）、:1097 附近（--fixed-by）、:1191（_cmd_show）、:1197-1210（_cmd_validate）＋ ops/tests/test_backlog.py（新增 7 條測試）＋ docs/reference/tech_index.md:131 ＋ docs/runbook/backlog/*.json（回填 38 筆 fixed_by，共約 51 顆 sha）\", \"cost\": \"M\"}, {\"detail\": \"`verdict` 是宣告過的閉集（`ops/backlog.py:116-122` 的 `VERDICTS` 五值，加 :123 的 `DUPLICATE-OF-<id>` 前綴），但只在 `extract_verdict_fields` 的抽取路徑上被守：`update <id> --verdict TOTALLY-BOGUS --commit` 實測 rc=0 落地，`validate` 仍報 0 problems，因為 `_check_vocabulary`（:190-205）只驗 stream/category/severity/status。今天真實 ledger 已有一筆越界值：IMP-20260805-0fc07a 的 verdict 是 `FIXED-ROOT-CAUSE`。\", \"category\": \"cli\", \"severity\": \"med\", \"plan\": \"**B1** 在 `ops/tests/test_backlog.py` 新增兩條測試，名字逐字：`test_validate_rejects_an_out_of_vocabulary_verdict`（三路都要驗，缺一路就會留洞：① 檔案路徑——手寫一筆 verdict=`TOTALLY-BOGUS` 的 JSON 進 tmp_path store，斷言 `{p[\\\"kind\\\"] for p in BACKLOG.validate_store(store)}` **精確等於** `{\\\"bad-verdict\\\"}`；② CLI 路徑——`BACKLOG.main([\\\"update\\\", <id>, \\\"--store\\\", str(store), \\\"--verdict\\\", \\\"TOTALLY-BOGUS\\\", \\\"--commit\\\"]) == 64`，並斷言檔案位元組未變；③ 合法值迴圈——`for v in BACKLOG.VERDICTS + (\\\"DUPLICATE-OF-IMP-0001\\\",)` 逐一 update 後 `validate_store(store) == []`，這條擋「一律拒絕」的假實作）與 `test_the_real_ledger_has_no_out_of_vocabulary_verdict`（斷言 `BACKLOG.validate_store(BACKLOG.DEFAULT_STORE) == []`）。\\n**B2** `_check_vocabulary`（`ops/backlog.py:190-205`）加：`verdict` 欄位存在且非空時，必須在 `VERDICTS` 內或以 `_DUPLICATE_VERDICT_PREFIX`（:123 的 `\\\"DUPLICATE-OF-\\\"`）開頭，否則 append `{\\\"kind\\\": \\\"bad-verdict\\\", \\\"value\\\": …}`。欄位不存在或為空字串**不算問題**（它是 optional 的重新取證欄位，見 :90-92 的註解），否則所有未重驗的 entry 會一起紅。CLI 那一路不必另寫碼：`update` 走 `_merged_and_validated` → `validate_entry` → :411-413 raise ValueError → `main` 的 except（:1371-1375）回 64。\\n**B3** 修真實 ledger 唯一的越界值：`./ops/backlog.py update IMP-20260805-0fc07a --verdict CONFIRMED-OPEN --commit`。定調理由（不要留給實作者猜）：`verdict` 回答「這問題當時還在嗎」，該筆的答案是「在，而且那一輪把它修了」；「修了」由 `status: fixed` 與 resolution 承載，閉集裡沒有也不該有 `FIXED-*` 值，否則 verdict 與 status 兩欄會爭同一個事實。resolution 原文一字不動（它永遠是權威）。\", \"acceptance\": \"cd /Users/chenliangyu/project/kg && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_validate_rejects_an_out_of_vocabulary_verdict && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_the_real_ledger_has_no_out_of_vocabulary_verdict\", \"acceptance_red_evidence\": \"★（上一輪對抗驗證者在同一顆 HEAD caca86e6c、cwd=/Users/chenliangyu/project/kg、/bin/zsh、前景、timeout 180 實測；本輪我因 Bash 中斷無法重跑）`-k test_validate_rejects_an_out_of_vocabulary_verdict` → **rc=5**，`43 deselected in 0.06s`；`-k test_the_real_ledger_has_no_out_of_vocabulary_verdict` → **rc=5**，`43 deselected in 0.05s`。紅因由我本輪親自確認：Read 完 ops/tests/test_backlog.py 全 586 行，今天 38 個 `def test_` 中最接近的是 `test_validate_rejects_out_of_vocabulary_values`（:207，參數化 status/severity/category/stream 四值），與 `test_validate_rejects_an_out_of_vocabulary_verdict` 逐字不同（`an_out_of` vs `out_of`、結尾 `verdict` vs `values`），故新名字不是任何既有 id 的子字串 ⇒ 選不到 ⇒ rc=5。負控（B3 的必要性）：IMP-20260805-0fc07a 的 verdict 今天是 `FIXED-ROOT-CAUSE`，而 `VERDICTS`（ops/backlog.py:116-122）＝CONFIRMED-OPEN/PARTIAL/MISSTATED/ALREADY-FIXED/OBSOLETE，加 :123 的 `DUPLICATE-OF-` 前綴，不含它——所以 B2 落地而不做 B3，第二段必紅。\", \"step_guards\": \"B1（兩條測試）→ acceptance 兩段 rc=5（名字不存在）。B2（`_check_vocabulary` 的 verdict 檢查）→ 第一段的三路：① 檔案路徑的 kinds **精確等式** `== {\\\"bad-verdict\\\"}`（不是 `in`，所以「順手多報別的 problem」也會紅）；② CLI 路徑 rc==64（鏈路已逐段讀過：main :1360-1375 → `_cmd_update` :1213 → `_merged_and_validated` :394-414 → validate_entry :234-261 → `_check_vocabulary` :190-205，寫入在 `update_entry` :432-433 的 validate 之後，故被拒的更新留下位元組相同的檔）；③ 合法值迴圈擋「一律拒絕」。B3（修 IMP-20260805-0fc07a）→ 第二段 `validate_store(BACKLOG.DEFAULT_STORE) == []`：B2 落地後那筆越界值會被報成 `bad-verdict`，不改就紅。本子項沒有第四個會改動 repo 的步驟。\", \"fix_site\": \"ops/backlog.py:190-205（_check_vocabulary）＋ ops/tests/test_backlog.py（新增 2 條測試）＋ docs/runbook/backlog/IMP-20260805-0fc07a.json（verdict 欄）\", \"cost\": \"S\"}, {\"detail\": \"backlog store 的測試不在任何自動執行面上：`ops/backlog.py validate` 全 repo 沒有自動呼叫者（`.github/workflows/` 與 `ops/*.sh` 皆無），而 `ops/tests/test_backlog.py` 與 `ops/tests/test_backlog_migration.py` 都不屬於 `ops/test_ops.sh` 的任何 group，所以既不進 `./ops/test_ops.sh`、也不進 `.github/workflows/ops-suite.yml`（該 workflow :49 逐字跑 `./ops/test_ops.sh $(./ops/tests/test_ops_ci_coverage.sh --print-linux-groups)`）。目前只有 cutover 的 ops-pytest gate 在 `ops/backlog.py` 或該測試檔被改動時會跑到 test_backlog.py；test_backlog_migration.py（50 passed / 7.62s，含 :497-532 打真 ledger 的 fidelity 斷言）今天完全沒有任何執行面。\", \"category\": \"tool\", \"severity\": \"med\", \"plan\": \"**C1** `ops/test_ops.sh` 的 `DEFAULT_TESTS=(`（:24-63）加一行 `backlog`（建議緊接 :33 的 `worktree-orchestrator` 之後，把 pytest group 聚在一起）。\\n**C2** `run_one`（:79-174）在 :92-97 的 `worktree-orchestrator)` case 之後新增，逐字：\\n```\\n    backlog)\\n      \\\"$UV_BIN\\\" run --no-project --python 3.13 --with pytest pytest -q \\\\\\n        ops/tests/test_backlog.py \\\\\\n        ops/tests/test_backlog_migration.py\\n      ;;\\n```\\n**不要**把 `ops/tests/test_worktree_orchestrate.py` 列進來：它已由 `worktree-orchestrator` group（:92-97）跑，而該 group 在 `ops/tests/test_ops_ci_coverage.sh:48` 的 LINUX_GROUPS 內、被 workflow :49 動態展開，也就是天天在 CI 跑（實測該檔單跑 151 passed / 69.91s，與 test_backlog.py 合跑 194 passed / 77.32s ⇒ 重複列入等於每次 CI 白付約 70 秒）。真正沒人跑的是 `test_backlog_migration.py`，本 group 的價值就在把它接上（新 group 約 16s，淨新增 93 筆 CI 覆蓋）。\\n**C3** `ops/tests/test_ops_ci_coverage.sh` 的 `LINUX_GROUPS=(`（:39-66）加 `backlog`。不得放進 `EXCLUDED_GROUPS`（:98-115）——這兩個測試檔是純離線 stdlib pytest，只需 git + uv。分類必須恰好一次，:143-157 會擋。\\n**C4** `docs/reference/tech_index.md:144` 的 `| \\\\`test_ops.sh\\\\` |` 那列（逐條列舉 group 名稱）加 `backlog`，並註明它跑哪兩個檔。依 KG CLAUDE.md「改 user/agent-facing 介面 → 同 PR 同步 tech_index」，group 名稱正是 agent-facing 入口。\\n**C5** 在 `ops/tests/test_backlog.py` 新增測試 `test_the_backlog_group_runs_every_backlog_test_file`，把 C2 的檔案選擇釘死（沒有它，下一個人新增 `ops/tests/test_backlog_foo.py` 又會漏，而「名字在 DEFAULT_TESTS」與「名字在 LINUX_GROUPS」兩個斷言都偵測不到）：\\n```python\\ndef test_the_backlog_group_runs_every_backlog_test_file():\\n    text = (ROOT / \\\"ops\\\" / \\\"test_ops.sh\\\").read_text(encoding=\\\"utf-8\\\")\\n    start = text.index(\\\"\\\\n    backlog)\\\\n\\\")          # ValueError = group 不存在，紅得具名\\n    branch = text[start:text.index(\\\";;\\\", start)]\\n    listed = set(re.findall(r\\\"ops/tests/test_backlog[a-z_]*\\\\.py\\\", branch))\\n    on_disk = {f\\\"ops/tests/{p.name}\\\" for p in (ROOT / \\\"ops\\\" / \\\"tests\\\").glob(\\\"test_backlog*.py\\\")}\\n    assert listed == on_disk\\n```\\n（檔頭已 import 的是 stdlib；`re` 若尚未 import 要補，`ops/tests/test_backlog.py:24-27` 的檔頭規則只要求 stdlib-only。）\", \"acceptance\": \"cd /Users/chenliangyu/project/kg && ./ops/test_ops.sh backlog && awk '/^DEFAULT_TESTS=\\\\(/{f=1;next} /^\\\\)/{f=0} f{gsub(/[[:space:]]/,\\\"\\\");if($0!=\\\"\\\")print}' ops/test_ops.sh | grep -qx backlog && ./ops/tests/test_ops_ci_coverage.sh --print-linux-groups | grep -qx backlog && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_the_backlog_group_runs_every_backlog_test_file && grep '^| `test_ops.sh` |' docs/reference/tech_index.md | grep -q '`backlog`'\", \"acceptance_red_evidence\": \"★（上一輪對抗驗證者在同一顆 HEAD caca86e6c 逐段實測，cwd=/Users/chenliangyu/project/kg、/bin/zsh、前景、timeout 180）段1 `./ops/test_ops.sh backlog` → **rc=1**，逐字 `✗ unknown test group: backlog` / `passed groups: 0` / `failed groups: 1` / `failed: backlog`（來源：`ops/test_ops.sh:172` 的 `*) return 64 ;;` 經彙總印出）。段2 awk 解 DEFAULT_TESTS 再 `grep -qx backlog` → **rc=1**（解出 38 個 group；反向控制 `grep -qx release` → rc=0，證明解析器可用，紅不是解析壞了）。段3 `./ops/tests/test_ops_ci_coverage.sh --print-linux-groups | grep -qx backlog` → **rc=1**（腳本自身輸出 24 行）。段5 `grep '^| \\\\`test_ops.sh\\\\` |' docs/reference/tech_index.md | grep -q '\\\\`backlog\\\\`'` → **rc=1**。段4（本版新增）`-k test_the_backlog_group_runs_every_backlog_test_file` → **rc=5**（no tests ran）：rc 依 pytest 契約與同檔同形狀 11 段的實測（皆 `43 deselected`）推得，紅因由我本輪親自證明——Read 完 ops/tests/test_backlog.py 全 586 行，今天 38 個 `def test_` 沒有任何一個含這個字串。段1/2/3/5 的紅因我也逐字核對：`ops/test_ops.sh` 的 DEFAULT_TESTS 由 :24 到 :63，內無 `backlog`；`run_one` 的 case 標籤止於 :167 的 `release-surfaces)`，無 `backlog)`；`ops/tests/test_ops_ci_coverage.sh:39-66` 的 LINUX_GROUPS 無 `backlog`；`docs/reference/tech_index.md:144` 該列逐條列 group 名稱且無 `backlog`。\", \"step_guards\": \"C1（DEFAULT_TESTS）→ 段2 rc=1；該 awk 與 `ops/tests/test_ops_ci_coverage.sh:132-134` 的 DECLARED 解析器逐字相同，所以段2 綠等於那支契約測試也看得到這個 group。C2（run_one case）→ 段1 rc=1（`unknown test group`，來自 :172）。**C2 的內容選錯**（多列已在 CI 的 test_worktree_orchestrate.py、或漏列 test_backlog_migration.py）→ 段4 的精確集合等式咬住：它讀 `ops/test_ops.sh` 的真實文字切出 `backlog)` 分支，與檔案系統 glob 做 `==`；段1（group 存在且綠）與段3（名字在 LINUX_GROUPS）對這個錯誤都是盲的，這一段是唯一看得到的。C3（LINUX_GROUPS）→ 段3 rc=1；另有 `ops/tests/test_ops_ci_coverage.sh:143-157` 的「每個 declared group 恰好分類一次」在 `./ops/test_ops.sh ops-ci-coverage` 與 CI 兩處會報 `backlog is unclassified`（:152），:159-165 的 stale-entry 則擋反向。C4（tech_index）→ 段5 rc=1（今天實測紅）。C5（新測試本身）→ 段4 rc=5。\", \"fix_site\": \"ops/test_ops.sh:24-63（DEFAULT_TESTS）與 :92-97 之後（run_one 的 backlog case）＋ ops/tests/test_ops_ci_coverage.sh:39-66（LINUX_GROUPS）＋ ops/tests/test_backlog.py（新增 1 條測試）＋ docs/reference/tech_index.md:144\", \"cost\": \"S\"}, {\"detail\": \"改動 `docs/runbook/backlog/*.json` 的 commit 在 cutover 上選不出任何 gate：`ops/worktree_orchestrate.py:373-376` 的桶只有 ios / design-system / docs(.md) / backend(.py)，加上後面的 ops_py / ops_sh，ledger 的 `.json` 不落在任何一個，也不在 NEUTRAL_RULES（:279-286）內。實測 `plan_gates([\\\"docs/runbook/backlog/IMP-20260805-9a51e9.json\\\"], ops_test_exists=lambda rel: True, base=\\\"main\\\")` 只回 `review-receipts block` 與 `coverage warn`，note 逐字 `no gate and no neutral rule covers: docs/runbook/backlog/IMP-20260805-9a51e9.json`。也就是 A/B/E 建立的規則在「有人改 ledger」的那一刻沒有任何執行點。\", \"category\": \"tool\", \"severity\": \"med\", \"plan\": \"**D1** `ops/worktree_orchestrate.py` 在 :373-376 的桶定義後加 `ledger = [p for p in changed_files if p.startswith(\\\"docs/runbook/backlog/\\\") and p.endswith(\\\".json\\\")]`，並在 docs 桶的 gate 區（:459-463）之後加 `if ledger: gates.append(_shell(\\\"backlog-validate\\\", \\\"docs\\\", [\\\"ops/backlog.py\\\", \\\"validate\\\"], \\\"block\\\"))`。gate 名稱逐字 `backlog-validate`（D6 的 PROOFS 要對上）。\\n**D2** 把 `ledger` 加進 :534 的 covered tuple（今天是 `for bucket in (ios, ds, docs, backend, ops_py, ops_sh)`），否則 ledger 檔即使有 gate 仍會被 `_coverage` 報成 uncovered。\\n**D3** 在 `ops/tests/test_worktree_orchestrate.py` 新增 `test_gate_plan_backlog_ledger_change_selects_backlog_validate`：對 `plan_gates([\\\"docs/runbook/backlog/IMP-0001.json\\\"], ops_test_exists=lambda rel: True, base=\\\"main\\\")` 斷言存在一道 `name == \\\"backlog-validate\\\"`、`level == \\\"block\\\"`、`cmd == [\\\"ops/backlog.py\\\", \\\"validate\\\"]` 的 gate（比對 cmd 而不只是 name，否則空殼 gate 也會過）。體例照既有的 `test_gate_plan_real_repo_plans_review_receipts`。\\n**D4** 新增 `test_gate_plan_backlog_ledger_change_is_covered`：同一組 changed_files 下，斷言 `coverage` gate 的 note 逐字等於 `every changed file is routed to a gate or declared neutral`（`ops/worktree_orchestrate.py:537`），且 `uncovered == []`。\\n**D5** `ops/tests/test_gate_can_fail.sh:143-152` 的 probes 清單（今天 10 筆）加一筆 `[\\\"docs/runbook/backlog/IMP-0001.json\\\"]`，讓 `backlog-validate` 真的被列進 BLOCK_GATES；同時在 `ops/tests/test_worktree_orchestrate.py` 新增 `test_gate_can_fail_probes_a_ledger_change`（跨檔斷言：讀 test_gate_can_fail.sh 的文字，切出 `probes = [` 到頂格 `]` 之間的區塊，斷言含 `docs/runbook/backlog/`）。\\n**D6** `ops/tests/test_gate_can_fail.sh:69-84` 的 PROOFS（今天 14 筆）加 `\\\"backlog-validate|cheap|cheap|一筆 fixed 卻沒有 fixed_by 的合成 entry 讓它紅；未改動的 store 讓它綠\\\"`；同時在 `ops/tests/test_worktree_orchestrate.py` 新增 `test_gate_can_fail_declares_the_ledger_gate_in_both_directions`（跨檔斷言 PROOFS 區塊含 `backlog-validate` 且第 2、3 欄都是 executed kind）。\\n**D7** 在 `ops/tests/test_gate_can_fail.sh` 的「cheap proofs are executed in BOTH directions」段（:190 之後）寫真的紅/綠 proof body：先對一個乾淨的合成 store 跑 `ops/backlog.py validate --store <tmp>` 走 `assert_green`，再對同一個 store 種一筆 `status: fixed` 而 `fixed_by` 空的 entry 跑 `assert_red`。兩個 helper（:110-121 / :123-135）都**強制** marker（`grep -qF`），所以 marker 要用 validate 的真實輸出字串（例如綠向用 `0 problems`、紅向用 `fixed-without-commit`），空 no-op 過不了。\", \"acceptance\": \"cd /Users/chenliangyu/project/kg && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_plan_backlog_ledger_change_selects_backlog_validate && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_plan_backlog_ledger_change_is_covered && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_can_fail_probes_a_ledger_change && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_can_fail_declares_the_ledger_gate_in_both_directions\", \"acceptance_red_evidence\": \"★（上一輪對抗驗證者在同一顆 HEAD caca86e6c 逐段實測，cwd=/Users/chenliangyu/project/kg、/bin/zsh、前景、timeout 180；本輪我因 Bash 中斷無法重跑，四個選擇器的名字不存在我沿用其 `151 deselected` 量測）四段各為 **rc=5**，分別 `151 deselected in 0.05s` / `0.03s` / `0.03s` / `0.04s`。實質紅因（不只是名字不存在），四項我本輪逐一 Read 核對：① `ops/worktree_orchestrate.py:373-376` 的桶只有 ios/ds/docs/backend，:459-463 只有 docs 的三道 gate，全檔沒有 `docs/runbook/backlog` 這個字串 ⇒ 上一輪實跑 `plan_gates([\\\"docs/runbook/backlog/IMP-20260805-9a51e9.json\\\"], ops_test_exists=lambda rel: True, base=\\\"main\\\")` 只得 `review-receipts block` + `coverage warn`；② 同一探針的 coverage note 逐字 `no gate and no neutral rule covers: docs/runbook/backlog/IMP-20260805-9a51e9.json`（產生位置 :538-541），而 :534 的 covered tuple 今天是 `(ios, ds, docs, backend, ops_py, ops_sh)`；③ `ops/tests/test_gate_can_fail.sh:143-152` 的 probes 今天 10 筆（ios ×3、docs/reference/tech_index.md、backend ×2、ops ×2、design-system、README.md），無任何 `docs/runbook/backlog/` 路徑；④ :69-84 的 PROOFS 今天 14 筆，無 `backlog-validate`。\", \"step_guards\": \"D1（ledger 桶 + gate）→ 第一段 rc=5，落地後由該測試的 `cmd == [\\\"ops/backlog.py\\\",\\\"validate\\\"]` 斷言擋空殼。D2（:534 covered tuple）→ 第二段：coverage note 的逐字等式 + `uncovered == []`；已查核 `_coverage`（:289-…）把 covered 當全桶聯集、NEUTRAL_RULES（:279-286）六條沒有一條命中 `docs/runbook/backlog/`，所以漏了 D2 這段必紅。D3/D4（兩條新測試）→ 第一、二段 rc=5。D5（probes 清單）→ 第三段的跨檔 regex（已確認 :143 是 `probes = [`、:152 是頂格 `]`，切塊可靠）；漏了 probes 而做了 D6，`ops/tests/test_gate_can_fail.sh:183-187` 的 stale-entry 檢查會報 `backlog-validate is declared in PROOFS but plan_gates never produces it as a block gate`。D6（PROOFS）→ 第四段；反向（有 probe 沒宣告）由 :166 的 `$g is a block gate with no declared proof` 擋，:170-172 另擋 kind 非法與空 note。D7（紅/綠 body）→ 由 `ops/tests/test_gate_can_fail.sh` 自己執行時對帳：:173-174 把 `cheap|cheap` 登記進 declared_cheap，:416-431 逐項核對「宣告 cheap 的都真的跑過」，而 `assert_red`（:110-121）/`assert_green`（:123-135）強制 `grep -qF` marker，空 no-op 過不了。這道權威守衛是機器的：改動 `ops/tests/test_gate_can_fail.sh` 必然在 cutover 觸發 `ops-shell:test_gate_can_fail.sh` block gate（已用 plan_gates 探針實測確認 cmd 就是該腳本本身），所以 D7 不做完就 cutover 不過。第三、四段只是靜態 proxy，這一點誠實標明。\", \"fix_site\": \"ops/worktree_orchestrate.py:373-376（ledger 桶）、:459-463 之後（backlog-validate gate）、:534（covered tuple）＋ ops/tests/test_worktree_orchestrate.py（新增 4 條測試）＋ ops/tests/test_gate_can_fail.sh:69-84（PROOFS）、:143-152（probes）、:190 之後（紅/綠 proof body）\", \"cost\": \"M\"}, {\"detail\": \"platform-steward 的「未結案必須有 next action」是純散文不變式，而且已經破了：今天 61 筆 open / 1 筆 triaged / 1 筆 in-progress 中有 **22 筆** 的 resolution 完全空白，其餘未結案的絕大多數是破折號開頭的 next action（慣例明確、可機器化）。空的那 22 筆在 `ops/backlog.py validate` 下 0 problems，在 `list --ungroomed` 下也看不出差別——它們是「filed 了但沒人說下一步」的沉默庫存。另一半的洞在入口：`add` 的 `--resolution` 預設空字串（ops/backlog.py:1054），所以 filing 一筆沒有 next action 的 open entry 是零阻力路徑。\", \"category\": \"cli\", \"severity\": \"med\", \"plan\": \"整筆必須**同一個 commit** 落地（規則先落地而回填未做，會讓子項 A 的 `test_the_real_ledger_is_fully_traceable` 與 `./ops/backlog.py validate` 立刻轉紅）。六個步驟：\\n\\n**E1** 在子項 A 建的 `check_traceability()` 的 else 分支加規則：`status` 為 `open` / `triaged` / `in-progress` 時，resolution 必須非空（strip 後）→ 否則 `{\\\"kind\\\": \\\"unfinished-without-next-action\\\"}`；同時 `fixed_by` 必須為空 → 否則 `{\\\"kind\\\": \\\"unfinished-entry-names-a-commit\\\"}`（未完成卻指著 commit 是 status 說謊）。新增測試 `test_an_unfinished_entry_must_state_a_next_action`（fixture：一筆 open + 空 resolution、一筆 open + 破折號 next action、一筆 open + 非空 resolution 但 `fixed_by` 非空；用 problem 集合的**精確等式**比對）。**不要**把規則放進 `validate_entry`/`validate_store`：`ops/tests/test_backlog.py:233-241` 的 `test_validate_is_clean_on_a_healthy_store` 用三筆 open + `resolution=\\\"\\\"` 的 fixture 斷言 `validate_store(store) == []`，放進去會讓它與一票 fixture 立刻紅。\\n**E2** 入口守衛只加在 CLI 層 `_cmd_add`（`ops/backlog.py:1155` 附近）：`status` 為未結案三值且 `--resolution` 空白時 raise `BacklogError`，訊息要教下一步（例如 `an open entry needs a next action: pass --resolution \\\"— next: …\\\"`），由 `main` 的 except（:1371-1375）轉成 rc=64。**不可**加在 `add_entry()`（:291-377）——`ops/tests/test_backlog.py:54-74` 的 `_entry_kwargs`/`_add` 預設就是 `status=\\\"open\\\"` + `resolution=\\\"\\\"`，加在 library 會讓將近四十條既有測試同時爆。新增測試 `test_add_refuses_an_open_entry_without_a_next_action`：走真入口 `BACKLOG.main([\\\"add\\\",\\\"--store\\\",str(store),\\\"--stream\\\",\\\"IMP\\\",\\\"--date\\\",\\\"2026-08-05\\\",\\\"--source\\\",\\\"test\\\",\\\"--category\\\",\\\"cli\\\",\\\"--severity\\\",\\\"med\\\",\\\"--detail\\\",\\\"x\\\"]) == 64`，並斷言 `list(store.glob(\\\"*.json\\\")) == []`（沒有半落地）；再補一腿：同樣命令加上 `--resolution \\\"— next: 讀 ops/backlog.py:1054\\\"` 回 0 且檔案生出來（防「一律拒絕」）。\\n**E3** 回填所有未結案且 resolution 空白的 entry，一律用 `./ops/backlog.py update <id> --resolution \\\"— next: …\\\" --commit`，不要手改檔。清單用這條命令自己撈（今天實測 22 筆，但**以實查為準**——本輪 grooming 自己 file 的子項若也是 open 就會進這個集合）：\\n`./ops/backlog.py list --json | uv run --no-project --python 3.13 python -c 'import json,sys; d=json.load(sys.stdin)[\\\"entries\\\"]; print(\\\"\\\\n\\\".join(e[\\\"id\\\"] for e in d if e[\\\"status\\\"] in (\\\"open\\\",\\\"triaged\\\",\\\"in-progress\\\") and not str(e.get(\\\"resolution\\\",\\\"\\\")).strip()))'`\\n next action 的體例沿用今天已存在的多數形式：破折號開頭、一句話說清楚下一個動作與落點（例如 `— next: 先讀 ops/backlog.py:190-205，決定要不要把規則放進 check_traceability`）。不得寫成純現況敘述，也不得只貼一顆 sha（裸 sha 是 wont-fix 那條規則明文禁止的形狀，這裡同理）。\\n**E4** 同步 `.claude/skills/kg-receipt/SKILL.md`：:21 與 :54 兩處 `ops/backlog.py add --stream IMP ...` 都要把 `--resolution` 寫進去。理由是**文件化的 `add` 契約必須含強制旗標**（不是「照舊範例抄會拿到 rc=64」——這兩處是散文裡帶省略號的提及，不是可直接複製的完整命令）。\\n**E5** 同一個 commit 內跑 `./ops/backlog.py render --commit` 再生成 `docs/runbook/improvement_backlog.md`。**不要**給 `--verified-against`：預設走 `_doc_anchor()`（:1256-1301）＝與 origin/main 的 merge-base，給 HEAD 會在 rebase 後變成孤立錨點（那正是 IMP-0038 的形狀）。為什麼這是必要步驟：該檔是 `kind: generated` / `generator: ops/backlog.py` / `triggers: improvement_filed, improvement_resolved`（`docs/registry.yml:575-583`），檔頭 :11-13 逐字「GENERATED — 不要手改」，而 render 的 IMP 表鎖在 legacy 8 欄且 `resolution` 是第 8 欄（`ops/backlog.py:1008` + :972-976 的理由；抽樣 `docs/runbook/improvement_backlog.md:123` 該列末尾今天逐字 `| — |`）。E3 改 22 筆 resolution ⇒ 該檔必然有 22 列過期。\\n**E6** 新增測試 `test_the_generated_view_matches_the_store` 把 E5 永久釘住（它會被子項 C 的 CI group 與 cutover 的 ops-pytest gate 反覆執行）：\\n```python\\ndef test_the_generated_view_matches_the_store():\\n    doc = BACKLOG.DEFAULT_VIEW.read_text(encoding=\\\"utf-8\\\")\\n    anchor = re.search(r\\\"^verified_against: (\\\\S+)$\\\", doc, re.M)\\n    assert anchor, \\\"the generated view lost its doc-meta anchor\\\"\\n    assert BACKLOG.render_view(BACKLOG.DEFAULT_STORE,\\n                              verified_against=anchor.group(1)) == doc, \\\\\\n        \\\"the store moved without a re-render — run ./ops/backlog.py render --commit\\\"\\n```\\nanchor **從檔案裡取**（今天是該檔第 7 行 `verified_against: 0c9e3b7c8`），不要呼叫 `_doc_anchor()`：呼叫它的話 origin/main 一往前 merge-base 就變，這條測試會在沒人動 ledger 的日子無故轉紅，然後被停用。從檔案取之後它只斷言「投影內容與 store 一致」這一件事，而 `render_view` 是確定性的（`list_entries` 依 (date,id) 排序，`ops/backlog.py:457-499`）。\", \"acceptance\": \"cd /Users/chenliangyu/project/kg && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_an_unfinished_entry_must_state_a_next_action && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_add_refuses_an_open_entry_without_a_next_action && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_the_generated_view_matches_the_store && test \\\"$(grep 'backlog.py add' .claude/skills/kg-receipt/SKILL.md | grep -c -- '--resolution')\\\" -eq \\\"$(grep -c 'backlog.py add' .claude/skills/kg-receipt/SKILL.md)\\\" && ./ops/backlog.py validate\", \"acceptance_red_evidence\": \"★（上一輪對抗驗證者在同一顆 HEAD caca86e6c 逐段實測，cwd=/Users/chenliangyu/project/kg、/bin/zsh、前景、timeout 180）`-k test_an_unfinished_entry_must_state_a_next_action` → **rc=5**，`43 deselected in 0.03s`；`-k test_add_refuses_an_open_entry_without_a_next_action` → **rc=5**，`43 deselected in 0.04s`；SKILL.md 等式 → **rc=1**（實測 `total=2 withflag=0`）；`./ops/backlog.py validate` → **rc=0，`0 problems`——今天就綠**（具名披露：它是 post-backfill regression guard，只抓「規則落地了但 22 筆沒回填」，對「規則有沒有落地」零判別力）。☆（本版新增）`-k test_the_generated_view_matches_the_store` → **rc=5**（no tests ran）：rc 依 pytest 契約與同檔同形狀 11 段實測（皆 `43 deselected`）推得，紅因由我本輪親自證明——Read 完 ops/tests/test_backlog.py 全 586 行、列出今天全部 38 個 `def test_`，沒有任何一個含這個字串（同檔另有 `test_import_carries_forward_every_field_the_legacy_table_does_not_own`、`test_validate_catches_a_groom_claim_written_straight_into_the_file` 等，皆非子字串關係）。E5 的必要性我也親自量到前提：`diff <(./ops/backlog.py render) docs/runbook/improvement_backlog.md` 上一輪實測 **IN-SYNC（rc=0、byte-identical）**，而 `docs/runbook/improvement_backlog.md:123`（IMP-20260805-24146e，status open）該列末尾今天逐字 `| — |` ⇒ E3 回填後不 render，該列必然與 render 輸出不同。SKILL.md 的紅因我逐字核對：該檔只有 :21 與 :54 兩處 `ops/backlog.py add`，兩處皆無 `--resolution`。\", \"step_guards\": \"E1（未結案規則 + 測試）→ 第一段 rc=5（名字不存在）；規則在而回填未做 → 第五段 `./ops/backlog.py validate` rc=2，且子項 A 的 `test_the_real_ledger_is_fully_traceable`（斷言 `check_traceability(BACKLOG.DEFAULT_STORE) == []`）一併紅。E2（`_cmd_add` 守衛 + 測試）→ 第二段 rc=5；守衛若誤放進 `add_entry()`，該測試的第二腿（帶 `--resolution` 必須 rc=0 並生出檔案）仍會綠，但 `ops/tests/test_backlog.py` 既有的四十條 `_add(...)` fixture 會集體變紅，`./ops/test_ops.sh backlog`（子項 C 的 group）立刻出聲。E3（回填）→ 第五段 rc=2 + 子項 A 第三段。E4（kg-receipt 同步）→ 第四段 rc=1（今天實測紅）。**E5（`render --commit`）→ 第三段 `test_the_generated_view_matches_the_store` 紅**：它把 store 重新 render（anchor 取自檔案本身）與檔案內容做逐位元組相等，E3 改了 22 筆 resolution 而沒 render 時，legacy 表的第 8 欄那 22 列必然不同。這是第 3 版完全缺席的守衛（當時 22 段鏈、`ops/docs_lint.sh:180-188`、cutover 的 docs 桶、`ops/tests/test_backlog_migration.py:497-532` 全部看不到投影過期）。E6（測試本身）→ 第三段 rc=5。誠實標註三點：① 第五段今天就綠，其判別力只在回填缺漏；② 第三段管的是「投影內容與 store 一致」，手改那 22 個 cell 改到位也算過（結果等價於 render 輸出，且檔頭已寫明不要手改）；③ 一個只新增 ledger JSON、不碰 `ops/**` 的後續 commit，cutover 只會排 `backlog-validate`（不驗投影）、CI 也不會被觸發（`.github/workflows/ops-suite.yml:15-21` 的 paths 只有 `ops/**` / `devops.sh` / `start.sh` / 該 workflow），所以那類 commit 造成的過期會延遲到下一次任何 `ops/**` 改動的 CI 才出聲。\", \"fix_site\": \"ops/backlog.py（check_traceability 的 else 分支、:1155 附近的 _cmd_add 守衛、:1054 --resolution 的預設語意）＋ ops/tests/test_backlog.py（新增 3 條測試）＋ .claude/skills/kg-receipt/SKILL.md:21,:54 ＋ docs/runbook/backlog/*.json（回填今天實測 22 筆的 next action）＋ docs/runbook/improvement_backlog.md（由 ./ops/backlog.py render --commit 再生成，不手改）\", \"cost\": \"M\"}]",
  "resolution": "已修，且與原設計有三處刻意偏離，各有量測理由。**`fixed_by: [sha,…]`** 成為可回溯的權威欄，`resolution` 保留為權威敘述。回填 63 筆全部靠考證而非抄寫（14 筆難案派三個唯讀 agent，48 筆走 workflow 加對抗性反駁，反駁殺掉 2 個更正）。**偏離①** 參考系是「HEAD 或 main」而非原案的 HEAD：只看 HEAD 則落地後失去意義，只看 main 則每次 cutover 都誤擋一顆合法的分支本地 sha——在正常路徑上會紅的 gate 就是會被靜音的 gate。**偏離②** `open` 不要求 next action（原案要求全部未結案）：實測會在上線當天紅 40 筆，唯一清法是替沒人 triage 的工作編出 plan。**偏離③** 寫入面分層：`add`/`import` 不驗（要能表達歷史），`update` 驗，`validate` 對全 store 驗。另加 `reanchor`（patch-id 相等才改，對不上具名不猜）與 `VERDICTS` 的 at-rest 檢查。**review 抓到四個 BLOCK 全數採納**，最重的一個是 mutation 證明「HEAD 或 main」的 `or main` 那半沒有任何測試觀察得到——刪掉它整個套件照樣綠，已補見證。原 commit 宣稱的「49/14」不重現（最寬鬆 47/16、嚴格 45/18），成因是在回填中途取數。",
  "schema": "kg.backlog.entry.v1",
  "severity": "high",
  "source": "backlog CLI 稽核（5-agent deep dive）",
  "status": "fixed",
  "stream": "IMP",
  "verdict": "CONFIRMED-FIXED",
  "verified_at": "2026-08-07",
  "verified_by": "agent:main-session",
  "verified_evidence": "./ops/backlog.py validate（0 problems）+ 兩個負控"
}
