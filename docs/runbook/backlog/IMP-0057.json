{
  "acceptance": "uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k \"test_a_shell_script_outside_ops_is_routed_like_any_other or test_a_frozen_shell_script_selects_no_shell_gate or test_shell_test_convention_stops_at_ops_and_root or test_shell_syntax_skips_a_shebang_it_cannot_run or test_the_workflow_triggers_on_every_routed_script_outside_ops\"",
  "acceptance_cmd": "uv run --no-project --python 3.13 --with pytest pytest -q -p no:cacheprovider \"ops/tests/test_worktree_orchestrate.py::test_a_shell_script_outside_ops_is_routed_like_any_other\" \"ops/tests/test_worktree_orchestrate.py::test_a_frozen_shell_script_selects_no_shell_gate\" \"ops/tests/test_worktree_orchestrate.py::test_shell_test_convention_stops_at_ops_and_root\" \"ops/tests/test_worktree_orchestrate.py::test_shell_syntax_skips_a_shebang_it_cannot_run\" \"ops/tests/test_worktree_orchestrate.py::test_the_workflow_triggers_on_every_routed_script_outside_ops\"",
  "acceptance_expect_rc": 0,
  "category": "tool",
  "cost": "M",
  "date": "2026-08-04",
  "detail": "shell gate 路由這輪從 `ops/**` 擴到 repo root，但 `ops/` 與 root 之外還有 **12 支** tracked `.sh` 仍然完全無路由，其中 `backend/view_logs.sh` **已經被 `ops/test_devops.sh:319-328` 測著**（2026-08-05 更正行號,建檔時記 308-318;且那是 `bash -n` + 兩個 grep 的**純靜態檢查,從不執行**該腳本——與 IMP-0055 指控的弱覆蓋同形）——測試檔改動會觸發 gate，被測的那支不會。跟 IMP-0052 一模一樣的形狀，只差一個目錄。**設計已定，別重新辯**：普查過 12 支的 shebang（`.claude/skills/app-debug/find-polluter.sh`、`backend/restart_kg.sh`、`backend/view_logs.sh`、`frozen/2026-06-14-web-chrome-parity/` 7 支、`lab/podcast/start.sh`、`scripts/ios_token_lint.sh`）**全部是 bash，零個非 bash**，所以「`frozen/`/`lab/` 可能不是 bash 會被 `bash -n` 誤判紅」是假想風險、不成立。但「全部 `*.sh`」也不對，理由與 shebang 無關：`frozen/` 是刻意冷凍的，掛上 gate 會讓 `ops-shell-untested` advisory 永遠列著 7 個沒人打算補測試的檔，而 advisory 一旦長期有雜訊就沒人看，比沒有 advisory 更糟。正解是第三條路：**全部 `*.sh` 減去一份具名排除清單（`frozen/`）**，語法地板改成看 shebang 決定直譯器、認不出來就跳過並具名——即 `worktree_orchestrate.py:489-491` 自己寫的「an enumerated hole beats an anonymous one」。不在本 PR 做：與「生產部署命令沒有 gate」是兩個關注點（鐵律 4 偏好小單位），且該改動要自帶測試，形狀比照 `ops/tests/test_worktree_orchestrate.py` 既有的 root-routing 兩條（`:460`/`:473`,另有 `:485` 的 basename 唯一性）。**2026-08-05 盤點更正**:`git ls-files '*.sh'` = **103** 支(ops/ 44、ops/tests/ 27、ops/lib/ 18、root 2 → 進得了 gate 的 91),無 shell gate 的 12 支數字正確;但**內部結構與原述不同**——其中 **8 支已被 `NEUTRAL_RULES`(`:279-286`)宣告 neutral**(`frozen/` 7 + `.claude/skills/app-debug/find-polluter.sh` 1),真正落在 `uncovered` 的只有 **4 支**(`backend/restart_kg.sh`、`backend/view_logs.sh`、`lab/podcast/start.sh`、`scripts/ios_token_lint.sh`)。即建議的「具名排除清單」有一半已落地,只是存在於 `NEUTRAL_RULES` 而非 `:499-500` 的 filter。實跑 `plan_gates(那 12 支)` → 只產 1 個 `coverage/warn` gate 且 `covered: []`。另注意 `.claude/` 那筆 neutral 的理由寫的是「agent skill 文本,由 docs registry 涵蓋」,對一支可執行腳本不成立(同 IMP-0054 打掉的那批假理由)。",
  "fix_site": "ops/worktree_orchestrate.py:499",
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom-all@v2",
  "id": "IMP-0057",
  "plan": "全部行號皆為 2026-08-05 於 HEAD c21dd8a58 實際 Read 過的行，附錨定內容。\n\n【今日實測到的現況（照做前不必再驗）】\n- `git ls-files '*.sh'` = 103 支。ops/ 之外且含目錄的有 12 支：`.claude/skills/app-debug/find-polluter.sh`、`backend/restart_kg.sh`、`backend/view_logs.sh`、`frozen/2026-06-14-web-chrome-parity/` 下 7 支、`lab/podcast/start.sh`、`scripts/ios_token_lint.sh`。\n- 實跑 `plan_gates([\"backend/view_logs.sh\"], ops_test_exists=<真實探針>)` 只產出 1 個 `coverage` gate，`uncovered=[\"backend/view_logs.sh\"]`、`covered=[]`。`lab/podcast/start.sh`、`scripts/ios_token_lint.sh` 同形。`frozen/.../web/start.sh` 落 `neutral=[[path,\"frozen/\"]]`。\n- `ops/tests/test_view_logs.sh`、`ops/test_view_logs.sh`、`ops/tests/test_restart_kg.sh`、`ops/test_restart_kg.sh`、`ops/tests/test_start.sh`、`ops/test_start.sh`、`ops/tests/test_ios_token_lint.sh`、`ops/test_ios_token_lint.sh` **全部不存在**（已 `ls` 過）。所以本改動後這 5 支只會落 advisory，不會被判 covered。\n- 103 支的 shebang 普查：84 支 `#!/usr/bin/env bash`、13 支 `#!/bin/bash`、**6 支完全沒有 shebang**（`ops/lib/fixture_dataset_env.sh`、`ops/lib/ios_ops_commands.sh`、`ops/lib/ios_ops_core.sh`、`ops/lib/ios_ops_release.sh`、`ops/lib/ios_run_verdict.sh`、`ops/lib/review_probe_lib.sh`）。零支非 bash。**「沒有 shebang」必須繼續當 bash 跑**，否則這 6 支會從「有語法地板」退化成「skipped」，反而弱化 gate。\n- 排除 `frozen/` 後，全 repo `*.sh` 唯一 basename 碰撞是 `start.sh`（repo root `start.sh` vs `lab/podcast/start.sh`）。\n\n【步驟 1（先寫測試，看紅）】在 `ops/tests/test_worktree_orchestrate.py` 檔尾（或緊接既有 `test_no_two_routable_shell_scripts_share_a_basename`，該函式 def 在 :485）新增下列**五條**測試。函式名必須與 acceptance 的 `-k` 逐字一致。檔頭已 import `re / shutil / subprocess / pytest / Path`，並已有 `ROOT`(:39)、`MODULE`(:44)、模組級 helper `_by_name` / `_names`、`plan_gates`，不需新增 import。\n\n```python\ndef test_a_shell_script_outside_ops_is_routed_like_any_other():\n    \"\"\"`backend/view_logs.sh` 是 ops/test_devops.sh:319-328 靜態測著的那支；在 ops/ 與\n    repo root 之外，shell gate 之前完全不路由（IMP-0057，與 IMP-0052 同形）。\"\"\"\n    real = lambda rel: (ROOT / rel).is_file()  # noqa: E731\n    gates = _by_name(plan_gates([\"backend/view_logs.sh\"], ops_test_exists=real))\n    assert gates[\"ops-shell-syntax\"][\"files\"] == [\"backend/view_logs.sh\"]\n    assert gates[\"ops-shell-syntax\"][\"level\"] == \"block\"\n    # ops/tests/test_view_logs.sh 與 ops/test_view_logs.sh 都不存在，所以不得有 ops-shell: gate\n    assert not any(n.startswith(\"ops-shell:\") for n in gates)\n    assert gates[\"ops-shell-untested\"][\"files\"] == [\"backend/view_logs.sh\"]\n    assert gates[\"coverage\"][\"uncovered\"] == []\n\n\ndef test_a_frozen_shell_script_selects_no_shell_gate():\n    \"\"\"frozen/ 是刻意冷凍的快照：掛上 gate 只會讓 advisory 永遠列著 7 個沒人打算補測試\n    的檔，advisory 一有長期雜訊就沒人看。排除必須具名，不能靠 anonymous hole。\"\"\"\n    rel = \"frozen/2026-06-14-web-chrome-parity/web/start.sh\"\n    real = lambda p: (ROOT / p).is_file()  # noqa: E731\n    gates = _by_name(plan_gates([rel], ops_test_exists=real))\n    assert not any(n.startswith(\"ops-shell\") for n in gates)\n    assert gates[\"coverage\"][\"neutral\"] == [[rel, \"frozen/\"]]\n    assert \"frozen/\" in dict(MODULE.SHELL_GATE_EXCLUDED_TREES)\n\n\ndef test_shell_test_convention_stops_at_ops_and_root():\n    \"\"\"basename 慣例（ops/tests/test_<base> / ops/test_<base>）只在 ops/ 與 repo root\n    成立。放寬 filter 後 lab/podcast/start.sh 會撞上 root start.sh 的候選，兩支互相\n    「覆蓋」而沒有執行過對方一行。\"\"\"\n    assert MODULE._ops_shell_test_candidates(\"lab/podcast/start.sh\") == []\n    assert MODULE._ops_shell_test_candidates(\"backend/view_logs.sh\") == []\n    assert MODULE._ops_shell_test_candidates(\".claude/skills/app-debug/find-polluter.sh\") == []\n    # 慣例在原本適用的兩處必須毫髮無傷\n    assert MODULE._ops_shell_test_candidates(\"start.sh\") == [\"ops/tests/test_start.sh\",\n                                                            \"ops/test_start.sh\"]\n    assert MODULE._ops_shell_test_candidates(\"ops/docs_lint.sh\")[0] == \"ops/tests/test_docs_lint.sh\"\n\n\ndef test_shell_syntax_skips_a_shebang_it_cannot_run(tmp_path):\n    \"\"\"語法地板拿 bash -n 打一支非 bash 腳本會產生假紅。認不出直譯器就跳過並具名——\n    an enumerated hole beats an anonymous one。\"\"\"\n    d = tmp_path / \"misc\"\n    d.mkdir()\n    (d / \"weird.sh\").write_text(\"#!/usr/bin/env python3\\nif [[ -z ]; then\\n\")\n    out = MODULE._run_shell_syntax(str(tmp_path), [\"misc/weird.sh\"])\n    assert out[\"status\"] == \"pass\"\n    assert \"misc/weird.sh\" in out[\"summary\"]\n    assert \"skipped\" in out[\"summary\"]\n    # 沒有 shebang 的檔（ops/lib/*.sh 有 6 支）必須仍走 bash -n，不得被降級成 skipped\n    (d / \"noshebang.sh\").write_text(\"if [[ -z ]; then\\n\")\n    bad = MODULE._run_shell_syntax(str(tmp_path), [\"misc/noshebang.sh\"])\n    assert bad[\"status\"] == \"block\"\n\n\n@pytest.mark.skipif(shutil.which(\"git\") is None, reason=\"git not on PATH\")\ndef test_the_workflow_triggers_on_every_routed_script_outside_ops():\n    \"\"\"cutover 路由面與 CI 觸發面對同一個檔案必須有相同意見。ops-suite.yml 的 `ops/**`\n    只涵蓋 ops/；其餘沒有共同前綴，只能逐條列，所以它是會漂的那一半——漏一條的後果\n    就是 IMP-0052 本身（本機 gate 擋得住、CI 完全看不到那支腳本）。\"\"\"\n    wf = (ROOT / \".github/workflows/ops-suite.yml\").read_text()\n    block = wf.split(\"paths: &ops_paths\", 1)[1].split(\"pull_request:\", 1)[0]\n    listed = set(re.findall(r\"^\\s*- '([^']+)'\", block, re.MULTILINE))\n    assert \"ops/**\" in listed, \"解析不到 ops/** 代表探針壞了，不是 workflow 壞了\"\n\n    out = subprocess.run([\"git\", \"ls-files\", \"*.sh\"], cwd=str(ROOT),\n                         capture_output=True, text=True, check=True)\n    excluded = tuple(pat for pat, _ in MODULE.SHELL_GATE_EXCLUDED_TREES)\n    routed_outside_ops = [p for p in out.stdout.split()\n                          if not p.startswith(\"ops/\") and not p.startswith(excluded)]\n    assert len(routed_outside_ops) >= 5, (\n        f\"git ls-files 只給了 {len(routed_outside_ops)} 支——探針壞了\")\n    missing = [p for p in routed_outside_ops if p not in listed]\n    assert missing == [], (\n        f\"cutover 會路由這些腳本，但 .github/workflows/ops-suite.yml 不會因它們觸發：{missing}\")\n```\n\n【步驟 2】`ops/worktree_orchestrate.py`：在 `NEUTRAL_RULES` 的收尾 `)`（:286）之後、`def _coverage(`（:289）之前，插入新的 module 常數：\n\n```python\n# shell gate 掃全 repo 的 *.sh，減去這份具名排除清單。frozen/ 是刻意冷凍的快照：\n# 掛上 gate 只會讓 ops-shell-untested advisory 永遠列著 7 個沒人打算補測試的檔，\n# 而 advisory 一旦長期有雜訊就沒人看——比沒有 advisory 更糟。排除必須具名。\nSHELL_GATE_EXCLUDED_TREES: tuple[tuple[str, str], ...] = (\n    (\"frozen/\", \"凍結快照，依定義不再變更行為\"),\n)\n```\n只列 `frozen/`，**不要**列 `.claude/`。`NEUTRAL_RULES` 的 `.claude/` 理由寫的是「agent skill / worktree 工作區文本，由 docs registry 涵蓋」（:283），那對 `.claude/skills/app-debug/find-polluter.sh` 這支可執行腳本不成立。放寬後它會進 `covered`（`_coverage` :300 的 `if rel in covered: continue` 先於 neutral 判定），落 advisory，這是正解。\n\n【步驟 3】改 `ops/worktree_orchestrate.py:499-500`，現值逐字為：\n```\n    ops_sh = [p for p in changed_files\n              if p.endswith(\".sh\") and (p.startswith(\"ops/\") or \"/\" not in p)]\n```\n改為：\n```\n    ops_sh = [p for p in changed_files\n              if p.endswith(\".sh\")\n              and not p.startswith(tuple(pat for pat, _ in SHELL_GATE_EXCLUDED_TREES))]\n```\n實測效果：新增 5 支進 gate = `backend/restart_kg.sh`、`backend/view_logs.sh`、`lab/podcast/start.sh`、`scripts/ios_token_lint.sh`、`.claude/skills/app-debug/find-polluter.sh`；frozen/ 那 7 支不進。\n\n【步驟 4（與步驟 3 同一口氣，否則會製造假覆蓋）】改 `_ops_shell_test_candidates`（def 在 :262，body :264-270，現值逐字為 `base = rel.rsplit(\"/\", 1)[-1]` / `if rel in OPS_SHELL_UNROUTABLE_TESTS: return []` / `if base.startswith(\"test_\"): return [rel]` / `alias = OPS_SHELL_TEST_ALIASES.get(rel)` / `return [c for c in (f\"ops/tests/test_{base}\", f\"ops/test_{base}\", alias) if c]`）。把 basename 慣例限定在 `ops/` 與 repo root：\n\n```python\ndef _ops_shell_test_candidates(rel: str) -> list[str]:\n    \"\"\"Test files that could cover a changed shell script, most specific first.\"\"\"\n    base = rel.rsplit(\"/\", 1)[-1]\n    if rel in OPS_SHELL_UNROUTABLE_TESTS:\n        return []\n    if base.startswith(\"test_\"):\n        return [rel]  # a changed test script is its own gate（path-exact，無碰撞風險）\n    alias = OPS_SHELL_TEST_ALIASES.get(rel)\n    # basename 慣例只在 ops/ 與 repo root 成立：排除 frozen/ 後，全 repo 唯一的\n    # basename 碰撞就是 lab/podcast/start.sh vs root start.sh，兩支會解析到同一個\n    # ops/test_start.sh。其他位置只認具名 alias。\n    if not (rel.startswith(\"ops/\") or \"/\" not in rel):\n        return [alias] if alias else []\n    return [c for c in (f\"ops/tests/test_{base}\", f\"ops/test_{base}\", alias) if c]\n```\n\n【步驟 5】改 `_run_shell_syntax`（def 在 :1111，函式體到 :1132，現在無條件 `subprocess.run([\"bash\", \"-n\", str(fp)], ...)`，在 :1119-1120）改成看 shebang 決定直譯器：\n- 讀檔第一行；若不以 `#!` 開頭 → 用 `bash`（維持現狀，那 6 支 ops/lib/ 無 shebang 的檔不得退化）。\n- 若以 `#!` 開頭：取該行所有 token 中最後一個非 flag 的 basename（處理 `#!/usr/bin/env bash` 與 `#!/bin/bash`）；命中 `bash` / `sh` / `zsh` → 用該直譯器 `-n`；否則不跑，記入 `skipped` list。\n- 收尾：`bad` 非空仍回 `{\"status\": \"block\", ...}`（現有訊息格式不變）；`bad` 為空但 `skipped` 非空時回 `{\"status\": \"pass\", \"rc\": 0, \"summary\": f\"{len(files) - len(skipped)} shell script(s) parse; {len(skipped)} skipped (interpreter not recognised): \" + \", \".join(skipped)}`；兩者皆空時維持現有 `f\"{len(files)} shell script(s) parse\"`。\n本步是防未來，不是修現在——今天 103 支全是 bash 或無 shebang，判定不得改變。已存在的 `test_shell_syntax_gate_names_the_file_that_will_not_parse`（:528）與 `test_shell_syntax_gate_ignores_a_file_the_diff_deleted`（:542）用的 fixture 都寫 `#!/usr/bin/env bash`，不受影響。\n\n【步驟 6】`.github/workflows/ops-suite.yml`：`paths: &ops_paths` 錨在 :15，現有條目 `'ops/**'`(:16)、`'devops.sh'`(:19)、`'start.sh'`(:20)、`'.github/workflows/ops-suite.yml'`(:21)。在 :20 與 :21 之間逐條補上：\n```yaml\n      # ops/ 之外的腳本也進 cutover 的 shell gate（IMP-0057）。這些路徑沒有共同前綴，\n      # 只能逐條列；由 test_the_workflow_triggers_on_every_routed_script_outside_ops 釘住。\n      - 'backend/restart_kg.sh'\n      - 'backend/view_logs.sh'\n      - 'lab/podcast/start.sh'\n      - 'scripts/ios_token_lint.sh'\n      - '.claude/skills/app-debug/find-polluter.sh'\n```\n`frozen/` 不加。（`backend/view_logs.sh` 那條是關鍵：`ops/test_devops.sh:319-328` 靜態測著它，少了這行就重演 IMP-0052 的「cutover 看得到、CI 看不到」。）\n\n【步驟 7】兩處敘述性文字同 PR 更新，否則會逐字說謊：\n- `ops/worktree_orchestrate.py:342-345` 的 `plan_gates` docstring，現在寫 `ops/**.sh -> bash -n on every one of them ...`。改成說明範圍是「全 repo `*.sh` 減 `SHELL_GATE_EXCLUDED_TREES`；語法地板依 shebang 選直譯器，認不出來就 skip 並具名；basename 慣例只在 `ops/` 與 repo root，其餘只認 `OPS_SHELL_TEST_ALIASES`」。\n- `ops/tests/test_ops_ci_coverage.sh:167-169` 的註解，現在逐字寫著「cutover 的路由條件是『`.sh` 且（在 ops/ 下或在 repo root）』」。改成「`.sh` 且不在 `SHELL_GATE_EXCLUDED_TREES` 之下」，並註明較寬的那半平面現由 `ops/tests/test_worktree_orchestrate.py::test_the_workflow_triggers_on_every_routed_script_outside_ops` 釘住，本檔 :188-206 的 root-only 檢查是 bash 側的地板、刻意保留。\n\n【會不會撞到別處（已逐條查過）】\n- `ops/tests/test_worktree_orchestrate.py:485` 的 `test_no_two_routable_shell_scripts_share_a_basename` **不會紅**：它在 :501 自己算 `routable = [p for p in ... if p.startswith(\"ops/\") or \"/\" not in p]`，與 `plan_gates` 的 filter 無耦合；:496-497 的 `_ops_shell_test_candidates(\"release.sh\") == _ops_shell_test_candidates(\"ops/release.sh\")` 在步驟 4 之後兩邊都仍在「慣例適用集合」內，仍相等。只需把它的 docstring 補一句：步驟 4 之後 `routable` 述詞剛好等於 basename 慣例適用的集合，這條守衛因此變成路由與慣例同步的釘子。\n- `ops/tests/test_gate_can_fail.sh:143-152` 的 `probes` 清單只含 `ios/`、`docs/`、`backend/`、`ops/`、`design-system/`、`README.md`，沒有 ops/ 之外的 `.sh`，不受影響。\n- `ops/tests/test_ops_ci_coverage.sh:189` 的 `root_sh` 只取 `git ls-files '*.sh' | grep -v '/'`（= `devops.sh`、`start.sh`，兩支都已在 workflow），:209-212 的反向 stale 檢查對含 `/` 的條目直接 `continue`，所以步驟 6 新增的 5 條**不會**讓它轉紅。\n- 既有 `ops-shell-syntax` 的兩條 tmp_path 測試（:528、:542）不受步驟 5 影響（fixture 皆為 bash shebang，或檔案不存在）。\n- **明確不做**：不要把 `backend/view_logs.sh` 加進 `OPS_SHELL_TEST_ALIASES`（:239-247）指向 `ops/test_devops.sh`。`ops/test_devops.sh:319-328` 只是 `bash -n` + 兩個 grep 的純靜態檢查，從不執行該腳本；加了 alias 等於在 block gate 上插一筆假覆蓋，正是 IMP-0055 指控的形狀。本改動後 view_logs 只落 advisory、不會被判 covered，相依警告因此不成立。\n\n【哪一條命令由紅轉綠】\n`uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k \"test_a_shell_script_outside_ops_is_routed_like_any_other or test_a_frozen_shell_script_selects_no_shell_gate or test_shell_test_convention_stops_at_ops_and_root or test_shell_syntax_skips_a_shebang_it_cannot_run or test_the_workflow_triggers_on_every_routed_script_outside_ops\"`\n今天 exit 5（150 deselected，0 collected）。步驟 1 之後應為紅（五條全 fail/error），步驟 2-6 全做完才會五條全綠。收尾另跑一次不帶 `-k` 的全檔（本機約 140 秒、150 passed）確認無回歸。",
  "resolution": "—(2026-08-05 驗證 CONFIRMED-OPEN,行號與內部結構已更正;落點 `worktree_orchestrate.py:499-500` 的 filter + `.github/workflows/ops-suite.yml:15-21` 的 `paths:` 必須同步,否則重演 IMP-0052 的「cutover 看得到、CI 看不到」;成本 S–M,**需自帶測試**三條。**相依警告**:修本條會放大 IMP-0055——`backend/view_logs.sh` 納入路由後會靠 `test_devops.sh:319-328` 的純靜態 grep 被判 covered,等於在 block gate 上再插一筆假覆蓋;應先落 0055 的覆蓋驗證器,或把 view_logs 明列為「靜態覆蓋」而非 alias)",
  "schema": "kg.backlog.entry.v1",
  "severity": "med",
  "source": "IMP-0052 的 review",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
