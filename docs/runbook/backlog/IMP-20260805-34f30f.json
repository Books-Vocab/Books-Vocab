{
  "acceptance": "uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_schema_section_blames_rebase_not_squash",
  "category": "doc",
  "cost": "S",
  "date": "2026-08-05",
  "detail": "ledger schema（`ops/backlog.py:822-824`，render 進 `docs/runbook/improvement_backlog.md:36-38`）警告 resolution hash 會被 PR squash merge 作廢。本 repo 的落地路徑早已不是 PR：最後一個 merge commit 是 `0611f3cac`（2026-07-09），其後 196 個 commit 全單親；`90e57ba7e` 已把 `.claude/commands/merge-prs.md` 與 `review-ui.md`（合計 198 行）整個刪除，PR 合併入口是被刻意拆掉的。**但真兇不是「過期」而是「指錯機制」**：`cmd_cutover`（`ops/worktree_orchestrate.py:1910-1913`）在 advance lock 內先對分支跑 `git rebase <local main>`，之後（:1923-1928）才在主樹 `merge --ff-only`——ff 只是後半段，前半段把分支鑄的每個 sha 都改寫。證據：最近 200 個 commit 有 79 個 committer date 不等於 author date；`83843b65f`（docs: re-anchor verified_against 888dde5f0 到 e14f295ad，rebase 後 orphan hash）是 PR 時代之後真實發生過的孤兒事件，起因是 rebase 不是 squash。**這條過期教條已經害過人**：IMP-0063 的 resolution 寫著分支名加「squash 後由 platform-steward 更新為 squashed hash」，在一個永遠不會 squash 的拓樸上等一個不會來的事件，分支已刪——而該工作其實是以五個普通 commit 落地的。同型錯誤另見 `docs/sop/doc_sync.md:26`（自己分支的 HEAD 只在 ff 拓樸下滿足、squash merge 下必炸；實際上這條路線的 merge 就是 ff，而 branch HEAD 照樣被前面的 rebase 孤立）。**仍然合法的 PR 用法別一起改掉**：`ops/branch_audit.sh:126`（PR state 當 metadata，`docs/runbook/system.md:73` 明言 commit 可達性才是真相）、三個 workflow 的 `pull_request:` 觸發（休眠且無害）、`docs/sop/figma-token-workflow.md`（Figma plugin 從機外推分支，PR 是真正的跨越機制）。另有約 30 處「同 PR」是「同一個落地單位」的同義詞，屬詞彙債，改它是 churn 且有打壞 docs_impact source hint 的風險。",
  "fix_site": "ops/backlog.py:892",
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom-all@v2",
  "id": "IMP-20260805-34f30f",
  "plan": "機制已於 2026-08-05 在 HEAD=c21dd8a58 複驗：`ops/worktree_orchestrate.py:1911-1913` 在 advance lock 內對 worktree 分支跑 `_git_mutation([\"rebase\", local], cwd=worktree, label=\"cutover-rebase\")`，:1924-1926 才在主樹跑 `_git_mutation([\"merge\", \"--ff-only\", wt_branch], cwd=primary, label=\"cutover-fast-forward\")`——ff 只是後半段，前半段的 rebase 已把分支鑄的每個 sha 改寫。所以「squash merge 才會孤兒化 hash」是指錯機制。本條只做機械段；`CLAUDE.md:138`（載著 2026-06-04 standing 授權那句）明示需執行長裁決，不在此範圍。\n\n**行號已重新錨定（entry detail 內的 :822-824 已漂移）**：\n\n1. `ops/backlog.py:892-893`（`_VIEW_HEADER` 字串起於 :857，這兩行是「resolution hash 慣例」那條 bullet）。現文逐字為：\n   `- **resolution hash 慣例**：PR 合併前為 branch-local；若該 PR 採 **squash merge**，合併後由`\n   `  \\`platform-steward\\` 更新為 squashed hash，維持 audit trail 不斷。**不得以分支名代替 hash**`\n   改成：落地路徑是 `ops/worktree_orchestrate.py` 的 cutover——**先 rebase 到本地 main 再 ff**，分支上鑄的每個 sha 一律被改寫，所以 resolution 必須在 cutover 之後回填「已進本地 main 的那顆 sha」。**改寫後的 header 不得再出現 `squash` 字樣，且必須字面出現 `rebase`**（下面第 6 步的斷言就靠這兩件事）。保留既有的「**不得以分支名代替 hash**——分支刪掉之後那筆 resolution 就再也指不到任何東西」，它是對的。正確機制不要重寫，抄 `ops/backlog.py:1227-1229` `_doc_anchor` docstring 已寫好的說法（`* HEAD — rebasing (adding review trailers did exactly this) or / squash-merging orphans every sha the branch minted`）。可引的事實：本 repo 最後一個 merge commit 是 `0611f3cac`（2026-07-09），其後全為單親 commit；`90e57ba7e` 已刪掉 `.claude/commands/merge-prs.md` 與 `review-ui.md`，PR 合併入口是被刻意拆掉的。\n\n2. `_VIEW_HEADER` 是 `str.format` 模板（:966 呼叫 `.format(...)`），改文時不得引入裸的大括號（render 時會炸 KeyError/IndexError），現有佔位符 `statuses` / `imp_categories` / `app_categories` / `severities` / `verdicts` 一個都不能動。\n\n3. 改完必跑 `./ops/backlog.py render --commit` 重生 `docs/runbook/improvement_backlog.md`（registry `runbook.improvement_backlog`，kind=generated、generator=ops/backlog.py，手改無效）。目前該檔 header 段（第 1-50 行，即 `## IMP` 標題之前）在 :36-37 仍在講 squash；不重生就會留一份仍在講 squash 的產出。全檔現有 7 處 `squash`，其中 2 處在 header 段、其餘在 entry 表格 row 內（那是 entry 原文，不要動）。\n\n4. `docs/sop/doc_sync.md:26`（全檔唯一含「SHA 不被改寫」的那行，實測 1 行）。現文結尾為：`自己分支的 HEAD 只在 ff 拓樸下滿足,**squash merge 下必炸**(squash 會鑄出新 SHA,分支上每個 sha 都變 orphan)。` 改成：自己分支的 HEAD 在本 repo 同樣不滿足，因為 cutover 是**先 rebase 後 ff**，`rebase` 就把分支上每個 sha 改寫成 orphan（squash merge 也會，但本 repo 沒有 squash 路徑）。**該行改完後必須字面含 `rebase`**。同檔 :32 的「**禁止**寫只存在於 PR branch 的 ephemeral hash」把 `PR branch` 改成 `worktree 分支`——`PR branch` 在該檔只出現這 1 次（實測 count=1），改完全檔不得再有這個字串。\n\n5. `.claude/agents/docs-steward.md:4`（frontmatter `description` 的那一整行，全檔唯一含「KG 文檔管家」的行，實測 1 行）：把「也用於 **PR 開出前**的 docs 健康檢查」改成「也用於 **cutover 前**的 docs 健康檢查」；同一行第二個 `<example>` 的 `user: \"這個 PR 要開了,docs 都對齊了嗎\"`（全檔出現 1 次）改成 `user: \"這條 worktree 要 cutover 了,docs 都對齊了嗎\"`。改完該行不得再含 `PR 開出前`、必須含 `cutover`。同檔 :28「不把既有 audit debt 當本次 **PR gate**」**不要動**——那是「同一個落地單位」的同義詞用法。\n\n6. `ops/tests/test_backlog.py`（現 585 行）檔尾新增，函式名逐字為 `test_schema_section_blames_rebase_not_squash`（acceptance 選擇器與它逐字一致）。`ROOT` 在該檔 :39、`BACKLOG` 在 :42 已備妥，直接用：\n```python\ndef test_schema_section_blames_rebase_not_squash():\n    header = BACKLOG._VIEW_HEADER\n    assert \"squash\" not in header\n    assert \"rebase\" in header\n\n    view = (ROOT / \"docs\" / \"runbook\" / \"improvement_backlog.md\").read_text(encoding=\"utf-8\")\n    assert \"squash\" not in view.split(\"## IMP\")[0]\n\n    sync = (ROOT / \"docs\" / \"sop\" / \"doc_sync.md\").read_text(encoding=\"utf-8\")\n    invariant = [ln for ln in sync.splitlines() if \"SHA 不被改寫\" in ln]\n    assert len(invariant) == 1\n    assert \"rebase\" in invariant[0]\n    assert \"PR branch\" not in sync\n\n    steward = (ROOT / \".claude\" / \"agents\" / \"docs-steward.md\").read_text(encoding=\"utf-8\")\n    desc = [ln for ln in steward.splitlines() if \"KG 文檔管家\" in ln]\n    assert len(desc) == 1\n    assert \"PR 開出前\" not in desc[0]\n    assert \"cutover\" in desc[0]\n```\n   這些斷言在修改前**全部紅**（2026-08-05 實測：`\"squash\" in _VIEW_HEADER`=True、`\"rebase\" in _VIEW_HEADER`=False、view header 段含 squash=True、doc_sync 那行含 rebase=False、`PR branch` count=1、docs-steward description 含「PR 開出前」=True 且不含 cutover）。斷言逐條對應第 1/3/4/5 步，**漏做任何一步 acceptance 都仍紅**——特別是漏跑 `render --commit`（第 3 步）會卡在 view header 那條。斷言 3 只驗 `## IMP` 之前的 header 段，所以表格 row 內 entry 原文的 squash 不會誤傷。\n\n7. gate：另跑 `./ops/docs_lint.sh`（驗 registry + changed docs）確認 `ERROR: 0`。今天基線實測 exit 0 / OK:9 / WARN:2 / ERROR:0，兩條 WARN 是 `docs/sop/ios.md` 與 `docs/sop/ui_flow_evidence.md` 的 STALE，與本條無關，不要為它們動手。\n\n**不要動**：`ops/branch_audit.sh:126`（PR state 當 metadata，`docs/runbook/system.md:73` 明言 commit 可達性才是真相）、三個 workflow 的 `pull_request:` 觸發（休眠且無害）、`docs/sop/figma-token-workflow.md`（Figma plugin 從機外推分支，PR 是那條路真正的跨越機制）、其餘約 30 處「同 PR」＝「同一個落地單位」的同義詞（改它是 churn，且有打壞 docs_impact source hint 的風險）。\n\n**會連坐轉紅的既有測試**：把 `_VIEW_HEADER` 改到破格式（裸大括號 / 動到佔位符）→ `test_render_is_deterministic`（`ops/tests/test_backlog_migration.py:281`）、`test_render_emits_doc_meta_the_lint_requires`（:306）、`test_render_says_it_is_generated`（:331）。收尾用 `uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py ops/tests/test_backlog_migration.py` 全綠回歸，今日基線為 **91 passed**（改完應為 92 passed）。",
  "resolution": "—(2026-08-05 驗證 CONFIRMED-OPEN；落點 `ops/backlog.py:822-824` 加 `docs/sop/doc_sync.md:26` 加 `.claude/agents/docs-steward.md:4,28`，成本 S——正確機制已經寫在 `ops/backlog.py:1126` 的 `_doc_anchor` docstring 裡，是抄過去不是寫新文。**另有一句需要執行長裁決、本條不自決**：`CLAUDE.md:138`「驗證全綠後直接 commit 加開 PR，不先問」是最後一條把 agent 導向已被拆除入口的指令，但同一句同時載著 2026-06-04 的 standing 授權，改動它等於改自主權政策。驗收：機械段可命令化（grep 那兩處必須同時具名 rebase/cutover 為主因），其餘是判斷不是命令——「有沒有 always-on 表面還在叫 agent 開 PR」grep 不出來，因為 40 個 hit 裡約 30 個是合法或同義詞用法。)",
  "schema": "kg.backlog.entry.v1",
  "severity": "med",
  "source": "backlog CLI 稽核（5-agent deep dive）",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
