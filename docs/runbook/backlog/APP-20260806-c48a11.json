{
  "acceptance": "紅（今天）：`AppAnalytics.swift` 沒有 podcast leg 的 enum case、沒有對應的 switch arm、沒有 `logger.info(\"event=podcast_catalog_sync_*`；`runPodcastLeg` 函式區段內沒有任何 `AppAnalytics.track(.podcastCatalogSync…` 呼叫；`PodcastSyncService.swift` 的 detail 計數 log 仍被「有跳過才印」的守衛包著（12 格縮排、無 `total=`）。判準 rc=1。\n\n綠（修完）需**同時**成立：\n1. `AnalyticsEvent` 內確實有 `case podcastCatalogSyncStarted` 與 `case podcastCatalogSyncCompleted(durationMs: Int, outcome: String)` 兩個**宣告行**（`^    case …$` 錨定，寫在註解裡不算）；\n2. `track(_:)` 的 switch 內確實有 `case .podcastCatalogSyncStarted:` 與 `case .podcastCatalogSyncCompleted(` 兩個 **arm**（`^        case \\.…` 錨定）；\n3. 兩條 `logger.info(\"event=podcast_catalog_sync_*` 是**程式行**（`^ +logger\\.info\\(` 錨定，`// logger.info(…` 這種註解不算）；\n4. 兩個 `AppAnalytics.track(.podcastCatalogSync…` 呼叫落在 **`runPodcastLeg` 的函式區段內**（awk 只餵該函式的 27 行給 grep，放進別處的死程式不算）；\n5. 該區段內 `KGFeatureFlags.podcastEnabled` 與 `JWTExpiry.isExpired` 兩道 early return **都排在 started 事件之前**（Release 零 podcast 足跡不變式）；\n6. detail 計數 log 是 `^ {8}AppLog.kg.info(\"[PodcastSync] detail fetch total=….*stale=`＝**方法本體頂層、無條件**（塞回任何 `if` 內即 12 格縮排，紅）；\n7. 四個正控仍在（`case backgroundSyncCompleted(durationMs: Int, success: Bool)`、其 switch arm、`private static func runPodcastLeg(`、`Self.fetchDetailsConcurrently(seriesIds:`）——檔案沒被搬走、已 land 的併發機制沒有退回序列迴圈。\nrc=0。\n\n行為層（機器判準不涵蓋，收尾人工做）：kg-pool-2 冷啟動的 log stream 裡，podcast 段從「一片空白」變成一對可讀的 started／completed + duration_ms，第二輪同步印出 `stale=0`。",
  "acceptance_cmd": "set -x; set -eu\nA=ios/BooksAndVocab/Services/AppAnalytics.swift\nS=ios/BooksAndVocab/Services/KGService+Sync.swift\nP=ios/BooksAndVocab/Services/PodcastSyncService.swift\nLEG='/private static func runPodcastLeg\\(/{f=1} f{print} f&&/^    \\}$/{exit}'\ngrep -qF 'case backgroundSyncCompleted(durationMs: Int, success: Bool)' \"$A\"\ngrep -qE '^        case \\.backgroundSyncCompleted\\(let durationMs, let success\\):$' \"$A\"\ngrep -qF 'private static func runPodcastLeg(' \"$S\"\ngrep -qF 'Self.fetchDetailsConcurrently(seriesIds:' \"$P\"\ngrep -qE '^    case podcastCatalogSyncStarted$' \"$A\"\ngrep -qE '^    case podcastCatalogSyncCompleted\\(durationMs: Int, outcome: String\\)$' \"$A\"\ngrep -qE '^        case \\.podcastCatalogSyncStarted:$' \"$A\"\ngrep -qE '^        case \\.podcastCatalogSyncCompleted\\(' \"$A\"\ngrep -qE '^ +logger\\.info\\(\"event=podcast_catalog_sync_started' \"$A\"\ngrep -qE '^ +logger\\.info\\(\"event=podcast_catalog_sync_completed duration_ms=' \"$A\"\nawk \"$LEG\" \"$S\" | grep -qF 'AppAnalytics.track(.podcastCatalogSyncStarted'\nawk \"$LEG\" \"$S\" | grep -qF 'AppAnalytics.track(.podcastCatalogSyncCompleted('\nawk '/private static func runPodcastLeg\\(/{f=1}\n     f{n++\n       if (/KGFeatureFlags\\.podcastEnabled/ && !g) g=n\n       if (/JWTExpiry\\.isExpired/ && !j) j=n\n       if (/AppAnalytics\\.track\\(\\.podcastCatalogSyncStarted/ && !t) t=n}\n     f && /^    \\}$/{exit}\n     END{exit (g>0 && j>0 && t>0 && g<t && j<t) ? 0 : 1}' \"$S\"\ngrep -qE '^ {8}AppLog\\.kg\\.info\\(\"\\[PodcastSync\\] detail fetch total=.*stale=' \"$P\" || { rc=$?; echo \"FAIL: APP-20260806-c48a11 acceptance clause failed rc=$rc\"; exit 1; }",
  "acceptance_expect_rc": 0,
  "brief": "開發測試版一開 app 的同步會一個一個慢慢抓播客資料、白花六秒；正式使用者不跑這段，被拖累的是我們自己的效能量測。",
  "build": "sim build 2026-08-06 06:18 (main 917ad3e4b)",
  "category": "perf",
  "date": "2026-08-06",
  "detail": "review-event watermark 落地後，冷啟動 backgroundSync 仍回報 duration_ms=7773，但所有有 log 的工作在 trigger 後 1.1s 就結束（11:05:07.496 triggered → 11:05:08.567 最後一條 Sync log → 11:05:15.269 completed），中間 6.7s 完全沒有任何 log。排水那輪同形狀：11:04:09.8 推完 → 11:04:21.8 才 completed，尾巴約 12s。也就是 15.4s 的 review-event push 拿掉之後，下一個瓶頸不是 review events，而是這段沒有儀器的尾巴。使用者手動 resync（11:04:25 / 11:04:36）只花 0.75s / 1.05s，所以這段尾巴看起來只出現在冷啟動路徑。先補 log 讓它可歸因，再談優化。",
  "fix_site": "ios/BooksAndVocab/Services/KGService+Sync.swift:688,ios/BooksAndVocab/Services/AppAnalytics.swift:197,ios/BooksAndVocab/Services/PodcastSyncService.swift:381",
  "groomed_at": "2026-08-08",
  "groomed_by": "workflow:groom-untriaged-33",
  "id": "APP-20260806-c48a11",
  "plan": "**根因與現況沿用原梳理（已逐條複驗屬實）**：d1f72461e 已把 PodcastSyncService 的序列 detail 迴圈換成「指紋跳過 + bounded 併發」（`needsDetailFetch`:519 / `fetchDetailsConcurrently`:530，呼叫點 :384），a677f1d37 已把 podcast leg 移成 `async let podcastLeg`（KGService+Sync.swift:541）。票面 fix_site 指的 `for summary in summaries { fetchSeriesDetail }` 今天不存在，穩態 detail 0 趟。**殘留＝票 resolution 最後一句「Phase 3 都該有進出 log」，今天完全沒做**：`runPodcastLeg`（KGService+Sync.swift:688-714）唯一的進出訊號是 `progress?(...)`，流向 `SyncProgress.swift`（全檔零 AppLog／零 AppAnalytics 的純 UI store），而冷啟動路徑在 `KGService+Sync.swift:484-485` 硬傳 `progress: nil` —— **任何掛在 SyncProgressStore 上的儀器對這張票無效，儀器必須放在 service 內**。（`SyncCoordinator.swift:643-659` 有形狀幾乎一樣的 `AppAnalytics.track(.syncStepCompleted(...))`，但那是詞庫頁另一條手寫管線、不在 backgroundSync 路徑上；照抄它的位置會做出對冷啟動沉默的儀器。）另 `PodcastSyncService.swift:381-383` 現有的計數 log 被 `if stale.count < summaries.count {` 包住＝**只有健康時才說話**，退回「全部都抓」那一輪反而零輸出，方向是反的。\n\n**修法（三處，全部照抄同檔既有形狀，不新增抽象；判準會逐條錨到下列字面，請逐字寫）：**\n\n(a) `ios/BooksAndVocab/Services/AppAnalytics.swift` —— `AnalyticsEvent` 的 App Lifecycle 段，在 :41 `case backgroundSyncCompleted(durationMs: Int, success: Bool)` 之後**各自獨立一行、行尾不加註解**加兩個 case，字面逐字為：\n```\n    case podcastCatalogSyncStarted\n    case podcastCatalogSyncCompleted(durationMs: Int, outcome: String)\n```\n再在 `track(_:)` 的 switch 內、:197-202 `backgroundSyncCompleted` arm **之後**加兩個 arm，逐字照抄 :193-203 的 `recordObservation(...)` + `logger.info(...)` 雙寫形狀：\n```\n        case .podcastCatalogSyncStarted:\n            recordObservation(level: .info, message: \"event=podcast_catalog_sync_started\")\n            logger.info(\"event=podcast_catalog_sync_started\")\n\n        case .podcastCatalogSyncCompleted(let durationMs, let outcome):\n            recordObservation(\n                level: .info,\n                message: \"event=podcast_catalog_sync_completed duration_ms=\\(durationMs) outcome=\\(outcome)\"\n            )\n            logger.info(\"event=podcast_catalog_sync_completed duration_ms=\\(durationMs) outcome=\\(outcome, privacy: .public)\")\n```\n（`outcome` 是固定枚舉字串、非使用者資料，`.public` 與既有 `backgroundSyncCompleted` 同一條隱私線。`SessionMetrics.record` 有 `default: break`（:309），不必改；只有 `track(_:)` 的 switch 是窮舉的。）\n\n(b) `ios/BooksAndVocab/Services/KGService+Sync.swift` 的 `runPodcastLeg`（:688）—— 在 :709 `progress?(.started(.podcast))` **之後**加 `AppAnalytics.track(.podcastCatalogSyncStarted)` 與 `let legStart = Date()`；在 :713 `progress?(podcastFinishedEvent(outcome))` 之後加 completed 事件。\n\n**⚠️ `PodcastSyncService.SyncOutcome` 是 `enum SyncOutcome: Equatable`（PodcastSyncService.swift:331），沒有 `rawValue`** —— 不要寫 `outcome.rawValue`，那不會編譯。用顯式映射：\n```\n        let outcomeLabel: String\n        switch outcome {\n        case .none: outcomeLabel = \"disabled\"\n        case .completed: outcomeLabel = \"completed\"\n        case .cancelled: outcomeLabel = \"cancelled\"\n        case .listFetchFailed: outcomeLabel = \"listFetchFailed\"\n        }\n        AppAnalytics.track(.podcastCatalogSyncCompleted(\n            durationMs: Int(Date().timeIntervalSince(legStart) * 1000),\n            outcome: outcomeLabel\n        ))\n```\n**兩個 early return 必須留在 started 事件之前**（:693 `guard KGFeatureFlags.podcastEnabled`、:707-708 的 `JWTExpiry.isExpired` 判斷）—— Release 零 podcast 足跡是既有不變式，加了事件不得破壞它，也不該在沒有有效 session 時發 started。**判準會驗這個順序**（awk 在 `runPodcastLeg` 函式區段內比對三者的相對位置），把事件搬到 guard 之前會紅。\n\n(c) `ios/BooksAndVocab/Services/PodcastSyncService.swift:381-383` —— 把 `if stale.count < summaries.count { ... }` 那三行整段換成**無條件、位於方法本體頂層（8 格縮排）**的一行：\n```\n        AppLog.kg.info(\"[PodcastSync] detail fetch total=\\(summaries.count) stale=\\(stale.count) skipped=\\(summaries.count - stale.count)\")\n```\n**不可把它放回任何 `if` 裡**——判準用 `^ {8}` 錨死縮排，包在 if 裡（12 格）會紅。這是刻意的：舊守衛「只有健康時才說話」正是缺陷本身，換個條件寫法把 log 塞回 if 內＝沒修。\n\n**不要做的事**：不要動後端（本票純 client 觀測性）；不要把儀器加進 `SyncProgress.swift`；不要順手改 `SyncOutcome` 的形狀去帶計數（會波及 `PodcastSyncTests` / `PodcastReconcileTests` 的等值斷言，且 log 已能承載計數）。\n\n**收尾驗證（安靜的機器上）**：`./ops/ios_ops.sh build` 過（判準只到源碼契約，不含編譯）；然後 kg-pool-2 冷啟動一次，用 `/usr/bin/log` 抓 `event=podcast_catalog_sync_completed` 與 `[PodcastSync] detail fetch total=`，把票面那個 6.08s 換成實測數字（第二輪同步應為 stale=0）。",
  "repro": "kg-pool-2 冷啟動 app → 讀 log stream 的 background_sync_triggered → background_sync_completed duration_ms",
  "resolution": "根因已定位（2026-08-06 11:53，kg-pool-2，backend 2.0.2）：那段空白是 backgroundSync 的 Phase 3（podcast catalog），不是無儀器的雜項。\n\n把 app log 與同時段伺服器 log 疊在同一條時間軸上才看得出來——app 側該段零輸出，單看 app log 只會得到「有個缺口」：\n  11:53:05.651 pullCardsToLocal completed（vocab 段共 0.53s）→ 7.6s app 全空 → 11:53:13.245 completed duration_ms=8127\n  伺服器同段：03:53:06.406 GET /api/podcasts → 4 次逐一序列的 /api/podcasts/<series>（0.8–2.2s each）→ 03:53:12.490 progress → 03:53:12.920 quota\n＝ 1 次 list + N 次序列 series 詳情 + progress = 6.1s，佔 8.1s 的四分之三；成本隨 series 數線性成長，每輪 sync 重跑，含使用者手動 resync。\n\n**嚴重度修正（這條最要緊，別漏）**：Phase 3 受 KGFeatureFlags.podcastEnabled 控制（KGFeatureFlags.swift:55/57，#if DEBUG true、else false），且 runPodcastCatalogSyncIfEnabled（KGService+Sync.swift:450）在建構 PodcastSyncService **之前**就 guard 短路，Release 因此零 podcast 網路/磁碟足跡。**所以這筆成本是 Debug／模擬器專屬，正式使用者不付。** 污染的是我們自己的效能量測與 debug 體感。\n\n程式碼座標與兩個補充（2026-08-06 12:00 併入）：\n- 序列迴圈在 PodcastSyncService.swift:361 —— `for summary in summaries { let detail = try await fetchSeriesDetail(...) }`，每個 series 一次往返。\n- 同一個檔案 :243 的 cacheCovers 已經有正確做法（withTaskGroup、maxConcurrent 3），註解甚至寫著「avoids first-sync waterfall latency without stampeding the backend」——那個 waterfall 就在它上面幾行，沒被套用。\n- 沒有變更偵測：PodcastSeriesSummary 只有 id/title/author/hostNames/color/coverPattern/coverImageURL/totalDurationSec/episodeCount，**沒有 updatedAt 或 etag**，client 無從得知哪個 series 沒變。要做 skip 得後端先在 summary 帶版本欄位。\n- 逐段實測（生產 log 03:53:05Z 那輪）：review-events 05.374 → dictionary-cards 05.753(+0.38) → podcasts list 06.406(+0.65) → series 07.204(+0.80) / 09.394(+2.19) / 10.622(+1.23) / 12.076(+1.45) → progress 12.490(+0.41) → quota 12.920(+0.43)；全輪 7.55s，podcast 段 6.08s。\n\n修法擇一或並用：① series 詳情併發而非序列（照 :243 的 taskgroup 形狀）；② list 回應帶 updated_at，未變更者跳過詳情；③ Phase 3 移出使用者主動 resync 的關鍵路徑。無論怎麼修，Phase 3 都該有進出 log——沒有它，這段時間在 app 側不可歸因。",
  "schema": "kg.backlog.entry.v1",
  "scope": "三個 iOS service/analytics 檔案、同步流程與人工 log 驗證。",
  "severity": "low",
  "source": "kg-pool-2 上驗證 7a8352f27 watermark 時的旁證",
  "status": "triaged",
  "stream": "APP",
  "surface": "vocabulary"
}
