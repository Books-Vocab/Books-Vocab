{
  "acceptance": "沿用 IMP-0040 的判準，但**補上前置 `test -f` 守衛**：原判準有四道 `! grep -qF <pattern> <file>` 是裸的多檔否定式，檔案若被刪或改名，grep 回 2、`!` 反轉成 0，那四道會靜默通過（正是本 repo 已付過一次代價的形狀）。硬化版先逐檔斷言存在，再跑原本十道 grep 與 docs_lint。今天實測 rc=1（四句新措辭都不存在、四句舊假陳述都還在），四處措辭改完且 docs_lint 綠之後才會變 0。expect rc = 0。",
  "acceptance_cmd": "test -f .claude/skills/worktree-flow/SKILL.md && test -f docs/runbook/system.md && test -f docs/reference/tech_index.md && test -f ops/worktree_orchestrate.py && grep -qF 'open` / `adopt` / `freeze` 沒有 `--commit`' .claude/skills/worktree-flow/SKILL.md && ! grep -qF '所有 mutation 子指令' .claude/skills/worktree-flow/SKILL.md && grep -qF 'open` / `adopt` / `freeze` take effect immediately' docs/runbook/system.md && ! grep -qF 'All mutation subcommands are dry-run by default' docs/runbook/system.md && grep -qF 'open/adopt/freeze 無 `--commit`,呼叫即生效' docs/reference/tech_index.md && ! grep -qF '所有 mutation **dry-run 預設' docs/reference/tech_index.md && grep -qF 'open / adopt / freeze take effect' ops/worktree_orchestrate.py && ! grep -qF 'dry-run is the default for every mutation' ops/worktree_orchestrate.py && ./ops/docs_lint.sh --files docs/runbook/system.md docs/reference/tech_index.md",
  "acceptance_expect_rc": 0,
  "category": "doc",
  "date": "2026-08-05",
  "detail": "worktree-flow skill 寫「所有 mutation 子指令 dry-run 預設，--commit 才落地」，但 worktree_orchestrate.py open 沒有 --commit 參數（直接落地）。照 skill 字面下 --commit 會得到 argparse error: unrecognized arguments。preflight/resolve/sync/deploy 確實有 --commit，只有 open（與 adopt？）是例外。修 skill 文字或補 open 的 no-op --commit。",
  "fix_site": ".claude/skills/worktree-flow/SKILL.md:12 + docs/runbook/system.md:62 + docs/reference/tech_index.md:133 + ops/worktree_orchestrate.py:18-20（即 IMP-0040 的落點）",
  "groomed_at": "2026-08-08",
  "groomed_by": "groom-wave-2026-08-08",
  "id": "IMP-20260805-a95e23",
  "plan": "本條與 IMP-0040 是同一個缺陷：worktree-flow skill 宣稱「所有 mutation 子指令 dry-run 預設、--commit 才落地」，而 open / adopt / freeze 沒有 --commit、呼叫即生效。IMP-0040（2026-08-03 立單、2026-08-05 已 CONFIRMED-OPEN 取證並梳理完成）不只涵蓋本條，還多找出三份同樣的假陳述（docs/runbook/system.md:62 的英文版、docs/reference/tech_index.md:133 的表格 cell、ops/worktree_orchestrate.py:18-20 的 module docstring），並已寫好逐行、逐字的落地步驟與六支/三支 CLI 的完整對照。**不要在本條上另開工**：照 IMP-0040 的 plan 執行一次，兩條一起關。若要保留本條當獨立紀錄，也請把 status 收成 duplicate 並指回 IMP-0040，避免兩份 plan 各改一半、互相蓋掉。",
  "resolution": "",
  "schema": "kg.backlog.entry.v1",
  "severity": "low",
  "source": "worktree-flow skill",
  "status": "open",
  "stream": "IMP"
}
