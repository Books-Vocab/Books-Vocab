{
  "acceptance": "uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py ops/tests/test_backlog_migration.py",
  "category": "doc",
  "cost": "S",
  "date": "2026-08-05",
  "detail": "ledger schema（`ops/backlog.py:822-824`，render 進 `docs/runbook/improvement_backlog.md:36-38`）警告 resolution hash 會被 PR squash merge 作廢。本 repo 的落地路徑早已不是 PR：最後一個 merge commit 是 `0611f3cac`（2026-07-09），其後 196 個 commit 全單親；`90e57ba7e` 已把 `.claude/commands/merge-prs.md` 與 `review-ui.md`（合計 198 行）整個刪除，PR 合併入口是被刻意拆掉的。**但真兇不是「過期」而是「指錯機制」**：`cmd_cutover`（`ops/worktree_orchestrate.py:1910-1913`）在 advance lock 內先對分支跑 `git rebase <local main>`，之後（:1923-1928）才在主樹 `merge --ff-only`——ff 只是後半段，前半段把分支鑄的每個 sha 都改寫。證據：最近 200 個 commit 有 79 個 committer date 不等於 author date；`83843b65f`（docs: re-anchor verified_against 888dde5f0 到 e14f295ad，rebase 後 orphan hash）是 PR 時代之後真實發生過的孤兒事件，起因是 rebase 不是 squash。**這條過期教條已經害過人**：IMP-0063 的 resolution 寫著分支名加「squash 後由 platform-steward 更新為 squashed hash」，在一個永遠不會 squash 的拓樸上等一個不會來的事件，分支已刪——而該工作其實是以五個普通 commit 落地的。同型錯誤另見 `docs/sop/doc_sync.md:26`（自己分支的 HEAD 只在 ff 拓樸下滿足、squash merge 下必炸；實際上這條路線的 merge 就是 ff，而 branch HEAD 照樣被前面的 rebase 孤立）。**仍然合法的 PR 用法別一起改掉**：`ops/branch_audit.sh:126`（PR state 當 metadata，`docs/runbook/system.md:73` 明言 commit 可達性才是真相）、三個 workflow 的 `pull_request:` 觸發（休眠且無害）、`docs/sop/figma-token-workflow.md`（Figma plugin 從機外推分支，PR 是真正的跨越機制）。另有約 30 處「同 PR」是「同一個落地單位」的同義詞，屬詞彙債，改它是 churn 且有打壞 docs_impact source hint 的風險。",
  "fix_site": "ops/backlog.py:880",
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom@v1",
  "id": "IMP-20260805-34f30f",
  "plan": "機制已複驗：ops/worktree_orchestrate.py:1912 在 advance lock 內對分支跑 git rebase <local main>，:1923-1928 才在主樹 merge --ff-only——分支上鑄的每個 sha 都在 ff 之前就被 rebase 改寫。entry 引的 :822-824 已漂移到 :880-882。本條只做 entry 自己劃定的機械段，CLAUDE.md:138 那句（載著 2026-06-04 standing 授權）明示需執行長裁決，不在此範圍。\n1. ops/backlog.py:880-882（_VIEW_HEADER 內 resolution hash 慣例 那條 bullet）。現文說「PR 合併前為 branch-local；若該 PR 採 squash merge，合併後由 platform-steward 更新為 squashed hash」。改成：落地路徑是 ops/worktree_orchestrate.py 的 cutover，它先 rebase 再 ff，所以分支上的 sha 一律被改寫，resolution 必須在 cutover 之後回填「已進本地 main 的 sha」。保留既有那句「不得以分支名代替 hash——分支刪掉之後那筆 resolution 就再也指不到任何東西」，它是對的。正確機制不要重寫，抄 _doc_anchor docstring（:1208-1230）已寫好的說法。可引的事實：本 repo 最後一個 merge commit 是 0611f3cac（2026-07-09），其後全為單親 commit；90e57ba7e 已刪掉 .claude/commands/merge-prs.md 與 review-ui.md，PR 合併入口是被刻意拆掉的。\n2. _VIEW_HEADER 是 str.format 模板，改文時不得引入裸的大括號（會在 render 時炸 KeyError/IndexError），現有佔位符 statuses / imp_categories / app_categories / severities / verdicts 一個都不能動。\n3. 改完必跑 ./ops/backlog.py render --commit 重生 docs/runbook/improvement_backlog.md：它是 registry runbook.improvement_backlog（kind=generated, generator=ops/backlog.py），手改無效，不重生就會留一份仍在講 squash 的產出。\n4. docs/sop/doc_sync.md:26（全檔唯一含「SHA 不被改寫」的那行）：現文「自己分支的 HEAD 只在 ff 拓樸下滿足，squash merge 下必炸」。改成：自己分支的 HEAD 在本 repo 同樣不滿足，因為 cutover 是先 rebase 後 ff，rebase 就把分支上每個 sha 改寫成 orphan（squash merge 也會，但本 repo 沒有 squash 路徑）。同檔 :32「禁止寫只存在於 PR branch 的 ephemeral hash」把 PR branch 改成 worktree 分支。\n5. .claude/agents/docs-steward.md:4（description 欄）：「也用於 PR 開出前的 docs 健康檢查」改成「也用於 cutover 前的 docs 健康檢查」，第二個 <example> 的「這個 PR 要開了,docs 都對齊了嗎」改成「這條 worktree 要 cutover 了,docs 都對齊了嗎」。同檔 :28 的「本次 PR gate」不要動——那是「同一個落地單位」的同義詞用法，屬 entry 自己判定為 churn 的那約 30 處。\n6. ops/tests/test_backlog.py 新增 test_schema_section_blames_rebase_not_squash()：assert \"squash\" not in BACKLOG._VIEW_HEADER 且 \"rebase\" in BACKLOG._VIEW_HEADER；讀 ROOT/docs/runbook/improvement_backlog.md，assert \"squash\" not in view.split(\"## IMP\")[0]（只驗 header 段——表格 row 內的 squash 是 entry 原文，全檔現有 13 次，其中 1 次在 header）；讀 ROOT/docs/sop/doc_sync.md，取唯一含「SHA 不被改寫」的那行，assert \"rebase\" in 該行。三條斷言在修改前全部紅（已實測：header 段含 squash=True、該行含 rebase=False），修好後全綠。ROOT 與 BACKLOG 在該檔 :39-44 已備妥。\n7. gate：另跑 ./ops/docs_lint.sh（驗 registry + changed docs）無 ERROR。\n不要動：ops/branch_audit.sh:126（PR state 當 metadata，docs/runbook/system.md:73 明言 commit 可達性才是真相）、三個 workflow 的 pull_request: 觸發（休眠且無害）、docs/sop/figma-token-workflow.md（Figma plugin 從機外推分支，PR 是那條路真正的跨越機制）、其餘約 30 處「同 PR」＝「同一個落地單位」的同義詞（改它是 churn，且有打壞 docs_impact source hint 的風險）。\n會紅的既有測試：把 _VIEW_HEADER 改到破格式 → test_render_is_deterministic / test_render_emits_doc_meta_the_lint_requires / test_render_says_it_is_generated（皆在 ops/tests/test_backlog_migration.py）。修改前基準 90 passed（0.31s）。",
  "resolution": "—(2026-08-05 驗證 CONFIRMED-OPEN；落點 `ops/backlog.py:822-824` 加 `docs/sop/doc_sync.md:26` 加 `.claude/agents/docs-steward.md:4,28`，成本 S——正確機制已經寫在 `ops/backlog.py:1126` 的 `_doc_anchor` docstring 裡，是抄過去不是寫新文。**另有一句需要執行長裁決、本條不自決**：`CLAUDE.md:138`「驗證全綠後直接 commit 加開 PR，不先問」是最後一條把 agent 導向已被拆除入口的指令，但同一句同時載著 2026-06-04 的 standing 授權，改動它等於改自主權政策。驗收：機械段可命令化（grep 那兩處必須同時具名 rebase/cutover 為主因），其餘是判斷不是命令——「有沒有 always-on 表面還在叫 agent 開 PR」grep 不出來，因為 40 個 hit 裡約 30 個是合法或同義詞用法。)",
  "schema": "kg.backlog.entry.v1",
  "severity": "med",
  "source": "backlog CLI 稽核（5-agent deep dive）",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
