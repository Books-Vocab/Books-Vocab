{
  "acceptance": "紅：今天 `pytest ops/tests/test_app_review_gate.py -k source_commit_blob` 回 exit 5（26 deselected，沒有這條測試存在），命令 rc=5。綠：該測試存在且通過；接著整支 test_app_review_gate.py 與 test_capture_profile.py 也全綠（保證新斷言沒有翻倒既有 26+ 條）；最後 ops/ios_test.sh 裡出現 `status --porcelain`，證明 (d) 的 dirty-tree 檢查真的落了地。",
  "acceptance_cmd": "bash -c 'set -e; uv run --no-project --python 3.13 --with pytest pytest -q -p no:cacheprovider ops/tests/test_app_review_gate.py -k \"source_commit_blob\" >/dev/null; uv run --no-project --python 3.13 --with pytest pytest -q -p no:cacheprovider ops/tests/test_app_review_gate.py ops/tests/test_capture_profile.py >/dev/null; test -f ops/ios_test.sh; grep -q \"status --porcelain\" ops/ios_test.sh'",
  "acceptance_expect_rc": 0,
  "brief": "送審用的證據包宣稱自己對應某一版程式，裝的其實是當下工作目錄的內容，而把關程式不查這件事。",
  "category": "arch",
  "date": "2026-08-05",
  "detail": "IMP-20260805-0fc07a 只把 **一個** bundle 輸入綁回它宣稱的 commit（pbxproj）。同一個「宣稱 commit／實讀工作樹」錯配仍存在於其餘輸入與下一層工具，而且 gate 不驗這條不變式，所以它現在是 [prompt] 不是 [machine]。\n\n**(1) 其餘五個 desired bundle 輸入仍讀工作樹**（`ops/app_review_evidence.py:produce_desired_bundle()`）：capture profile（`--profile` 傳進來的路徑，寫進 manifest `capture.profile.sha256`）、render PNG（`--render-output-dir` 底下的檔，寫進 `outputs[].sha256`）、兩支 demo generator（`ops/demo/marketing_account/shape_history.py`、`ops/demo/build_demo.py`，以工作樹版本執行）、以及 `generator_files=[capture_profile.py, reviewer_evidence.py]`（`ops/capture_profile.py` → `ops/reviewer_evidence.py` 的 `provenance.generators[].sha256`）。任何一支在 sourceCommit 之後被改，bundle 就再次「宣稱是那顆 commit 的證據、裝著 HEAD 的東西」，只是這次沒有錨點會叫——因為錨點本身就是這些輸入的函數，它會跟著一起漂。\n\n**(2) 下一層工具的預設值就是原缺陷**：`ops/capture_profile.py` 的 `reviewer-evidence` 子指令，`--project-file` 預設 `IOS_PROJECT_FILE`（工作樹路徑），`--source-commit` 預設 `HEAD`。任何人直接跑 `capture_profile.py reviewer-evidence --source-commit <sha>` 而不給 `--project-file`，就完整重現 0fc07a。不變式現在住在 caller 裡，不在那支「鑄造這個宣稱」的工具裡。\n\n**(3) gate 不驗這條不變式**：`ops/app_review_gate.py` 只驗 bundle 內部一致性（檔案 bytes vs manifest digest），沒有任何 git 呼叫。用舊程式產的 bundle、或走 (2) 的路徑產的 bundle，一樣通過。要變成 [machine]，gate 需斷言 `build.project.sha256 == sha256(git cat-file blob <source.commit>:<pbxproj>)`（以及推廣到其他輸入）。\n\n**(4) 同形問題在 iOS 那側**：`ops/ios_test.sh:1734-1737` 把 `git rev-parse HEAD` 寫進 run verdict 的 `sourceCommit`，同時用 awk 從**工作樹** pbxproj 取 marketingVersion/buildNumber/bundleID，且不檢查 tree 是否 dirty。`produce_journey`（`ops/app_review_evidence.py`）接著要求它等於 `spec.target.sourceCommit`——於是一個 dirty build 會被歸給一顆乾淨的 commit。`ops/lib/ios_ops_catalog.sh:519-525` 同形。\n\n**方向（未實作，需先定範圍）**：把 `_materialize_tracked_file()` 推廣成「所有 commit-pinned 輸入都從那顆 commit 取」；generator 腳本要不要也 pin 是真的取捨（pin 了就得用歷史版本執行，行為可能與現行不同），需要決定而不是照抄。gate 那條斷言相對單純且收益最大，可先做。ios_test.sh 那條至少該加 dirty-tree 檢查。",
  "fix_site": "ops/app_review_gate.py:_verify_desired_bundle  (build.project / source.commit 那段，約 :700-800)",
  "fixed_by": [
    "9458a91bf"
  ],
  "groomed_at": "2026-08-08",
  "groomed_by": "groom-wave-2026-08-08",
  "id": "IMP-20260805-c7178a",
  "plan": "本單有四個子項，只做 entry 自己標為「相對單純且收益最大」的 (3)，外加 (2) 的預設值與 (4) 的 dirty 檢查；(1)（其餘五個 bundle 輸入是否也要 pin）entry 自己說是「真的取捨、需要決定而不是照抄」，**本次不得動**，收案時要在 resolution 明寫 (1) 仍開著。步驟：(a) ops/app_review_gate.py 加 helper `_git_blob_sha256(root, commit, rel_path)`，內部 `git -C <root> cat-file blob <commit>:<rel_path>` 取 bytes 算 sha256；git 不可用／commit 不可達／路徑不存在一律回 None 並轉成一條具名 block，**不得靜默略過**（該檔目前完全沒有 subprocess/git 呼叫，要新 import subprocess）。(b) 在既有 `project = _require_exact_keys(build.get(\"project\"), {\"path\",\"sha256\"})` 與 `source = _require_exact_keys(manifest.get(\"source\"), {\"commit\"})` 之後加斷言：`_git_blob_sha256(root, source[\"commit\"], project[\"path\"]) == project[\"sha256\"]`，不等就 raise GateError(\"desired build.project sha256 does not match source commit blob\")。這一條把不變式從 caller 搬進 gate，讓它從 [prompt] 變 [machine]。(c) ops/capture_profile.py:1126 的 `--project-file` 拿掉「預設 IOS_PROJECT_FILE（工作樹路徑）」，改成未給時從 `--source-commit` 具體化；:1130 的 `--source-commit` 拿掉 default=\"HEAD\"、改成 required——「宣稱哪顆 commit」必須是呼叫者明講的。(d) ops/ios_test.sh:1740 的 write_json_verdict 在寫 sourceCommit 前加 dirty-tree 檢查：`git -C \"$PROJECT_ROOT\" status --porcelain` 非空時，verdict 要帶明確標記（或直接拒絕把這次 run 歸給那顆乾淨 commit）；ops/lib/ios_ops_catalog.sh:519-525 同形，一併處理。(e) 測試：ops/tests/test_app_review_gate.py 新增 `test_desired_project_sha_must_match_source_commit_blob`（函式名**必須逐字含 source_commit_blob**，驗收的 -k 選擇器與它對應），造一個 build.project.sha256 是工作樹版本、source.commit 指向另一顆 commit 的 manifest，斷言 gate BLOCK；再加一個正例。ops/tests/test_capture_profile.py 補「不給 --source-commit 就紅」。",
  "resolution": "",
  "schema": "kg.backlog.entry.v1",
  "scope": "三支運維腳本各改一處（多一道比對、多一個必填、多一個未存檔偵測），各配一支測試；另外一半的輸入要不要一起綁定，刻意留給後續決定。",
  "severity": "med",
  "source": "IMP-20260805-0fc07a 修復後的殘留範圍，由該修復的 code review 量出（review 的 findings 5/6/7）",
  "status": "fixed",
  "stream": "IMP",
  "verdict": "CONFIRMED-FIXED",
  "verified_at": "2026-08-09",
  "verified_by": "agent:main-session",
  "verified_evidence": "acceptance_cmd 由本次 verify --commit 於 cutover 後的 main 上實跑（rc 不符即拒絕落地）；修法已落地於 9458a91bf"
}
