{
  "acceptance": "sed -n '/^## Receipt Checklist$/,/^## Minimal Format$/p' .claude/skills/kg-receipt/SKILL.md | grep -q -- \"--stream APP\" && sed -n '/^## Receipt Checklist$/,/^## Minimal Format$/p' .claude/skills/kg-receipt/SKILL.md | grep -q -- \"--surface\" && sed -n '/^## Verification Rules$/,/^## Handoff Prompt Rule$/p' .claude/skills/kg-receipt/SKILL.md | grep -q -- \"--stream APP\" && sed -n '/^## Verification Rules$/,/^## Handoff Prompt Rule$/p' .claude/skills/kg-receipt/SKILL.md | grep -q -- \"ios-engineer\" && grep -q -- \"--stream APP\" .claude/agents/ios-engineer.md && grep -q -- \"--surface\" .claude/agents/ios-engineer.md && grep -q -- \"list --stream APP\" .claude/agents/ios-engineer.md && grep -q -- \"--stream APP\" .claude/agents/backend-engineer.md && grep -q -- \"--surface\" .claude/agents/backend-engineer.md && grep -q -- \"list --stream APP\" .claude/agents/backend-engineer.md && grep -q -- \"--stream APP\" docs/sop/agent_org.md && ! grep -q \"verified_against: a6ad9d5d\" docs/sop/agent_org.md && ./ops/docs_lint.sh --files docs/sop/agent_org.md >/dev/null",
  "category": "doc",
  "cost": "S",
  "date": "2026-08-05",
  "detail": "APP stream 有完整 schema、專屬詞彙、專屬欄位與指定 owner，但沒有任何路由把工作導進去。`.claude/skills/kg-receipt/SKILL.md:21,54` 兩處「Tooling Debt 強制表態」條款都硬寫 `--stream IMP`；`CLAUDE.md`、`docs/sop/agent_org.md`、`kg-router` 完全不提 APP；`.claude/agents/platform-steward.md:16` 把 APP owner 指給 `ios-engineer` 與 `backend-engineer`，而這兩個 agent 檔案裡「backlog」出現 0 次。測試面 `test_backlog.py` 與 `test_backlog_migration.py` 有 17 處 APP，但全部走 Python API `add_entry`，零測試走 CLI 的 APP 路徑。**病因要寫對**：APP stream 誕生於 `e644791b2`（2026-08-05 08:14:40），零筆是「還沒開張」不是「久置腐朽」——原始框架寫成 dead surface 已被證偽，這條記下來以免下一個人重犯同一個推論。與同輪的 APP round-trip bug 是兩個關注點：那條是碼的缺陷，本條是路由缺口，owner 不同。",
  "fix_site": ".claude/skills/kg-receipt/SKILL.md:21",
  "fixed_by": [
    "f4263217e",
    "743fd511d"
  ],
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom-all@v2",
  "id": "IMP-20260805-7ac60d",
  "plan": "問題：`APP` stream 有完整 schema、專屬 category、專屬欄位與指定 owner，但沒有任何 agent-facing 文字把工作導進去，所有 debt 都被文案導向 `--stream IMP`。本條只補「路由」，不動 `ops/backlog.py`。\n\n【本輪（HEAD = 7d39c89ba）實際讀過並逐字核對的錨點】\n- `.claude/skills/kg-receipt/SKILL.md` 全檔 65 行。`:12` = `## Receipt Checklist`；`:21` = 「6. **Tooling Debt 強制表態**:`none` 或一筆。非 trivial 且未當場修 → 用 `ops/backlog.py add --stream IMP ...` 登記(**不要手改** `improvement_backlog.md`,那是產出物)。撞到摩擦無聲妥協(硬幹）= 違鐵律9。」；`:23` = `## Minimal Format`；`:48` = `## Verification Rules`；`:54` = 「- Tooling Debt 不可留空：`none` 或一筆 filed item;沉默不合法(andon · 反硬幹)。非 trivial 未當場修者用 `ops/backlog.py add --stream IMP ...` 登記,由 `platform-steward` 追到 resolved。」；`:56` = `## Handoff Prompt Rule`。\n- `.claude/agents/ios-engineer.md` 全檔 30 行。`:24` = `## Gate（definition of done，必有當下輸出）`；`:27` = 「- build 不可取代相關測試。」；`:28` = 空行；`:29` = `## 收尾`。全檔 `backlog` / `APP` 命中數皆 0。\n- `.claude/agents/backend-engineer.md` 全檔 31 行。`:26` = `## Gate（definition of done，必有當下輸出）`；`:28` = 「- 改前先寫 failing test 重現。」；`:29` = 空行；`:30` = `## 收尾`。全檔 `backlog` / `APP` 命中數皆 0。\n- `docs/sop/agent_org.md` 全檔 92 行。`:10` = `verified_against: a6ad9d5d`；`:92`（檔案最後一行）= 「凡非當場修掉者一律經 `ops/backlog.py add` 進 `docs/runbook/backlog/`(一筆一檔的 SoT),owner=`platform-steward` 追到 resolved。」← 這句對 APP entry 是錯的。\n- 分流語彙早已齊備，不需新增碼：`ops/backlog.py:59-62` 是解釋兩條 stream 為何分開的註解、`:63` = `STREAMS = (\"IMP\", \"APP\")`、`:65-68` = `CATEGORIES`（IMP: tool/cli/doc/arch；APP: ux/correctness/perf/data/content）、`:88` = `APP_ONLY_FIELDS = (\"surface\", \"repro\", \"build\")`、`:1023-1025` 是 `--surface` / `--repro` / `--build` 三個 CLI 旗標（help 皆標 \"APP only:\"）。\n\n【步驟】\n1) 先跑一次本條 acceptance（見 acceptance 欄），確認 EXIT=1。今天十二個 clause 全紅（我逐條實測過，無一今天已綠），所以任何一步漏做都會讓它保持紅。\n\n2) 改 `.claude/skills/kg-receipt/SKILL.md:21`（= 本條 fix_site），整行替換為：\n`6. **Tooling Debt 強制表態**:\\`none\\` 或一筆。非 trivial 且未當場修 → 用 \\`ops/backlog.py add\\` 登記(**不要手改** \\`improvement_backlog.md\\`,那是產出物),並分流:工具 / CLI / 文檔 / 架構摩擦 → \\`--stream IMP\\`;自己在 app 裡實際碰到、使用者也會碰到的問題 → \\`--stream APP\\` 並補 \\`--surface\\` / \\`--repro\\` / \\`--build\\`。category 與欄位定義見 \\`ops/backlog.py --help\\`,此處不複述(SoT 零重複)。撞到摩擦無聲妥協(硬幹) = 違鐵律9。`\n   **編輯地雷**：原 `:21` 結尾是 `(硬幹）`——半形開括號配全形收括號。做 exact-match 替換時 old_string 必須逐位元組照抄原行，別自行「修正」成對稱括號後才去比對，否則 Edit 會失敗。\n\n3) 改同檔 `:54`，整行替換為：\n`- Tooling Debt 不可留空：\\`none\\` 或一筆 filed item;沉默不合法(andon · 反硬幹)。非 trivial 未當場修者用 \\`ops/backlog.py add\\` 登記——工具面走 \\`--stream IMP\\`,由 \\`platform-steward\\` 追到 resolved;app 使用面走 \\`--stream APP\\`,由對應 Line 部門(\\`ios-engineer\\` / \\`backend-engineer\\`)追到 resolved。`\n\n4) `.claude/agents/ios-engineer.md`：在 `:28`（空行）與 `:29`（`## 收尾`）之間插入新一節，與既有 `## Gate…` 平行：\n```\n## APP backlog（本部門是 `APP-*` 的 owner）\n- 在自己 scope 內發現**使用者面**問題而本回合不修 → `./ops/backlog.py add --stream APP --category <見 --help> --surface <對應 feature scope:reader / vocabulary / notebook / bookshelf / podcast / settings / discover> --repro <重現步驟> --build <看到問題的 build>`。\n- **不要塞進 `--stream IMP`** —— 那是 `platform-steward` 的工具摩擦 queue,混流會讓 triage 失效(理由寫在 `ops/backlog.py` 的 `STREAMS` 註解)。\n- 收自己的 queue:`./ops/backlog.py list --stream APP`。schema 與 category 細節一律見 `ops/backlog.py --help`,本檔不複述。\n```\n   （`docs/reference/feature_boundary/` 今日實存七檔：bookshelf / discover / notebook / podcast / reader / settings / vocabulary，與上列一致。）\n\n5) `.claude/agents/backend-engineer.md`：在 `:29`（空行）與 `:30`（`## 收尾`）之間插入同構的一節，只把 surface 例子換成 backend 語彙：\n```\n## APP backlog（本部門是 `APP-*` 的 owner）\n- 在自己 scope 內發現**使用者面**問題而本回合不修 → `./ops/backlog.py add --stream APP --category <見 --help> --surface <受影響的 surface,如 sync / quota / vocab-intake / podcast> --repro <重現步驟> --build <看到問題的 build>`。\n- **不要塞進 `--stream IMP`** —— 那是 `platform-steward` 的工具摩擦 queue,混流會讓 triage 失效(理由寫在 `ops/backlog.py` 的 `STREAMS` 註解)。\n- 收自己的 queue:`./ops/backlog.py list --stream APP`。schema 與 category 細節一律見 `ops/backlog.py --help`,本檔不複述。\n```\n\n6) `docs/sop/agent_org.md:92`，整行替換為：\n`凡非當場修掉者一律經 \\`ops/backlog.py add\\` 進 \\`docs/runbook/backlog/\\`(一筆一檔的 SoT),分兩條 stream 各自追到 resolved:工具 / CLI / 文檔 / 架構摩擦走 \\`--stream IMP\\`,owner=\\`platform-steward\\`;在 app 裡實際碰到、使用者也會碰到的問題走 \\`--stream APP\\`,owner=對應 Line 部門(\\`ios-engineer\\` / \\`backend-engineer\\`)。category 與 APP 專屬欄位見 \\`ops/backlog.py --help\\`,本文不複述。`\n\n7) 同檔 `:10` 把 `verified_against: a6ad9d5d` 換成本次編輯當下 `git rev-parse --short HEAD` 的輸出（必須是 HEAD 可達的 commit，否則 `ops/docs_lint.sh` 會 ERROR）。**誠實說明**：`ops/docs_lint.sh` 只在 sha 不是有效 commit（`:403-408`）或 `merge-base --is-ancestor` 失敗（`:417-426`）時 ERROR；staleness 走 `STALE_THRESHOLD`（`:39`，預設 30）且只是 WARN，所以「沒 bump」本身不會被 docs gate 擋。本條的 acceptance 因此自帶一條 `! grep -q \"verified_against: a6ad9d5d\"` 把這步釘住——這是它唯一的機器守衛。\n\n8) **不要碰 `ops/backlog.py`**。本條是路由缺口不是碼缺陷；APP round-trip 的碼缺陷是另一筆 entry、另一個 owner。也不要改上述四個 `## ` 標題（`## Receipt Checklist` / `## Minimal Format` / `## Verification Rules` / `## Handoff Prompt Rule`）的文字——acceptance 用它們當 `sed` 區段邊界，改了會讓斷言失效。\n\n9) 病因寫進 commit message：APP stream 誕生於 `e644791b2`（2026-08-05 08:14:40），零筆是「還沒開張」不是「久置腐朽」；把它當 dead surface 刪掉是錯的解法。\n\n【會不會撞到別處（本輪實測）】\n- 這五個檔沒有任何 `*.py` / `*.sh` / `*.yml` 引用（`grep -rn \"kg-receipt/SKILL.md\\|ios-engineer.md\\|backend-engineer.md\" --include=…` 排除 backlog ledger 後零命中），所以沒有既有測試會因此轉紅；不需跑 backend pytest，也不需跑任何 iOS gate。\n- `.claude/` 不在 docs gate 掃描範圍：`ops/docs_lint.sh:205-212` 的 `filter_docs()` 只吃 `^docs/` 並排除 `docs/assets/` 與 `docs/legal/`。\n- `docs/sop/agent_org.md` 有進 docs gate：`docs/registry.yml:606-617` 的 `id: sop.agent_org`，其 `sources` 含 `.claude/agents/`。本輪實跑 `./ops/docs_lint.sh --files docs/sop/agent_org.md` → `OK: 2  WARN: 0  ERROR: 0`（EXIT=0），改完後應維持 0 ERROR。\n- `./ops/docs_impact.py --files .claude/agents/ios-engineer.md .claude/agents/backend-engineer.md .claude/skills/kg-receipt/SKILL.md` 會提示 `sop.agent_org`（warn-only，不是 gate）。\n- ledger 本身不動，`./ops/backlog.py validate` 不受影響。\n\n【acceptance 的能與不能】\n它釘住的是「那條 filing 命令與 owner 分流的字面是否真的落在每個目標區段裡」——擋得住「完全沒寫」與「只改一半」，擋不住「寫得爛」。文案品質靠 review。",
  "resolution": "已修（委派給 ops-engineer，本 session 整合落地）。APP stream 的四個路由缺口全部關上，判準寫成可判定的形式——問「**這個缺陷誰碰得到**」而不是「改哪個檔」，因為 App Store 文案／EULA 的修法不改任何 repo 檔，路徑判準對它們沒有答案。路徑表降格成速查並帶例外。補上走 CLI 的 APP 路徑測試（含三個 APP-only 欄位 round-trip）。**受派者誠實回報 TDD 沒有紅燈**：CLI 接線本來就在，測試第一次跑就綠，改用四個一行 mutation 各自證明對應測試會紅。**剩餘 risk 已具名**：判準的自律那一半沒有工具擋（該進 APP 的填成 IMP、`--surface`/`--repro`/`--build` 不填照樣 exit 0，皆實測確認），文件裡明講此不對稱，沒有假裝它是 `[machine]`。",
  "schema": "kg.backlog.entry.v1",
  "severity": "low",
  "source": "backlog CLI 稽核（5-agent deep dive）",
  "status": "fixed",
  "stream": "IMP",
  "verdict": "CONFIRMED-FIXED",
  "verified_at": "2026-08-07",
  "verified_by": "agent:ops-engineer（委派）",
  "verified_evidence": "entry 自帶的 12-clause acceptance EXIT=1 → EXIT=0"
}
