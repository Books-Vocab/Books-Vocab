{
  "acceptance": "H=$(mktemp); R=$(mktemp); bash ops/test_infra_health.sh >$H 2>&1; hx=$?; bash ops/tests/test_kg_reconcile.sh >$R 2>&1; rx=$?; test $hx -eq 0 && test $rx -eq 0 && grep -qF \"✓ REMOTE_BUNDLE 含五個漂移 key\" $H && grep -qF \"✓ deploy_drift 兩 sha 不同 → warn 且 overall 非 crit\" $H && grep -qF \"✓ reconciler_tick_age_s 2000s → crit\" $H && grep -qF \"✓ reconciler_tick_age_s 缺值 → 非 ok\" $H && grep -qF \"✓ reconciler_poison_active 100s → warn\" $H && grep -qF \"✓ KG_HEALTH_DEPLOY_DRIFT=0 → 三個 metric 不出現\" $H && grep -qF \"✓ noop: tick 心跳已寫入 state\" $R && grep -qF \"✓ deployed: infra_health 以 KG_HEALTH_DEPLOY_DRIFT=0 呼叫\" $R",
  "acceptance_cmd": "bash -c 'H=$(bash ops/test_infra_health.sh 2>&1); hx=$?; R=$(bash ops/tests/test_kg_reconcile.sh 2>&1); rx=$?; test $hx -eq 0 && test $rx -eq 0 && printf \"%s\\n\" \"$H\" | grep -qF \"✓ REMOTE_BUNDLE 含五個漂移 key\" && printf \"%s\\n\" \"$H\" | grep -qF \"✓ deploy_drift 兩 sha 不同 → warn 且 overall 非 crit\" && printf \"%s\\n\" \"$H\" | grep -qF \"✓ reconciler_tick_age_s 2000s → crit\" && printf \"%s\\n\" \"$H\" | grep -qF \"✓ reconciler_tick_age_s 缺值 → 非 ok\" && printf \"%s\\n\" \"$H\" | grep -qF \"✓ reconciler_poison_active 100s → warn\" && printf \"%s\\n\" \"$H\" | grep -qF \"✓ KG_HEALTH_DEPLOY_DRIFT=0 → 三個 metric 不出現\" && printf \"%s\\n\" \"$R\" | grep -qF \"✓ noop: tick 心跳已寫入 state\" && printf \"%s\\n\" \"$R\" | grep -qF \"✓ deployed: infra_health 以 KG_HEALTH_DEPLOY_DRIFT=0 呼叫\"'",
  "acceptance_expect_rc": 0,
  "brief": "正式站有沒有部署到最新版、自動部署的排程還活著沒，這些只寫進沒人看的日誌，健康檢查完全不報。",
  "category": "tool",
  "cost": "M",
  "date": "2026-07-09",
  "detail": "felix 生產後端 git HEAD 靜默落後 origin/main 80 commits(0cea7f67 vs main),無任何告警。stale 容器的 `ops-edit seed` 舊碼**靜默丟棄** spec 的 counter-form 複習日期(review_count>0=0、next_review_at 全 null)、不合成 review_events(heatmap/streak=0)、不做 is_default→default remap(多殘留空 notebook)——差點注入「看起來全新未使用」的行銷帳號。本 session 已 deploy felix→main(0611f3ca)解當下阻塞,但**監控 gap 仍在**。**2026-08-05 指標對象更正**:架構已改為 launchd 週期 reconciler 收斂 felix→`origin/prod`(`ops/kg_reconcile.sh:1-9`),`origin/main` 已降為備份鏡像、reconciler 不看 main → 原候選的「commits behind **main**」已非正確指標。監控 gap 仍為真但形狀不同:drift 判定在 `kg_reconcile.sh:341-343`(VERSION vs origin/prod)+ crash-consistency `:356-366`,但 `alert()`(`:72`)只 printf 到 stderr→launchd err log,**未進 health**;`ops/infra_health.sh` 的 12 個 metric(`:137,143,145,149,154,166,174,183,187,191,194,196`:host_collect/disk/inode/mem/swap/container_restarts/uptime/ingress/http_probe/log_errors_1h/cert_days/data_dir_mb)無任何版本或漂移項。資料面損害(counter-form 複習日期、review_events 合成、is_default→default remap)**已修**:`ops_edit_seed_commands.py:115-140` docstring 明載支援。候選:`infra_health.sh` 加 deployed_sha vs origin/prod、reconciler last-tick 新鮮度、poison state 三項 metric。",
  "fix_site": "ops/infra_health.sh:196",
  "fixed_by": [
    "538b50716"
  ],
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom-all@v2",
  "id": "IMP-0022",
  "plan": "目標：把「felix 生產是否收斂到 origin/prod、reconciler 是否還活著、是否卡在 poison」三件事，從只存在於 launchd err log 的 stderr 告警（ops/kg_reconcile.sh:72 `alert() { printf 'ALERT: %s\\n' \"$*\" >&2; }`）搬進 ops/infra_health.sh 的機讀 metric 面。以下每個行號都是本輪實讀（HEAD=7d39c89ba），附錨文可對位。 /\n\n0) 先讀這條硬約束再動手：ops/infra_health.sh 不只給人看，它被 ops/kg_reconcile.sh:187（`set +e; \"$KG_INFRA_HEALTH\" >/dev/null 2>&1; ih=$?; set -e`）當「部署後健康 gate」消費——:191 的 `*) HEALTH_FAIL_REASON=\"infra\"` 表示 exit 2（crit）會讓剛部署成功的版本被 rollback+poison，:190 的 exit 1（warn）才是「記警告仍 pass」。故新 metric 若判 crit，會出現「origin/prod 在 build 視窗內又前進 → drift crit → 把一次健康的部署自己回滾」的自噬迴圈。解法是給 infra_health 一個關閉開關並在 reconciler 自我 gate 時關掉（步驟 5）。 /\n\n1) reconciler 心跳（全新寫入面，今日不存在）。ops/kg_reconcile.sh 在 write_poison（:119-125，末行 `mv \"$tmp\" \"$KG_STATE_FILE\"`）之後、clear_poison（:127）之前，加入同形的 write_tick：\n```\nwrite_tick() {\n  local tmp\n  mkdir -p \"$(dirname \"$KG_STATE_FILE\")\"\n  tmp=\"$(mktemp)\"\n  { grep -v \"^tick \" \"$KG_STATE_FILE\" 2>/dev/null || true; printf 'tick %s\\n' \"$(date +%s)\"; } > \"$tmp\"\n  mv \"$tmp\" \"$KG_STATE_FILE\"\n}\n```\n呼叫點：main() 的 :318（`[[ \"$dry_run\" == \"1\" ]] && log \"[dry-run] 只預覽，不執行任何 mutation\"`）之後緊接一行\n```\n  [[ \"$dry_run\" == \"1\" ]] || write_tick || true\n```\n（`|| true` 不可省：檔案系統暫時寫不進去不該讓整輪 reconcile 在 :43 `set -euo pipefail` 下中止；心跳量的是 liveness，缺一拍比停機好。）必須放在 fetch（:332-336）之前、且對所有 verdict（noop / ff-only / deployed / poisoned-skip / locked）都寫，因為它量的是「reconciler 還在跑嗎」不是「有沒有部署」。dry-run 短路是必要護欄：ops/tests/test_kg_reconcile.sh:432 `[[ ! -f \"$STATE\" ]] && ok \"dry-run: state not written\"` 會抓無條件寫。state 檔實際路徑由 launchd 決定：ops/launchd/com.kg.reconcile.plist:41-42 `<key>KG_RECON_REPO</key><string>/Users/chenliangyu/kg-prod</string>` + kg_reconcile.sh:51 `KG_STATE_FILE=\"${KG_STATE_FILE:-$KG_RECON_REPO/backups/reconciler.state}\"` → /Users/chenliangyu/kg-prod/backups/reconciler.state。相容性已核：state 檔既有讀寫者只認 `^poison ` 前綴（:107 is_poisoned、:123 write_poison、:131 clear_poison、測試 :312/:333），新增 `tick ` 行不會被誤配；且全 repo 的 \"tick \" 目前只出現在註解，`grep -q \"^tick \"` 不會被同形字串滿足。 /\n\n2) ops/infra_health.sh 新增設定。在 :39（`DATA_DIR=\"${KG_DATA_DIR:-/Users/chenliangyu/kg-data}\"`）之後加\n```\nPROD_REPO=\"${KG_PROD_REPO:-/Users/chenliangyu/kg-prod}\"\n```\n在 :55（`RESTART_WARN=\"${KG_HEALTH_RESTART_WARN:-1}\"`）之後加\n```\nTICK_WARN=\"${KG_HEALTH_TICK_WARN:-600}\";  TICK_CRIT=\"${KG_HEALTH_TICK_CRIT:-1800}\"\nPOISON_COOLDOWN=\"${KG_RECON_POISON_COOLDOWN:-3600}\"\nDEPLOY_DRIFT=\"${KG_HEALTH_DEPLOY_DRIFT:-1}\"\n```\n（StartInterval=90s，見 plist:45-46，故 600s≈連漏 6 拍；POISON_COOLDOWN 刻意沿用 kg_reconcile.sh:59 `KG_RECON_POISON_COOLDOWN=\"${KG_RECON_POISON_COOLDOWN:-3600}\"` 的同名 env，兩邊語意一致。）並在檔頭 :24-30 的 env / seam 說明區各補一行提及新 env 與新 seam。 /\n\n3) REMOTE_BUNDLE 新增五個 key（**這是唯一觸及生產資料源的一步，stub 測不到，故步驟 6 另立結構斷言把它釘住**）。REMOTE_BUNDLE 是 :61-84 的單引號字串（:61 `REMOTE_BUNDLE='`、:84 收尾單獨一行 `'`），**內部只能用雙引號**，出現單引號會提前結束字串。先比照既有 splice 手法（:63 `C='\"$CONTAINER\"'`、:64 `D='\"$DATA_DIR\"'`）在 :64 之後加一行\n```\nP='\"$PROD_REPO\"'\n```\n再在 :83（`printf \"data_dir_mb\\t%s\\n\" \"$(du -sm \"$D\" 2>/dev/null | cut -f1 || echo 0)\"`）之後、:84 收尾單引號之前，追加五行（全用雙引號、不得用 awk，因為 awk 程式碼需要單引號）：\n```\nprintf \"deployed_sha\\t%s\\n\" \"$(cat \"$P/backend/VERSION\" 2>/dev/null | head -1 | tr -d \"[:space:]\" || true)\"\nprintf \"origin_prod_sha\\t%s\\n\" \"$(git -C \"$P\" rev-parse --short origin/prod 2>/dev/null || echo unknown)\"\nprintf \"reconciler_tick_epoch\\t%s\\n\" \"$(sed -n \"s/^tick //p\" \"$P/backups/reconciler.state\" 2>/dev/null | tail -1 || true)\"\nprintf \"reconciler_poison_epoch\\t%s\\n\" \"$(sed -n \"s/^poison [^ ]* //p\" \"$P/backups/reconciler.state\" 2>/dev/null | sort -n | tail -1 || true)\"\nprintf \"host_now_epoch\\t%s\\n\" \"$(date +%s)\"\n```\n年齡一律用遠端時鐘（host_now_epoch）相減，免本機時鐘偏移污染。bundle 內 :62 有 `set -e`，但以上皆為 printf 的引數內命令替換且各自有 `|| true` / `|| echo`，printf 本身恆回 0，不會中止 bundle。註：`deployed_sha` 這個字串在 ops/kg_reconcile.sh 也存在（verdict JSON 欄位），但步驟 6 的結構斷言只掃 ops/infra_health.sh 的 REMOTE_BUNDLE 區段，不會被它滿足。 /\n\n4) 判讀段。在 :196（`ddir=\"$(getm data_dir_mb)\"; add data_dir_mb \"資料目錄大小\" \"${ddir:-?}MB\" ok \"$ddir\"`）之後、:198 `# ── 5. 輸出` 之前，整段包在 `if [[ \"$DEPLOY_DRIFT\" == \"1\" ]]; then … fi` 內（用字串比較，不用 `-eq`，避免非數值 env 讓 `[[ ]]` 報錯）：\n```\nif [[ \"$DEPLOY_DRIFT\" == \"1\" ]]; then\n  dsha=\"$(getm deployed_sha)\"; osha=\"$(getm origin_prod_sha)\"; hnow=\"$(getm host_now_epoch)\"\n  if [[ -z \"$dsha\" || -z \"$osha\" || \"$dsha\" == \"unknown\" || \"$osha\" == \"unknown\" ]]; then\n    add deploy_drift \"部署漂移(VERSION vs origin/prod)\" \"${dsha:-?} vs ${osha:-?}\" warn \"\"\n  elif [[ \"$dsha\" == \"$osha\" ]]; then\n    add deploy_drift \"部署漂移(VERSION vs origin/prod)\" \"$dsha 已收斂\" ok \"\"\n  else\n    add deploy_drift \"部署漂移(VERSION vs origin/prod)\" \"$dsha vs $osha\" warn \"\"\n  fi\n\n  tick=\"$(getm reconciler_tick_epoch)\"; tick_age=\"\"\n  [[ -n \"$tick\" && -n \"$hnow\" ]] && tick_age=$(( hnow - tick ))\n  add reconciler_tick_age_s \"Reconciler 心跳年齡\" \"${tick_age:-?}s\" \"$(th_high \"${tick_age:-}\" $TICK_WARN $TICK_CRIT)\" \"$tick_age\"\n\n  pz=\"$(getm reconciler_poison_epoch)\"; pz_age=\"\"; pz_st=\"ok\"; pz_val=\"無\"\n  [[ -n \"$pz\" && -n \"$hnow\" ]] && pz_age=$(( hnow - pz ))\n  if [[ -n \"$pz_age\" ]]; then\n    if (( pz_age < POISON_COOLDOWN )); then pz_st=\"warn\"; pz_val=\"${pz_age}s 前 poison（冷卻中）\"\n    else pz_val=\"${pz_age}s 前 poison（已過冷卻）\"; fi\n  fi\n  add reconciler_poison_active \"Reconciler poison\" \"$pz_val\" \"$pz_st\" \"$pz_age\"\nfi\n```\n契約說明：(a) deploy_drift **永不 crit**——release 後到 reconciler 收斂完成之間的 drift 是設計上的正常瞬態，判 crit 就是步驟 0 的自噬迴圈。(b) reconciler_tick_age_s 可以 crit：它只在 reconciler 真的停擺時才紅，而步驟 5 的開關已讓自我 gate 不消費它；空值時 th_high（:128-129 `[[ -z \"$v\" ]] && { echo unknown; return; }`）回 unknown，經 bump（:123-126 `warn|unknown) [[ \"$overall\" == \"ok\" ]] && overall=\"warn\"`）升成 warn，不會靜默假綠。(c) `[[ ... ]] && x=$(( ... ))` 與 `if (( ... ))` 在 :32 `set -euo pipefail` 下皆不會中止——前者是 `&&` 列表中非最末命令（既有 :148 `[[ -n \"$mt\" && -n \"$ma\" && \"$mt\" -gt 0 ]] && mem_pct_avail=$(( ma * 100 / mt ))` 已在 KG_TEST_EMPTY 路徑下驗證過同一寫法），後者是 if 的 test 部分。(d) 既有五條要求 overall 精確值的斷言（ops/test_infra_health.sh:68 全綠 ok、:121 warn、:125 warn、:130 crit、:135 warn）在步驟 6 的健康預設值下不受影響；KG_TEST_EMPTY 案例（stub :27 提前 exit）三個新 metric 會是 warn/warn/ok，而 overall 已被 host_collect 判 crit，bump 只升不降故不會被稀釋。 /\n\n5) 關掉自我 gate 的迴圈：ops/kg_reconcile.sh:187 現為\n```\n  set +e; \"$KG_INFRA_HEALTH\" >/dev/null 2>&1; ih=$?; set -e\n```\n改成\n```\n  # 部署收斂是 reconciler 自己的職責；讓它把自己的收斂狀態當健康條件會構成迴圈\n  # （release 後到收斂完成之間必然 drift → 回滾一次本來健康的部署）。故關掉該組 metric。\n  set +e; KG_HEALTH_DEPLOY_DRIFT=0 \"$KG_INFRA_HEALTH\" >/dev/null 2>&1; ih=$?; set -e\n```\n（同一行前綴賦值即可，會傳進子程序。） /\n\n6) 測試 ops/test_infra_health.sh。\n(6a) STUB（:25-41，heredoc 為 `<<'EOF'`）在 :40 `printf 'data_dir_mb\\t393\\n'` 之後補五行。**預設值必須是健康的**（兩 sha 相同、心跳年齡 60s、無 poison），否則 :68 的「全綠 overall=ok」既有斷言會紅：\n```\nprintf 'deployed_sha\\t%s\\n' \"${KG_TEST_DEPLOYED_SHA-abc1234}\"\nprintf 'origin_prod_sha\\t%s\\n' \"${KG_TEST_ORIGIN_SHA-abc1234}\"\nprintf 'reconciler_tick_epoch\\t%s\\n' \"${KG_TEST_TICK_EPOCH-$(( $(date +%s) - 60 ))}\"\nprintf 'reconciler_poison_epoch\\t%s\\n' \"${KG_TEST_POISON_EPOCH-}\"\nprintf 'host_now_epoch\\t%s\\n' \"${KG_TEST_NOW_EPOCH-$(date +%s)}\"\n```\n**這五行一律用無冒號的 `${VAR-default}`，不得寫成 `${VAR:-default}`。原因（已實測）**：冒號形式在變數「已設但為空字串」時也會代入 default，於是下面「心跳缺值」案例拿到的會是健康的 now-60 而非空值，斷言必敗且 acceptance 永遠綠不了。實測佐證：`KG_TEST_TICK_EPOCH=\"\" bash -c 'printf \"colon=[%s] plain=[%s]\\n\" \"${KG_TEST_TICK_EPOCH:-D}\" \"${KG_TEST_TICK_EPOCH-D}\"'` → `colon=[D] plain=[]`。對應地，缺值案例一律用 `KG_TEST_TICK_EPOCH= run_health --json`（前綴賦空值）；已實測此寫法會把空值傳到 stub 子程序，且不污染後續案例。\n(6b) 在 :136（`&& ok \"cert 量不到 → unknown→warn\" || fail_t ...`）之後、:138 `echo \"\"` 之前追加六個 section。`ok`/`fail_t` 定義在 :18-19（`ok() { echo \"  ✓ $*\"; ... }`），下列 ok 字串必須**逐字照抄**，acceptance 會 grep 它們：\n```\nsection \"REMOTE_BUNDLE 結構：五個部署漂移 key 真的在遠端 bundle 內\"\nbundle_src=\"$(sed -n \"/^REMOTE_BUNDLE='/,/^'$/p\" \"$SCRIPT\")\"\nmiss=\"\"\nfor k in deployed_sha origin_prod_sha reconciler_tick_epoch reconciler_poison_epoch host_now_epoch; do\n  printf '%s' \"$bundle_src\" | grep -qF \"$k\" || miss=\"$miss $k\"\ndone\n[[ -z \"$miss\" ]] && ok \"REMOTE_BUNDLE 含五個漂移 key\" || fail_t \"REMOTE_BUNDLE 缺:$miss\"\n\nNOWE=\"$(date +%s)\"\n\nsection \"deploy_drift：兩 sha 不同 → warn 且 overall 不得 crit\"\necho \"$(KG_TEST_ORIGIN_SHA=deadbee run_health --json 2>/dev/null)\" | py 'import sys,json;d=json.load(sys.stdin);g=[m for m in d[\"metrics\"] if m[\"key\"]==\"deploy_drift\"][0];assert g[\"status\"]==\"warn\",g;assert d[\"overall\"]!=\"crit\",d[\"overall\"]' \\\n  && ok \"deploy_drift 兩 sha 不同 → warn 且 overall 非 crit\" || fail_t \"deploy_drift 未 warn 或誤判 crit\"\n\nsection \"reconciler_tick_age_s：2000s → crit\"\necho \"$(KG_TEST_NOW_EPOCH=$NOWE KG_TEST_TICK_EPOCH=$(( NOWE - 2000 )) run_health --json 2>/dev/null)\" | py 'import sys,json;d=json.load(sys.stdin);g=[m for m in d[\"metrics\"] if m[\"key\"]==\"reconciler_tick_age_s\"][0];assert g[\"status\"]==\"crit\",g' \\\n  && ok \"reconciler_tick_age_s 2000s → crit\" || fail_t \"tick 2000s 未 crit\"\n\nsection \"reconciler_tick_age_s：缺值 → 不得假綠\"\necho \"$(KG_TEST_TICK_EPOCH= run_health --json 2>/dev/null)\" | py 'import sys,json;d=json.load(sys.stdin);g=[m for m in d[\"metrics\"] if m[\"key\"]==\"reconciler_tick_age_s\"][0];assert g[\"status\"]!=\"ok\",g' \\\n  && ok \"reconciler_tick_age_s 缺值 → 非 ok\" || fail_t \"tick 缺值 假綠\"\n\nsection \"reconciler_poison_active：距今 100s → warn；99999s → ok\"\necho \"$(KG_TEST_NOW_EPOCH=$NOWE KG_TEST_POISON_EPOCH=$(( NOWE - 100 )) run_health --json 2>/dev/null)\" | py 'import sys,json;d=json.load(sys.stdin);g=[m for m in d[\"metrics\"] if m[\"key\"]==\"reconciler_poison_active\"][0];assert g[\"status\"]==\"warn\",g' \\\n  && ok \"reconciler_poison_active 100s → warn\" || fail_t \"poison 100s 未 warn\"\necho \"$(KG_TEST_NOW_EPOCH=$NOWE KG_TEST_POISON_EPOCH=$(( NOWE - 99999 )) run_health --json 2>/dev/null)\" | py 'import sys,json;d=json.load(sys.stdin);g=[m for m in d[\"metrics\"] if m[\"key\"]==\"reconciler_poison_active\"][0];assert g[\"status\"]==\"ok\",g' \\\n  && ok \"poison 冷卻已過 → ok\" || fail_t \"poison 冷卻已過 未 ok\"\n\nsection \"開關：KG_HEALTH_DEPLOY_DRIFT=0 → 三個 metric 完全不出現\"\necho \"$(KG_HEALTH_DEPLOY_DRIFT=0 run_health --json 2>/dev/null)\" | py 'import sys,json;d=json.load(sys.stdin);ks={m[\"key\"] for m in d[\"metrics\"]};assert not (ks & {\"deploy_drift\",\"reconciler_tick_age_s\",\"reconciler_poison_active\"}),ks' \\\n  && ok \"KG_HEALTH_DEPLOY_DRIFT=0 → 三個 metric 不出現\" || fail_t \"開關無效\"\n```\n（`$SCRIPT` 定義在 :15 = ops/infra_health.sh；`py` 定義在 :21；`run_health` 定義在 :49-53。sed 的區段擷取已實測：今日抓到 24 行（:61-84），五個 key 命中數皆為 0。此斷言掃的是 infra_health.sh，不是測試檔自己，故不會自我滿足。） /\n\n7) 測試 ops/tests/test_kg_reconcile.sh。\n(7a) 把 :206-209 的 infra_health stub\n```\n  cat >\"$BIN/infra_mock.sh\" <<'EOF'\n#!/usr/bin/env bash\nexit \"${MOCK_INFRA_EXIT:-0}\"\nEOF\n```\n改為**展開式** heredoc（去掉 EOF 的引號，讓 `$SC` 展開；其餘 `$` 要跳脫）：\n```\n  cat >\"$BIN/infra_mock.sh\" <<EOF\n#!/usr/bin/env bash\nprintf '%s\\n' \"\\${KG_HEALTH_DEPLOY_DRIFT:-unset}\" >> \"$SC/infra_env.log\"\nexit \"\\${MOCK_INFRA_EXIT:-0}\"\nEOF\n```\n:210 的 `chmod +x \"$BIN/git_mock.sh\" \"$BIN/compose_mock.sh\" \"$BIN/infra_mock.sh\"` 在替換範圍之外，不必動。run_recon（:213-230）已 `export KG_INFRA_HEALTH=\"$BIN/infra_mock.sh\"`（:219），故 stub 一定被叫到。\n(7b) 在 \"no-change → noop\" 段（:256-264）末尾 :264 之後加一行（ok/bad 定義在 :28-29，STATE 定義在 :116）：\n```\ngrep -q \"^tick \" \"$STATE\" 2>/dev/null && ok \"noop: tick 心跳已寫入 state\" || bad \"noop: tick 心跳未寫入 state\"\n```\n(7c) 在 \"backend change + smoke 全綠 → deployed\" 段（:278-293）末尾 :293 之後加一行：\n```\n[[ \"$(tail -1 \"$SC/infra_env.log\" 2>/dev/null)\" == \"0\" ]] && ok \"deployed: infra_health 以 KG_HEALTH_DEPLOY_DRIFT=0 呼叫\" || bad \"deployed: infra_health 未關掉 deploy_drift（自噬迴圈風險）\"\n```\n(7d) 既有 :432 `[[ ! -f \"$STATE\" ]] && ok \"dry-run: state not written\"` 維持綠，即是心跳的 dry-run 護欄，不要改它。 /\n\n8) docs 同 PR：docs/sop/deploy.md:78（該行含「健康 gate = localhost `/api/system/info`… → `ops/infra_health.sh`（exit 0 pass／1 warn 仍 pass／2 crit fail）」）補一句：reconciler 以 `KG_HEALTH_DEPLOY_DRIFT=0` 呼叫 infra_health，理由是部署收斂是 reconciler 自身職責，納入自我健康條件會構成「release 瞬態 drift → 回滾健康部署」的迴圈。docs/reference/tech_index.md:155（`kg_reconcile.sh` 那列，現以「…seam 全可注入(測試 `ops/tests/test_kg_reconcile.sh`);SOP 見 `docs/sop/deploy.md` §reconciler…」收尾）補：每輪（含 noop）寫 `tick <epoch>` 心跳到 `backups/reconciler.state`（dry-run 不寫）；同列或 `infra_health.sh` 相關敘述補上新增的三個 metric `deploy_drift` / `reconciler_tick_age_s` / `reconciler_poison_active` 與開關 `KG_HEALTH_DEPLOY_DRIFT`。PR 開出前跑 `./ops/docs_lint.sh`。",
  "resolution": "—(2026-08-05 驗證 PARTIAL:前提已更正為 origin/prod、資料面已修;deploy 0611f3ca 解當下,監控候選未做;落點 `ops/infra_health.sh`,成本 S,測試 `ops/test_infra_health.sh` 已有 `KG_BASE` seam 可注入)",
  "schema": "kg.backlog.entry.v1",
  "scope": "兩支運維腳本各加幾段：一個心跳寫入、三個新健康指標與一個開關，連同兩份測試與兩處說明；不碰生產資料。",
  "severity": "med",
  "source": "行銷帳號 Phase5(felix 漂移)",
  "status": "fixed",
  "stream": "IMP",
  "verdict": "CONFIRMED-FIXED",
  "verified_at": "2026-08-09",
  "verified_by": "agent:main-session",
  "verified_evidence": "acceptance_cmd 由本次 verify --commit 於 cutover 後的 main 上實跑（rc 不符即拒絕落地）；修法已落地於 538b50716"
}
