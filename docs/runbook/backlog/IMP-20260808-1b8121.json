{
  "acceptance": "任何 destructive failure 不得只輸出 committed=false 讓操作者誤判為未寫入；若採 restore，失敗後資料與前狀態等價且 data_mutated=false；若採 marker，必有 data_mutated=true、backup 與 recovery path；seed --replace malformed spec 在 wipe 前失敗；dry-run 零寫入。",
  "acceptance_cmd": "cd backend && uv run pytest -q tests/test_ops_edit.py tests/test_ops_edit_shared_unit.py",
  "brief": "資料工具寫到一半失敗時會說「沒有寫入」,但先清空後重建的操作其實已經把資料清掉了。",
  "category": "arch",
  "date": "2026-08-08",
  "detail": "`EditContext.run`(`backend/src/kg/ops_edit_shared.py`)的失敗輸出對「破壞性步驟是否已經執行」說謊:apply_fn 中途 raise 時走 except 分支,輸出 `mode=error` + `\"committed\": false`,但那個布林講的是「這次操作有沒有成功完成」,operator 讀成的是「磁碟沒被動過」。對只會新增東西的子指令兩者差別不大;對 `seed --replace`(先清整層 vocab 再灌)這類**先破壞後建構**的操作,`committed: false` 可以同時代表「帳號已被清空」。\n\nIMP-0024 當時的具體實例已修(replace 的 notebook 參照移到 `_prevalidate`,wipe 前就紅),但那是打地鼠:`_prevalidate` 仍不驗 `notebooks[].color` / `cover_pattern` / `source_version` 與 `cards[].difficulty` / `note` / `pos` 的型別,型別壞的 spec 照樣能在 wipe 之後才 raise,同一個謊就回來了。\n\n正解是結構性的,擇一或並用:① `EditContext` 讓 apply_fn 得以申報「破壞性步驟已執行」(例如 apply 回傳前先記一個 checkpoint,或提供 `ctx.mark_destructive()`),失敗輸出改帶 `data_mutated: true` 與 backup 路徑並在人類輸出明講「資料已變更,可從 <backup> 還原」;② 破壞性操作失敗時自動從剛建的備份 restore,回到操作前狀態再報錯。②更接近事務語意但要處理 restore 自身失敗。\n\n現況的緩解是備份確實在 apply 之前建立且路徑有出現在 error payload —— 但沒有任何一個字告訴 operator「你的資料已經沒了、去還原」。",
  "fix_site": "backend/src/kg/ops_edit_shared.py::EditContext.run; backend/src/kg/ops_edit_seed_commands.py::{cmd_seed,_prevalidate}; backend/tests/test_ops_edit_shared_unit.py; backend/tests/test_ops_edit.py",
  "groomed_against": "d54fce7bc195f0b823081f228bddd6472c3916d4",
  "groomed_at": "2026-08-09",
  "groomed_by": "workflow:groom@v1",
  "id": "IMP-20260808-1b8121",
  "plan": "先升級上一階決定 EditContext 的 destructive failure contract：選擇可驗證的 auto-restore，或明確標示 data_mutated 與 backup/recovery；本票不自行選擇架構分支。定案後在 EditContext.run 建立 destructive checkpoint，讓 seed --replace 的 wipe 與後續失敗可被正確表達；補齊 seed prevalidation 的 notebook/card typed fields，確保 malformed spec 在 wipe 前拒絕。",
  "resolution": "",
  "schema": "kg.backlog.entry.v1",
  "scope": "動 ops 寫入工具的共用編排層,牽涉所有子指令的失敗輸出契約,要配回歸測試。",
  "severity": "med",
  "source": "IMP-0024 code review 的結構性建議(backend-engineer)",
  "status": "open",
  "stream": "IMP"
}
