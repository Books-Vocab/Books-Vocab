{
  "category": "tool",
  "cost": "M",
  "date": "2026-08-05",
  "detail": "schema 稱 `resolution` 為「可回溯的關鍵欄」並明令不得以分支名代替 hash，但 `validate_entry`（:190-215）只驗必填欄存在與 stream/category/severity/status 詞彙——`status: fixed` 配空的、純散文的、或分支名的 resolution 一律 0 problems。同族第三個洞：`verdict` 是宣告過的閉集（`VERDICTS`），但只在 extractor 裡被守；`update <id> --verdict TOTALLY-BOGUS --commit` 實測 rc=0 落地，validate 仍報 0 problems。**受害者四筆不是兩筆**（判準取 `docs_lint.sh` 的兩段式：`rev-parse --verify <sha>^{commit}` 再 `merge-base --is-ancestor <sha> HEAD`，看 resolution 位置 0 的 sha；34 筆 fixed 中 30 清 4 紅）：IMP-0005（`813356b1`，物件根本不在 odb）、IMP-0063（位置 0 是分支名 `feat/freeze-ui-world`，分支已刪）、IMP-0001（`7c95a02`）與 IMP-0002（`3671b89`）——**物件存在但不是 HEAD 或 origin/main 的祖先**，PR #968 被 rebase-merge 遺下的 dangling commit。後兩筆是主線用 `git cat-file -e` 掃描漏掉的，因為該呼叫對 dangling object 回 0；那是同一輪裡我第二次被自己寫的偵測器騙（第一次是 incidental hash 滿足 any 斷言）。**四筆全部可救、無永久損失**：IMP-0001 到 `98aa04fe6`、IMP-0002 到 `fc2c7d454`（`git patch-id --stable` 位元組相同，確定）；IMP-0005 到 `52a89295a`（寫入該 hash 的 commit `a52519c4c` 的直接父，其 docs_lint 改動與 detail 逐字相符，極高信心；literal 字串本身確定不可復原）；IMP-0063 從來沒有 squash，工作以五個普通 commit 落地（`e1164bc3f` `59376883e` `e2e343a14` `8e5e79a9e` `f918d6ecc`）。**另一條 prose-only 不變式也已破**：platform-steward 的「open 無 next action」在 IMP-0064、IMP-0065、IMP-20260805-afc14b 三筆為空，而其餘 27 筆未結案有 26 筆是破折號開頭的 next action，慣例明確且可機器化。（反之 wont-fix 須附理由 3/3 成立、未破，原始框架的推測被證偽。）**而且 `validate` 根本沒有自動呼叫者**：`.github/workflows/` 與 `ops/*.sh` 全無，只有 `ops/tests/test_backlog.py` import 該模組——所以不是「機器面只到詞彙為止」，是機器面根本沒被排程。散文欄位的可檢查性上限就是位置啟發式：位置 0 規則實測 33/34 成立，但它表達不了 IMP-0063 的五個 sha，遇到 resolution 以重新取證戳記開頭就崩，且 IMP-0028 那筆裸的未加反引號 sha 會逼 regex 放寬到重新引入歧義。",
  "fix_site": "ops/backlog.py:190-284",
  "id": "IMP-20260805-9a51e9",
  "plan": "[二度遭退回 2026-08-05，兩版草稿皆未通過對抗驗證。最新退回理由：四段 acceptance 今天全紅、紅因全部正確、行號抽查 12 處全部逐字命中、回填配方我實跑模擬過（40 顆 sha 零歧義）。推翻理由只有一個，但它打在要害：**A 的步驟 7 沒有任何驗收守得住，而步驟 7 是整條 A 唯一連到可執行面的那一行**。\n\n具體：A 的 plan 步驟 7 是把 `ops/backlog.py:1162` 的 `problems = validate_store(args.store)` 改成 `validate_store(args.store) + check_traceability(args.store)`。若實作者做了步驟 1-6、8 而**唯獨漏掉步驟 7**：\n- A 的驗收 `-k test_a_fixed_entry_must_name_a_reachable_commit` 綠（該測試直接呼叫 `BACKLOG.check_traceability(store, probe=probe)`，不經 CLI）；\n- `test_the_real_ledger_is_fully_traceable` 也綠（同樣直接呼叫 `BACKLOG.check_traceability(BACKLOG.DEFAULT_STORE)`）；\n- 母條四段鏈全綠（段 1 `./ops/test_ops.sh backlog` 跑整個 test_backlog.py，裡面沒有任何一條碰 `_cmd_validate`）；\n- 而 `./ops/backlog.py validate` **仍然只跑 validate_store**，仍然 `0 problems` rc=0 —— 也就是母條 detail 的頭號指控（「`status: fixed` 配空的 / 分支名的 resolution 一律 0 problems」）原封不動；\n- 更糟：D 新增的 block gate `[\"ops/backlog.py\",\"validate\"]` 跟著變成永遠綠的空 gate，D 的測試只斷言 gate 被規劃出來、cmd 與 level 對，從不斷言它能偵測到壞 ledger。A+D 兩條都「驗收通過」，缺陷卻一點沒動。\n\n我實查過沒有既有測試補得上這個洞：`grep -rn \"_cmd_validate\" ops/ docs/` 只有 `ops/backlog.py:1161` 定義與 `:1330` dispatch 表兩處；`ops/tests/test_backlog_migration.py` 的三個 `main([\"validate\"...])`（:610/:616/:621）分別是 open 乾淨 store→0、status 改成 \"done\"→2、store 不存在→2，三條在有沒有步驟 7 之下輸出都一樣。\n\n**下一位 groomer 照著改即可**：在 A 的步驟 1 新測試 `test_a_fixed_entry_must_name_a_reachable_commit` 末尾（`unfixed-entry-names-a-commit` 那段之後）補一條走 CLI 的斷言，例如\n```python\n    # step 7 is the only line that connects the check to an executable surface;\n    # without this assertion it can be skipped with every test still green.\n    solo = tmp_path / \"solo\"\n    _add(solo, detail=\"fixed with nothing to point at\", status=\"fixed\")\n    assert BACKLOG.main([\"validate\", \"--store\", str(solo)]) == 2\n```\n（用獨立的 `solo` store，該筆 status=fixed 且無 `fixed_by` → `check_traceability` 走 `fixed-without-commit` 分支、**不會呼叫 probe**，所以不引入 git 相依，`_cmd_validate` 回 2；漏掉步驟 7 則回 0，測試立刻紅。）母條 acceptance 不必改，段 1 與段 2 都會吃到它。\n\n另有兩個較輕但該一併補的洞（單獨不足以推翻，但既然要重送就一起修）：\n1. **C 的步驟 3 無驗收**。C 的 acceptance 是 `./ops/test_ops.sh backlog`，它不跑 `ops-ci-coverage`。漏掉步驟 3（`ops/tests/test_ops_ci_coverage.sh:48` 後插 `backlog` 進 `LINUX_GROUPS`）→ 該檔 :142-156「every declared group is classified exactly once」的 `fail_t \"$g is unclassified\"` 會紅，但母條四段鏈仍全綠；cutover 也看不到（`ops/test_ops.sh` 在 `ops/worktree_orchestrate.py:254-259` 的 `OPS_SHELL_UNROUTABLE_TESTS` 內，只拿到 syntax + advisory）。建議把 C 的 acceptance 改成 `./ops/test_ops.sh backlog && ./ops/tests/test_ops_ci_coverage.sh`，並在 plan 內警告後者在裝了 Xcode 的機器上會對 `ios-ops` / `lldb-forensics` 兩個 bin-xcode 排除跑 undenied+denied 兩趟（`ops/tests/test_ops_ci_coverage.sh:409-427`），耗時數分鐘。\n2. **D 新增的 block gate 逃出 gate-health 契約**。`ops/tests/test_gate_can_fail.sh:137-188` 要求「每個 block gate 都要在 `PROOFS` 宣告紅/綠兩向證明」，但它的探針清單 :143-152 只有 ios/docs(.md)/backend/ops/design-system/README，**沒有任何 `docs/runbook/backlog/*.json`**，所以 `backlog-validate` 不會被列舉、也不會被要求證明。今天不會轉紅（我確認過），但等於新開一個「沒人問過它能不能變紅」的 block gate——正是那份檔案開頭列的三個歷史事故的形狀。D 應加第 6 步：在 :152 的 `[\"README.md\"],` 之前插 `[\"docs/runbook/backlog/IMP-0001.json\"],`，並在 `PROOFS` 加一筆 `backlog-validate|cheap|cheap|<note>`（紅證明＝對含 fixed-without-fixed_by 的 fixture store 跑 validate 期待 rc=2 且輸出含 `fixed-without-commit`；綠證明＝對真實 store 跑期待 rc=0）。\n\n其餘部分我沒能推翻，重送時可原樣保留：B 完全站得住（`_check_vocabulary` 路線、不加 argparse `choices=` 的理由、`_ID_RE` 定義在後不影響、真實 store verdict 只有 CONFIRMED-OPEN×34 + PARTIAL×2 皆合法）；A 不進 `validate_entry` 的設計正確且我逐一驗過 8 個 `validate_store` 與 3 個 `main([\"validate\"])` 呼叫點都不會轉紅；步驟 3+5 成對維持 `test_every_update_flag_reaches_a_field_and_every_field_has_a_flag`（:558-564）的兩側等式；回填配方零人工判斷。]\n\n以下為第二版草稿，重做時請先讀上面的退回理由：\n本條拆為 4 筆執行（A 可回溯性、B verdict 詞彙、C 測試 group、D cutover 路由）：\n\nA. `ops/backlog.py` 加 `fixed_by` 結構化欄位與 `check_traceability()`，並回填全部 36 筆 fixed；`./ops/backlog.py validate` 才會對「status=fixed 卻沒指名任何可達 commit」轉紅。\nB. `_check_vocabulary` 加 `verdict` 閉集檢查，`update --verdict TOTALLY-BOGUS --commit` 不再 rc=0 落地。\nC. `ops/test_ops.sh` 新增 `backlog` group（今天 `ops/tests/test_backlog.py` 在 shell/CI 全 repo 零自動呼叫者），並在 `ops/tests/test_ops_ci_coverage.sh:39` 的 `LINUX_GROUPS` 分類。\nD. `ops/worktree_orchestrate.py:plan_gates` 新增 `ledger` bucket，讓 `docs/runbook/backlog/*.json` 的改動選到 `ops/backlog.py validate`（今天它落進 `coverage.uncovered`，零 gate）。\n\n執行順序建議 B → A → C → D（B 最小、A 最大且含回填、C 把 A/B 的測試接進套件、D 把 validate 接進 cutover）。四筆彼此不互相阻塞，但 C 的 group 命令會同時跑 A 與 B 的新測試，所以 C 若先做會在 A/B 落地前保持綠（那是預期，母條驗收鏈的第 2/3/4 段各自獨立守住 A/B/D）。\n\n母條驗收（見 acceptance 欄）是四段 `&&` 鏈，每段只用**單一** `-k` 名稱或單一 group：任何一筆子項沒落地，該段就 rc=5（pytest「一條都沒選到」）或 rc=1，`&&` 立刻斷鏈。**刻意不用** `-k \"A or B or D\"`——`or` 選擇器只要任一名稱命中且通過就 rc=0，未存在的名字算 deselected 不算失敗，那會讓「只落地一部分」照樣驗收綠（本輪已實測此語意）。\n\n[該版 acceptance] ./ops/test_ops.sh backlog \\\n && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_a_fixed_entry_must_name_a_reachable_commit \\\n && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_backlog.py -k test_validate_rejects_an_out_of_vocabulary_verdict \\\n && uv run --no-project --python 3.13 --with pytest pytest -q ops/tests/test_worktree_orchestrate.py -k test_gate_plan_backlog_ledger_change_selects_backlog_validate",
  "resolution": "—(2026-08-05 驗證 CONFIRMED-OPEN；落點 `ops/backlog.py:190-215`，成本 M（約 40 行驗證加一次 git shell-out，對一個目前純檔案系統、測試無 git fixture 的模組是新相依；回填 7 個檔；CI 接線一行）。**設計**：加專用結構化欄位 `fixed_by: [sha,…]`，沿用 VERDICT_FIELDS 的附加式前例，resolution 原文仍權威——incidental hash 永遠進不了 `fixed_by`。每個 status 各有規則：fixed 要 `fixed_by` 非空且每個 sha 可解析並為 HEAD 祖先；wont-fix 要非空理由且不得是裸 sha；open/triaged/in-progress 要非空 next action 且 `fixed_by` 必須空（未完成卻指著 commit 是 status 說謊）。**參考系是 HEAD 不是 origin/main，這是刻意偏離 `docs/sop/doc_sync.md:25-27`**：錨點指向後方的 base，`fixed_by` 指向改動本身；實測本地 main 超前 origin 27 個 commit，IMP-0063 那五顆全在超前段，用 origin/main 判會把正確答案在建檔當天判紅。`--is-ancestor <sha> origin/main` 只能當 WARN（意為尚未推送）。復用 `ops/docs_lint.sh:403-426` 的兩段式機制，但**不可**復用 `:417-419` 的 audit 降級——那正是讓 IMP-0038 的 22 個孤兒錨點本機綠而 CI 紅的東西。**不設 grandfather list**：四筆全可解，約 20 分鐘機械回填，留清單等於複製 IMP-0038 已失敗的手動 re-bump 待辦。驗收：`./ops/backlog.py validate` 今天 0 problems rc=0；檢查落地後、回填前必須 7 problems（4 筆 resolution-not-traceable 加 3 筆 open-without-next-action），回填後回 0。負控證明 incidental-hash 洞已關：IMP-0063 的 resolution 仍含可達的 `0c9e3b7c` 但 `fixed_by` 空，必須仍紅。未驗：34 次 git shell-out 的 validate 執行成本；`813356b1` 是否存活於他機 clone。)",
  "schema": "kg.backlog.entry.v1",
  "severity": "high",
  "source": "backlog CLI 稽核（5-agent deep dive）",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
