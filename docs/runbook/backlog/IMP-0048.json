{
  "acceptance": "KG_INJECTION_BASELINE=/tmp/kg-nonexistent-baseline.txt ./ops/ui_quality_gate.sh --files ios/BooksAndVocab/Views/Vocabulary/Components/VocabCalendarGrid.swift --tier fast --dry-run --json | jq -e \".results[] | select(.id==\\\"static.injection\\\") | select(.warning != null)\"  # today: exit 4, no output",
  "category": "tool",
  "date": "2026-08-03",
  "detail": "`ops/tests/test_ui_quality_gate.sh:177-203` 用 `mv` 把**已版控**的 `ops/injection_baseline.txt` 搬走再還原，只靠 `trap restore EXIT` 復原。程序被 SIGKILL 或機器掉電落在中間 → 工作樹少一個已版控檔案，而測試本身不會說。同檔其他測試已改用 `KG_INJECTION_BASELINE` 環境變數重導（`ops/tests/test_lint_baselines.sh:51-55` 是範例）。下一步：讓 `ui_quality_gate.sh` 尊重既有的 baseline 環境變數覆寫，測試改用環境變數，`mv` 整段刪掉。 **2026-08-04 實際發生(嚴重度 low→med)**:本輪一個 repo-root `git add -A` 撞上「baseline 被搬走」的時窗，git 直接把缺席 staged 進 commit `400bd6c5f`,`ops/injection_baseline.txt` 就這樣從分支上消失,由 `9037aca00` 還原(內容與 main 位元組相同)。**原條目假設的觸發是 SIGKILL 落在 mv 與 trap 之間,但一次普通的並發 `git add -A` 就足夠,且兩邊都不會出聲**。 **2026-08-05 補:修法有嚴格順序,原「下一步」漏了前置。** 測試之所以只能 `mv`,是因為 `ops/ui_quality_gate.py:147-153 injection_args()` **硬編** baseline 路徑、只用 `.exists()` 決定 `--baseline-check` vs `--report`,完全不看 `KG_INJECTION_BASELINE`——環境變數騙不過它。環境變數本身在 lint 層已存在(`ops/injection_lint.py:31`,並由 `ops/tests/test_injection_lint.sh:42-58` 驗著)。事故複核:`git show --stat 400bd6c5f` 確為 `ops/injection_baseline.txt | 44 -----` 純刪除(12:38,夾在一個 docs commit 裡),`9037aca00`(12:43)還原且 commit message 自述成因是 repo-root `git add -A` 撞上 baseline 被搬走的時窗。",
  "fix_site": "ops/ui_quality_gate.py:144",
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom@v1",
  "id": "IMP-0048",
  "plan": "1) ops/ui_quality_gate.py:144-151 injection_args(root) — today it reads baseline = root / \"ops\" / \"injection_baseline.txt\", then returns ([--baseline-check], None) when it exists and ([--report], warning) when it does not. The hardcoded path is the whole defect: it is why the test can only move the tracked file aside. Change the first statement to honour the override the lint layer already reads (ops/injection_lint.py:31, BASELINE_FILE = Path(os.environ.get(\"KG_INJECTION_BASELINE\", \"ops/injection_baseline.txt\"))): read env = os.environ.get(\"KG_INJECTION_BASELINE\"); when set, baseline = Path(env) if that is absolute else root / env; otherwise keep root / ops / injection_baseline.txt. Leave the two return branches alone but put the resolved path into the warning string so the message names the file it actually looked for. os is already imported at :24 — no new import.\n2) No propagation code. run_mech (:205-213) calls subprocess.run(cmd, cwd=root, ...) with no env= argument, so the child inherits KG_INJECTION_BASELINE, and ops/injection_lint.sh cds to the repo root before exec, so a relative override resolves identically on both sides. Record that as a comment rather than re-exporting the variable — an explicit env= dict here would drop UV_BIN/PATH.\n3) ops/tests/test_ui_quality_gate.sh:261-287, section \"Missing injection baseline is handled gracefully\" — delete BASE, MOVED, the mv, restore(), trap restore EXIT and the trailing bare restore call, and run the gate as KG_INJECTION_BASELINE=\"$(mktemp -u)\" $GATE --files \"$SAMPLE_FILE\" --tier fast --execute. Keep both existing assertions (rc 0, output mentions the baseline / WARN) and add a third: ops/injection_baseline.txt must still be a file when the section ends. The 2026-08-04 incident (400bd6c5f staged the absence, 9037aca00 restored it) happened with nothing watching for exactly that.\n4) Also fix the now-false comment at ops/tests/test_ui_quality_gate.sh:233-235, which tells the next editor that the following section installs its own EXIT trap to put a version-controlled baseline back, and is the reason that section cleans up inline. After step 3 there is no such trap; leaving the note in place re-teaches the pattern this entry exists to delete.\n5) The order is load-bearing and the entry text originally missed it: step 3 before step 1 turns the section into a tautology — the env var cannot fool the gate, the real baseline answers, and the test still prints a pass.\n6) Measured red/green, so an implementation that only looks right is detectable: today KG_INJECTION_BASELINE=/tmp/kg-nonexistent-baseline.txt ./ops/ui_quality_gate.sh --files ios/BooksAndVocab/Views/Vocabulary/Components/VocabCalendarGrid.swift --tier fast --dry-run --json reports static.injection with command \"ops/injection_lint.sh --baseline-check\" and warning null; after the change it must report \"ops/injection_lint.sh --report\" with a non-null warning. Full gate afterwards: bash ops/tests/test_ui_quality_gate.sh (the --execute section is slow but safe once the mv is gone).\n7) Nothing else consumes injection_args; ops/ui_quality_plane.yml:105-112 keeps requires: injection-baseline unchanged and HANDLED_RESOURCES at ops/ui_quality_gate.py:59 needs no edit. ops/tests/test_lint_baselines.sh:51-55 already asserts the injection_lint.sh/KG_INJECTION_BASELINE pairing and must stay green.",
  "resolution": "—(2026-08-05 驗證 CONFIRMED-OPEN + 事故坐實;**順序不可反**:① `ui_quality_gate.py:147` 改讀 `KG_INJECTION_BASELINE` 並透傳給子進程,② 才刪 `test_ui_quality_gate.sh:179-190` 的 `mv`/`trap restore` 改用環境變數;各 S。改法本身即回歸證明)",
  "schema": "kg.backlog.entry.v1",
  "severity": "med",
  "source": "IMP-0044 的 review 面",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
