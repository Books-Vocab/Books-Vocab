{
  "category": "arch",
  "date": "2026-08-05",
  "detail": "ops/app_review/2.0.0.json 的 `target.sourceCommit` 釘在 60b7030e346e5316b8bf249c900fe585f1aeebbb，宣告這份 App Review evidence bundle 是「那顆 commit 的證據」。但 `ops/app_review_evidence.py:produce_desired_bundle()`（`:485`）傳給 `capture_profile.py reviewer-evidence` 的是 `--project-file <workspace>/ios/BooksAndVocab.xcodeproj/project.pbxproj`——**工作樹當下**那份 pbxproj，不是 `sourceCommit` 那個版本的。`ops/reviewer_evidence.py:_read_build_identity()` 把整個 pbxproj 的 bytes 做 sha256 當 build identity，`:479` 另把該檔原封複製進 bundle 的 `inputs/`。於是：一份宣稱描述特定 commit 的 evidence bundle，實際內容會隨 HEAD 漂移。\n\n實際發生（本條目來源）：commit fe4a82355 把 MARKETING_VERSION / CURRENT_PROJECT_VERSION 提升到 project-level、刪掉六個 target-level 覆寫。**build identity 沒有變**（`-showBuildSettings` 仍讀出 2.0.0 / 6，已實測），變的只是同一份設定的位置與檔案 byte 表示。但 pbxproj bytes 一動，bundle manifest 就變，`target.desiredManifestSHA256` 立刻過期，`ops/tests/test_app_review_evidence.py::test_checked_spec_desired_manifest_anchor_matches_reproducible_bundle_without_self_reference` 判紅（2e33c747… vs 459076c3…）。\n\n**真正的代價是修復迴圈，不是那一次紅。** 錨點過期後最自然的動作是「重新產生錨點」——2026-07-17 d5f65efb4、2026-08-03 615418868 都是這樣做的，2026-08-05 本回合上一階也知情地選擇這樣做。每做一次，就把「evidence 跟著 HEAD 漂移」正常化一次，把一個結構性缺陷洗成例行維護。錨點是唯一會對漂移出聲的東西，而現行 SOP 就是讓它閉嘴。漂移本身是靜默的，只有錨點比對會紅。\n\n同族摩擦（一併記錄）：**目前沒有任何工具入口會把新錨點寫回 spec**。`app_review_evidence.py` 的九個子指令（plan/status/desired/urls/journey/journey-run/demo-run/appearance/attest）與 `app_review_gate.py` 的 dry-run/verify 都不寫 `ops/app_review/*.json`（`grep -rn desiredManifestSHA256 ops/` 只命中 gate 的讀取比對、spec 本身、與測試）。歷來四次錨點更新都是手改 JSON 貼 hash——那正是這類錨點腐爛的機制。\n\n建議方向（記錄，未實作）：① `produce_desired_bundle()` 改用 `git show <sourceCommit>:ios/BooksAndVocab.xcodeproj/project.pbxproj` 取那顆 commit 的 pbxproj，而不是讀工作樹；如此 evidence 內容與它宣稱的 commit 綁定，pbxproj 的後續重構不再打紅錨點，錨點才重新變成「內容真的變了」的訊號。② 若採納 ①，同時補一個把錨點寫回 spec 的工具入口（例如 `desired --refresh-anchor --commit`），讓罕見的合法更新有工具而非手抄。③ 用同一個形狀掃 spec 裡其他 `*SHA256` 欄位（datasetSHA256 等）有沒有同樣的「宣稱 commit／實讀工作樹」錯配。",
  "fix_site": "ops/app_review_evidence.py:_materialize_tracked_file / refresh-anchor",
  "id": "IMP-20260805-0fc07a",
  "resolution": "已修（根因 + 入口皆落地，未手貼任何 hash）。\n\n**① 根因（建議方向 ① 採納）**：commit 398046fa2。`produce_desired_bundle()` 不再把工作樹的 pbxproj 交給 `capture_profile.py reviewer-evidence`，改由新的 `_materialize_tracked_file()` 以 `git cat-file blob <sourceCommit>:<path>` 取出那顆 commit 的 bytes 寫進 temp file 再傳入。它跑在所有 generator 之前，且三種失敗（SHA 格式錯 / commit 不可達 / 該 path 在那顆 commit 不存在）全部 terminal——**刻意沒有 fall back 讀工作樹**，因為那個 fall back 就是本條目的缺陷本身。commit 826f34b01 再補兩個洞：`git cat-file blob` 對 symlink（mode 120000）會 exit 0 並回傳 link target（實測回 `../../../etc/hosts`），現改為先用 `git ls-tree --full-tree` 驗 tree entry 必須是 regular-file blob；git 不存在時的 FileNotFoundError 也改走 EvidenceError，不再繞過 `{status: block}` 契約。\n\n**② 入口（建議方向 ② 採納）**：commit 9826b4a06 新增 `app_review_evidence.py refresh-anchor`。重建 bundle 到 temp、雜湊實際落地的 manifest.json（與 gate 雜湊同一份 bytes）、印 old -> new、dry-run 預設、`--commit` 才寫；`rewrite_anchor()` 走文字替換而非重新序列化，並三重驗證（literal 恰一處、reparse 後只有該欄位不同、byte 長度不變）。這是唯一合法的錨點更新路徑。實測寫入路徑：在種了 0000… 過期錨點的 spec 副本上，dry-run 不動檔、`--commit` 回 status=written 且 `diff` 只有第 389 行變、再跑一次回 status=current。\n\n**關鍵結果：這一輪沒有更新錨點，因為修完根因後不需要。** 先前 resolution 實測的自相矛盾（修復前 manifest 同時帶 source.commit=60b7030e… 與 build.project.sha256=e89db61c…＝工作樹那份）已被消除：現在產出的 bundle 是 source.commit=60b7030e…、build.project.sha256=28b6f986…=inputs/project.pbxproj，兩欄一致。錨點回到 checked-in 的 2e33c747…（`refresh-anchor` 實跑回報 status=current changed=false）。錨點從來沒錯過——它是在工作樹與 60b7030e 一致的年代量的，一直在描述那顆 commit。先修根因再看錨點的順序是刻意的：若先手動 refresh，這次修復就會與它要終結的迴圈無法區分。不變性實證：連跑三次真實 `desired --commit`（乾淨工作樹、再一次、以及故意加一行後的工作樹），manifest sha256 皆為 2e33c747…。\n\n**殘留（誠實記錄）**：(a) 入口存在不代表沒人會拿它掩蓋真實漂移——`refresh-anchor` 不判斷該不該更新，只讓更新可追溯；真正買到的是「無關的 pbxproj 改動不再打紅錨點」，所以錨點再紅時比較可能是真的。(b) 建議方向 ③ 未做，且 review 指出範圍比原本記的更大：bundle 其餘五個輸入（capture profile、render PNG、兩支 demo generator、generator files 自身）仍讀工作樹；`capture_profile.py reviewer-evidence` 的 `--project-file` 仍預設工作樹、`--source-commit` 仍預設 HEAD，直接呼叫它就能重現原缺陷；gate 只驗 bundle 內部一致性，不驗 build.project.sha256 是否等於該 commit 的 blob，所以這條不變式目前是 [prompt] 不是 [machine]。已另開條目承接，不留在本條目假裝收斂。",
  "schema": "kg.backlog.entry.v1",
  "severity": "med",
  "source": "debug/ios-version-sot 的 cutover gate（fe4a82355 的 pbxproj 重構打紅了 App Review 錨點，查證時發現）",
  "status": "fixed",
  "stream": "IMP",
  "verdict": "FIXED-ROOT-CAUSE",
  "verified_at": "2026-08-05"
}
