{
  "category": "tool",
  "cost": "M",
  "date": "2026-08-04",
  "detail": "`OPS_SHELL_TEST_ALIASES` 的契約測試只驗「target 提到該腳本」，那是**必要非充分**條件，且已經漏掉真案例：`ops/release_changelog.sh → ops/test_release.sh` 通過全部三項斷言（存在、提到、非慣例可解），但 `test_release.sh` 對它只有 `[[ -f ]]` 存在檢查與**對 `release.sh` 的 grep**，從未執行它——把 `release_changelog.sh` 整個換成 `exit 0` 會通過一道 **block** gate。更糟的是 `ops_sh` 整組被加進 `covered`，所以假 alias 會把一個檔案同時擠出 `uncovered` **和** `ops-shell-untested`，把路由洞換成打勾。九筆手寫 alias 裡兩筆是假的（另一筆是 `ios_clean_derived_data.sh → test_ios_cache_evict.sh`），**憑名字推斷的錯誤率 2/9**。已移除該筆並在程式碼註記此檢查非充分。下一步：加一支獨立的 alias 覆蓋驗證（把腳本 body 換成 `exit 0` 或注入哨兵，要求 target 測試轉紅；不成立即 fail），不要塞進 `ops-pytest`（部分 target 要 60-80s）。慣例解析出的那一大半目前**完全沒有**這種檢查（`_ops_shell_test_candidates`(`:262-270`) 的慣例分支同樣只查 `exists()`），同一個問題。**2026-08-05 取證**:表已收斂為 **7 筆**(兩筆假 alias 確已移除),`ops/tests/test_worktree_orchestrate.py:430-448` 的 docstring 已寫明 `NECESSARY, NOT SUFFICIENT (IMP-0055)`——**註記補了、機械檢查沒補**;全 repo 無 alias 覆蓋驗證檔。結構複核:`worktree_orchestrate.py:499-509` 找到 hit 就進 `sh_targets`,`:527-528` 把**整個 `ops_sh` bucket 無條件計入 `covered`**,故假 alias 確實同時擠出 `uncovered` 與 advisory。**額外實測(原條目未含)**:現存 7 筆 alias 逐筆查過 target 的實際呼叫,**都至少有一次真執行**(例:`ios_archive.sh` 在 `ops/test_ios_ops.sh:910-912` 有 `bash \"$IOS_ARCHIVE\" list/latest`)→ 當下表是乾淨的,缺的純粹是防回歸的器械。",
  "fix_site": "ops/tests/test_shell_alias_coverage.sh",
  "id": "IMP-0055",
  "plan": "[2026-08-05 第三輪收工：三版皆被對抗驗證推翻（軌跡 REVOKED→REVOKED→REVOKED）。最新退回理由：HEAD 全程 0fe439a6d（開工與收尾各驗一次，未前進；18 個未提交檔為另一 session）。acceptance 今天確實紅、紅因確實歸屬本條、所有行號逐條實測未漂、A1–A17 絕大多數是真守衛——但仍有**兩個步驟沒人看守**，其中一個是致命的「漏做仍全綠」。\n\n【R1｜S8 零守衛（groomer 自承，我實測坐實）】漏做 `docs/reference/tech_index.md:10` 的 verified_against bump，acceptance 四段全綠、docs_lint 也綠。實測：`git merge-base --is-ancestor cf5dd5df5 HEAD` → true（舊值永遠可達）；`ops/docs_lint.sh:13/403-421` 只在 sha 無效或不可達時 ERROR，staleness 只在 `--audit` WARN（`ops/docs_lint.sh:10`）。groomer 把選項框成「保留無守 vs 刪掉 S8」，漏掉第三個而且最便宜的選項：**加一條斷言**。修法（照抄即可，加成 acceptance 第 5 段或 self-test A18，斷言數改 18）：\n  v=$(awk -F': *' '/^verified_against:/{print $2; exit}' docs/reference/tech_index.md)\n  [ \"$v\" != \"cf5dd5df5\" ] && git merge-base --is-ancestor \"$v\" HEAD\n落地時 bump 到落地前的 HEAD（main 可達，符合 CLAUDE.md「指到 main 可達 code commit」），此斷言在 commit 前即可跑綠，無雞生蛋問題。\n\n【R2｜S4 只有文字守衛，dispatch 是否真的接通完全沒人驗】A14 只做 `grep -qF './ops/tests/test_shell_alias_coverage.sh --self-test'` on ops/test_ops.sh。只要 case 標籤打錯（例 `alias-cov)` 而 DEFAULT_TESTS 寫 `alias-coverage`），A13 綠（DEFAULT_TESTS 有該行）、A14 綠（呼叫式字串存在）、acceptance 四段全綠——但 `./ops/test_ops.sh` 全量跑到該 group 會落到 `ops/test_ops.sh:172 *) return 64`，等於武裝一個永遠紅的 group，且是本 entry 自己種下的。我實查全 repo 沒有任何「DEFAULT_TESTS ↔ case 分支對齊」的既有守衛可以補位：`ops/tests/test_ops_ci_coverage.sh:133` 只 awk 解析 DEFAULT_TESTS 名單，全檔（section 於 136/143/159/173/285/310/326/343/391）沒有一段碰 case 分支。\ngroomer 拒絕加這條守衛的理由是假的：「要補得讓 acceptance 也走一次 `./ops/test_ops.sh alias-coverage`，但那會讓 self-test 遞迴自呼叫」——不會。路徑是 `./ops/test_ops.sh alias-coverage` → `run_one` → `./ops/tests/test_shell_alias_coverage.sh --self-test` → `self_test`，而 self_test 對 test_ops.sh 只做 awk/grep（A13/A14），從不執行它。零遞迴，成本 1 秒（groomer 自己的拋棄式 repo 實測就是 `✓ alias-coverage passed (1s)`）。修法：acceptance 加第 5 段\n  if ! out3=$(./ops/test_ops.sh alias-coverage 2>&1); then printf \"FAIL(12)\\n%s\\n\" \"$out3\" >&2; exit 12; fi\n  printf \"%s\" \"$out3\" | grep -q '✓ alias-coverage passed' || exit 13\n這一段同時把 S2（exec bit，`./` 呼叫）、S3、S4 三步從「文字對得上」升級成「真的跑得起來」。\n\n【其餘全部站得住，下一位不要重寫】驗證細節見 evidence/attacks_tried：S1a–S1f、S2、S3、S5、S6、S7 的守衛我逐條讀了斷言本體並在真檔上實跑過模式，皆為活靶；行號 15 處全中；撞擊面（gate_can_fail / script_help / lib_sourcing / python_entrypoints / workflow paths）全部如 groomer 所述不需改。只要補上 R1、R2 兩條斷言（斷言數 17→18，acceptance 四段→五段），本條即可 CONFIRMED。\n\n[無人看守的步驟] 逐步查過 14 步（S1a–S1f + S2–S8），12 步有真守衛、**2 步無守或形同無守**：\n\n有守（我讀了斷言本體並確認碰得到）：\n- S1a 突變形狀 → A1（NOT-COVERED 整行相等）+ A2/A3（PROVEN）+ A5（原文保留）雙向夾住；naive 整檔 exit 0 會撞 A1/A5，no-op 會撞 A2/A3。\n- S1b baseline 短路 → A4（BASELINE-RED），且三個 verdict token 互不為子字串、A1–A4 用整行 `=` 比對。\n- S1c resolve_target 走真模組 → A8/A9/A10 三分支各一，我用真模組實跑對過答案；硬編要同時騙三條，且 acceptance 第 9/10 段的真 --prove 仍得靠它解出 ops/test_devops.sh。\n- S1d cleanup → acceptance 第 11 段 `git worktree list --porcelain | grep -q kg-alias-cov`（用自有前綴而非 wc -l，不被另一 session 汙染）。\n- S1e dispatcher 預設值 → acceptance 第 7/8 段（無參數呼叫）＋ cutover block gate 同樣無參數。\n- S1f 斷言計數/末行格式 → acceptance 第 8 段逐字 `self-test: 17 passed, 0 failed`。\n- S2 chmod +x → A11 `[ -x … ]` + A12 以 `./` 實走一次；我實測非 exec 檔 `./` = rc 126、`bash` = rc 0，兩條確實可分辨（前一版死因已修）。\n- S3 DEFAULT_TESTS → A13（我用真檔跑過該 awk|grep，正負控皆正確）＋ test_ops_ci_coverage.sh:163 stale-entry 第二道。\n- S5 LINUX_GROUPS → A15 ＋ test_ops_ci_coverage.sh:152 unclassified 第二道（獨立）。\n- S6 docstring → A16（今日 grep -c = 0，活靶）。\n- S7 tech_index 收錄 → A17（今日 grep -c = 0，活靶）。\n\n★ 無守 1｜**S8 — docs/reference/tech_index.md:10 `verified_against` bump**：零機械守衛。實測 `git merge-base --is-ancestor cf5dd5df5 HEAD` → true，ops/docs_lint.sh:403-421 只在 sha 無效／不可達時 ERROR，staleness 僅 `--audit` WARN（:10）。漏做 → acceptance 四段全綠、docs_lint 全綠、CI 全綠，完全靜默。\n  該補：acceptance 加一段（或 self-test 加 A18，總數改 18）：\n    v=$(awk -F': *' '/^verified_against:/{print $2; exit}' docs/reference/tech_index.md)\n    [ \"$v\" != \"cf5dd5df5\" ] || exit 14\n    git merge-base --is-ancestor \"$v\" HEAD || exit 15\n  （bump 到落地前的 HEAD 即可同時滿足「main 可達」與 commit 前可驗。）\n\n★ 無守 2｜**S4 — ops/test_ops.sh case 分支「真的接得到 dispatcher」**：A14 只 `grep -qF` 呼叫式字串，不驗標籤、不驗位置。反例：DEFAULT_TESTS 寫 `alias-coverage`、case 標籤誤植 `alias-cov)` → A13 綠、A14 綠、acceptance 五項全綠，而 `./ops/test_ops.sh` 跑到該 group 會落 `ops/test_ops.sh:172 *) return 64`，本 entry 自己在聚合入口種下一個恆紅 group。我實查 repo 無任何 DEFAULT_TESTS↔case 對齊守衛可補位（test_ops_ci_coverage.sh:133 只解析名單，全檔無 case 檢查）。groomer 以「會遞迴自呼叫」為由拒絕補守衛，該理由不成立：`./ops/test_ops.sh alias-coverage` → run_one → `test_shell_alias_coverage.sh --self-test`，而 self_test 對 test_ops.sh 只做 awk/grep，從不執行它。\n  該補：acceptance 加第 5 段（約 1 秒）\n    if ! out3=$(./ops/test_ops.sh alias-coverage 2>&1); then printf \"FAIL(12)\\n%s\\n\" \"$out3\" >&2; exit 12; fi\n    printf \"%s\" \"$out3\" | grep -q '✓ alias-coverage passed' || { printf \"FAIL(13)\\n%s\\n\" \"$out3\" >&2; exit 13; }\n  這一段同時把 S2/S3/S4 從「字串對得上」升級為「真的跑得起來」，是本條 CP 值最高的一條斷言。]\n\n以下為最新（第 3 版）草稿，重做時請先讀上面的退回理由：\n[第 5 版。前 4 版的紅因、行號、裁決方向皆保留；本版只修「有步驟沒人看守」的兩個致命缺口（chmod +x 無守衛、INT/TERM trap 假守衛），並把整支器械與五處接線在拋棄式 git repo 裡逐條實跑驗證過（含三種蓄意殘缺實作的證偽）。斷言總數 17。]\n\n【本條範圍】只造出「以突變證明執行」的器械並把它接進 test_ops.sh / CI / 文檔。27 對 pair 的全量 ledger、FLOOR 棘輪、以及 `ops/worktree_orchestrate.py:535` `covered.update(bucket)` 的 covered 折疊修正，**不在本條**，另開 entry（此範圍切法已於前一輪對抗驗證中被接受，理由：本條 fix_site 就是 `ops/tests/test_shell_alias_coverage.sh` 單一交付物）。\n\n【裁決落實】resolution 三要素逐字落實：① 隔離工作樹（`git worktree add --detach <tmp> HEAD`）；② 突變被宣稱涵蓋的腳本（body 換成 `exit 0`）；③ 要求對應 target 轉紅，不紅即 NOT-COVERED。採用「`exit 0` 在第 2 行 + 原文包進引號 heredoc」的變體（前一輪已認可為合理變體，非自行改道）：本 repo 大量 target 是對被測腳本**原文**做 grep/awk（例 `ops/test_devops.sh:21 grep -q 'SSH_OPTS=(' \"$KG\"`），naive 地把整檔換成兩行 `exit 0` 會讓它們因「原文消失」而假性轉紅，正是本 entry 要殺的病換個形狀再犯。\n\n════════ S1. 新建 `ops/tests/test_shell_alias_coverage.sh`（逐字照抄）════════\n內容如下（已在拋棄式 repo 實跑通過，勿改動字串常數，acceptance 逐字比對）：\n\n#!/usr/bin/env bash\n# test_shell_alias_coverage.sh — 證明「某支 ops shell 腳本真的被它的 target 測試執行過」。\n#\n# Usage:\n#   ./ops/tests/test_shell_alias_coverage.sh                  # 等同 --self-test\n#   ./ops/tests/test_shell_alias_coverage.sh --self-test      # 正負控自測，不碰 git\n#   ./ops/tests/test_shell_alias_coverage.sh --prove <script> # 隔離工作樹實證一對\n#   ./ops/tests/test_shell_alias_coverage.sh --help\n#\n# 為什麼存在（IMP-0055）：worktree_orchestrate.py 的 ops-shell 路由把「target 檔案存在\n# 且提到這支腳本」當成覆蓋。那是必要非充分條件，已經漏過真案例。這支器械把腳本在隔離\n# 工作樹裡改成惰性（保留全部原文、只讓它不再執行），要求 target 轉紅；不紅就是沒覆蓋。\nset -uo pipefail\n\nROOT=\"$(cd \"$(dirname \"$0\")/../..\" && pwd)\"\nSELF=\"$ROOT/ops/tests/test_shell_alias_coverage.sh\"\nWT_PREFIX=\"kg-alias-cov\"\n\nUV_BIN=\"${UV_BIN:-}\"\nif [ -z \"$UV_BIN\" ]; then\n  if [ -x \"$HOME/.local/bin/uv\" ]; then UV_BIN=\"$HOME/.local/bin/uv\"; else UV_BIN=\"uv\"; fi\nfi\n\ncleanup() {\n  if [ -n \"${WT:-}\" ]; then\n    git -C \"$ROOT\" worktree remove --force \"$WT\" >/dev/null 2>&1 || true\n    rm -rf \"$WT\"\n    git -C \"$ROOT\" worktree prune >/dev/null 2>&1 || true\n  fi\n  if [ -n \"${SBROOT:-}\" ]; then rm -rf \"$SBROOT\"; fi\n  return 0\n}\ntrap cleanup EXIT\n\nusage() { awk 'NR==1{next} /^#/{sub(/^# ?/,\"\"); print; next} {exit}' \"$SELF\"; }\n\n# 突變：保留腳本全部原文（掃描原文的 target 不會因此假性轉紅），只讓它不再執行。\n# `exit 0` 在第 2 行，bash 逐命令解析，執行到這裡就結束，後面那段永遠不被解析；\n# 包成引號 heredoc 是為了讓 `bash -n` 這種全檔解析也不會把原文當程式碼。\nmutate_in_place() {  # $1 = 要改成惰性的腳本路徑\n  local f=\"$1\" tmp\n  tmp=\"$(mktemp \"${TMPDIR:-/tmp}/kg-alias-mut.XXXXXX\")\" || return 1\n  {\n    printf '#!/usr/bin/env bash\\n'\n    printf 'exit 0\\n'\n    printf \": <<'KG_IMP0055_INERT_BODY'\\n\"\n    cat \"$f\"\n    printf '\\nKG_IMP0055_INERT_BODY\\n'\n  } >\"$tmp\" || return 1\n  cat \"$tmp\" >\"$f\" || return 1\n  rm -f \"$tmp\"\n}\n\nrun_target() {  # $1=工作目錄 $2=target 相對路徑；回傳 target 的 rc\n  ( cd \"$1\" && bash \"$2\" ) >/dev/null 2>&1\n}\n\nprove_in_dir() {  # $1=工作目錄 $2=腳本相對路徑 $3=target 相對路徑\n  local dir=\"$1\" script=\"$2\" target=\"$3\" rc\n  run_target \"$dir\" \"$target\"; rc=$?\n  if [ \"$rc\" -ne 0 ]; then\n    printf 'verdict=BASELINE-RED %s -> %s\\n' \"$script\" \"$target\"\n    return 3\n  fi\n  if ! mutate_in_place \"$dir/$script\"; then\n    printf 'verdict=MUTATE-FAILED %s -> %s\\n' \"$script\" \"$target\"\n    return 4\n  fi\n  run_target \"$dir\" \"$target\"; rc=$?\n  if [ \"$rc\" -ne 0 ]; then\n    printf 'verdict=PROVEN %s -> %s\\n' \"$script\" \"$target\"\n    return 0\n  fi\n  printf 'verdict=NOT-COVERED %s -> %s\\n' \"$script\" \"$target\"\n  return 1\n}\n\nresolve_target() {  # $1=腳本相對路徑；印出 target 相對路徑（空 = 無法路由）\n  \"$UV_BIN\" run --no-project --python 3.13 python - \"$ROOT\" \"$1\" <<'PY'\nimport importlib.util, pathlib, sys\nroot = pathlib.Path(sys.argv[1]).resolve()\nrel = sys.argv[2]\nsys.path.insert(0, str(root / \"ops\"))\nspec = importlib.util.spec_from_file_location(\n    \"worktree_orchestrate\", root / \"ops\" / \"worktree_orchestrate.py\")\nmod = importlib.util.module_from_spec(spec)\nsys.modules[spec.name] = mod\nspec.loader.exec_module(mod)\nprint(next((c for c in mod._ops_shell_test_candidates(rel) if (root / c).is_file()), \"\"))\nPY\n}\n\ndo_prove() {  # $1 = 腳本相對路徑（例：devops.sh）\n  local script=\"$1\" target\n  if [ ! -f \"$ROOT/$script\" ]; then\n    printf 'verdict=NO-SUCH-SCRIPT %s\\n' \"$script\"\n    return 6\n  fi\n  target=\"$(resolve_target \"$script\" | tail -1)\"\n  if [ -z \"$target\" ]; then\n    printf 'verdict=UNROUTABLE %s\\n' \"$script\"\n    return 2\n  fi\n  WT=\"$(mktemp -d \"${TMPDIR:-/tmp}/${WT_PREFIX}.XXXXXX\")\" || return 5\n  rm -rf \"$WT\"\n  if ! git -C \"$ROOT\" worktree add --detach \"$WT\" HEAD >/dev/null 2>&1; then\n    printf 'verdict=WORKTREE-FAILED %s -> %s\\n' \"$script\" \"$target\"\n    return 5\n  fi\n  prove_in_dir \"$WT\" \"$script\" \"$target\"\n}\n\npass=0\nfail=0\nchk() {  # $1=rc $2=label\n  if [ \"$1\" -eq 0 ]; then printf '  ✓ %s\\n' \"$2\"; pass=$((pass+1))\n  else printf '  ✗ %s\\n' \"$2\"; fail=$((fail+1)); fi\n}\neq() {  # $1=label $2=got $3=want\n  if [ \"$2\" = \"$3\" ]; then printf '  ✓ %s\\n' \"$1\"; pass=$((pass+1))\n  else printf '  ✗ %s\\n     got:  [%s]\\n     want: [%s]\\n' \"$1\" \"$2\" \"$3\"; fail=$((fail+1)); fi\n}\n\nmake_case() {  # $1=case 名 $2=target 檔內容\n  local d=\"$SBROOT/$1\"\n  mkdir -p \"$d\"\n  cat >\"$d/s.sh\" <<'EOS'\n#!/usr/bin/env bash\nKG_MARKER_STRUCTURE=1\necho KG_MARKER_RUNTIME\nEOS\n  printf '%s\\n' \"$2\" >\"$d/t.sh\"\n  chmod +x \"$d/s.sh\" \"$d/t.sh\"\n}\n\nself_test() {\n  SBROOT=\"$(mktemp -d \"${TMPDIR:-/tmp}/kg-alias-selftest.XXXXXX\")\" || return 1\n  local out rc\n\n  echo \"── 突變形狀的正負控（合成 sandbox，不碰 git）──\"\n\n  # 只掃描原文、從不執行腳本的 target：突變後照樣綠 → 這種 alias 是假的\n  make_case scanner '#!/usr/bin/env bash\ngrep -q \"KG_MARKER_STRUCTURE=1\" s.sh'\n  out=\"$(prove_in_dir \"$SBROOT/scanner\" s.sh t.sh)\"\n  eq \"A1 只掃描原文的 target 判為 NOT-COVERED\" \"$out\" \"verdict=NOT-COVERED s.sh -> t.sh\"\n\n  # 真的執行腳本的 target：突變後轉紅 → 覆蓋成立\n  make_case exec '#!/usr/bin/env bash\n[ \"$(bash s.sh)\" = \"KG_MARKER_RUNTIME\" ]'\n  out=\"$(prove_in_dir \"$SBROOT/exec\" s.sh t.sh)\"\n  eq \"A2 真的執行腳本的 target 判為 PROVEN\" \"$out\" \"verdict=PROVEN s.sh -> t.sh\"\n\n  # 既掃描又執行：原文保留讓掃描仍綠，執行面轉紅 → 仍判 PROVEN\n  make_case both '#!/usr/bin/env bash\ngrep -q \"KG_MARKER_STRUCTURE=1\" s.sh && [ \"$(bash s.sh)\" = \"KG_MARKER_RUNTIME\" ]'\n  out=\"$(prove_in_dir \"$SBROOT/both\" s.sh t.sh)\"\n  eq \"A3 又掃描又執行的 target 判為 PROVEN\" \"$out\" \"verdict=PROVEN s.sh -> t.sh\"\n\n  # 本來就紅的 target：不得冒充 PROVEN\n  make_case baseline '#!/usr/bin/env bash\ngrep -q \"KG_MARKER_NEVER_PRESENT\" s.sh'\n  out=\"$(prove_in_dir \"$SBROOT/baseline\" s.sh t.sh)\"\n  eq \"A4 baseline 就紅的 target 判為 BASELINE-RED\" \"$out\" \"verdict=BASELINE-RED s.sh -> t.sh\"\n\n  echo \"\"\n  echo \"── 突變本身的性質 ──\"\n  grep -qF 'echo KG_MARKER_RUNTIME' \"$SBROOT/scanner/s.sh\"\n  chk $? \"A5 突變保留腳本全部原文\"\n  out=\"$(bash \"$SBROOT/scanner/s.sh\" 2>&1)\"; rc=$?\n  eq \"A6 突變後的腳本執行為 no-op\" \"$rc:$out\" \"0:\"\n  bash -n \"$SBROOT/scanner/s.sh\" 2>/dev/null\n  chk $? \"A7 突變後的腳本仍通過 bash -n\"\n\n  echo \"\"\n  echo \"── target 解析走 worktree_orchestrate.py 本尊 ──\"\n  out=\"$(resolve_target devops.sh | tail -1)\"\n  eq \"A8 慣例解析 devops.sh\" \"$out\" \"ops/test_devops.sh\"\n  out=\"$(resolve_target ops/ios_test.sh | tail -1)\"\n  eq \"A9 alias 解析 ops/ios_test.sh\" \"$out\" \"ops/test_ios_test_discovery.sh\"\n  out=\"$(resolve_target ops/test_ops.sh | tail -1)\"\n  eq \"A10 UNROUTABLE 名單回空\" \"$out\" \"\"\n\n  echo \"\"\n  echo \"── 交付物與接線 ──\"\n  [ -x \"$ROOT/ops/tests/test_shell_alias_coverage.sh\" ]\n  chk $? \"A11 交付物具 executable bit\"\n  ( cd \"$ROOT\" && ./ops/tests/test_shell_alias_coverage.sh --help >/dev/null 2>&1 )\n  chk $? \"A12 ./ 入口可直接執行\"\n  awk '/^DEFAULT_TESTS=\\(/,/^\\)$/' \"$ROOT/ops/test_ops.sh\" | grep -qx '  alias-coverage'\n  chk $? \"A13 test_ops.sh DEFAULT_TESTS 註冊 alias-coverage\"\n  grep -qF './ops/tests/test_shell_alias_coverage.sh --self-test' \"$ROOT/ops/test_ops.sh\"\n  chk $? \"A14 test_ops.sh 以 ./ 呼叫本器械並帶 --self-test\"\n  awk '/^LINUX_GROUPS=\\(/,/^\\)$/' \"$ROOT/ops/tests/test_ops_ci_coverage.sh\" | grep -qx '  alias-coverage'\n  chk $? \"A15 alias-coverage 已歸類為 CI-runnable\"\n  grep -qF 'ops/tests/test_shell_alias_coverage.sh' \"$ROOT/ops/tests/test_worktree_orchestrate.py\"\n  chk $? \"A16 契約測試 docstring 指向本器械\"\n  grep -qF 'alias-coverage' \"$ROOT/docs/reference/tech_index.md\"\n  chk $? \"A17 tech_index 收錄 alias-coverage group\"\n\n  echo \"\"\n  printf 'self-test: %d passed, %d failed\\n' \"$pass\" \"$fail\"\n  [ \"$fail\" -eq 0 ]\n}\n\nmode=\"${1:-}\"\n[ -n \"$mode\" ] || mode=\"--self-test\"\ncase \"$mode\" in\n  -h|--help)   usage; exit 0 ;;\n  --self-test) self_test; exit $? ;;\n  --prove)\n    if [ \"$#\" -ne 2 ]; then\n      printf '✗ --prove 需要一個腳本相對路徑（例：--prove devops.sh）\\n' >&2\n      exit 2\n    fi\n    do_prove \"$2\"; exit $?\n    ;;\n  *)\n    printf '✗ unknown argument: %s\\n' \"$mode\" >&2\n    usage >&2\n    exit 2\n    ;;\nesac\n\n════════ S2. `chmod +x ops/tests/test_shell_alias_coverage.sh` ════════\n**不可省**。兩個真實消費者都走 `./`：① S4 的 case 分支；② cutover 的 `ops-shell:test_shell_alias_coverage.sh` block gate——`ops/worktree_orchestrate.py:522` `gates.append(_shell(f\"ops-shell:{t.rsplit('/', 1)[-1]}\", \"ops\", [t], \"block\"))` 把命令設成 list，經 `ops/lib/streaming_command.py` 的 `subprocess.Popen`（非 shell=True）exec，缺 exec bit → EACCES → `worktree_orchestrate.py` 的 ENOENT/EACCES 分支判 `block, rc=127, executed=False`，等於武裝一道永遠紅的 gate（IMP-0039 病）。守衛見 A11 / A12。\n\n════════ S3. `ops/test_ops.sh` DEFAULT_TESTS 註冊 group ════════\n今日（HEAD 0fe439a6d）`ops/test_ops.sh:24` 是 `DEFAULT_TESTS=(`，`:61` 是 `  lib-sourcing`（2 空格縮排），`:63` 是行首 `)`。在 `  lib-sourcing` 那行**之後**插入一行（2 空格縮排，不加註解——`ops/tests/test_ops_ci_coverage.sh` 用 `awk '/^DEFAULT_TESTS=\\(/{flag=1;next} /^\\)/{flag=0} flag {...}'` 逐行取 group 名，註解會被當成 group）：\n  alias-coverage\n\n════════ S4. `ops/test_ops.sh` case 分支 ════════\n今日 `ops/test_ops.sh:156` 是 `    lib-sourcing)       ./ops/tests/test_lib_sourcing.sh ;;`。在該行之後插入（縮排 4 空格，逐字，A14 用 `grep -qF` 咬住 `./ops/tests/test_shell_alias_coverage.sh --self-test` 這串）：\n    alias-coverage)     ./ops/tests/test_shell_alias_coverage.sh --self-test ;;\n\n════════ S5. `ops/tests/test_ops_ci_coverage.sh` 歸類為 CI-runnable ════════\n今日 `:39` 是 `LINUX_GROUPS=(`，`:50` 是 `  lib-sourcing`，`:51` 是 `  # ── 以下 13 筆原本被排除…（IMP-0054）──`，`:66` 是行首 `)`。在 `  lib-sourcing`（:50）之後、註解行（:51）之前插入：\n  alias-coverage\n理由：self-test 只需要 bash + uv（workflow 已有 `astral-sh/setup-uv@v6`），不碰 git worktree、不碰 Xcode，1 秒級。不加這行，`ops/tests/test_ops_ci_coverage.sh:152` 的 `fail_t \"$g is unclassified …\"` 會紅。LINUX_GROUPS 成員不會被 `bin-*` 證偽器實跑（那段只迭代 `EXCLUDED_GROUPS` 裡 token 為 `bin-*` 者），所以不增加該支測試的耗時。\n\n════════ S6. `ops/tests/test_worktree_orchestrate.py` docstring 指路 ════════\n今日 `:448` 是 `def test_every_shell_test_alias_points_at_a_test_that_mentions_its_script():`，`:456` 是該 docstring 末行 `    coverage means mutating the script and requiring the target to go red (IMP-0055).\"\"\"`。把 `:456` 整行換成兩行：\n    coverage means mutating the script and requiring the target to go red — that is what\n    ops/tests/test_shell_alias_coverage.sh --prove does (IMP-0055).\"\"\"\n（只改 docstring，不改任何斷言；此檔今日 `grep -c 'ops/tests/test_shell_alias_coverage.sh'` = 0，故 A16 不是死靶。）\n\n════════ S7. `docs/reference/tech_index.md` 收錄 group ════════\n今日 `:144` 是 `| \\`test_ops.sh\\` | ops regression 聚合入口。…` 那一列；該列中字串 `)/ \\`podcast-ops\\`;optional release surface:` 在全檔唯一（`grep -c` = 1）。把該處改成：\n)/ `alias-coverage`(`ops/tests/test_shell_alias_coverage.sh`:把腳本在隔離工作樹改成惰性——保留全部原文、只讓它不再執行——要求 target 轉紅才算覆蓋；`--self-test` 跑正負控，`--prove <script>` 實證單一對)/ `podcast-ops`;optional release surface:\n（此檔今日 `grep -c 'alias-coverage'` = 0，故 A17 不是死靶。）\n\n════════ S8. `docs/reference/tech_index.md` bump verified_against ════════\n今日 `:10` 是 `verified_against: cf5dd5df5`。改成本次落地 commit 的 sha（main 可達）。\n**此步無機械守衛，明說**：`ops/docs_lint.sh` 只在 sha 無效或不可達時報 ERROR，staleness 僅在 `--audit` 模式 WARN；舊值 `cf5dd5df5` 永遠可達，忘記 bump 完全靜默、acceptance 也不會轉紅。靠 commit review 承擔。不要引用 docs_lint 當這一步的守衛。\n\n════════ 落地順序與唯一硬約束 ════════\nS1–S8 **必須同一個 commit 落地**。理由：新增 `ops/tests/test_shell_alias_coverage.sh` 會讓 cutover 自動規劃一道 `ops-shell:test_shell_alias_coverage.sh` block gate（`ops/worktree_orchestrate.py:268` `return [rel]  # a changed test script is its own gate` → `:522`），該 gate 以**無參數**執行這支腳本，走 dispatcher 預設值 → `--self-test` → A11–A17 會檢查 S2–S7 是否都在。少任何一步，cutover 當場 block。\n\n════════ 會不會撞到別處（實查過，全部不撞）════════\n- `ops/tests/test_gate_can_fail.sh`:157 `names.add(g[\"name\"].split(\":\")[0])` → 新 gate 名正規化為既有的 `ops-shell`，PROOFS(:69) 與 CHEAP_FLOOR(:99) 皆已宣告，**不需修改**。\n- `ops/tests/test_script_help.sh`:1-12 是 7 支白名單、不 glob → 不受影響。\n- `ops/tests/test_lib_sourcing.sh`:42 `for script in ops/*.sh` 不遞迴 `ops/tests/` → 不受影響。\n- `ops/tests/test_python_entrypoints.sh` 只管 `ops/*.py` → 不受影響。\n- `.github/workflows/ops-suite.yml`:49 `./ops/test_ops.sh $(./ops/tests/test_ops_ci_coverage.sh --print-linux-groups)`；CI 觸發 path filter 已含 `ops/**` glob → **不需**改 workflow。\n- `./ops/test_ops.sh`（全量）會多跑一個約 1 秒的 group。\n\n════════ 哪條命令紅轉綠 ════════\n見 acceptance 欄。今日 exit 7（交付物不存在）；S1–S7 落地後 exit 0 印 `ACCEPTANCE OK`。（S8 不影響 acceptance 顏色，見上。）\n\n════════ 已知限制（誠實列出，不假裝有守）════════\n① CI 只跑 `--self-test`，`--prove` 這條路徑在 CI 上不執行（會要 `git worktree add` 與數十秒）；它由 acceptance 在落地當下實跑一次覆蓋。要讓 `--prove` 常態化＝27 對 ledger，屬另開的後續 entry。\n② `--prove` 在主 repo 上以 `git worktree add --detach <tmp> HEAD` 建立**暫態**工作樹（存活約 15–40 秒、路徑前綴 `kg-alias-cov`、位於 `$TMPDIR` 之外於 repo 樹）；它未登記進 worktree registry，若恰好與他人的 preflight sweep 撞上可能被列為 orphan。撞到 index.lock 時輸出 `verdict=WORKTREE-FAILED`（可見的紅，非假綠），重跑即可。\n③ 突變會使被測腳本增加 3 行（shebang / `exit 0` / heredoc 起始）並在檔尾加 heredoc 結束行；若某 target 斷言的是「整檔行數」或「`exit 0` 出現次數」，會有誤判。今日對 `devops.sh → ops/test_devops.sh` 實測無此問題（100 條斷言 baseline 全綠）。\n\n[該版 acceptance] cwd 任意，前景執行（單一命令，約 30–60 秒）：\n\ntimeout 900 bash -c 'set -uo pipefail\ncd /Users/chenliangyu/project/kg\nif ! out1=$(bash ops/tests/test_shell_alias_coverage.sh); then printf \"FAIL(7) self-test 非零離開\\n%s\\n\" \"$out1\" >&2; exit 7; fi\nif [ \"$(printf \"%s\\n\" \"$out1\" | tail -1)\" != \"self-test: 17 passed, 0 failed\" ]; then printf \"FAIL(8) self-test 末行不符\\n%s\\n\" \"$out1\" >&2; exit 8; fi\nif ! out2=$(bash ops/tests/test_shell_alias_coverage.sh --prove devops.sh); then printf \"FAIL(9) --prove 非零離開\\n%s\\n\" \"$out2\" >&2; exit 9; fi\nif [ \"$(printf \"%s\\n\" \"$out2\" | tail -1)\" != \"verdict=PROVEN devops.sh -> ops/test_devops.sh\" ]; then printf \"FAIL(10) prove 判決不符: [%s]\\n\" \"$out2\" >&2; exit 10; fi\nif git -C /Users/chenliangyu/project/kg worktree list --porcelain | grep -q \"kg-alias-cov\"; then printf \"FAIL(11) 隔離工作樹殘骸未清\\n\" >&2; exit 11; fi\nprintf \"ACCEPTANCE OK\\n\"'\n\nexit 0 且印出 `ACCEPTANCE OK` 即綠。四段各自的職責：\n- exit 7/8 段：釘死 self-test 末行逐字 `self-test: 17 passed, 0 failed`（不帶參數 → 咬住 dispatcher 預設值；17 條斷言全綠 → 咬住 S1 各子步與 S2–S7 五處接線）。\n- exit 9/10 段：走真實 `--prove` 路徑，對真實 pair `devops.sh → ops/test_devops.sh` 實跑（建隔離工作樹 → 跑 baseline → 突變 → 再跑），逐字比對 `verdict=PROVEN devops.sh -> ops/test_devops.sh`。這段是唯一會執行 `git worktree add` 與 `do_prove` 接線的一段。\n- exit 11 段：`git worktree list --porcelain` 不得殘留 `kg-alias-cov` 前綴 → 咬住 cleanup。\n\n[該版 step_guards] 共 8 個會改動 repo 的步驟（S1 依內部子步再拆 6 列）。每列＝「只漏這一步、其他都做」時 acceptance 會不會轉紅，以及被哪條斷言咬住。★＝本輪實測證偽過（蓄意殘缺實作，看哪條斷言變紅）。\n\nS1a 突變形狀（mutate_in_place 保留原文 + `exit 0` 惰性化）— **有守，且雙向**★。漏做（函式改成 `return 0` 什麼都不突變）→ A2「A2 真的執行腳本的 target 判為 PROVEN」與 A3 轉紅（實測 v1：✗ A2 ✗ A3 ✗ A6）。做成 naive 版（整檔換成兩行 `exit 0`、刪掉原文）→ A1「A1 只掃描原文的 target 判為 NOT-COVERED」與 A5「A5 突變保留腳本全部原文」轉紅（實測 v2：✗ A1 ✗ A5）。三個 verdict token（PROVEN / NOT-COVERED / BASELINE-RED）互不為子字串，且 A1–A4 用整行相等比對，無法以子字串矇混。\n\nS1b baseline 先跑短路（prove_in_dir 開頭那段 `run_target` + `return 3`）— **有守**★。整段刪掉 → 本來就紅的 target 被判成 PROVEN → A4「A4 baseline 就紅的 target 判為 BASELINE-RED」轉紅（實測 v3：✗ A4）。\n\nS1c resolve_target 走 `ops/worktree_orchestrate.py` 本尊（不自行複寫 alias 表）— **有守**。改成硬編或刪掉 → A8（devops.sh → ops/test_devops.sh，慣例分支）／A9（ops/ios_test.sh → ops/test_ios_test_discovery.sh，alias 分支，兩個慣例名今日實查皆不存在）／A10（ops/test_ops.sh → 空，UNROUTABLE 分支）任一轉紅。三條分支各一，硬編四答案是這裡的作弊天花板，但要作弊得同時對三條分支硬編，且 acceptance 第二段的真實 `--prove` 仍要靠它解出 `ops/test_devops.sh`。\n\nS1d cleanup（`git worktree remove --force` + `worktree prune`）— **有守**★。把 cleanup 內容掏空 → 實測（拋棄式 repo）`--prove` 仍印 PROVEN rc 0，但 `git worktree list` 多出 `/…/T/kg-alias-cov.kkSAjK  6715b9a (detached HEAD)` → acceptance 第 11 段 `grep -q \"kg-alias-cov\"` 命中 → exit 11。註：**不用 `git worktree list | wc -l`**，那會被另一 session 的工作樹污染；改用本器械自有前綴。註 2：本版**刻意不放** `trap … INT TERM`——實測 bash 對未捕捉的 SIGTERM 仍執行 EXIT trap（wait_rc=143 且 EXIT trap 有跑），所以那條 trap 只影響離開碼、無法被殘骸數分辨，留著就是一條無分辨力的假守衛。\n\nS1e dispatcher 預設值（不帶參數 = `--self-test`）— **有守**。漏做（無參數印 usage 或 exit 2）→ acceptance 第一段 `bash ops/tests/test_shell_alias_coverage.sh` 非零或末行不符 → exit 7/8。這一步同時決定 cutover block gate（無參數執行）的顏色。\n\nS1f self-test 斷言計數與末行格式 — **有守**。掏空任何一條斷言或改動 `printf 'self-test: %d passed, %d failed\\n'` → acceptance 第 8 段逐字比對 `self-test: 17 passed, 0 failed` 失敗 → exit 8。\n\nS2 `chmod +x` — **有守**（本輪新補，前一版正是死在這裡）。漏做 → 用 `bash <path>` 呼叫照樣全綠，但 A11「[ -x \"$ROOT/ops/tests/test_shell_alias_coverage.sh\" ]」直接轉紅，A12「( cd \"$ROOT\" && ./ops/tests/test_shell_alias_coverage.sh --help )」以 `./` 實走一次入口、rc 126 轉紅（實測：非 exec 檔 `bash x.sh` rc 0 / `./x.sh` permission denied rc 126）。這兩條把 `ops/test_ops.sh` 的 `./…` 呼叫與 cutover block gate 的 list-exec（EACCES → rc 127 恆 block）兩個真實消費者的前提釘住。全 repo 沒有任何既有 exec-bit 契約能補位：`ops/tests/test_python_entrypoints.sh` 只管 `ops/*.py`，`grep -rn '\\[ -x ' ops/tests/*.sh` 只有針對特定工具的個別斷言，現有 29 支 `ops/tests/*.sh` 全 755 純屬慣例。\n\nS3 DEFAULT_TESTS 加 `  alias-coverage` — **有守**。漏做 → A13（`awk '/^DEFAULT_TESTS=\\(/,/^\\)$/' | grep -qx '  alias-coverage'`，今日已實查該區塊為 2 空格縮排、`)` 在行首）轉紅。另有獨立守衛：`ops/tests/test_ops_ci_coverage.sh:160` 的 stale-entry 檢查會因 S5 加了卻沒宣告而紅。\n\nS4 case 分支 `alias-coverage)     ./ops/tests/test_shell_alias_coverage.sh --self-test ;;` — **有守（本輪加強）**。漏做或分支體寫成 `true ;;`／指錯路徑 → A14 用 `grep -qF './ops/tests/test_shell_alias_coverage.sh --self-test'` 咬住**逐字的呼叫式**（含 `./` 與 `--self-test`），不再只驗標籤存在。前一版的 A12 只 grep `^[[:space:]]*alias-coverage\\)`，`true ;;` 照過，那是本輪修掉的次要缺陷。殘餘缺口誠實列出：A14 不驗縮排、也不驗這行落在 `run_one()` 的 case 內；但本輪已在拋棄式 repo 實跑 `./ops/test_ops.sh alias-coverage` 得到 `✓ alias-coverage passed (1s)`，照抄本 plan 的行不會落錯位置。\n\nS5 LINUX_GROUPS 加 `  alias-coverage` — **有守（雙重）**。漏做 → A15（`awk '/^LINUX_GROUPS=\\(/,/^\\)$/' | grep -qx '  alias-coverage'`，今日已實查為 2 空格縮排、`)` 在行首）轉紅；另 `ops/tests/test_ops_ci_coverage.sh:152` `fail_t \"$g is unclassified — add it to LINUX_GROUPS or give it an EXCLUDED_GROUPS reason\"` 是完全獨立的第二道守衛（該 group 進 DEFAULT_TESTS 後未分類即紅）。\n\nS6 `ops/tests/test_worktree_orchestrate.py` docstring 指路 — **有守（弱但機械）**。漏做 → A16 `grep -qF 'ops/tests/test_shell_alias_coverage.sh'` 轉紅；今日實查該檔 `grep -c` = 0，不是死靶。弱點誠實列出：只驗字串出現，不驗它出現在那條 docstring 裡。\n\nS7 `docs/reference/tech_index.md` 收錄 group — **有守（弱但機械）**。漏做 → A17 `grep -qF 'alias-coverage'` 轉紅；今日實查該檔 `grep -c 'alias-coverage'` = 0，不是死靶。\n\nS8 `docs/reference/tech_index.md:10` bump `verified_against` — **無守衛，明說**。`ops/docs_lint.sh` 只在 sha 無效或不可達時報 ERROR，staleness 僅 `--audit` WARN，舊值 `cf5dd5df5` 永遠可達 → 忘記 bump 靜默通過、acceptance 不會轉紅。本步靠 commit review 承擔；**不要**再引用 docs_lint 的 reachable-from-HEAD 檢查當這一步的守衛（它抓的是相反的病）。保留此步而非刪除，因為 CLAUDE.md 對 reference/(SoT) 文檔要求同 PR bump。",
  "resolution": "—(2026-08-05 裁決（總經理自決，執行長授權）：隔離機制走 **隔離工作樹**，理由是 repo 已有 `worktree_orchestrate.py` 那套原語，不必新造容器路徑。證偽方式：把被宣稱涵蓋的腳本在隔離工作樹的原路徑上改壞（body 換 exit 0），要求對應 target 轉紅；不紅即該 alias 為假，從 covered 移除並列入 advisory。落點 `ops/worktree_orchestrate.py:499-509` 的 alias 准入與 `:527-528` 的 covered 折疊。)",
  "schema": "kg.backlog.entry.v1",
  "severity": "med",
  "source": "IMP-0051 的 review",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
