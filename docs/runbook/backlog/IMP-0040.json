{
  "acceptance": "! grep -qF \"All mutation subcommands are dry-run by default\" docs/runbook/system.md && ! grep -qF \"所有 mutation 子指令\" .claude/skills/worktree-flow/SKILL.md && ! grep -qF \"所有 mutation **dry-run 預設\" docs/reference/tech_index.md && ./ops/docs_lint.sh --files docs/runbook/system.md docs/reference/tech_index.md",
  "category": "doc",
  "cost": "S",
  "date": "2026-08-03",
  "detail": "`worktree-flow` skill 宣稱「所有 mutation 子指令 dry-run 預設、`--commit` 才落地」，但 `worktree_orchestrate.py open` 沒有 `--commit` 旗標、直接落地。第一次照文檔呼叫即撞到 usage error。下一步：修文檔措辭（列出例外），或給 `open` 加上 dry-run 語意。**2026-08-05 取證 + 落點更正**:宣稱句在 `.claude/skills/worktree-flow/SKILL.md:12`,**另有第二份**(原條目漏列):`docs/runbook/system.md:62` 的英文版「All mutation subcommands are dry-run by default; --commit to land.」——只修 skill 會留下另一個假陳述。實跑 `open --intent x --slug y --commit` → `unrecognized arguments: --commit` rc=2(重現);`adopt`/`freeze` 同。完整對照(`worktree_orchestrate.py:2062-2168`):**有 `--commit` 且 dry-run 預設 6 支**=preflight/sync-main/sync/deploy/cutover/resolve;**無 `--commit` 且直接落地 3 支**=open(`git worktree add`+登記簿)/adopt(登記簿)/freeze(寫 lock,有 `--force` 無 dry-run);gate 為驗證非 mutation(寫 verdict cache+history.jsonl)。正確措辭應為「**landing 平面的** mutation 子指令 dry-run 預設;open/adopt/freeze 是即時生效的登記/鎖操作,無 `--commit`」。",
  "fix_site": ".claude/skills/worktree-flow/SKILL.md:12 + docs/runbook/system.md:62 + docs/reference/tech_index.md:133",
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom@v1",
  "id": "IMP-0040",
  "plan": "根因已核實(讀 build_parser，未動 code)：ops/worktree_orchestrate.py:2062-2168 只有 6 支子指令帶 --commit 且 dry-run 預設 = preflight / sync-main / sync / deploy / cutover / resolve；open / adopt / freeze 沒有 --commit、呼叫即生效(open = git worktree add + 登記簿；adopt = 登記簿；freeze = 寫鎖，只有 --force、無 dry-run)；gate 是驗證不是 mutation。三份文檔各有一句「所有 mutation 子指令都 dry-run 預設」的一體適用假陳述。本條目純改措辭，不動任何 .py。\n\n1. 落點三處(原條目只列兩處；tech_index 那處是本次新發現)：\n(a) .claude/skills/worktree-flow/SKILL.md:12，整行現為 — 所有 mutation 子指令 **dry-run 預設，`--commit` 才落地**。`--json` 給機器判讀。\n(b) docs/runbook/system.md:62(C) Worktree Lifecycle 第 9 點)，整行現為 — 9. All mutation subcommands are dry-run by default; `--commit` to land.\n(c) docs/reference/tech_index.md:133(worktree_orchestrate.py 那一列，超長單行)，其中的子字串 — 所有 mutation **dry-run 預設、`--commit` 落地**;\n\n2. (a) 整行換成 — 落地平面的 mutation 子指令(preflight / sync-main / sync / deploy / cutover / resolve)**dry-run 預設，`--commit` 才落地**；`open` / `adopt` / `freeze` 是即時生效的登記／鎖操作，**沒有 `--commit`**(freeze 以 `--force` 覆寫既有鎖)；`gate` 是驗證不是 mutation。`--json` 給機器判讀。\n\n3. (b) 整行換成 — 9. The landing-plane mutation subcommands (`preflight` / `sync-main` / `sync` / `deploy` / `cutover` / `resolve`) are dry-run by default; `--commit` to land. `open` / `adopt` / `freeze` take effect immediately: they are ledger/lock writes with NO `--commit` (`freeze` uses `--force` to overwrite an existing lock). `gate` verifies rather than mutates.\n\n4. (c) 只把那段子字串換成 — 落地平面的 mutation(preflight/sync-main/sync/deploy/cutover/resolve)**dry-run 預設、`--commit` 落地**，open/adopt/freeze 無 `--commit` 即時生效; — 該表格列其餘內容一字不動(它是單行 markdown table cell，整列重打必出錯)。\n\n5. 絕對不要改 docs/runbook/improvement_backlog.md。它是 ops/backlog.py 從 docs/runbook/backlog/*.json render 出來的產物，手改會被下次 render 蓋掉；裡面那份同樣字句是**條目文字**的複本，不是文檔陳述。\n\n6. docs/runbook/system.md 與 docs/reference/tech_index.md 都有 doc-meta frontmatter：改完把兩者的 verified_against 更新為 git rev-parse --short HEAD 的輸出(system.md 目前值為 8da8c5516)。SKILL.md 不在 docs lint 掃描範圍，無 frontmatter 要求。\n\n7. 沒有任何測試斷言這三句措辭。會變紅的只有 docs gate：frontmatter 打壞、或 verified_against 指到 main 不可達的 commit 時，ops/docs_lint.sh 回 1。\n\n8. commit prefix 用 docs:。",
  "resolution": "—(2026-08-05 驗證 CONFIRMED-OPEN,落點由 1 份文檔更正為 2 份;成本 S=兩處各一行;與 IMP-0046 同屬 orchestrator CLI 契約)",
  "schema": "kg.backlog.entry.v1",
  "severity": "low",
  "source": "harness 可信度改造（開場）",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
