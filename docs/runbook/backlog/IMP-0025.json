{
  "acceptance": "(cd backend && uv run pytest tests/test_ops_edit_user_commands_unit.py -q -k UserDelete)  # 今日 exit 5、17 deselected、0 collected(TestUserDelete 尚不存在);六條測試落地並實作完成後應為 6 passed",
  "acceptance_cmd": "cd backend && uv run pytest tests/test_ops_edit_user_commands_unit.py -q -k UserDelete -p no:cacheprovider",
  "acceptance_expect_rc": 0,
  "category": "cli",
  "cost": "M",
  "date": "2026-07-09",
  "detail": "無 `ops-edit user-delete` 原語 → reshape 用的 throwaway scratch 帳號(及一般 demo 帳號)會殘留在 users.json(`_` 前綴者 `is_real_user=False` 自動隱藏於 fleet-overview,但實體條目仍在),只能靠手寫 scoped script 清(本 session 即如此)。候選:`ops-edit user-delete <uid>`(移 user dir + users.json 條目 + email/subscription index scrub,dry-run 預設、寫前自動備份)。**2026-08-05 取證**:`ops_edit.py --help` 列 20 個子指令(user-create/card-add/card-update/card-set-review/card-delete/card-import/notebook-create/user-config-set/link-add/link-delete/seed/clone-demo/world-snapshot/world-restore/list-backups/restore/notebook-update/notebook-delete/card-move/link-list/link-update),**無 `user-delete`**。",
  "fix_site": "backend/src/kg/ops_edit_user_commands.py:100",
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom-all@v2",
  "id": "IMP-0025",
  "plan": "目標：為 `ops_edit.py` 補上缺失的 `user-delete` 子指令（今日 `ops_edit.py` 只有 20 個子指令，無任何刪除用戶的原語，reshape 用的 throwaway scratch 帳號只能靠手寫 scoped script 清）。以下每個 file:line 皆為 2026-08-05 於 HEAD=c21dd8a58 實際讀過的行，附錨點內容。\n\n**步驟 1（TDD 紅燈）— 先寫測試：backend/tests/test_ops_edit_user_commands_unit.py**\n該檔現況：`_make_args` 在 :18-41（defaults 已含 `\"uid\"/\"commit\"/\"json\"/\"backup\": None`，故 delete 與 restore 都不必加欄位）；`class TestUserCreate` 在 :47-75（:47 `class TestUserCreate:`，:75 最後一行 `        assert rc == 0`）；:78 是 `# ── cmd_user_config_set ─────` 區段註解。把新 class 插在 :76 之後、:78 註解之前。\n\n新增 `class TestUserDelete:`（class 名稱是硬約束：驗收指令用 `-k UserDelete` 精確選中它），共 6 個測試，全部沿用 `monkeypatch.setenv(\"KG_DATA_DIR\", str(tmp_path))` + `_make_args(...)` 慣例：\n\n(a) `test_dry_run_keeps_user`：先 `user_cmd.cmd_user_create(_make_args(uid=\"doomed\", email=\"doomed@test.com\", commit=True))`、`capsys.readouterr()` 清緩衝；`rc = user_cmd.cmd_user_delete(_make_args(uid=\"doomed\"))`；斷言 `rc == 0`、輸出含 `\"dry-run\"`、`(tmp_path / \"users\" / \"doomed\").exists()` 仍為 True。\n\n(b) `test_commit_removes_dir_record_and_email_index`：同樣先 create（帶 email）；`cmd_user_delete(_make_args(uid=\"doomed\", commit=True))` 回 0；`users = load_users_from(tmp_path / \"users.json\", lambda x: (x, False))`（此 helper 已於檔頭 :11 `from kg.user_store import load_users_from` 匯入）；斷言 `\"doomed\" not in users`、`not (tmp_path / \"users\" / \"doomed\").exists()`、`\"doomed@test.com\" not in users.get(\"_email_index\", {})`。\n\n(c) `test_commit_scrubs_subscription_index_and_spares_others`：create `doomed` 與 `keeper` 兩帳號（皆 `commit=True`，email 各異）；用檔頭已匯入的 `json` 讀 `tmp_path / \"users.json\"`，塞入 `payload[\"_subscription_index\"] = {\"txn-doomed\": \"doomed\", \"txn-keep\": \"keeper\"}` 後寫回；`cmd_user_delete(_make_args(uid=\"doomed\", commit=True))`；重讀後斷言 `users[\"_subscription_index\"] == {\"txn-keep\": \"keeper\"}`、`\"keeper\" in users`、`(tmp_path / \"users\" / \"keeper\").exists()`。這條是「忘記 scrub subscription index」唯一的紅燈。\n\n(d) `test_absent_uid_raises`：`from kg.ops_edit_support import EditError`（該檔既有測試就是這樣拿 EditError，見 :165 一帶的 `TestUserConfigSet.test_no_updates_raises`）；`with pytest.raises(EditError): user_cmd.cmd_user_delete(_make_args(uid=\"ghost\", commit=True))`。\n\n(e) `test_delete_then_restore_round_trip`：create `doomed --commit`（`cmd_user_create` 的 apply_fn 會呼叫 `_notebook_store(...).ensure_default()`，見 backend/src/kg/ops_edit_user_commands.py:93 `_notebook_store(user_dir_for(dd, uid)).ensure_default()`，所以 user dir 內必有 `notebooks.db`）→ `cmd_user_delete(... commit=True)` → `rc = user_cmd.cmd_restore(_make_args(uid=\"doomed\", commit=True))`；斷言 `rc == 0`、`(tmp_path / \"users\" / \"doomed\" / \"notebooks.db\").exists()`、重讀 users.json 後 `\"doomed\" in users`。備份只會有一份（`user-create` 執行時 user dir 尚不存在，`backup_user_dir` 於 backend/src/kg/ops_edit_shared.py:182-183 `if not src.exists(): return None` 直接回 None、不落檔），故 `cmd_restore` 取 `backups[0]` 不會取錯。\n\n(f) `test_parser_registers_user_delete`：`from kg.ops_edit_parser import build_parser`；`ns = build_parser().parse_args([\"user-delete\", \"u1\", \"--commit\"])`；斷言 `ns.func is user_cmd.cmd_user_delete`、`ns.uid == \"u1\"`、`ns.commit is True`。**這條不可省**：沒有它的話，只做步驟 2 而漏掉步驟 3、4（匯出與 parser 註冊）時，(a)-(e) 因為直呼函式仍會全綠，CLI 卻根本叫不出 `user-delete`。\n\n此時跑驗收指令應為紅（`cmd_user_delete` 不存在 → AttributeError / ImportError）。\n\n**步驟 2 — 實作：backend/src/kg/ops_edit_user_commands.py**\n`cmd_user_create` 的最後一行在 :100（`    return ctx.run(action=\"user-create\", plan=plan, apply_fn=apply_fn, verify_fn=verify_fn)`），:101 為空行，:102 為 `def cmd_user_config_set(args: argparse.Namespace) -> int:`。把新函式插在 :101 之後、:102 之前（維持該檔既有的單一空行分隔風格）：\n\n```python\ndef cmd_user_delete(args: argparse.Namespace) -> int:\n    \"\"\"刪除用戶:移除 user dir + users.json record + email/subscription index。\n\n    可逆性沿用 EditContext:寫前自動 tar 備份 user_dir 到 data_dir/_ops_backups/\n    (在 users/<uid>/ 之外,rmtree 吃不到),`ops-edit restore <uid>` 可還原。\n    **一個非對稱**:還原只回復 record + email index,永不回復 _subscription_index,\n    故 delete→restore 會遺失 App Store transaction-id → uid 的對應。\n    \"\"\"\n    uid = args.uid\n    assert_safe_uid(uid)\n    dd = data_dir()\n    uf = users_file(dd)\n    users = load_users_from(uf, _passthrough_normalize) if uf.exists() else {}\n\n    def _keys_pointing_at(bucket_name: str) -> list[str]:\n        bucket = users.get(bucket_name)\n        if not isinstance(bucket, dict):\n            return []\n        return [k for k, v in bucket.items() if v == uid]\n\n    plan = {\n        \"uid\": uid,\n        \"removes_dir\": str(user_dir_for(dd, uid)),\n        \"had_record\": isinstance(users.get(uid), dict),\n        \"email_index_keys\": _keys_pointing_at(\"_email_index\"),\n        \"subscription_index_keys\": _keys_pointing_at(\"_subscription_index\"),\n        \"restore_caveat\": \"restore 只回復 record + email index,不回復 _subscription_index\",\n    }\n\n    ctx = EditContext(data_dir=dd, uid=uid, commit=args.commit, json_mode=args.json)\n\n    def apply_fn() -> dict[str, Any]:\n        def mutate(current: dict[str, Any]) -> dict[str, Any]:\n            removed_record = current.pop(uid, None)\n            dropped: dict[str, list[str]] = {}\n            for bucket_name in (\"_email_index\", \"_subscription_index\"):\n                bucket = current.get(bucket_name)\n                if not isinstance(bucket, dict):\n                    continue\n                stale = [k for k, v in bucket.items() if v == uid]\n                for key in stale:\n                    bucket.pop(key, None)\n                dropped[bucket_name] = stale\n            return {\"removed_record\": removed_record is not None,\n                    \"dropped_index_keys\": dropped}\n\n        result = _mutate_users(dd, mutate)\n        target = user_dir_for(dd, uid)\n        if target.exists():\n            shutil.rmtree(target)\n        return result\n\n    def verify_fn() -> dict[str, Any]:\n        current = load_users_from(uf, _passthrough_normalize) if uf.exists() else {}\n        leftovers = [\n            key\n            for bucket_name in (\"_email_index\", \"_subscription_index\")\n            if isinstance(current.get(bucket_name), dict)\n            for key, value in current[bucket_name].items()\n            if value == uid\n        ]\n        return {\"ok\": uid not in current\n                and not user_dir_for(dd, uid).exists()\n                and not leftovers}\n\n    return ctx.run(action=\"user-delete\", plan=plan, apply_fn=apply_fn, verify_fn=verify_fn)\n```\n\n四個不可改的細節：\n- **不要傳 `require_user=False`**。`EditContext.__init__` 的預設在 backend/src/kg/ops_edit_shared.py:340（`        require_user: bool = True,`），配合 :348-350（`        if require_user and not self.user_dir.exists():` → `raise EditError(f\"user not found: {uid}...\")`）；正是這個預設讓「刪不存在的 uid」丟 EditError 而非靜默成功。`cmd_user_create`（:65）與 `cmd_restore`（:346）才需要 `require_user=False`。\n- **必須先 `assert_safe_uid(uid)`**（雖然 `EditContext.__init__` 於 :342 也會再呼一次）。它在 backend/src/kg/ops_edit_shared.py:66 定義，明確擋掉 `.` / `..` / 以 `.` 開頭；若不擋，`user_dir_for(dd, \".\")` 會坍縮成 `data_dir/users/` 根目錄，`shutil.rmtree` 就會刪光全體用戶。\n- **users.json 的改動一律走 `_mutate_users`**（backend/src/kg/ops_edit_support.py:326 `def _mutate_users(data_dir_path: Path, mutate) -> dict[str, Any]:`，:329 在 `with FileLock(str(users_lock_file(data_dir_path))):` 內 load→mutate→save）。不要自己開檔寫。\n- **不新增任何 import**。`shutil` / `_mutate_users` / `user_dir_for` / `users_file` / `load_users_from` / `_passthrough_normalize` / `assert_safe_uid` / `EditContext` / `data_dir` / `Any` / `argparse` 全數已在該檔 :3-35 的 `from .ops_edit_support import (...)` 區塊中。\n\n**可逆性是繼承來的，不要重造**：`EditContext.run` 在 backend/src/kg/ops_edit_shared.py:379-382（`        backup = None` / `        try:` / `            backup = backup_user_dir(self.data_dir, self.uid)` / `            result = apply_fn()`）保證備份在 apply 之前；`backup_user_dir`（:175 定義）在 :192-205 把 users.json record 與該 uid 的 email index 一併打包進 manifest（:197 `\"schema\": \"kg.ops_user_backup.v2\",`、:195 `scoped_email_index = {k: v for k, v in email_index.items() if v == uid}`）；備份落點 `_backup_root`（:125-126 `return Path(data_dir) / _BACKUP_DIRNAME`，`_BACKUP_DIRNAME = \"_ops_backups\"` 於 :37）在 `users/<uid>/` 之外，rmtree 吃不到。`cmd_restore`（backend/src/kg/ops_edit_user_commands.py:342-393）本就用 `require_user=False` 並會重建目錄與 record。唯一非對稱寫進上面的 docstring 與 plan payload 的 `restore_caveat`：`_restore_user_record_snapshot`（backend/src/kg/ops_edit_support.py:336-363，其 mutate 於 :350 `idx = users.setdefault(\"_email_index\", {})` 起只處理 email index）從不碰 `_subscription_index`。該 bucket 由 backend/src/kg/billing/index.py:14（`    index = users.get(\"_subscription_index\")`，於 `upsert_subscription_index` 內）寫入、由同檔 :25 `def resolve_user_id_from_subscription_index(` 讀取；殘留條目會讓 App Store transaction id 解析到一個已不存在的 uid。\n\n**步驟 3 — backend/src/kg/ops_edit_commands.py**\n把 :15 的 `from .ops_edit_user_commands import cmd_list_backups, cmd_restore, cmd_user_config_set, cmd_user_create` 改為加入 `cmd_user_delete`（依字母序放在 `cmd_user_create` 之後）；並在 `__all__`（:17 `__all__ = [` 起至 :40 `]`）中，於 :19 `    \"cmd_user_create\",` 之後插入 `    \"cmd_user_delete\",`。\n\n**步驟 4 — backend/src/kg/ops_edit_parser.py**\n① 匯入區塊在 :8-31（:8 `from .ops_edit_commands import (`、:28 `    cmd_user_create,`、:31 `)`），在 :28 之後插入 `    cmd_user_delete,`（維持字母序）。\n② `user-create` 的 subparser 區塊在 :47-52（:47 `    p = sub.add_parser(\"user-create\", parents=[jp, cp], help=\"建立用戶 + 預設筆記本\")` … :52 `    p.set_defaults(func=cmd_user_create)`），:53 為空行、:54 起是 `card-add`。在 :52 之後、:54 之前插入：\n```python\n    p = sub.add_parser(\"user-delete\", parents=[jp, cp],\n                       help=\"刪除用戶(移除 user dir + users.json record + email/subscription index;寫前自動備份,可 restore)\")\n    p.add_argument(\"uid\")\n    p.set_defaults(func=cmd_user_delete)\n```\n\n**步驟 5 — 同 PR 的 agent-facing surface 同步（CLAUDE.md「改 user/agent-facing 介面」scope 規則；漏了就等於下一個 agent 不知道有這指令）**\n- `.claude/skills/devops/SKILL.md`：ops-edit 子指令 code block 起於 :113（`\\`\\`\\`bash`），:114 為 `ops-edit user-create <uid> [--email E] [--provider google|apple|demo] [--allow-existing]`。在 :114 之後插入一行 `ops-edit user-delete <uid>                                        # 移 user dir + users.json record + email/subscription index;寫前備份,可 restore(但 restore 不回復 subscription index)`。\n- `docs/reference/tech_index.md:184`：該行是「**寫入面**:`backend/ops_edit.py`…」整段；在列舉 `建用戶 / 增改刪卡 /…` 的地方把 `user-delete` 與 `user-create` 並列，並註明 restore 不回復 `_subscription_index` 的非對稱。同時依 docs 控制面規則把該 doc 的 `verified_against` bump 到 main 可達的 code commit，PR 前跑 `./ops/docs_lint.sh`。\n\n**步驟 6 — 刻意不做的事**：不加任何額外的「真實帳號保護」旗標（例如 `--force` 或 `is_real_user` 檢查）。`user-create` 也沒有，現有安全模型 = dry-run 預設 + 寫前自動備份 + restore；臨時加一個未經審閱的 policy 旗標會擴大這條的範圍。也不要動 `_restore_user_record_snapshot` 去補 subscription index 還原——那是另一條獨立改動，本條只負責把非對稱記錄下來。\n\n**會不會撞到別處**：已 grep 過 `backend/tests` 與 `ops/tests`，沒有任何測試枚舉 `ops_edit` 的子指令總數或完整清單（只有零散的 `_edit(..., \"user-create\", ...)` 呼叫），故新增第 21 個子指令不會讓既有測試轉紅。`_subscription_index` 的現有讀寫者（backend/src/kg/deps.py:209、billing_handlers.py:211/268、routers/billing.py、billing/snapshots.py:63 及 backend/tests/test_billing_handlers.py 一系列）都不經 users.json 檔案層，本改動不觸及它們。\n\n**哪一條命令由紅轉綠**：`(cd backend && uv run pytest tests/test_ops_edit_user_commands_unit.py -q -k UserDelete)` —— 今日 exit 5（17 deselected、0 collected），六條測試全數落地後應為 6 passed。",
  "resolution": "—(2026-08-05 驗證 CONFIRMED-OPEN;落點新 `cmd_user_delete`+parser,寫前備份沿用既有 world-snapshot,成本 M,測試 `backend/tests/test_ops_edit_user_commands_unit.py`。**不併入 0024/0026**:無共用程式碼,是 reshape 流程的收尾清理,可獨立出貨;若 0024 的 `--replace` 落地,本條的觸發源(throwaway scratch)會消失、降為一般 demo 清理需求)",
  "schema": "kg.backlog.entry.v1",
  "severity": "low",
  "source": "行銷帳號 Phase5(scratch 清理)",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
