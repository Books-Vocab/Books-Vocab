{
  "acceptance": "每張派工票在 claim 前缺少一個只讀、可重跑的總檢查器：fix_site 不存在、acceptance 只描述不驗 defect、blocked_by 未解、active worktree 實際 diff 重疊、以及 acceptance 已經綠或因環境缺依賴，現在都可能等到 open 後才發現，造成無效 worktree、測試重跑與 teardown。工具需要把這些結果編譯成具名分類與可執行修復，不得在分類失敗時取得 claim。",
  "acceptance_cmd": "set -eu\nuv run --no-project --python 3.13 --with pytest pytest -q -p no:cacheprovider ops/tests/test_worktree_orchestrate.py -k 'dispatch_preflight'",
  "acceptance_expect_rc": 0,
  "brief": "派工前無法可靠辨認無效票，導致開樹後才拆除並重跑測試。",
  "category": "tool",
  "contract_baseline": "red",
  "contract_checked_at": "2026-08-11",
  "contract_checked_by": "agent:platform-steward",
  "contract_evidence": "2026-08-11 readonly contract probe at primary 0d880b60a: acceptance command `uv run --no-project --python 3.13 --with pytest pytest -q -p no:cacheprovider ops/tests/test_worktree_orchestrate.py -k 'dispatch_preflight'` returned rc=5 because no dispatch_preflight tests exist yet; fix_site existing anchors are present; acceptance path itself exists; no active claim for the new ticket. This is the intended RED baseline for the new preflight compiler.",
  "contract_status": "ready",
  "date": "2026-08-11",
  "detail": "每張派工票在 claim 前缺少一個只讀、可重跑的總檢查器：fix_site 不存在、acceptance 只描述不驗 defect、blocked_by 未解、active worktree 實際 diff 重疊、以及 acceptance 已經綠或因環境缺依賴，現在都可能等到 open 後才發現，造成無效 worktree、測試重跑與 teardown。工具需要把這些結果編譯成具名分類與可執行修復，不得在分類失敗時取得 claim。",
  "fix_site": "ops/backlog.py; ops/worktree_orchestrate.py; ops/tests/test_worktree_orchestrate.py; ops/tests/test_backlog.py",
  "fixed_by": [
    "fe903e7060ac3200d035c6d725b2045a29ac453b"
  ],
  "groomed_against": "0d880b60a37cb5c787af38d9d4e1171ffb89450f",
  "groomed_at": "2026-08-11",
  "groomed_by": "agent:platform-steward",
  "id": "IMP-20260811-103a53",
  "plan": "1. 新增 ops/lib/dispatch_preflight.py，定義 typed 結果與分類 executable、contract-blocked、baseline-green、active-overlap、environment-blocked、dependency-blocked；所有路徑、blocked_by 與 registry diff 檢查必須是純函式/唯讀。\n2. 新增 `ops/backlog.py preflight <id> --json`（或等價的 ops 控制面入口），先跑既有 contract_preflight，再檢查 fix_site/acceptance dependency、unresolved blocked_by、active worktree changed-file overlap；輸出穩定 schema、分類、problems、repair hints，不寫 store/queue/registry。\n3. 加入明示 `--probe-acceptance` 的 bounded 執行器，只在使用者明示時執行 ticket acceptance；expected rc=0 且成功標 baseline-green/no-op，缺 executable/module/uv dependency 標 environment-blocked，其他 rc 標 red 或 acceptance-error，不把 probe 結果當 fixed。\n4. 將 worktree open/adopt 的 claim gate 改用同一 preflight compiler；除明示 investigation/allow-ungroomed 外，contract-blocked、baseline-green、overlap、dependency/environment blocked 均在 claim 前拒絕，且拒絕 payload 含完整分類與修復命令。\n5. 在既有 ops/tests/test_worktree_orchestrate.py 與 backlog tests 加入 malformed path、missing dependency、unresolved blocker、active diff overlap、baseline-green/no-op、valid RED 與 stable JSON schema 負/正控；不依賴網路、iOS、真實 registry mutation。",
  "resolution": "",
  "schema": "kg.backlog.entry.v1",
  "scope": "一個唯讀 preflight 命令、open claim gate 的共用 wiring、以及一組離線 fixture 測試；acceptance probe 另有明示的受控模式。",
  "severity": "high",
  "source": "agent:throughput-control/p0-dispatch-preflight",
  "status": "fixed",
  "stream": "IMP",
  "verdict": "CONFIRMED-FIXED",
  "verified_at": "2026-08-11",
  "verified_by": "agent:delivery-team-integrator",
  "verified_evidence": "P0 child hand-back 3b7aae0211b7; focused 27 plus full orchestrator 380 and backlog 330; review PASS"
}
