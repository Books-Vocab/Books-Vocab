<!-- doc-meta
tier: runbook
authority: SoT
update_trigger: manual
scope:
  - .claude/agents/platform-steward.md
  - .claude/skills/kg-receipt/
verified_against: febc68ebb
-->
# 改善 Backlog（kaizen ledger）

> 自我提升迴圈的 **SoT**:所有「工具 / CLI / 文檔 / 架構」摩擦的 open 問題單一登記處。
> 原則見**鐵律9**(摩擦優先修工具)、分級見 `kg-router`「Tool Friction」、表態見 `kg-receipt`「Tooling Debt」——本文**不複述**,只負責**持久化、追蹤、收斂**。

## 為什麼存在

receipt 裡的 tooling debt 會隨 transcript 蒸發。本 ledger 讓每個 raised 問題**進 git、可回溯、有 owner、追到 resolved**,杜絕 agent 無聲妥協(硬幹)。owner = `platform-steward`(Staff)。

## Andon — 任何節點怎麼提一筆

1. 撞到摩擦 → 先第一性原理判根因(鐵律9),分級(kg-router「Tool Friction」)。
2. 在 receipt 表態(規則見 `kg-receipt`「Tooling Debt」)。
3. 非 trivial 且未當場修掉 → 由上一階 / `platform-steward` 追加一列到下表。
4. 中大型 / 結構級 → 不只登記:停手修工具,或升級上一階(鐵律9 + `docs/sop/agent_org.md`「反硬幹升級階梯」)。

## Entry schema

- `status`: `open` → `triaged` → `in-progress` → `fixed` / `wont-fix`(附理由)
- `category`: `tool` / `cli` / `doc` / `arch`
- `severity`: `low` / `med` / `high`
- `resolution`: 解決 commit hash,或 wont-fix 理由(這是「可回溯」的關鍵欄)
- resolution hash 慣例:PR 合併前為 branch-local;若該 PR 採 **squash merge**,合併後由 `platform-steward` 更新為 squashed hash,維持 audit trail 不斷。

## Ledger

| id | date | source | category | severity | status | detail | resolution |
|---|---|---|---|---|---|---|---|
| IMP-0001 | 2026-06-13 | docs-steward 首測 | doc | low | fixed | agent 檔寫「依 kg-receipt 格式」但未給欄位指標,靠 brief 餵 | `7c95a02`(補欄位指標) |
| IMP-0002 | 2026-06-13 | review gate | doc | low | fixed | agent_org.md 3 處 borderline 複述鐵律判準 | `3671b89`(收斂為純指標) |
| IMP-0003 | 2026-06-13 | docs-steward 首測 | cli | low | triaged | `docs_impact.py` 對 CLAUDE.md 純政策段新增穩定產生 5 條 exact 誤報(`via=CLAUDE.md`);hint≠命令故不阻擋,但每次需人工判讀 | open — 候選 enhancement:registry 對 CLAUDE.md source 加 section-anchor 機制 |
| IMP-0004 | 2026-06-13 | backend-engineer smoke | tool | low | wont-fix | 首次 `uv run` 觸發 .venv bootstrap(~100MB),新 worktree / CI 首跑有感 | 正常行為,快取後即解;不修 |
| IMP-0005 | 2026-06-13 | kaizen-loop review gate | cli | low | fixed | `docs_lint.sh` 不接受裸 doc 路徑當位置參數(`docs_lint.sh foo.md` → Unknown arg),已加可接受裸路徑並保留清楚提示 | `813356b1` |
| IMP-0006 | 2026-07-08 | iOS 2.0.0 發版檢討 | tool | low | fixed | `asc.sh`/`ios_release.sh` 靜默吞 ASC API 錯誤(403 agreement 無輸出 exit 1) | `ef5fcfb00`(fd3 透出+403 GUI 指引+test_asc §17) |
| IMP-0007 | 2026-07-08 | iOS 2.0.0 發版檢討 | arch | med | fixed | 升級觸發清單漏「執行中發現 human-only 動作」,blocker 批到 receipt 才告知(ASC 403 損失 40 min 可平行人工時間) | `9a8209a4c`(agent_org.md 補即時升級觸發)+`fbf2221cb`(review 修正) |
| IMP-0008 | 2026-07-08 | iOS 2.0.0 發版檢討 | arch | med | fixed | 委派無成本下限,trivial 工作也燒全套 agent+receipt | `9a8209a4c`(agent_org.md 補 trivial 門檻)+`fbf2221cb`(trivial vs 豁免釐清) |
| IMP-0009 | 2026-07-08 | iOS 2.0.0 發版檢討 | doc | low | fixed | 逐項 review 固定檢查項靠 GM 每次手寫 brief,重複且易漏 | `9a8209a4c`(code-reviewer.md 內建 checklist)+`fbf2221cb`(SoT 收斂:複述→指標) |
| IMP-0010 | 2026-07-08 | iOS 2.0.0 發版檢討 | cli | med | fixed | `release.sh bump` 跑了立即寫檔,違 dry-run 預設慣例(實際咬人:預覽即污染 pbxproj) | `34cd97866`+`9c88b55b2`(dry-run 預設+--yes+全面語意同步+迴歸) |
| IMP-0011 | 2026-07-08 | iOS 2.0.0 發版檢討 | cli | low | fixed | `ios_test.sh --ui` 缺 dataset 錯誤不列可用清單,需二次查找 | `27d61ecb5`+`d56efeabe`(錯誤附 ui_worlds 名單+set -e 防死+迴歸) |
| IMP-0012 | 2026-07-08 | iOS 2.0.0 發版檢討 | doc | low | wont-fix | `docs/sop/ios.md` §UI World dataset 契約為單行 ~4000 字牆,知識密度高但不可讀不可維護;候選:結構化為表格+分節 | **frozen: UI World / catalog 一致性平面已凍結（2026-08-05，見 IMP-0063）**。UI World 自該日起停止擴張,那段契約不再演進,重構它只是把一段不會再被讀來指導動作的文字排版得好看一點。復業（送審 / 重做行銷截圖 / 大改 UI 風格）時重開此條。 |
| IMP-0013 | 2026-07-08 | 版本控制盤點 | cli | med | fixed | api 元件版號分叉:tag api/1.6.0 vs pyproject 0.1.0,339 筆 commit 未發版(版本真相兩處互相矛盾) | `208e41437`(bump+tag api/2.0.0,漂移清零) |
| IMP-0014 | 2026-07-08 | 版本控制盤點 | cli | low | fixed | 同版重送(被拒後最高頻操作)無 build-only bump typed 入口,須手改 pbxproj | `9578d9cb0`+`2cd12c8de`(release.sh bump-build ios,dry-run 預設+11 斷言+docs 同步) |
| IMP-0016 | 2026-07-08 | Phase 1 review(world-export) | tool | low | open | Card/Notebook schema 變更無 lint 抓「export/seed 未同 PR 對齊」→ roundtrip 靜默有損;現靠 ops_state_plane §1.1 紀律 + roundtrip 測試抓 export 側漏導。符合 gate 刻意延後政策([[ops-gate-enforcement-deferred-by-design]]),升級訊號=同錯第三次 | — |
| IMP-0017 | 2026-07-08 | ops-engineer（spec 投影 Phase 2） | tool | med | open | `ios_test.sh` 在 set -e + pipefail 下任何內部管線非零會**靜默中止且 exit 0**（cleanup trap 吞 exit code、無錯誤輸出）——cache-key 缺檔觸發點已修（`4d11a11d2`），但「mute rc-0 abort」這一類故障模式仍在:任何未 `|| true` 防護的管線都可能無聲假綠。候選:EXIT trap 保留原 exit code + set -e 中止時印 stderr 診斷行（兩度撞號:原 IMP-0013→改 0014 又撞 release 盤點→定 0017） | — |
| IMP-0015 | 2026-07-08 | rebase 收斂事故(92da32e64) | cli | med | fixed | `docs_lint.sh` 對 changed docs 不掃 git 衝突標記（`<<<<<<<`）也不驗 `verified_against` hash 在 main 可達——衝突標記+不可達 hash 雙雙進 main 而 lint 全綠（當次資料修復見 6cec330c8）。兩檢查都已進日常 gate | `c6c1b1619`(衝突標記 gate + 修 5 個 invalid verified_against)+`a4a100603`(verified_against HEAD 可達性，堵 rebase 後 orphan anchor 假綠) |
| IMP-0018 | 2026-07-08 | deflate 修復(c272101c9) | tool | med | fixed | `ui_world_manifest.py validate` 與 Swift decoder 有 nullability gap:todayReview `dateAdded` null 過 validator 卻在 app preconditionFailure——validator 應對齊 Swift date 非空要求 | `6a9761d5e`(validator dateAdded 必填 + spec_world 非空導出)；`5a25ae6ac` 追加 graph link in-seed 驗證（同類 nullability/dangling gap） |
| IMP-0019 | 2026-07-08 | deflate 修復(c272101c9) | tool | med | fixed | catalog content-pinned seeds（wordDetail/wordEdit/search/archived 等釘死特定內容的 scenario）無 pre-flight 檢查,world 覆蓋 pinned domain 只能跑到該 surface 才 fatal;候選:emit/validate 層列舉 pinned fixture 清單並擋覆蓋 | `6a9761d5e`+`5a25ae6ac`(emit 層:vocabulary 8 個 + reviewDeck probe/notebookReviewDeck 共 10 個 pinned fixture 入 SPEC_BASELINE_KEPT_FIXTURES,spec 模式強制 byte-equal baseline;standalone validate 不驗 pin——pin 是 spec 投影語意,非 world 格式契約) |
| IMP-0020 | 2026-07-08 | deflate 修復(c272101c9) | cli | low | open | `catalog snapshots --reuse-build` 在「cache hit 但 test run 失敗」時回 `status:"cache-miss"`,語意誤導（實為 run failure） | — |
| IMP-0021 | 2026-07-09 | spec_world 修復 review(5a25ae6ac) | tool | low | wont-fix | `ui_world_manifest.py` 對 vocabulary/reviewDeck 的 graphLinksByKind **link 元素**無 schema 驗證（只驗頂層 Mapping[str→list]）:非 str word/cardId 或非 mapping link 會雙層靜默放行,Swift decode 才炸;todayReview link 已有 `_validate_today_review_link` 全鍵驗證,建議比照補上 | **frozen: UI World / catalog 一致性平面已凍結（2026-08-05，見 IMP-0063）**。**理由修正(reviewer 實測推翻我的原始理由)**:原本寫「world 不再改」是錯的——world 有兩種,repo world(`marketing_demo.json`)確實凍結,但 **spec-mode world 每次送審都重生**(`ops/app_review_evidence.py:445` 呼叫 `emit-ios --spec`,而 §FROZEN 自己就寫「送審仍經這條路」),所以 `spec_world.py` 的 graphLinksByKind 投影在唯一被保留的活動中**仍會執行**,該洞仍可觸發。維持 wont-fix 的**正確**理由是:該投影是確定式的,且輸出受 baseline byte-lock(`emit-ios --check`)約束,實際風險低。復業時**必須**重開此條。 |
| IMP-0022 | 2026-07-09 | 行銷帳號 Phase5(felix 漂移) | tool | med | open | felix 生產後端 git HEAD 靜默落後 origin/main 80 commits(0cea7f67 vs main),無任何告警。stale 容器的 `ops-edit seed` 舊碼**靜默丟棄** spec 的 counter-form 複習日期(review_count>0=0、next_review_at 全 null)、不合成 review_events(heatmap/streak=0)、不做 is_default→default remap(多殘留空 notebook)——差點注入「看起來全新未使用」的行銷帳號。本 session 已 deploy felix→main(0611f3ca)解當下阻塞,但**監控 gap 仍在**。候選:`devops_kg_safe.sh health` 加「commits behind main」metric + warn 閾值,讓生產漂移顯式化。 | —(deploy 0611f3ca 解當下;monitoring 候選未做) |
| IMP-0023 | 2026-07-09 | 行銷帳號 Phase5(backup) | cli | med | open | `devops.sh cmd_backup`(line 596)無條件用 GNU-rsync 專屬旗標 `rsync -az --delete --info=progress2 --human-readable`,macOS 內建 openrsync 不支援 `--info=progress2`(`unrecognized option`)→ 從 oscar 跑 `devops_kg_safe.sh backup` 直接失敗、備份命令不可用。候選:偵測 rsync flavor(GNU vs openrsync)擇旗標,或改用兩者皆支援的 `--progress`,或該旗標包 `\|\| true` 降級。 | — |
| IMP-0024 | 2026-07-09 | 行銷帳號 Phase5(reshape 機制) | cli | med | open | `ops-edit seed` 為 additive-upsert,`CardStore.add` 查重綁 `notebook_id`(`cards/mutations.py:51-55`)→ 對「卡已存在但在別的 notebook」會**新增重複卡**而非搬移;把一個既有帳號 reshape 成 spec(結構不同時)會造大量重複卡且 verify_fn 報綠(false-green)。現只能靠「seed 一個 throwaway scratch → clone-demo 原子替換」的迂迴。候選:`ops-edit seed --replace <uid> <spec>`——單一 EditContext 內先 wipe 整層 vocab(複用 clone-demo 的 `_is_vocab_file` 移除,含 review_events.db)再 seed,self-verifying,把 spec→exact-state 收成一個原語。 | — |
| IMP-0025 | 2026-07-09 | 行銷帳號 Phase5(scratch 清理) | cli | low | open | 無 `ops-edit user-delete` 原語 → reshape 用的 throwaway scratch 帳號(及一般 demo 帳號)會殘留在 users.json(`_` 前綴者 `is_real_user=False` 自動隱藏於 fleet-overview,但實體條目仍在),只能靠手寫 scoped script 清(本 session 即如此)。候選:`ops-edit user-delete <uid>`(移 user dir + users.json 條目 + email/subscription index scrub,dry-run 預設、寫前自動備份)。 | — |
| IMP-0026 | 2026-07-09 | 行銷帳號 Phase5(驗證) | cli | low | open | `ops-cli world-diff` 硬性要求 `schema==kg.ops_world_expectation.v1`(`ops_world_diff.py:18`),無法驗「帳號 == seed_spec」(注入格式是 `kg.seed_spec.v1`)。缺一個 first-class「reshape 是否精確落地」檢查,只能靠 `world-export` + 外部 normalized diff 橋接(本 session 自寫 python 比對)。候選:教 world-diff(或新 world-verify)吃 seed_spec,或 seed/clone-demo 自吐比對 verdict。 | — |
| IMP-0027 | 2026-07-09 | 行銷帳號 Phase5(provenance) | tool | low | fixed | 行銷 spec provenance 曾未 pin，無機制保證「重建／注入的 artifact == 被審核的 artifact」。現由 tracked source spec + history plan 重建，要求生成 dataset SHA 精確等於 release spec，reviewer bundle 再封閉綁定 profile / dataset / render spec / 每張輸出 digest；live-demo identity 另沿 spec→ASC mirror→runner→receipt 同一 SHA 收斂，故不另開重複的 closed-world debt。 | `98ec95539`(typed producer + exact reviewer-bundle closure)+`4d320ff5b`(live identity closure) |
| IMP-0028 | 2026-07-09 | 行銷帳號 Phase5(時鐘凍結對抗驗證) | tool | med | fixed | iOS `SettingsCoordinator.applyServerReviewClock`(`SettingsCoordinator.swift:113`)用 strict `AppDateFormatters.iso8601`(要小數秒)解析伺服器 `paused_at`,而非旁邊的 `AppDateFormatters.parseISO8601` fallback(`AppDateFormatters.swift:56-57`)→ 缺小數秒的 `paused_at` 靜默回 nil → 凍結退回 wall-clock 卻仍報 is_paused=true(false-green)。本 session 注入用 `.000Z` 規避,但潛伏 bug 仍在。**已修復**:改用 `AppDateFormatters.parseISO8601`(小數秒優先 + 不含小數秒 fallback),對稱寫入側恆產小數秒;TDD 兩案回歸測試(有/無小數秒)。 | bb45e0f85(#981;rebase onto main 後 CI ui-quality-gate pass、squash merge) |
| IMP-0029 | 2026-07-09 | 行銷帳號 Phase5(時鐘凍結對抗驗證) | arch | low | open | iOS 伺服器 review-clock push 只在 cold-start 套用(`SettingsCoordinator.swift:111` gate `pauseClockSnapshot.updatedAt == nil`)+ `KGFeatureFlags.serverReviewClockLwwEnabled` dead-`false`(`KGFeatureFlags.swift:27`)→ **任何曾本地 toggle 過複習時鐘的裝置,伺服器凍結會被忽略**。屬當前同步狀態的 by-design(LWW 未啟用),非 bug;行銷截圖走 catalog fixture(offline UserDefaults seed 恆套用)故不受影響。候選(產品決策):是否讓 server-authoritative review-clock LWW 出貨。 | —(by-design,待產品決策) |
| IMP-0030 | 2026-07-10 | Explore P1 docs-sync | tool | med | open | `ios_test.sh --ui <bareMethod>`(裸方法名)對**不在** class `BooksAndVocabUITests` 的 UITest method 回 `tests=0` 卻 exit 0(**FALSE GREEN**):bare method 只在預設 suite 內解析,別的 UITest class(如 `ExploreNavigationUITests`)的 method 靜默零執行、假綠。workaround=用完整限定 `Class/method`。fix 候選:runner auto-resolve method→其所屬 class,或 `--help`/零匹配錯誤明示「bare method 僅限預設 suite,跨 class 用 Class/method」 | — |
| IMP-0031 | 2026-07-13 | iOS 2.0.1 誤標事故 | cli | high | fixed | `release.sh` 把 iOS semver 建議當成可執行 release，未要求 previous marketing version 已完成審查的 typed attestation，且先 tag/push 再 upload；被拒中的 2.0.0 因而可被誤升 2.0.1，upload 失敗也會留下 false release marker | `6ff5bcf10`(離線 `--new-version-after-ready` guard + semver 單調性 + upload-before-tag + 71 斷言) |
| IMP-0032 | 2026-07-14 | App Review 2.0.0 evidence producer | cli | med | fixed | `app_review_evidence.py` 的 desired shape/build/bundle、journey、physical demo、gate evaluation 曾以 `capture_output=True` 靜默等待。現統一走 bounded streaming runner：stderr 最長每 20 秒回 phase/elapsed/PID/alive，stdout 保持單一 JSON；raw argv 不進 progress，中斷會清整個 isolated process group。 | `4e2e89b55`(producer heartbeat + stdout purity + secret/process-group adversarial regressions) |
| IMP-0033 | 2026-07-14 | App Review 2.0.0 Release iphoneos compile gate | cli | high | fixed | `ios_ops.sh test --ui --configuration Release --destination generic/platform=iOS --prepare-cache --json` 曾在 xcodebuild exit 0、iphoneos xctestrun 與兩個 `.app` 都存在時，同份 JSON 回 `status:"prepared"` 與 `productsReady:false`。根因＝readiness predicate 以 `platform=iOS,`（**帶逗號**）判 device SDK，comma-less 的 `generic/platform=iOS` 落回 iphonesimulator。調查發現嚴重度高於原評：**雙向錯誤**（iphonesimulator 產物會被當成 device build 的證據）、sentinel 走同一 predicate 故該 cache 永久冷、且 `prepare` 硬寫 `"prepared"` 而只有 `status=="error"` 才 exit 1 → `worktree_orchestrate` 的 live-only Release compile **block** gate 在「build exit 0 但零產物」時仍綠（假綠）。順帶修 `--json` 下 progress banner 污染 stdout。 | `febc68ebb`(`ios_test_sdk_suffix` seam + `ios_test_prepare_status` 導出 status + stdout 純度 + hermetic iphoneos fixture 迴歸；365 passed/0 failed) |
| IMP-0034 | 2026-07-14 | App Review 2.0.0 worktree gate | tool | med | fixed | 修改 `LiveDemoAccessUITests.swift` 時，cutover 原先誤套 Debug simulator + marketing fixture，與該測試的 Release / physical / no-fixture 契約必然衝突。現 live-only class 改走 Release generic-iOS compile gate，runtime evidence 明示交給 `app_review_evidence.py demo-run`。 | `534acb68a` |
| IMP-0035 | 2026-07-14 | App Review 2.0.0 live runner review | tool | high | fixed | live-demo staging 曾把 account identity marker/hash 注入 xctestrun 的所有 test targets，權限面大於固定 live-only method。現先 sanitize/scan 每個 configuration/target，只注入唯一 `BlueprintName=BooksAndVocabUITests` target；0 或多個匹配在注入前 fail-closed，其他 target 保留 key-absent。 | `30df7f5f1` |
| IMP-0036 | 2026-07-17 | App Review evidence worktree gate | cli | med | fixed | `worktree_orchestrate.py gate` 曾把每個 shell gate 的合併輸出靜默 capture 到結束；長 iOS/pytest gate 無 PID/heartbeat，操作者無法分辨排隊、執行或卡死。現共用 bounded streaming runner，stderr 有 start/spawn/20 秒 heartbeat（正常 exit 另有 done），JSON stdout 不受污染，中斷清整個 process group，並保留 merged tail、rc/signal 語意。 | `4e2e89b55`(gate heartbeat + bounded capture regressions) |
| IMP-0037 | 2026-07-17 | worktree resolve 實測 | cli | high | fixed | committed `resolve` 的 `git worktree remove --force` 實測約 30 秒、CPU 約 40%，期間因 mutation 共用 silent `subprocess.run(PIPE)` 完全無輸出，操作者無法分辨忙碌或卡死；同一路徑也涵蓋 fetch/rebase/merge/push/branch teardown 與網路 probe。現所有潛在長時 mutation、`ls-remote` 與 committed sweep 統一走 bounded streaming runner，stderr 立即顯示安全語意 phase/PID/alive、最長每 20 秒 heartbeat、正常 exit done；JSON stdout、dry-run 與 failure detail 保持，raw argv 不顯示。 | `457b8ea74`(mutation heartbeat + caller-route/JSON/secret regression) |
| IMP-0038 | 2026-08-03 | harness 可信度改造 Phase 1 | doc | med | open | 約 10 份 doc 的 `verified_against` 錨點指向 **從未進入 origin 的 pre-squash commit**（`docs/policy/safety.md` f0d37ca4、多份 `feature_boundary/*` …）。本機能過 `docs_lint` 只是因為本地 dangling object 仍在，CI 上（即使 `fetch-depth: 0`）一律解析失敗。Doc Tier 契約要求錨點「main 可達」，故這是實質假綠而非 CI 怪癖。`docs-lint` 因此暫時排除於 linux CI set。下一步：逐份把錨點 bump 到 main 可達 commit，然後把 group 移回 `LINUX_GROUPS`。 | — |
| IMP-0039 | 2026-08-03 | harness 可信度改造 Phase 2 | tool | high | fixed | `ios-build-catalyst` 是 cutover 的 **block** gate，但本機**結構性不可能通過**：`No "Mac Development" signing certificate matching team ID XNSH5U9FNV`。未改動的 main 亦然（exit 65）。一道環境上不可能綠的 block gate 與一道不可能紅的 gate 同樣有害，方向相反——它讓每個 iOS cutover 都被擋，而唯一的出路是繞過流程。原本列的兩個選項（裝憑證／降為 warn）都沒採用：**採第三案**——該 gate 宣稱驗的是編譯（"sim green != Catalyst green"），而編譯與簽章正交，故 `--catalyst` 改為隱含不簽章編譯。已知覆蓋缺口（本 repo 從不簽 Catalyst 產物，故 entitlement-vs-profile 相容性無處驗證）已在 `docs/sop/ios.md` §Step 1 具名接受。 | `19f70c4e4`（branch-local；`--catalyst` 隱含 `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO`，plan/run 共用 `xcodebuild_argv`） |
| IMP-0040 | 2026-08-03 | harness 可信度改造（開場） | doc | low | open | `worktree-flow` skill 宣稱「所有 mutation 子指令 dry-run 預設、`--commit` 才落地」，但 `worktree_orchestrate.py open` 沒有 `--commit` 旗標、直接落地。第一次照文檔呼叫即撞到 usage error。下一步：修文檔措辭（列出例外），或給 `open` 加上 dry-run 語意。 | — |
| IMP-0041 | 2026-08-03 | harness 可信度改造 Phase 3.1（未完成） | tool | med | open | `ops/ui_quality_gate.py:46-66` 的 `FAST_COMMANDS`/`SLOW_COMMANDS`/`UI_WORLD_REQUIRED` 是**第二份手寫 mechanism id 清單**，與 `ops/ui_quality_plane.yml` 無交叉驗證：yml 新增機制但忘了在 runner 登記 → `resolve_args` 回 (None,None) → status=`planned` → 不計入 failed → gate 全綠。違反 CLAUDE.md「SoT 零重複鐵則」。下一步：把命令併進 yml 成 `run:`/`requires:` 欄位，`cmd_validate` 對 `gate: manual` 而無 `run:` 報 ERROR，runner 改讀 yml 且讀不到即 failed（非 planned）。 | — |
| IMP-0042 | 2026-08-03 | harness 可信度改造 Phase 5（未完成） | arch | med | open | exit code 有兩個互斥家族：家族1 `0/1/2=pass/warn/block`（`ios.gate`/`branch_audit`/`review_audit`/`docs_lint`）與家族2 `0=pass,2=block,1=工具錯誤`（app_review 全家）。決定性理由：這些腳本跑在 `set -euo pipefail` 下，未捕捉的中途失敗天然 exit 1，所以家族1 讓「腳本半路死掉」與「乾淨判 warn」不可區分——同一種病換一層。裁定應為 `0=pass/1=tool error/2=block/3=warn/64=usage`。遷移成本經查證極低（branch_audit/review_audit 的 exit code 無機器消費者；worktree 測試幾乎全用符號常數）。附帶：sha256 五份、canonical JSON 四份（`ops/demo/emit_ios.py:280` separators 不同導致 hash 不相容）、ISO8601 三份、原子寫檔四份，應抽進 `ops/lib/`。 | — （2026-08-03 補：`ops/lib/provenance.py` 已建立為 tool-identity hashing 的 SoT，是 sha256 抽取的指定落點。實際 streaming-sha256 副本是 `ops/reviewer_evidence.py:32` 與 `ops/ui_world_manifest.py:515`；`ops/capture_profile.py` **沒有**副本，它是 import 的——遷移前先核對清單。） |
| IMP-0046 | 2026-08-03 | IMP-0039 收尾觀察 | tool | low | open | `worktree_orchestrate.py gate --receipt-line` 是**互斥的輸出模式**，不是附加輸出（`cmd_gate` 照常跑完 `for spec in plan: _run_gate(...)`，只換 print 格式）。重跑本身是刻意的——那行的價值就是「沒跑過就產不出來」，讀快取會讓陳舊 record 可被貼上。問題在於拿不到「報告 + receipt 行」的**單次**執行，所以照工作流（先看結果再產 receipt）必付兩倍完整 gate 時間；本次實測第二次呼叫 3 分鐘未完成。下一步：正常（非 `--json`）報告結尾**恆印**那一行，`--receipt-line` 退化為「只印那行」的靜音模式。 | — |
| IMP-0044 | 2026-08-03 | IMP-0039 收尾觀察 | tool | med | fixed | `ops/tests/test_gate_can_fail.sh` 只驗**單一方向**（「餵壞輸入會紅嗎」）。`ios-build-catalyst` 完美通過那個問題卻毫無用處——它只會紅，永遠不可能綠，於是每個 iOS cutover 被擋、唯一出路是繞過流程（IMP-0039）。一道 block gate 的健康條件是**兩個方向都可達**。下一步：三道 cheap gate 的證明改成雙向——先在未注入的乾淨樹上斷言綠，再注入壞輸入斷言紅（成本約 +21s，`ui-quality-fast` 是長桿）；expensive 的無法當場證綠，改以「該 gate 在最近 N 次 gate record 內至少綠過一次」代替（`.cache/worktree_gates/*.json` 已有歷史）。 | `feat/gate-provenance-bidirectional`。**原 next step 的前提有誤**：`.cache/worktree_gates/*.json` 沒有歷史、也不可能有——那是 per-worktree 單檔、每次 gate 覆寫、`resolve` 還會刪掉（三條測試釘住）。所以先新增 append-only journal `.cache/worktree_gates/history.jsonl`（`_append_gate_history`，寫 log 絕不可讓呼叫端失敗，同 `ops/lib/ios_run_metrics.sh` 硬規則），再據以判綠。判準取「**有史以來從未綠過**且嘗試 ≥3 次跨 ≥2 個 head」而非「最近沒綠」——後者會對正在修真壞 build 的人誤報。cheap gate 三道改成同一棵樹上先綠後紅的成對證明；`cmd_gate` 另在 block 當下附「never green in N attempts」提示。**過程中發現本檔另有兩個同族假綠**：docs-lint 的紅燈證明餵絕對路徑，被 `docs_lint.sh:272-284` 當 usage error 擋在讀檔之前；review-receipts 的紅燈證明因 `review_audit.sh:21-22` 會 `cd` 回自己的 repo root 而實際在稽核 **KG 自己的歷史**，並已於 main 上轉綠（=該測試現在是紅的）。故新增契約：紅燈必須指定 `red_expect` 原因字串、declared-cheap 集合須與實際執行集合相等、PROOFS 不得有 stale entry（首次執行即抓到 `coverage` 其實是 warn gate）。 |
| IMP-0045 | 2026-08-03 | IMP-0039 收尾觀察 | tool | med | fixed | `worktree_orchestrate.py gate` 跑哪套規則**取決於從哪個 cwd 呼叫**：從主 repo 呼叫用 main 版工具（本次排 8 道），從工作樹呼叫用分支版（排 11 道，多 `review-receipts`/`ui-quality-fast`/`coverage`）。兩者輸出格式完全相同，**沒有任何欄位標示這份判決出自哪一版 orchestrator**，實測會讓人把舊規則的全綠誤讀為新規則的全綠。下一步：`kg.worktree.gate.v1` 加 orchestrator provenance（工具檔的 commit/sha256 + 解析到的路徑），並在文字報告首行印出來。 | `feat/gate-provenance-bidirectional`。**原條目低估了嚴重度**：這不只是「看不出來」，是**繞過**——`cmd_cutover` 只核對 `head_sha` 與 `verdict`，所以「主 repo 跑 gate（舊規則全綠）→ 工作樹跑 cutover（新規則）」會被接受。根因是 `_run_gate` 以 worktree 為 cwd 執行 gate **工具**，但 `plan_gates`（路由）來自呼叫端 cwd 解析到的那份 `.py`。修法：`ops/lib/provenance.py`（tool-identity hashing 的 SoT）+ `_orchestrator_identity` 寫進 `kg.worktree.gate.v1`、文字報告首行與 `--receipt-line` 皆印 `<sha8> (<source>)`；`gate`/`cutover` 在 worktree 副本 sha256 與執行中這份**不同**時 refuse，位元組相同則放行（閘門只在分支真的改了本工具時觸發）；cutover 另核對紀錄的 orchestrator 是否等於 worktree 現行副本。review 抓到「存在但讀不到」原本與「不存在」渲染完全相同、會讓 guard 放行，已改為第三態 fail-closed。skill/SoT 同步改用 `<worktree>/ops/worktree_orchestrate.py` 形式，讓 refuse 落在正道之外。 |
| IMP-0043 | 2026-08-03 | IMP-0039 的 review | tool | low | open | 同一堵簽章牆在 test route 上還在：`ops/ios_test.sh` 記載可用 `--destination` 跑 Mac Catalyst，但沒有對應的不簽章規則，下一個跑 Catalyst 測試的 agent 會撞到一模一樣的 exit 65。目前無 gate 走這條，故列 low。下一步：`ios_test.sh` 偵測 Catalyst destination 時比照 `ios_build.sh` 帶 `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO`，或明確擋下並指向 IMP-0039 的說明。 | — |
| IMP-0047 | 2026-08-03 | IMP-0044 實作中撞到 | tool | med | fixed | `_run_gate` 對**路由到但不存在的工具**是 raise 而非回報：`worktree_orchestrate.py:836` 直接 `run_streamed_command`，缺檔時 `FileNotFoundError` 打穿整個 `gate`，吐 traceback 而不是一道 block 判決。這正是 IMP-0045 那類「router 與被路由工具不同代」的下游症狀（分支刪了某腳本、主 repo 的 router 仍指向它）——IMP-0045 的 guard 擋掉了主因，但工具消失的失敗模式仍應是**可讀的 block**，不是 crash。實測：合成 fixture repo 無 `ops/` 樹時，`docs-lint` gate 讓整個 `cmd_gate` 拋例外（測試裡靠植入 stub 繞開）。下一步：`_run_gate` 捕捉 `OSError`，回 `{"status": "block", "rc": 127, "summary": "gate tool not found: <cmd[0]>"}`，並補一條測試。 | `a8e6a18a6`+`a366b89a3`(review 修正)。`_run_gate` 捕捉 `OSError` 回可讀 block(rc 127)而非讓例外打穿整個 `cmd_gate`——後者會連帶丟掉**其他每一道 gate 的結果**。review 抓到兩個真問題:①原斷言 `"<cmd>" in summary` 被**例外自己的 repr** 滿足,把整段 prefix 刪掉全套仍 129 passed → 改 `startswith`,並補「cwd 缺失」案例(該情況下 OS 指的是缺的**目錄**不是命令,舊訊息會指錯對象);②rc 127 的 spawn 失敗會寫進 journal 當 block attempt,而 `_never_green` 只讀 level/status 不讀 rc → 三次缺檔就讓**下一次真正的紅**繼承「這道 gate 從沒綠過」的錯誤假設。修法是 row 加 `executed: false` 並在 `_never_green` 跳過(rc 扛不了這語意:真的跑完再 exit 127 是真紅)。另依 `exc.errno` 分流:ENOENT/EACCES/ENOTDIR 才說「tool not runnable」,EMFILE/ENOMEM 這類改說「could not be spawned (errno N)」不對工具下判斷。 **第二輪 review 再抓到同族第三例**:修①時寫的新斷言 `"backend" in summary` 被程式自己回顯的 `cwd=` 段滿足,「把缺的目錄講成命令」與「整段診斷刪掉」兩個 mutant 皆存活 143 tests → 改為只斷言破折號後那段（唯一來自例外的部分）並加反向斷言（cwd 案例的診斷不得出現命令名），修於 `4b88c563e`。**三輪都是同一個形狀:`in` 斷言碰到的是輸出裡的某個代理物,不是被測的那條路徑。** |
| IMP-0048 | 2026-08-03 | IMP-0044 的 review 面 | tool | med | open | `ops/tests/test_ui_quality_gate.sh:177-203` 用 `mv` 把**已版控**的 `ops/injection_baseline.txt` 搬走再還原，只靠 `trap restore EXIT` 復原。程序被 SIGKILL 或機器掉電落在中間 → 工作樹少一個已版控檔案，而測試本身不會說。同檔其他測試已改用 `KG_INJECTION_BASELINE` 環境變數重導（`ops/tests/test_lint_baselines.sh:51-55` 是範例）。下一步：讓 `ui_quality_gate.sh` 尊重既有的 baseline 環境變數覆寫，測試改用環境變數，`mv` 整段刪掉。 **2026-08-04 實際發生(嚴重度 low→med)**:本輪一個 repo-root `git add -A` 撞上「baseline 被搬走」的時窗，git 直接把缺席 staged 進 commit `400bd6c5f`,`ops/injection_baseline.txt` 就這樣從分支上消失,由 `9037aca00` 還原(內容與 main 位元組相同)。**原條目假設的觸發是 SIGKILL 落在 mv 與 trap 之間,但一次普通的並發 `git add -A` 就足夠,且兩邊都不會出聲**。 | — |
| IMP-0049 | 2026-08-03 | IMP-0044 實作中撞到 | tool | low | fixed | `ops/review_audit.sh:21-22` 是 `ROOT="${KG_REVIEW_AUDIT_ROOT:-<腳本自己的 repo>}"; cd "$ROOT"`——**呼叫端的 cwd 完全無效**。後果不是理論的：`test_gate_can_fail.sh` 的 review-receipts 紅燈證明用 `( cd "$TMP/repo" && review_audit.sh ... )` 寫成，實際稽核的是 **KG 自己的歷史**，於是它的verdict 取決於 KG 最近幾個 commit 有沒有 trailer，而非 fixture；KG 的 commit 都補上 trailer 後該證明就轉綠（測試變紅）。工具沒錯，但「看起來吃 cwd、其實不吃」是會誘人寫錯的介面。下一步：無 `KG_REVIEW_AUDIT_ROOT` 時改以 cwd 的 git toplevel 為預設（或至少在 `--help` 與 stderr 首行印出實際稽核的 root）。 | `b0375a9d7`。解析序改為 `$KG_REVIEW_AUDIT_ROOT` → 呼叫端 cwd 的 git toplevel → 腳本自己的 repo,且**每次執行把選中的 root 印到 stderr**;解析移到 arg parsing 之後,故 `--help` 不 chdir 也不輸出。cutover gate 不受影響(cwd=worktree 且跑的是 worktree 自己的副本,兩種規則同解)。review 抓到綠向斷言 `exit 0` 是空的(指錯 repo 時 `main..HEAD` 通常為空,稽核零個 commit 也 exit 0)→ 改綁 `.summary.total==1`;並補上游一個沒包 `set +e` 的呼叫——它會在 `set -euo pipefail` 下讓整個檔案中止,底下所有 section 靜默不執行。 |
| IMP-0050 | 2026-08-03 | IMP-0044 實作中撞到 | tool | med | open | `ops/ui_quality_gate.py:352` 的 `planned` 是**狀態計數**（仍停在 `planned` 狀態、沒被執行的機制數），不是「計畫了幾道」。所以健康的一次 `--all-mechanisms` 執行印的是 `summary: planned=0 passed=5 failed=0` —— 而 **`planned=0` 正是 CI 空轉兩個月的招牌字串**（那次是五個計數**全部**為 0）。同一個 token 同時代表「一切都跑完了」與「什麼都沒選到」，欄位名長得像 plan size。本輪寫斷言時我自己就讀錯了一次，寫成 `passed == planned` 而在真實輸出上炸掉。下一步：`planned` 改名為 `unrun`（或 `deferred`），另加真正的 `selected` = `len(impacted)`，讓「什麼都沒選到」有專屬且不可能與健康態混淆的表示；`--json` 的 `summary` 同步。 | — |
| IMP-0051 | 2026-08-04 | 本輪 gate 的 `coverage` 自己指出來的 | tool | med | fixed | `plan_gates` 對 `ops/**/*.sh` **選不出任何 gate**（`ops/tests/test_worktree_orchestrate.py` 明文斷言為 `[]`），所以本輪改寫的 `ops/tests/test_gate_can_fail.sh` 在自己的 cutover gate 上是 `coverage` 的 uncovered 項——**偵測器沒有任何自動觸發點**：它不在 `ops-suite` CI（`test_ops_ci_coverage.sh:35`，理由是需要 UI World 資產），cutover 也不排它。被它防的失效模式撐了兩個月，而它自己的執行節奏是手動，形狀相同。**卡住的地方是自指**：把 `gate-can-fail` 排成 block gate 之後，它枚舉 block gate 時會枚舉到自己，於是必須為自己宣告一組 red/green 證明——這需要想清楚語意（自己的紅向證明是什麼？），做壞比不做糟。下一步：先讓 `ops/**/*.sh` 有路由（至少 `ops/tests/test_*.sh` → 跑該檔），再單獨處理 gate-can-fail 的自指（可能的解：枚舉時排除自身，並改由一條「meta gate 必須被某個 gate 涵蓋」的契約測試守住）。 | `703c03e6e`(路由)+`c0bf310dd`(雙向證明)+`400bd6c5f`(docs)。三層:`ops-shell-syntax`=`bash -n` 全體(唯一涵蓋那 ~1/3 沒有測試的腳本的檢查,且只需 bash)、`ops-shell:<test>`=慣例/alias 解析出的該腳本測試、`ops-shell-untested`=**具名** warn。alias map 由契約測試釘死(target 存在、真的提到該腳本、且非慣例可解),**第一次跑就抓到我自己憑名字推的 `ios_clean_derived_data.sh → test_ios_cache_evict.sh` 是假的**(那支測的是 `lib/ios_cache_evict.sh`)。自指問題的解:gate 名是 per-capability(`ops-shell`)不是 per-file,所以 `test_gate_can_fail.sh` 枚舉時只多兩個名字,各自宣告證明即可;`ops-shell` 沒有固定工具(被路由到的腳本就是命令),故新增第三種證明類 `fixture`——證的是 gate 的契約「跑它、傳遞它的 exit code」,紅向斷言 rc=3 讓「因別的原因紅」無法冒充。**副作用正是 IMP-0051 原本的訴求**:改 `test_gate_can_fail.sh` 現在會觸發跑它自己。仍未涵蓋:repo root 的 `devops.sh`(見 IMP-0052)。 |
| IMP-0052 | 2026-08-04 | IMP-0051 實作中的實測 | tool | high | fixed | `devops.sh:418` 的部署命令在 `a67653fdf`（2026-06-19「retarget devops wrapper from terminated Lightsail to standby」）掉了 `--force-recreate`：`docker compose up -d --build --force-recreate` → `docker compose up -d --build`。`ops/test_devops.sh:141` 正是斷言該旗標存在（理由「so VERSION is re-read」），所以**那條測試自 2026-06-19 起就是紅的、紅了約 6.5 週而無人知**——它既不在 `ops-suite` CI（被 `test_ops_ci_coverage.sh` 以「reaches the production host over ssh」排除，見 IMP-0054），也不被任何 gate 路由（IMP-0051 本體）。本輪的 shell 路由只覆蓋 `ops/**/*.sh`，而 `devops.sh` 在 repo root，故**仍然無 gate**。**未修**：改動生產部署命令屬生產熱路徑，需明確決策——是回補旗標，還是這條 assertion 已被 felix reconciler 取代而該刪。裁定與落地見 resolution 欄。 | `8aeb9e54b`。**裁定**：reconciler 是例行部署 SoT（`docs/sop/release.md`），`devops.sh deploy` 是同一個 checkout（`~/kg-prod/backend`，felix 實查確認）的手動 break-glass 路徑，兩者都必須正確——故回補旗標而非刪斷言。旗標的必要性是可查的機制而非慣例：VERSION 是單檔 bind mount、`routers/system.py:23` 於 import 快取，自報版本只隨行程重啟改變；`up -d --build` 只在 image 或解析後 compose config hash 變了才 recreate。**原本寫的『reconciler 因 BACKEND_TRIGGER_RE 成員全部進 image 所以 by construction 必然 recreate』是錯的**，reviewer 用 `docker compose config --hash='*'` 實測推翻（見 IMP-0056）。同時：repo-root `devops.sh` 已納入 shell gate 路由、納入 `ops-suite` 的 trigger paths、納入 `docs/registry.yml` 的 `runbook.system` 與 `sop.deploy` sources（先前 `docs_impact.py --files devops.sh` 回 0 筆，現在回 2 筆 exact）。 |
| IMP-0053 | 2026-08-04 | 同 IMP-0052 | tool | low | fixed | `ops/test_devops.sh:337,342` 斷言 `caddy-status` → `sudo systemctl status caddy`、`caddyfile` → `cat /etc/caddy/Caddyfile`，但 `ops/devops_kg_safe.sh:172-179` 在遷 felix + Cloudflare Tunnel 後已改成 `pgrep -lf 'cloudflared.*tunnel'` 與「不適用（CF remotely-managed config）」。陳舊的是測試不是工具。與 IMP-0052 同一支測試、同一個成因：沒有執行面。見 resolution。 | `8aeb9e54b`。`caddy-status` 改為前綴比對 cloudflared 探測（保留 fallback 訊息的變動自由）；`caddyfile` 沒有遠端命令可比，契約改成**不得發出任何 remote 命令**，並用 stderr 的 ingress SoT 指標證明 arm 真的跑過——空 stdout 本身是假證據（arm 被刪、subcommand 打錯、腳本提早 exit 都是空的）。修的過程順帶發現`set -o pipefail` 讓這段全部六條捕捉在 arm 被刪時會**殺掉腳本而非報錯**，已一併改走 `typed_all` helper。 |
| IMP-0054 | 2026-08-04 | 同 IMP-0052 | tool | med | fixed | `ops/tests/test_ops_ci_coverage.sh` 的 `EXCLUDED_GROUPS` **理由從未被任何東西驗證**，實測至少 6 條不成立：本機離線實跑 `backup-verify` 3s、`deploy-smoke` 5s、`infra-health` 2s、`reconcile` 18s、`branch-audit` 3s、`review-audit`（全部 rc=0），而它們的排除理由寫的是「reaches the production host over ssh」「needs an authenticated gh token」。理由描述的是**被測工具**而不是**測試**——這些測試用 fake ssh／mocked curl／注入 http_code。「reason required」讓排除看起來被稽核過，但沒有任何東西查過理由本身是否為真，於是 CI 白白少跑 6 個 group（IMP-0052 的紅就藏在其中一個裡）。下一步：逐條確認 linux runner 相依後把可離線者移進 `LINUX_GROUPS`；或給理由一個機械檢查（斷網 sandbox 跑一遍，通過＝理由不成立）。 | `330b87fad`(路由)+`03e887978`(證偽 + linux 重現)。`LINUX_GROUPS` 10 → 23（排除 27 → 14，**收回 13 個 group**），每條理由改帶封閉字彙 token（`DEP_TOKENS`）。`bin-*` 可證偽：把 token 指名的命令用 exit 127 的 stub 遮在 PATH 前面重跑該 group，還能過＝理由是假的。**三段自我防護都是被實測逼出來的**：① 證偽器原本分不出「shim 擋住了」與「這台機器根本沒裝」——把 /usr/bin/ssh 移走後，遮蔽完全失效的版本仍回 8 passed / 0 failed，故補 undenied baseline；② 也分不出「因為被 deny 而死」與「本來就紅」，少了這腿一個壞掉的 group 會被永久認證成合法排除、永不進 CI（IMP-0052 原封搬到上一層），故每個 group 先跑一次未遮蔽必須綠；③ 我寫的排除理由裡用反引號引述那條有問題的 grep 當例證，bash 在雙引號陣列元素裡做命令替換、真的執行、吃到 SIGPIPE，整支 rc=141 一個字都沒印——**理由描述的 bug 在描述它的那一行上重演**。為此加的「表是資料」檢查**第一版是錯的**：它查陣列元素的**值**，但賦值當下 bash 早就把反引號換成輸出了，所以對危險寫法永遠不會紅、只對單引號（本來就無害）紅——極性反的。reviewer 用注入實測打掉（注入的命令跑了、檢查照樣印 ✓、總結 13 passed 0 failed），改成查**原始碼文字**（awk 取陣列 literal 範圍）並附「解析不到就自曝」。**驗證方式從讀理由改成實跑**：新增 `ops/ci_linux_repro.sh` 在 runner 等價容器裡重跑 workflow 兩個 step，23/23 全綠、STEP1/STEP2 皆 rc=0。過程中量到兩個會騙人的儀器：bare `ubuntu:24.04` 少了 jq/sqlite3/python3 造成 5 個假紅、以 root 跑讓 chmod 000 那兩條測試假紅，而一次 root 跑留下的 `backend/.venv` 又讓後續非 root 跑多冒兩個假紅（`devops` 在 rc=2 前印的是 `passed: 100 failed: 0`）。因此 workflow 改成**明列** apt 依賴（ripgrep jq sqlite3 python3 openssh-client），不再賴 ubuntu-latest 碰巧預裝——runner image 的工具清單不是契約。先收進去又被打回的 3 個 group 見 IMP-0058；剩餘結構性限制見 IMP-0059。另補兩條同族契約：workflow 的 `paths:` 必須涵蓋每一支 tracked repo-root `.sh`（否則 cutover 路由得到、CI 看不到＝IMP-0052 的形狀），以及沒有兩支可路由 shell 腳本共用 basename、也不得有腳本解析到`OPS_SHELL_UNROUTABLE_TESTS` 宣告不可路由的測試（`_ops_shell_test_candidates` 只認 basename，未來一支 root `release.sh` 會被 `ops/test_release.sh` 假涵蓋，一支 `ops.sh` 會把聚合 runner 武裝成 block gate）。 **第四與第五個盲點是上線後才現形的，記在這裡而不是新開一條，因為它們是同一個器械的同一種病**：④ baseline 腿的前提寫成「任一個指名命令存在」，而 GitHub runner **有 swift、沒有 xcodebuild**，於是它跑了一次注定失敗的 baseline，然後指控 ios-ops / lldb-forensics「已經紅了，先去修」——兩個其實健康的 group（CI run 30902917711，`11 passed, 2 failed`）。**半套工具鏈比完全沒有更會騙人**：全都沒有時它至少誠實宣告跳過。前提改成整組齊全。⑤ 「在 PATH 上」不等於「能用」：只裝 CLT 的 macOS（felix）四個名字全都 resolve，`xcodebuild -version` 卻報 CLT-instance 錯，所以 ④ 的修法在那台會犯一模一樣的錯。`DEP_TOKENS` 因此加第三欄功能探針，兩種機器形狀都覆蓋（各自用容器與 PATH shim 實測）。同時修掉 review 找到的兩個「一次表格編輯就會踩到」的洞（`bin-*` 配空 denial set 會指控健康 group 理由造假；`falsify_will_run` 與 skip 條件各寫一份會漂），並給收尾行加 skip 計數器——skip 記成 pass，所以「整段跳過」與「整段真的驗過」原本印一樣的字，而被繳械的跑分數還比抓到問題的跑高。**由此得到的誠實結論**：linux 沒有 Xcode → CI 上兩條 bin-* 排除都跳過 → **CI 實際證偽的排除數是 0**，唯一真的執行點是開發者本機 macOS。修復 commit `9bb518e8d`。 |
| IMP-0055 | 2026-08-04 | IMP-0051 的 review | tool | med | open | `OPS_SHELL_TEST_ALIASES` 的契約測試只驗「target 提到該腳本」，那是**必要非充分**條件，且已經漏掉真案例：`ops/release_changelog.sh → ops/test_release.sh` 通過全部三項斷言（存在、提到、非慣例可解），但 `test_release.sh` 對它只有 `[[ -f ]]` 存在檢查與**對 `release.sh` 的 grep**，從未執行它——把 `release_changelog.sh` 整個換成 `exit 0` 會通過一道 **block** gate。更糟的是 `ops_sh` 整組被加進 `covered`，所以假 alias 會把一個檔案同時擠出 `uncovered` **和** `ops-shell-untested`，把路由洞換成打勾。九筆手寫 alias 裡兩筆是假的（另一筆是 `ios_clean_derived_data.sh → test_ios_cache_evict.sh`），**憑名字推斷的錯誤率 2/9**。已移除該筆並在程式碼註記此檢查非充分。下一步：加一支獨立的 alias 覆蓋驗證（把腳本 body 換成 `exit 0` 或注入哨兵，要求 target 測試轉紅；不成立即 fail），不要塞進 `ops-pytest`（部分 target 要 60-80s）。慣例解析出的那一大半目前**完全沒有**這種檢查，同一個問題。 | — |
| IMP-0056 | 2026-08-04 | IMP-0052 的 review | tool | high | fixed | `ops/kg_reconcile.sh` 的 `BACKEND_TRIGGER_RE` 假設「命中 = 會改 image」，**不成立**，而後果不是靜默分岔是**自傷式假失敗**。反例（reviewer 實測）：`backend/docker-compose.yml` 命中觸發、但 compose 的 recreate 判準是解析後 service config 的 hash，**註解與 obsolete key 不進 hash**——`docker compose config --hash='*'` 在「加一行註解」與「刪掉第 1 行 obsolete 的 `version: '3.8'`」兩種編輯下都輸出同一個 hash。而該檔既不進 image（Dockerfile 無 `COPY . .`、repo 無 `.dockerignore`），於是 `up -d --build` **不 recreate**。接著 `kg_reconcile.sh:233` 已寫 `backend/VERSION`=new → 容器沒重啟仍自報 old → `localhost_health_ok "$new_sha"`（`:140-155`）比對版本不符 → 5×3s 全敗 → `HEALTH_FAIL_REASON=health` → **ROLLBACK + write_poison + ALERT**。也就是「改一行 compose 註解」會換來一次生產回滾與 poison 冷卻。同成因（機制清楚、未實測）：`backend/Dockerfile` 純註解改動、`backend/pyproject.toml` 只改 `[tool.*]`。另外正則多數成員沒有尾錨，`backend/Dockerfile.dev`、`backend/docker-compose.yml.bak`、`backend/pyproject.toml.example` 會誤觸發且必定不進 image。**未修**：`kg_reconcile.sh` 是唯一自動碰生產的路徑，改它要明確決策。下一步（三選一，需定論）：① deploy 路徑也加 `--force-recreate`（代價同 IMP-0052，且每次 backend 部署都吃）；② 收緊正則到真正進 image 的成員並加尾錨；③ 健康 gate 的版本比對改成「容器自報值 == 它實際跑的 image」而非比對 VERSION 游標。rollback 路徑（`:265`）不受影響——它把 git tree 與 VERSION 一起還原，「沒 recreate」剛好等價於「容器從頭到尾沒被動過」，狀態自洽，**不該**在那裡加 force-recreate。**後果不是一次假失敗而是每小時自我重複的告警迴圈**：`deploy_and_gate` 失敗後 `git reset --hard "$rollback_sha"` 把 prod clone 退回、`write_poison` 只 poison 3600 秒；cooldown 一過下一 tick 又看到 origin/prod 領先 → 又部署 → 又因版本不符失敗 → 又回滾又告警，直到有人推一個真的會動 image 的 commit 為止。**引爆條件是 range 層級不是 commit 層級**：`kg_reconcile.sh:346` 判斷用的是 `git diff --name-only "${DEPLOYED_SHA}..origin/prod"`，所以真正會炸的是「某次 deploy 的整段 range 裡，命中 `BACKEND_TRIGGER_RE` 的成員**全部**不進 image」。**本 branch 自己就有一個實例**：它改了 `backend/docker-compose.yml` 的註解（命中觸發、不進 image、config hash 不變），只因為同一 range 裡也有 `backend/src/kg/sentry_init.py` 的註解改動（進 image → image ID 變 → 必然 recreate）才不會觸發這個迴圈——兩者刻意放進同一個 commit，讓這份保護從偶然變成刻意，也擋掉日後單獨 cherry-pick 到 prod 的路。這種「靠同 range 碰巧有 src 改動」的安全不是安全，但它是個現成的 test case。 | `6e5f11bb1`。**修法是①的變體，但適用範圍比原本寫的更大：deploy 與 rollback 兩處都加 `--force-recreate`。** 選①不選②③的理由：② 收緊正則治不了根（「這個檔會不會進 image」不是路徑可判定的），而且把便宜的失敗換成昂貴的——false positive 有了 force-recreate 只值一次重啟，false negative 是生產靜默腐爛；reviewer 另外實測出正則的極性是反的（`backend/docker-compose.override.yml` 真的會改變解析後 config **卻不命中**，而物理上不可能進 image 的 `.bak` 反而命中），所以那個洞已經在那，不是收緊就能補。③ 不可實作：容器自報的版本根本不是 image 的屬性——`backend/VERSION` 是 bind mount 的**宿主檔**、於 import 快取，所以它是「上次行程啟動時的部署游標」而非「我正在跑的碼」。要讓版本相等真的證明「跑的是新碼」，得把 sha 在 build 時烤進 image（另記）。**原條目寫的『rollback 路徑不該加 force-recreate』在 deploy 路徑加了之後就過期**，而且不只是失效是**反向**：reviewer 用兩-tick 探針證出回滾不 recreate 時，下一 tick 的 crash-consistency 交叉驗證會看到 live(new) != VERSION(old) 且 new 仍可解析 → **把游標寫回剛被回滾且被 poison 的那個 sha** → changed 變空 → noop 早退、poison 檢查走不到；deploy.log 記著 ROLLED_BACK、state 有 poison，游標卻宣告已收斂在該 sha 上，回滾在游標層被靜默撤銷。**代價實測**（felix 唯讀）：app 啟動 1.2s、gate 首次成功探針 +3.4s，而 gate 預算最壞 62s（15-20x headroom）；compose project `backend` 只有單一 service；全部是 bind mount、**零 named/anonymous volume**，故未帶 `-V` 的 force-recreate 不可能刪資料。新增停機窗約 2-4s，且只落在原本會炸的那條路上。唯一新成本在 build 失敗分支：deploy 根本沒 recreate 過，回滾仍無條件重建一顆好好的容器（多 2-4s）；接受，因為改成有條件就是重新引入「我能預測 compose 會不會 recreate」這個假設，而本條目整件事就是它被證偽。**測試替身才是這個 bug 藏得住的原因**：compose stub 原本無條件模擬 recreate，比 docker 聽話，等於把待測的失效模式從世界上刪掉——reviewer 實測「舊 stub + 拿掉修法」仍 83/0，證明承重的是 stub 的忠實度而非新斷言。已補：recreate 另記計數檔（不改變版本的 recreate 在「版本變了」的觀測下隱形）、build 失敗分支（原本零覆蓋）、雙壞告警的**正向**測試（原本只有反向斷言，把告警字串改名照樣全綠）。 |
| IMP-0057 | 2026-08-04 | IMP-0052 的 review | tool | med | open | shell gate 路由這輪從 `ops/**` 擴到 repo root，但 `ops/` 與 root 之外還有 **12 支** tracked `.sh` 仍然完全無路由，其中 `backend/view_logs.sh` **已經被 `ops/test_devops.sh:308-318` 測著**——測試檔改動會觸發 gate，被測的那支不會。跟 IMP-0052 一模一樣的形狀，只差一個目錄。**設計已定，別重新辯**：普查過 12 支的 shebang（`.claude/skills/app-debug/find-polluter.sh`、`backend/restart_kg.sh`、`backend/view_logs.sh`、`frozen/2026-06-14-web-chrome-parity/` 7 支、`lab/podcast/start.sh`、`scripts/ios_token_lint.sh`）**全部是 bash，零個非 bash**，所以「`frozen/`/`lab/` 可能不是 bash 會被 `bash -n` 誤判紅」是假想風險、不成立。但「全部 `*.sh`」也不對，理由與 shebang 無關：`frozen/` 是刻意冷凍的，掛上 gate 會讓 `ops-shell-untested` advisory 永遠列著 7 個沒人打算補測試的檔，而 advisory 一旦長期有雜訊就沒人看，比沒有 advisory 更糟。正解是第三條路：**全部 `*.sh` 減去一份具名排除清單（`frozen/`）**，語法地板改成看 shebang 決定直譯器、認不出來就跳過並具名——即 `worktree_orchestrate.py:489-491` 自己寫的「an enumerated hole beats an anonymous one」。不在本 PR 做：與「生產部署命令沒有 gate」是兩個關注點（鐵律 4 偏好小單位），且該改動要自帶測試，形狀比照 `ops/tests/test_worktree_orchestrate.py` 既有的 root-routing 兩條。 | — |
| IMP-0058 | 2026-08-04 | IMP-0054 的 review（ubuntu 容器實跑） | tool | med | open | 兩支 ops 測試綁死 BSD userland，在 linux 必紅——**是測試不可攜，不是工具**。① `ops/tests/test_ios_cache_evict.sh:35,117` 用 `date -v -${h}H`、`:79` 用 `stat -f '%m'`；GNU 版兩者皆錯（`date: invalid option -- 'v'` 實測出現 44 次，`touch` 收到空日期使全部 mtime 塌成同一瞬間，被測的 LRU 排序整個失效），ubuntu 實測 10 passed / 10 failed。② `ops/test_ios_test_discovery.sh:312` 的 `grep -rhoE ... \| head -1`：GNU grep 收到 SIGPIPE 就死，`set -o pipefail` 把 141 傳出去殺掉整支；ubuntu 5/5 rc=141、macOS 5/5 rc=0。**②在 macOS 也只是還沒炸**——同檔 `:272` 形狀相同卻活著，差別只在上游輸出 1852 bytes 一次寫完就退場，而 `:312` 是 6816 bytes 且要走完目錄樹。修法 repo 內已有現成範式：`ops/release.sh:264` 的 `stat -f '%Lp' \|\| stat -c '%a'`、`ops/podcast_upload.sh:323` 同形；SIGPIPE 那條把 `head -1` 換成 `awk 'NR==1{print;exit}'` 或在 grep 後補 `\|\| true`。修好即可把兩個 group 移進 `LINUX_GROUPS`（目前以 `bsd-userland` token 具名排除）。 | — |
| IMP-0059 | 2026-08-04 | IMP-0054 的 review | tool | med | open | 證偽器有兩個**結構性**限制，已在程式碼註解裡明講而非假裝有守，但都該補。① **絕對路徑穿透**：PATH 遮蔽擋不住 `/usr/bin/log` 這類寫法（`ops/lib/ios_ops_logs.sh:67,90`、`ops/ios_device_logs.sh:88` 就是），原本有一條 grep 守衛但它掃的是 `ops/test_ops.sh`——那只是 dispatcher，對任何 binary 都零命中，是一條寫著卻永遠不會響的檢查，已刪。要補得從 `test_ops.sh` 的 case arm 解出 group 的真實命令鏈再掃。② **逃生口管的是 token 集合不是指派**：11 條排除裡 9 條走不可證偽 token，把 `bin-xcode` 改成 `data-indexstore` 是一個單字 diff，證偽器立刻不再碰它且沒有任何檢查會出聲；而指派**確實會錯**——`review-probe` 這輪就被我指派成可進 CI，實際上它相依的正是本表已經用來排除 `ui-quality-gate` 的那份 UI World 資產。制度化的解法是把「產生這條 PR 的那個實驗」變成 CI step：以 **expected-fail** 跑 `EXCLUDED_GROUPS`，某條排除竟然在 ubuntu 上**通過** → gate 紅、訊息「這條排除是假的，收進 LINUX_GROUPS」。對 `data-*` / `net-external` 一樣有效、不需要 PATH 把戲，成本低（linux 上 ios-ops / lldb-forensics 各約 1 秒就死）。**但它自己有 C3 同型的弱點**：group 因別的原因失敗一樣會「確認」排除，所以訊息要寫成假設不是判決。 | — |
| IMP-0063 | 2026-08-05 | 執行長裁示：維護成本超出可負擔 | arch | high | fixed | **Catalog / UI World 一致性平面吃掉的維護量與它的產出不成比例。** 量測（過去 90 天）：2381 個 commit 中 175 個（7.4%）落在本平面,其中 `ops/demo` 投影鏈 77 個——而投影鏈的活消費者只有行銷截圖與 App Store 送審,一年跑個位數次。根因不是「catalog 複雜」,是**兩個必須永遠一致的 world**（`ops/fixtures/ui_worlds/marketing_demo.json` 手維護 + `ops/demo/generated/ios_fixture_dataset.json` 投影產出）被 6 處 `==` 釘死成 `XxxFixtureID.allCases`,於是加一個 fixture 要動七面。**兩項先前的口頭判斷經實測推翻,記於此以免重蹈**：① repo world 一直是單向 subset（`RepoFixtureDatasetsContractTests.expectKnownKeys`）,不是雙向——真正的雙向鎖只有 5 個 `*FixturesTests.repoAndGeneratedDatasetsDeclareEvery*` 與 `generatedDemoDatasetDecodes` 的 5 行；② catalog 對 `Views/` 的**真連坐率僅 2.6%**（76 個 View commit 只有 2 個被迫改 `Debug/`,且都是全專案改名）,所以 **catalog 不移除**——它本身不是負擔,一直餵它才是。 | `feat/freeze-ui-world`（squash 後由 platform-steward 更新為 squashed hash）。**squash 後另須一併把 `catalog_scope.md` / `product_surface.md` / `tech_index.md` / `ops_state_plane.md` 四份的 `verified_against` re-bump 到該 squashed hash**——目前四個 anchor 都指在 base `0c9e3b7c`，那個 commit 不含本次的 `isSubset` 改動，而 §FROZEN 的「本次實際解除了什麼」描述的正是那段 code。**做了什麼**：6 處雙向 parity `==` → 單向 `isSubset`；`ops/demo/*` 投影鏈加 FROZEN 標頭；`docs/reference/catalog_scope.md` 加 §FROZEN（凍結紅線 + 復業條件,為本決策正本）；product_surface / tech_index / ops_state_plane / sop.ios / sop.ui_flow_evidence 加指標；`ops/ui_quality_plane.yml` 的 `snapshot.catalog{,_coverage}` 加 frozen notes。**刻意沒做的三件事,各有實測理由**：① **一個測試都沒 skip**——計劃原訂 skip `test_demo_ios_spec_emitter.py`(1008行) 等,實測 **88 passed / 1 skipped / 3.7s**(sandbox route,被 skip 的正是最重的 backend e2e;full-fat route `backend/ uv run pytest` 為 **80 passed / 18s**),且全掃過零時鐘 / 零日期 / 零隨機依賴,凍結後不需維護就一直綠,skip 掉是用零節省換走保護；`emit-ios --check` 的 byte drift gate 在凍結期角色反轉成**守衛**（誰偷改凍結的投影鏈它會紅）。② **validator `ops/ui_world_manifest.py` 一行沒動**——它是唯一擋 invalid world 進 simulator 的護欄。**但「零成本」的說法要修正**：它的 `FIXTURE_DOMAIN_IDS` / `PREFERENCE_DOMAIN_KEYS` 是硬編碼的 Python 鏡像,原本被 `test_ui_world_manifest.py` 以**雙向 `==`** 鎖到即時解析的 Swift enum(12+2 個 param),那是第 7 面同類的跨語言稅。本次把 fixture-id 那 12 條放寬成 `FIXTURE_DOMAIN_IDS ⊆ Swift enum`(保留健全性方向:Python 絕不可認得 Swift 不認得的 id;拿掉的反方向只讓 validator 更保守地拒絕凍結期不該新增的 id)。preference 那 2 條**刻意未放寬**——新增可 seed 的 preference key 本身就是 UI World 擴張。所以正確說法是**零維護成本,但 validator 的 allowlist 仍會擋新 fixture key**(exit 64),那是預期行為。③ **沒動 `CatalogCoverageTests`的 76 條禁字與 registry↔硬編碼清單比對**——那些是本地的、一處的、編譯期或單檔可見的,不是跨七面的稅。 |
| IMP-0064 | 2026-08-05 | IMP-0063 凍結驗收時的差分量測 | tool | high | open | **`ios_ops.sh catalog snapshots` 在 main 上已經是壞的——「看畫面」目前半殘,而這正是 IMP-0063 決定要保住的東西。** 症狀:`exitCode=65`,app 端 `VocabularyListViewScenarios.swift:241` 的 `preconditionFailure`(「UI World vocabulary.<id> must declare N visible vocabulary entries, got M」)。後果分兩層:245 張 surface PNG 仍被 salvage 機制撈回(所以「看單張圖」還能用),但 `catalog_index.json` 沒產出 → `UIreview.html` gallery 渲染失敗(`review-render-failed`),且 `catalog_appearance.json` proofCount=0 → `catalog-appearance-proof` error=`appearance.missing`。**歸因已用差分證明,不是推論**:在乾淨的 `main`(`0c9e3b7c8`, 工作區乾淨)跑同一條指令得到**完全相同**的 `pngCount=245` + `exitCode=65` + `appearance.missing`;而 `feat/freeze-ui-world` 對 `ios/BooksAndVocab/`(app target,含 `Debug/` 與 `ops/fixtures/ui_worlds/`)是**逐位元零 diff**,該次執行又是 `-only-testing:BooksAndVocabTests/CatalogSnapshotTests`,本分支改動的測試型別根本沒被執行。**與凍結無關,凍結也沒讓它變糟**——但凍結的前提之一是「catalog 還能看畫面」,所以這條該修。下一步:讀 `VocabularyListViewScenarios.swift:230-245` 的 `VocabularyListExpectedShape` 期望值,對照 `marketing_demo.json` 的 `vocabulary.vocabList*` seed 實際可見筆數(`shouldAppearInKnowledgeList` 過濾後),判斷是 seed 漂移還是期望值寫死過時。**注意這條與凍結紅線的交互**:修法若需要改 world,受 §FROZEN 紅線 2 約束(validator 會擋新 fixture key);但「改既有 seed 的 entry 內容以修復既有 bug」不算擴張,不受紅線限制。 | — |
