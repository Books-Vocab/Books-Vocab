import SwiftUI

/// Tag / chip 內距與圓角 — shell 層共用（AppTag、TodayReview 字數徽章）。
enum AppTagMetrics {
    static let horizontalPadding: CGFloat = 10
    static let verticalPadding: CGFloat = 5
    static let cornerRadius: CGFloat = 6
}
