{
  "acceptance": "bash -c 'set -e; KG_INJECTION_BASELINE=/tmp/kg-nonexistent-baseline.txt ./ops/ui_quality_gate.sh --files ios/BooksAndVocab/Views/Vocabulary/Components/VocabCalendarGrid.swift --tier fast --dry-run --json | jq -e \".results[] | select(.id==\\\"static.injection\\\") | select(.warning != null)\" >/dev/null; grep -q KG_INJECTION_BASELINE ops/tests/test_ui_quality_gate.sh; ! grep -qE \"^[[:space:]]*mv \" ops/tests/test_ui_quality_gate.sh; echo ALL-GREEN'   # 今天 exit 4",
  "category": "tool",
  "cost": "S",
  "date": "2026-08-03",
  "detail": "`ops/tests/test_ui_quality_gate.sh:177-203` 用 `mv` 把**已版控**的 `ops/injection_baseline.txt` 搬走再還原，只靠 `trap restore EXIT` 復原。程序被 SIGKILL 或機器掉電落在中間 → 工作樹少一個已版控檔案，而測試本身不會說。同檔其他測試已改用 `KG_INJECTION_BASELINE` 環境變數重導（`ops/tests/test_lint_baselines.sh:51-55` 是範例）。下一步：讓 `ui_quality_gate.sh` 尊重既有的 baseline 環境變數覆寫，測試改用環境變數，`mv` 整段刪掉。 **2026-08-04 實際發生(嚴重度 low→med)**:本輪一個 repo-root `git add -A` 撞上「baseline 被搬走」的時窗，git 直接把缺席 staged 進 commit `400bd6c5f`,`ops/injection_baseline.txt` 就這樣從分支上消失,由 `9037aca00` 還原(內容與 main 位元組相同)。**原條目假設的觸發是 SIGKILL 落在 mv 與 trap 之間,但一次普通的並發 `git add -A` 就足夠,且兩邊都不會出聲**。 **2026-08-05 補:修法有嚴格順序,原「下一步」漏了前置。** 測試之所以只能 `mv`,是因為 `ops/ui_quality_gate.py:147-153 injection_args()` **硬編** baseline 路徑、只用 `.exists()` 決定 `--baseline-check` vs `--report`,完全不看 `KG_INJECTION_BASELINE`——環境變數騙不過它。環境變數本身在 lint 層已存在(`ops/injection_lint.py:31`,並由 `ops/tests/test_injection_lint.sh:42-58` 驗著)。事故複核:`git show --stat 400bd6c5f` 確為 `ops/injection_baseline.txt | 44 -----` 純刪除(12:38,夾在一個 docs commit 裡),`9037aca00`(12:43)還原且 commit message 自述成因是 repo-root `git add -A` 撞上 baseline 被搬走的時窗。",
  "fix_site": "ops/ui_quality_gate.py:145",
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom-all@v2",
  "id": "IMP-0048",
  "plan": "1)【先改 gate,順序不可反】`ops/ui_quality_gate.py:144-151` 的 `injection_args(root)`,今天實際內容為:\n```\n144 def injection_args(root: Path) -> tuple[list[str], str | None]:\n145     baseline = root / \"ops\" / \"injection_baseline.txt\"\n146     if baseline.exists():\n147         return [\"--baseline-check\"], None\n148     return [\"--report\"], (\n149         \"ops/injection_baseline.txt missing; running --report only. \"\n150         \"Run `ops/injection_lint.sh --baseline` to establish a baseline.\"\n151     )\n```\n:145 的硬編路徑就是缺陷本體——因為環境變數騙不過它,測試才只能把已版控的 `ops/injection_baseline.txt` 用 `mv` 搬走。改成尊重 lint 層早已存在的覆寫（`ops/injection_lint.py:31` 今天是 `BASELINE_FILE = Path(os.environ.get(\"KG_INJECTION_BASELINE\", \"ops/injection_baseline.txt\"))`）,整個函式改寫為:\n```python\ndef injection_args(root: Path) -> tuple[list[str], str | None]:\n    override = os.environ.get(\"KG_INJECTION_BASELINE\")\n    if override:\n        candidate = Path(override)\n        baseline = candidate if candidate.is_absolute() else root / candidate\n    else:\n        baseline = root / \"ops\" / \"injection_baseline.txt\"\n    if baseline.exists():\n        return [\"--baseline-check\"], None\n    return [\"--report\"], (\n        f\"{baseline} missing; running --report only. \"\n        \"Run `ops/injection_lint.sh --baseline` to establish a baseline.\"\n    )\n```\n兩個 return 分支的語意不動,只把解析後的路徑放進 warning,讓訊息指名它真正找的那個檔。`os` 已於 `ops/ui_quality_gate.py:24`（`import os`）匯入,不需新增 import。\n\n2) 不需要任何轉發程式碼,但要在 `run_mech` 上方留一行註解說明為何不需要。`run_mech` 今天在 `ops/ui_quality_gate.py:206-214`,第 208-213 行是 `proc = subprocess.run(cmd, cwd=root, capture_output=True, text=True,)`——**沒有 `env=` 參數**,子進程直接繼承 `KG_INJECTION_BASELINE`;而 `ops/injection_lint.sh` 在 `exec \"$UV_BIN\" run ...` 前有 `ROOT_DIR=\"$(cd \"$(dirname \"$0\")/..\" && pwd)\"` + `cd \"$ROOT_DIR\"`,所以相對路徑的覆寫在父子兩邊解析結果一致。**不要**改成顯式 `env={...}` dict,那會弄丟 `UV_BIN`/`PATH`,`injection_lint.sh` 會找不到 uv。\n\n3)【再改測試】`ops/tests/test_ui_quality_gate.sh:261-287`,section \"Missing injection baseline is handled gracefully\"。今天該段是 :262 註解 `# Move the real baseline aside, run execute, then restore.`、:263 `BASE=\"ops/injection_baseline.txt\"`、:264 `MOVED=\"\"`、:265-268 `if [[ -f \"$BASE\" ]]; then MOVED=\"$(mktemp)\"; mv \"$BASE\" \"$MOVED\"; fi`、:269-273 `restore() { ... mv \"$MOVED\" \"$BASE\" ... }`、:274 `trap restore EXIT`、:287 裸呼叫 `restore`。把 :262-274 與 :287 全部刪掉,保留 :275-286 的執行與兩條斷言,並把 :275 改成:\n```bash\nOUT=\"$(KG_INJECTION_BASELINE=\"$(mktemp -u)\" $GATE --files \"$SAMPLE_FILE\" --tier fast --execute 2>&1)\"\n```\n（`$GATE` 定義於同檔 :20 = `./ops/ui_quality_gate.sh`,`$SAMPLE_FILE` 於 :28 = `ios/BooksAndVocab/Views/Vocabulary/Components/VocabCalendarGrid.swift`;測試在 :9 已 `cd \"$ROOT\"`。`mktemp -u` 給的是絕對路徑,走步驟 1 的 `is_absolute()` 分支。）在該 section 結尾再補第三條斷言,守住這條 entry 真正在乎的東西:\n```bash\nif [[ -f ops/injection_baseline.txt ]]; then\n  ok \"tracked injection baseline was never moved aside\"\nelse\n  fail_t \"ops/injection_baseline.txt is missing — the test moved a version-controlled file\"\nfi\n```\n（2026-08-04 的事故:`400bd6c5f` 把缺席 staged 進 commit、`9037aca00` 還原,當時沒有任何東西在看這一點。）\n\n4) 既有第二條斷言 :282 `grep -qi 'injection_baseline\\|missing baseline\\|WARN' <<<\"$OUT\"` 在步驟 1 改了 warning 字串（不再含字面 `injection_baseline`）之後仍會綠,不必改:`--execute` 下帶 warning 且 rc=0 的機制 status 是 `warn`（`ops/ui_quality_gate.py:132-133`）,`human_summary` 於 :224 輸出 `[WARN] {command} ({warning})`,命中 `WARN` 這個 alternative。:277 的 rc=0 斷言也仍成立:`main()` 在 :426 只有 `summary[\"failed\"] > 0` 才回 1,`warn` 不計入 failed。\n\n5) 順手修 `ops/tests/test_ui_quality_gate.sh:233-236` 的註解,今天寫著「Cleaned up inline, deliberately not via `trap ... EXIT`: bash keeps one EXIT trap and the next section installs its own to put a *version-controlled* baseline back (see IMP-0048). Registering a second one here would silently replace it and leave the tracked file moved aside.」。步驟 3 之後下一個 section 不再裝 EXIT trap,這段註解會把本 entry 要刪掉的模式再教給下一個編輯者。改成一句「ghost plane 用完直接 `rm -rf`（見 :259）」即可,不得再提 EXIT trap 與版控 baseline。\n\n6) 順序是負載中的,不可倒:先做步驟 3 再做步驟 1,那個 section 會變成同義反覆——環境變數騙不過 gate、真 baseline 照樣回答 `--baseline-check`、測試仍印 pass。務必 1 → 3。\n\n7) 影響面盤點:`injection_args` 只有一個呼叫點 `ops/ui_quality_gate.py:195`（`fallback, warn = injection_args(root)`）。`ops/ui_quality_plane.yml:105-115` 的 `static.injection`（`requires: - injection-baseline`）不動;`HANDLED_RESOURCES = {\"ui-world\", \"injection-baseline\"}`（`ops/ui_quality_gate.py:59`）不動,動它會撞 import-time 的 drift 檢查。`ops/tests/test_lint_baselines.sh:51-55`（`\"injection_lint.sh|ops/injection_baseline.txt|KG_INJECTION_BASELINE\"`）與 `ops/tests/test_injection_lint.sh:42-58` 只測 lint 層,不受本改動影響,須維持綠。未設 `KG_INJECTION_BASELINE` 時 gate 行為位元不變,無其他測試會轉紅。\n\n8) 驗收:acceptance 欄那條 compound 命令,今天 exit 4,三個 clause 都紅（gate 忽略環境變數 / 測試檔沒有 `KG_INJECTION_BASELINE` / 測試檔仍有 `mv` 行）。修完必須 `ALL-GREEN` 且 exit 0。之後另跑 `bash ops/tests/test_ui_quality_gate.sh` 確認全綠（含 `--execute` section,較慢,但步驟 3 之後不再動版控檔,可安全反覆跑）。",
  "resolution": "—(2026-08-05 驗證 CONFIRMED-OPEN + 事故坐實;**順序不可反**:① `ui_quality_gate.py:147` 改讀 `KG_INJECTION_BASELINE` 並透傳給子進程,② 才刪 `test_ui_quality_gate.sh:179-190` 的 `mv`/`trap restore` 改用環境變數;各 S。改法本身即回歸證明)",
  "schema": "kg.backlog.entry.v1",
  "severity": "med",
  "source": "IMP-0044 的 review 面",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
