# Fixture Reader Passage

This tracked fixture keeps the UI World reader and bookshelf states replayable
from every clean worktree. It is deliberately plain Markdown so its bytes,
size, and source path remain stable across machines.

The passage includes a durable context for vocabulary lookup, notebook binding,
and long-title rendering without depending on a generated podcast workspace.
