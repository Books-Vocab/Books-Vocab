{
  "acceptance": "紅：ops/tests/test_shell_alias_coverage.sh 今天不存在，`test -x` 立刻 exit 1。綠：該檔存在且有執行位元；`--self-test` 的正負控（NOT-COVERED / PROVEN / BASELINE-RED 三種 verdict、突變後 target 必須轉紅）全過；`./ops/test_ops.sh alias-coverage` 走 dispatcher 真的跑得到並回 0；ops/tests/test_ops_ci_coverage.sh 的 LINUX_GROUPS 收了這個 group；docs/reference/tech_index.md 收錄了這支測試且 verified_against 已離開今天的錨點 9582044dd 並仍為 HEAD 可達——七項全過才 exit 0。",
  "acceptance_cmd": "bash -c 'set -e; t=ops/tests/test_shell_alias_coverage.sh; test -x \"$t\"; ./ops/tests/test_shell_alias_coverage.sh --self-test; ./ops/test_ops.sh alias-coverage; grep -q \"alias-coverage\" ops/tests/test_ops_ci_coverage.sh; test -f docs/reference/tech_index.md; grep -q \"test_shell_alias_coverage.sh\" docs/reference/tech_index.md; v=$(awk -F\": *\" \"/^verified_against:/{print \\$2; exit}\" docs/reference/tech_index.md); test \"$v\" != \"9582044dd\"; git merge-base --is-ancestor \"$v\" HEAD'",
  "acceptance_expect_rc": 0,
  "category": "tool",
  "cost": "M",
  "date": "2026-08-04",
  "detail": "`OPS_SHELL_TEST_ALIASES` 的契約測試只驗「target 提到該腳本」，那是**必要非充分**條件，且已經漏掉真案例：`ops/release_changelog.sh → ops/test_release.sh` 通過全部三項斷言（存在、提到、非慣例可解），但 `test_release.sh` 對它只有 `[[ -f ]]` 存在檢查與**對 `release.sh` 的 grep**，從未執行它——把 `release_changelog.sh` 整個換成 `exit 0` 會通過一道 **block** gate。更糟的是 `ops_sh` 整組被加進 `covered`，所以假 alias 會把一個檔案同時擠出 `uncovered` **和** `ops-shell-untested`，把路由洞換成打勾。九筆手寫 alias 裡兩筆是假的（另一筆是 `ios_clean_derived_data.sh → test_ios_cache_evict.sh`），**憑名字推斷的錯誤率 2/9**。已移除該筆並在程式碼註記此檢查非充分。下一步：加一支獨立的 alias 覆蓋驗證（把腳本 body 換成 `exit 0` 或注入哨兵，要求 target 測試轉紅；不成立即 fail），不要塞進 `ops-pytest`（部分 target 要 60-80s）。慣例解析出的那一大半目前**完全沒有**這種檢查（`_ops_shell_test_candidates`(`:262-270`) 的慣例分支同樣只查 `exists()`），同一個問題。**2026-08-05 取證**:表已收斂為 **7 筆**(兩筆假 alias 確已移除),`ops/tests/test_worktree_orchestrate.py:430-448` 的 docstring 已寫明 `NECESSARY, NOT SUFFICIENT (IMP-0055)`——**註記補了、機械檢查沒補**;全 repo 無 alias 覆蓋驗證檔。結構複核:`worktree_orchestrate.py:499-509` 找到 hit 就進 `sh_targets`,`:527-528` 把**整個 `ops_sh` bucket 無條件計入 `covered`**,故假 alias 確實同時擠出 `uncovered` 與 advisory。**額外實測(原條目未含)**:現存 7 筆 alias 逐筆查過 target 的實際呼叫,**都至少有一次真執行**(例:`ios_archive.sh` 在 `ops/test_ios_ops.sh:910-912` 有 `bash \"$IOS_ARCHIVE\" list/latest`)→ 當下表是乾淨的,缺的純粹是防回歸的器械。",
  "fix_site": "ops/tests/test_shell_alias_coverage.sh（新建）+ ops/test_ops.sh 的 DEFAULT_TESTS/run_one + ops/tests/test_ops_ci_coverage.sh 的 LINUX_GROUPS + docs/reference/tech_index.md",
  "groomed_at": "2026-08-08",
  "groomed_by": "groom-wave-2026-08-08",
  "id": "IMP-0055",
  "plan": "既有 plan（第 5 版，含 test_shell_alias_coverage.sh 全文與 S1a-S1f/S2-S8 十四步）**維持不變、逐字照抄**，我複核過它的裁決落實（隔離工作樹 + body 換 exit 0 + 要求 target 轉紅）與突變手法（`exit 0` 置於第 2 行、原文包進引號 heredoc 以免掃描原文的 target 假性轉紅）都成立，`ops/lib/ios_lock_wait.sh` 之類共用 lib 不受影響。只補上前一輪對抗驗證點名的兩個無守步驟，兩者都改在 acceptance 而不動 plan 主體：\n（R2 / S4）新 group 的 dispatcher 必須真的接得到——acceptance 直接跑 `./ops/test_ops.sh alias-coverage`。case 標籤打錯（例如 DEFAULT_TESTS 寫 alias-coverage 而 case 寫 alias-cov）時，ops/test_ops.sh 的 `*)` 分支會讓整條指令回非 0（我實測 `./ops/test_ops.sh nosuchgroup` rc=1），所以標籤錯不再靜默。這一段同時把 S2（chmod +x，以 `./` 呼叫）與 S3（DEFAULT_TESTS 登錄）從「字串對得上」升級成「真的跑得起來」。\n（R1 / S8）docs/reference/tech_index.md 的 verified_against 必須 bump——今天的值實測是 `9582044dd`（不是 plan 裡寫的 cf5dd5df5，那個早已被 bump 過，照抄會寫出一條死斷言），所以 acceptance 釘的是「必須離開 9582044dd 且新值在 HEAD 可達」。落地時 bump 到落地前的 HEAD 即可，commit 前就能跑綠，無雞生蛋問題。\n另補一條 LINUX_GROUPS 登錄的廉價守衛（`grep -q alias-coverage ops/tests/test_ops_ci_coverage.sh`），避免為了省下該檔 2 分鐘的全跑而漏掉 CI 分類。",
  "resolution": "—(2026-08-05 裁決（總經理自決，執行長授權）：隔離機制走 **隔離工作樹**，理由是 repo 已有 `worktree_orchestrate.py` 那套原語，不必新造容器路徑。證偽方式：把被宣稱涵蓋的腳本在隔離工作樹的原路徑上改壞（body 換 exit 0），要求對應 target 轉紅；不紅即該 alias 為假，從 covered 移除並列入 advisory。落點 `ops/worktree_orchestrate.py:499-509` 的 alias 准入與 `:527-528` 的 covered 折疊。)",
  "schema": "kg.backlog.entry.v1",
  "severity": "med",
  "source": "IMP-0051 的 review",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
