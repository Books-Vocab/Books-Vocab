{
  "acceptance": "./ops/test_ops.sh install-hooks && ./ops/install_hooks.sh --check && [ \"$(git config --get core.hooksPath)\" = \".githooks\" ]",
  "acceptance_cmd": "./ops/test_ops.sh install-hooks && ./ops/install_hooks.sh --check && [ \"$(git config --get core.hooksPath)\" = \".githooks\" ]",
  "acceptance_expect_rc": 0,
  "brief": "有一道「送出改動前先自我檢查」的防護寫好了卻從沒被開啟，讓人以為有防護而不去補別的。",
  "category": "tool",
  "cost": "S",
  "date": "2026-08-05",
  "detail": "`.githooks/pre-commit` 存在但沒有安裝：`git config --show-origin --get core.hooksPath` 指向 `.git/hooks`，而該目錄內沒有任何非 sample hook。因此本 checkout 的 design-system 本地 pre-commit 路徑是失效的，DS 覆蓋目前只靠 CI 加 worktree gate。宣稱存在但未接線的守衛比沒有守衛更糟：它會讓人以為那層防護在，於是不去補別的。",
  "fix_site": "ops/install_hooks.sh:1",
  "groomed_at": "2026-08-05",
  "groomed_by": "workflow:groom-all@v2",
  "id": "IMP-20260805-20cfc4",
  "plan": "目標：讓 `.githooks/pre-commit` 從「存在但沒接線」變成「每個 clone 有一條可重跑的安裝命令、且本 checkout 真的裝上了」。裁決已定案，驗收兩腿都要跑：(a) `core.hooksPath` 指到 `.githooks`，(b) 故意違規的 staged 改動真的被 pre-commit 擋下。以下每個行號都是 2026-08-05 在 HEAD=7d39c89ba 實際讀過的行（工作樹當時只有 ops/demo/generated/ios_fixture_dataset.json、ops/fixtures/ui_worlds/marketing_demo.json、ops/ui_world_manifest.py 三個未提交修改，與本條無關）。\n\n── 現況（已實測，不必再查）──\n* `git config --list --show-origin | grep -i hookspath` → `file:.git/config\tcore.hookspath=/Users/chenliangyu/project/kg/.git/hooks`。不是「忘了設」而是「被明確釘到別處」，所以安裝動作是**覆寫**既有值。\n* `.git/hooks/` 只有 14 個 `*.sample`，沒有任何真 hook。`.githooks/pre-commit` 存在、1840 bytes、`-rwxr-xr-x`。\n* `git config --global --get core.hooksPath` → rc=1（本機無全域值）。`git config --get extensions.worktreeConfig` → `true`，但 `.git/config.worktree` 與 `.git/worktrees/*/config.worktree` **都不存在**（`ls` 無此檔），所以 `core.hooksPath` 寫進 common config 後對 7 個 linked worktree 一體適用；相對路徑 `.githooks` 由 git 以「hook 執行時的工作樹頂層」解析，每個 worktree 都吃到自己那份 tracked 的 `.githooks/`。\n* `ops/install_hooks.sh` 不存在（`ls` → No such file or directory）。\n* 全 repo `grep -rn hooksPath --include='*.md' --include='*.sh' --include='*.py' --include='*.yml' --include='*.yaml'`（排除 docs/runbook/backlog 與 improvement_backlog）**只命中一處**：`docs/reference/tech_index.md:201`。`docs/reference/product_surface.md`（全檔 190 行）grep `core\\.hooksPath` → rc=1，**零命中，不要動它**。\n\n── 步驟 1：先寫會紅的測試（TDD）──\n新增 `ops/tests/test_install_hooks.sh`（chmod +x），逐字如下。它不碰本 checkout 的 `.git/config`：所有 git 動作都在 mktemp 造的拋棄式 repo 內，且 `GIT_CONFIG_GLOBAL` / `GIT_CONFIG_SYSTEM` 被導到拋棄式空檔，fixture 不繼承本機設定。\n\n```bash\n#!/usr/bin/env bash\n# test_install_hooks.sh — ops/install_hooks.sh 與 .githooks/pre-commit 的接線回歸。\n#\n# 守兩件事：\n#   1. install_hooks.sh 的 help / check / install / uninstall 語意。\n#   2. 裝上之後，違規的 staged 改動**真的會被 pre-commit 擋下**（不是只證它會綠）。\n#      做法：mktemp 造拋棄式 git repo，把本 repo 真正那份 .githooks/pre-commit 複製進去，\n#      用 exit 1 的 stub ops/verify_design_system.sh 當被觸發的 guard。\n#      反控是「未安裝時同一筆改動 commit 得過」——沒有反控就分不出「hook 擋的」與\n#      「這個 fixture 本來就 commit 不了」。\nset -o pipefail\n\nWORKTREE=\"$(cd \"$(dirname \"$0\")/../..\" && pwd)\"\nTMP=\"$(mktemp -d \"${TMPDIR:-/tmp}/kg_install_hooks_XXXXXX\")\"\ntrap 'rm -rf \"$TMP\"' EXIT\n\nexport GIT_CONFIG_GLOBAL=\"$TMP/gitconfig-global\"\nexport GIT_CONFIG_SYSTEM=\"$TMP/gitconfig-system\"\n: >\"$GIT_CONFIG_GLOBAL\"\n: >\"$GIT_CONFIG_SYSTEM\"\n\nmkdir -p \"$TMP/bin\"\nfor b in uv node rg; do\n  printf '#!/bin/sh\\nexit 0\\n' >\"$TMP/bin/$b\"\n  chmod +x \"$TMP/bin/$b\"\ndone\nSTUB_PATH=\"$TMP/bin:$PATH\"\n\npass=0; fail=0\nok()     { echo \"  ✓ $*\"; pass=$((pass+1)); }\nfail_t() { echo \"  ✗ $*\" >&2; fail=$((fail+1)); }\nsection(){ echo \"\"; echo \"── $* ──\"; }\n\nmake_fixture() {   # $1 = 目錄\n  local d=\"$1\"\n  mkdir -p \"$d/.githooks\" \"$d/ops\" \"$d/design-system\"\n  cp \"$WORKTREE/.githooks/pre-commit\" \"$d/.githooks/pre-commit\"\n  chmod +x \"$d/.githooks/pre-commit\"\n  printf '#!/bin/sh\\necho \"STUB_DS_GUARD_FIRED\" >&2\\nexit 1\\n' >\"$d/ops/verify_design_system.sh\"\n  chmod +x \"$d/ops/verify_design_system.sh\"\n  git init -q \"$d\"\n  git -C \"$d\" symbolic-ref HEAD refs/heads/main\n  git -C \"$d\" config user.email kg-test@example.com\n  git -C \"$d\" config user.name kg-test\n  printf 'seed\\n' >\"$d/design-system/tokens.json\"\n  git -C \"$d\" add -A\n  git -C \"$d\" commit -qm seed\n}\n\ncommit_count() { git -C \"$1\" rev-list --count HEAD; }\n\ntry_violating_commit() {   # $1 = 目錄, $2 = log 檔；回傳 git commit 的 rc\n  local d=\"$1\" log=\"$2\" rc=0\n  printf 'change %s\\n' \"$RANDOM\" >>\"$d/design-system/tokens.json\"\n  git -C \"$d\" add design-system/tokens.json\n  PATH=\"$STUB_PATH\" git -C \"$d\" commit -m \"ds change\" >\"$log\" 2>&1 || rc=$?\n  return \"$rc\"\n}\n\nsection \"syntax + help\"\nbash -n \"$WORKTREE/ops/install_hooks.sh\" && ok \"install_hooks.sh syntax\" || fail_t \"install_hooks.sh syntax\"\n\"$WORKTREE/ops/install_hooks.sh\" --help >\"$TMP/help.txt\" 2>&1; rc=$?\n[[ $rc -eq 0 ]] && ok \"--help exits 0\" || fail_t \"--help expect rc=0 got rc=$rc\"\ngrep -q 'core.hooksPath' \"$TMP/help.txt\" \\\n  && ok \"--help 說明 core.hooksPath\" \\\n  || { fail_t \"--help 未提 core.hooksPath\"; sed 's/^/      /' \"$TMP/help.txt\" >&2; }\n\nsection \"反控：未安裝時，違規改動 commit 得過\"\nA=\"$TMP/repo_a\"; make_fixture \"$A\"\nbefore=\"$(commit_count \"$A\")\"\nif try_violating_commit \"$A\" \"$TMP/a.log\"; then\n  after=\"$(commit_count \"$A\")\"\n  [[ \"$after\" -eq $((before + 1)) ]] \\\n    && ok \"未安裝時 commit 成立（反控有效）\" \\\n    || fail_t \"未安裝時 rc=0 但 commit 數沒增加（$before → $after）\"\nelse\n  fail_t \"未安裝時 commit 就被擋 — fixture 不乾淨，後面的斷言會是假綠\"\n  sed 's/^/      /' \"$TMP/a.log\" >&2\nfi\n\nsection \"安裝後：--check 綠，且違規 commit 被擋\"\nB=\"$TMP/repo_b\"; make_fixture \"$B\"\n( cd \"$B\" && \"$WORKTREE/ops/install_hooks.sh\" --yes ) >\"$TMP/b_install.log\" 2>&1; rc=$?\n[[ $rc -eq 0 ]] && ok \"--yes 安裝 rc=0\" \\\n  || { fail_t \"--yes 安裝 rc=$rc\"; sed 's/^/      /' \"$TMP/b_install.log\" >&2; }\nval=\"$(git -C \"$B\" config --get core.hooksPath || true)\"\n[[ \"$val\" == \".githooks\" ]] && ok \"fixture core.hooksPath = .githooks\" \\\n  || fail_t \"fixture core.hooksPath = ${val:-<未設定>}\"\n( cd \"$B\" && \"$WORKTREE/ops/install_hooks.sh\" --check ) >\"$TMP/b_check.log\" 2>&1; rc=$?\n[[ $rc -eq 0 ]] && ok \"--check 安裝後 rc=0\" \\\n  || { fail_t \"--check 安裝後 rc=$rc\"; sed 's/^/      /' \"$TMP/b_check.log\" >&2; }\nbefore=\"$(commit_count \"$B\")\"\nif try_violating_commit \"$B\" \"$TMP/b.log\"; then\n  fail_t \"安裝後違規 commit 仍然成立 — hook 沒有被執行\"\n  sed 's/^/      /' \"$TMP/b.log\" >&2\nelse\n  ok \"安裝後違規 commit 被擋下\"\nfi\ngrep -q 'STUB_DS_GUARD_FIRED' \"$TMP/b.log\" \\\n  && ok \"擋下的原因是 design-system guard 被觸發\" \\\n  || { fail_t \"commit 失敗了，但輸出沒有 STUB_DS_GUARD_FIRED — 擋它的是別的原因\"; sed 's/^/      /' \"$TMP/b.log\" >&2; }\nafter=\"$(commit_count \"$B\")\"\n[[ \"$after\" -eq \"$before\" ]] && ok \"被擋下時 commit 數未增加\" \\\n  || fail_t \"被擋下卻多了一個 commit（$before → $after）\"\n\nsection \"--check 未安裝時 rc=1；--uninstall 是有效逃生口\"\nC=\"$TMP/repo_c\"; make_fixture \"$C\"\n( cd \"$C\" && \"$WORKTREE/ops/install_hooks.sh\" --check ) >\"$TMP/c_check0.log\" 2>&1; rc=$?\n[[ $rc -eq 1 ]] && ok \"--check 未安裝時 rc=1\" || fail_t \"--check 未安裝時 rc=$rc（期望 1）\"\n( cd \"$C\" && \"$WORKTREE/ops/install_hooks.sh\" --yes ) >/dev/null 2>&1\n( cd \"$C\" && \"$WORKTREE/ops/install_hooks.sh\" --uninstall ) >\"$TMP/c_uninstall.log\" 2>&1; rc=$?\n[[ $rc -eq 0 ]] && ok \"--uninstall rc=0\" \\\n  || { fail_t \"--uninstall rc=$rc\"; sed 's/^/      /' \"$TMP/c_uninstall.log\" >&2; }\nval=\"$(git -C \"$C\" config --get core.hooksPath || true)\"\n[[ -z \"$val\" ]] && ok \"--uninstall 後 core.hooksPath 已清空\" \\\n  || fail_t \"--uninstall 後 core.hooksPath = $val\"\nif try_violating_commit \"$C\" \"$TMP/c.log\"; then\n  ok \"--uninstall 後違規 commit 又能成立（逃生口有效）\"\nelse\n  fail_t \"--uninstall 後 commit 仍被擋\"\n  sed 's/^/      /' \"$TMP/c.log\" >&2\nfi\n\necho \"\"\necho \"══════════════════════════════\"\necho \"  passed: $pass  failed: $fail\"\necho \"══════════════════════════════\"\n[[ $fail -eq 0 ]]\n```\n\n為什麼 fixture 一定要自備 stub uv/node：已讀 `.githooks/pre-commit:23-26`，`command -v uv` 或 `node` 任一缺就印告警並 `return 0`（不擋 commit）。ubuntu CI 沒有 node，不塞 stub 的話那條「被擋下」斷言會在 CI 假紅。stub 只需存在，永遠不會被呼叫——真正被呼叫的是 `.githooks/pre-commit:29` 的 `\"$REPO/ops/verify_design_system.sh\"`，也就是 fixture 裡那支 exit 1 的 stub；`pre-commit:9` 的 `set -euo pipefail` 讓它的非零往上傳成 hook 的非零。fixture 改的是 `design-system/tokens.json`，命中 `pre-commit:14` 的 `DS_PATTERN`（`^(design-system/|...)`），不命中 `pre-commit:16` 的 `UQ_PATTERN`，所以 `run_ui_quality_gate` 在 `:33` 就 return，不會去跑真的 ui_quality_gate。\n\n── 步驟 2：新增 `ops/install_hooks.sh`（chmod +x）──\n逐字如下。help 慣例抄 `ops/test_ops.sh:69-71` 的 `usage() { awk 'NR==1{next} /^#/{sub(/^# ?/,\"\"); print; next} {exit}' \"$0\"; }`。\n\n```bash\n#!/usr/bin/env bash\n# install_hooks.sh — 把本 clone 的 git hooks 指到 repo 內的 `.githooks/`。\n#\n# core.hooksPath 是 per-clone 的本地設定（寫在 .git/config），commit 帶不過去：\n# 每個 clone、每台機器、每次重新 clone 都要各跑一次。裝上之後 .githooks/pre-commit\n# 才會在 commit 時執行（design-system 與 UI quality 的本地 best-effort 檢查；\n# CI 與 worktree cutover gate 仍是硬 gate，本 hook 不取代它們）。\n#\n# 用法:\n#   ops/install_hooks.sh              # 安裝（等同 --yes）\n#   ops/install_hooks.sh --yes        # 安裝\n#   ops/install_hooks.sh --check      # 只讀不寫；未安裝則 exit 1\n#   ops/install_hooks.sh --uninstall  # 逃生口: git config --unset core.hooksPath\n#   ops/install_hooks.sh --help\n#\n# Exit: 0 = 成功 / 已安裝；1 = --check 判定未安裝；2 = 用法錯誤。\nset -euo pipefail\n\nREPO=\"$(git rev-parse --show-toplevel)\"\ncd \"$REPO\"\n\nHOOKS_DIR=\".githooks\"\n\nusage() { awk 'NR==1{next} /^#/{sub(/^# ?/,\"\"); print; next} {exit}' \"$0\"; }\n\ncurrent_path() { git config --get core.hooksPath 2>/dev/null || true; }\n\ndo_check() {\n  local val\n  val=\"$(current_path)\"\n  if [[ \"$val\" != \"$HOOKS_DIR\" ]]; then\n    printf '✗ core.hooksPath = %s（期望 %s）\\n' \"${val:-<未設定>}\" \"$HOOKS_DIR\" >&2\n    printf '  修法: ops/install_hooks.sh --yes\\n' >&2\n    return 1\n  fi\n  if [[ ! -x \"$HOOKS_DIR/pre-commit\" ]]; then\n    printf '✗ %s/pre-commit 不存在或不可執行\\n' \"$HOOKS_DIR\" >&2\n    return 1\n  fi\n  printf '✓ core.hooksPath = %s，%s/pre-commit 可執行\\n' \"$HOOKS_DIR\" \"$HOOKS_DIR\"\n  return 0\n}\n\ndo_install() {\n  git config core.hooksPath \"$HOOKS_DIR\"\n  do_check\n}\n\ndo_uninstall() {\n  git config --unset core.hooksPath 2>/dev/null || true\n  local val\n  val=\"$(current_path)\"\n  printf '✓ 已移除 core.hooksPath（現值: %s）\\n' \"${val:-<未設定>}\"\n}\n\nif [[ $# -gt 1 ]]; then\n  printf '✗ 一次只接受一個參數\\n' >&2\n  usage >&2\n  exit 2\nfi\n\ncase \"${1:-}\" in\n  \"\"|--yes)    do_install ;;\n  --check)     do_check ;;\n  --uninstall) do_uninstall ;;\n  -h|--help)   usage; exit 0 ;;\n  *)\n    printf '✗ unknown option: %s\\n' \"$1\" >&2\n    usage >&2\n    exit 2\n    ;;\nesac\n```\n\n刻意用相對值 `.githooks`（不是絕對路徑）：`.githooks/` 是 tracked 的，7 個 linked worktree 各有一份，相對值讓每個工作樹吃到自己那份。\n\n── 步驟 3：三處接線（少任一處都會讓別的 group 轉紅）──\n(a) `ops/test_ops.sh` 的 `DEFAULT_TESTS=(`（起始於 :24）；:60 現為 `  script-help`、:61 為 `  podcast-ops`、:62 為 `)`。在 :60 與 :61 之間插入一行 `  install-hooks`。\n(b) `ops/test_ops.sh:149` 現為 `    script-help)        ./ops/tests/test_script_help.sh ;;`。在其後插入 `    install-hooks)      ./ops/tests/test_install_hooks.sh ;;`。\n(c) `ops/tests/test_ops_ci_coverage.sh` 的 `LINUX_GROUPS=(`（起始於 :39）；:49 現為 `  script-help`、:50 為註解 `  # ── 以下 13 筆原本被排除...`。在 :49 與 :50 之間插入 `  install-hooks`。**這行不可省**：`test_ops_ci_coverage.sh:142-156` 的 \"every declared group is classified exactly once\" 用 :131-133 的 awk 解析 `DEFAULT_TESTS`，新 group 沒被分類就會讓 `ops-ci-coverage` 這個 group 轉紅。放 LINUX_GROUPS 而不是 EXCLUDED_GROUPS 的理由：新測試只用 git / bash / mktemp / cp，stub 由測試自造，沒有 BSD-only 語法（`mktemp -d \"${TMPDIR:-/tmp}/…XXXXXX\"`、`git init -q` + `symbolic-ref`、`GIT_CONFIG_GLOBAL`(git ≥2.32)），ubuntu runner 跑得動。\n\n不需要動 `.github/workflows/ops-suite.yml`：它的 paths anchor 已含 `ops/**`（:15-16），執行面是 :48-49 的 `./ops/test_ops.sh $(./ops/tests/test_ops_ci_coverage.sh --print-linux-groups)`，新 group 自動進 CI。`test_ops_ci_coverage.sh:189` 的 root-script 檢查是 `git ls-files '*.sh' | grep -v '/'`，只看 repo root，`ops/` 下的新檔不受影響。`ops/tests/test_script_help.sh:53-56` 與 `:24` 的 `run_help` 都是**具名**清單，`ops/tests/test_python_entrypoints.sh:22` 也是具名四支（ui_token_lint / injection_lint / i18n_lint / plain_deadzone_lint），`ops/capability_matrix.py` 與 `ops/tests/test_capability_matrix.py` 沒有任何 glob / ls-files / rglob——新增 `ops/install_hooks.sh` 不會被任何既有 group 自動掃到。\n\n── 步驟 4：修三處不實敘述（不要多修，也不要少修）──\n(a) `.githooks/pre-commit:4-5`，現內容逐字為：\n```\n# 啟用（一次性，已由建置時設好）: git config core.hooksPath .githooks\n# 停用: git config --unset core.hooksPath\n```\n「已由建置時設好」是假的（本 checkout 從來沒被設過）。改成：\n```\n# 啟用（每個 clone 各跑一次，不隨 commit 傳播）: ops/install_hooks.sh\n# 驗證: ops/install_hooks.sh --check   停用: ops/install_hooks.sh --uninstall\n```\n(b) `docs/reference/tech_index.md:201`，該行是「聚合入口 + 強制」那一列，內含手打的 `(\\`git config core.hooksPath .githooks\\`;DS 檔被 stage 才跑,缺 uv/node 告警跳過,CI 為硬 gate)`。把 `` `git config core.hooksPath .githooks` `` 這段咒語換成 `` 需先跑 `ops/install_hooks.sh` 裝到本 clone（`--check` 可驗） ``，該列其餘文字不動。\n(c) `README.md` 的「## 開發」段（:96），程式碼區塊內 :104-105 是 iOS 那兩行、:107-108 是 `# 設計系統 token 重新生成` 與 `npm run build`、:109 是收尾的三個反引號。在 :108 之後、:109 之前插入一個空行加：\n```\n# 本地 git hooks（每個 clone 各裝一次；core.hooksPath 是本地設定，commit 帶不過去）\n./ops/install_hooks.sh          # 安裝\n./ops/install_hooks.sh --check  # 驗證已安裝\n```\n\n**不要動的東西**：`docs/reference/product_surface.md`（全檔 190 行，`grep -rn 'core\\.hooksPath'` rc=1，零命中，沒有東西可換）；`ops/worktree_orchestrate.py:52`（`the .githooks pre-commit is best-effort only and must not be relied on`）與 `.claude/skills/worktree-flow/SKILL.md:54`（`.githooks 只 best-effort，不可依賴`）——那是刻意設計不是疏漏：hook 在缺 uv/node/rg 時告警跳過，裝上之後仍不是硬 gate。\n\n── 步驟 5：把本 checkout 真的裝上（本條唯一改變現實狀態的一步）──\n在 `/Users/chenliangyu/project/kg` 跑 `./ops/install_hooks.sh --yes`。它會把 `.git/config` 的 `core.hooksPath` 從 `/Users/chenliangyu/project/kg/.git/hooks` 覆寫成 `.githooks`。這是本地 config，不進 commit。跳過這步 acceptance 第二、三腿必紅。\n\n── 步驟 6：驗證與收尾 ──\n1. 跑 acceptance（本條 acceptance 欄那條鏈），三腿全綠。\n2. 跑 `./ops/test_ops.sh ops-ci-coverage`（macOS 上約 130 秒，不是卡住）確認分類完整性沒被打破；跑 `./ops/test_ops.sh script-help` 確認沒有連帶損傷。\n3. 跑 `./ops/docs_lint.sh`。今天基線是 `OK: 9 / WARN: 2 / ERROR: 0`（兩個 WARN 是既有的 `docs/sop/ios.md`、`docs/sop/ui_flow_evidence.md` STALE，與本條無關）。改動 `docs/reference/tech_index.md` 後若它變成 ERROR，把該檔 frontmatter :10 的 `verified_against: 48eec9e9a` bump 到本次改動落地後、main 可達的 commit sha。\n4. 這次改動本身不會觸發剛裝上的 hook：`.githooks/pre-commit:14` 的 `DS_PATTERN` 只列 `ops/(gen_web_tokens|gen_figma_sets|token_drift_check|component_fidelity_check|verify_design_system)\\.(py|sh)`，`:16` 的 `UQ_PATTERN` 只列 `ops/(ui_quality_gate|ui_quality_plane|ui_token_lint|plain_deadzone_lint|i18n_lint|catalyst_lint|injection_lint|_i18n_|_inject_shared)`，本次動到的 `ops/install_hooks.sh` / `ops/tests/test_install_hooks.sh` / `ops/test_ops.sh` / `ops/tests/test_ops_ci_coverage.sh` 一個都不命中。\n5. commit prefix 用 `ops:`（文檔那部分若分開 commit 用 `docs:`）。依鐵律 4 逐項 review。\n6. 未驗、留給執行者且**不納入本條 acceptance**：felix 上的兩個 clone（`~/project/kg` 與生產的 `~/kg-prod`）是否同樣未安裝。安裝是 per-clone 本地 config，commit 帶不過去，所以文檔已寫「每個 clone 各跑一次」；要不要去 felix 跑一次由總經理決定（`~/kg-prod` 是部署機，通常不需要本地 hook）。",
  "resolution": "—(2026-08-05 驗證 CONFIRMED-OPEN；落點 `.githooks/` 與 repo 的環境 setup 說明，成本 S。驗收：`git config --get core.hooksPath` 回 `.githooks`，且故意違規的 staged 改動會被 pre-commit 擋下（兩腿都要跑，別只證它會綠）。未驗：felix 的 clone 與兩個 worktree 是否同樣未安裝。)",
  "schema": "kg.backlog.entry.v1",
  "scope": "新增一支安裝小工具和它的測試、接進測試套件，並修掉三處說明文件寫錯的敘述。",
  "severity": "low",
  "source": "backlog CLI 稽核（5-agent deep dive）",
  "status": "open",
  "stream": "IMP",
  "verdict": "CONFIRMED-OPEN",
  "verified_at": "2026-08-05"
}
